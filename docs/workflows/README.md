# Workflow documentation

Per-workflow documentation is added only after the inventory, public API contract, and test harness are merged. Documentation must be generated or validated against the actual `workflow_call` schema.
