version: 1
source_root: src/ci_workflows
generated_from_sha: fa9b921594954c23a1c4ea433376a6f39ca6e564
inventory_complete: true
new_module_creation_locked: false
summary:
  python_files_discovered: 160
  status: complete-human-reviewed-ast-verified
  note: 'Protected-main inventory reconciled for #593 after retiring the global action/tool policy and custom validator runtime; private-source confidentiality remains independently
    enforced.'
rules:
- Read this inventory before creating or substantially modifying Python implementation.
- Reuse or refactor an existing owner before creating a parallel module/function/class.
- Cross-cutting infrastructure belongs to product-neutral domain providers.
- Default to one cohesive implementation module per domain; use a second only for a real boundary.
- More than two implementation modules for one domain/build/release concern requires owner review first.
- Every finalized Python add/remove/rename/move/material responsibility change updates this inventory.
status_values:
- keep
- consolidate
- compatibility
- remove-after-migration
- review
review:
  mode: human-reviewed-classification-with-deterministic-ast-coverage
  issue: 590
  notes:
  - 'Domain ownership is the reviewed #590 map derived from the prior exact-tree review and explicitly reconciled to #576 removals; no filename-prefix fallback exists.'
  - Keep/compatibility sets are explicit. Every remaining reviewed module is deliberately classified consolidate.
  - File responsibilities come from module docstrings; declaration names/signatures and internal dependency direction come from deterministic AST inspection.
  - Direct class methods are inventoried except trivial data-model dunders.
multi_file_domains:
- domain: android
  current_file_count: 8
  files:
  - src/ci_workflows/android.py
  - src/ci_workflows/android_contract.py
  - src/ci_workflows/android_execution.py
  - src/ci_workflows/android_policy.py
  - src/ci_workflows/android_resource_metrics.py
  - src/ci_workflows/android_types.py
  - src/ci_workflows/ciw_android.py
  - src/ci_workflows/ciw_android_completion.py
  assessment: consolidation-required
  reason: Contract/execution/metrics/types are fragmented; retain policy as the second boundary.
  intended_target: android.py + android_policy.py
- domain: apple
  current_file_count: 11
  files:
  - src/ci_workflows/apple.py
  - src/ci_workflows/apple_contract.py
  - src/ci_workflows/apple_contract_fragments.py
  - src/ci_workflows/apple_execution.py
  - src/ci_workflows/apple_multistage.py
  - src/ci_workflows/apple_plan_guard.py
  - src/ci_workflows/apple_primitives.py
  - src/ci_workflows/apple_simulator_confidence.py
  - src/ci_workflows/apple_simulator_script.py
  - src/ci_workflows/apple_types.py
  - src/ci_workflows/ciw_apple.py
  assessment: consolidation-required
  reason: Contracts/execution/simulator/types are fragmented; reusable primitives are the second boundary.
  intended_target: apple.py + apple_primitives.py
- domain: core
  current_file_count: 8
  files:
  - src/ci_workflows/__init__.py
  - src/ci_workflows/ciw.py
  - src/ci_workflows/ciw_docs.py
  - src/ci_workflows/ciw_types.py
  - src/ci_workflows/foundation_cli.py
  - src/ci_workflows/foundation_docs.py
  - src/ci_workflows/foundation_types.py
  - src/ci_workflows/language_primitives.py
  assessment: consolidation-required
  reason: Registry/types/foundation/docs are fragmented; keep the registry and language primitives.
  intended_target: ciw.py + language_primitives.py
- domain: device
  current_file_count: 21
  files:
  - src/ci_workflows/ciw_device.py
  - src/ci_workflows/ciw_device_lock.py
  - src/ci_workflows/device.py
  - src/ci_workflows/device_admission.py
  - src/ci_workflows/device_cleanup.py
  - src/ci_workflows/device_contract.py
  - src/ci_workflows/device_contract_common.py
  - src/ci_workflows/device_evidence.py
  - src/ci_workflows/device_execution.py
  - src/ci_workflows/device_inventory.py
  - src/ci_workflows/device_lifecycle.py
  - src/ci_workflows/device_live.py
  - src/ci_workflows/device_live_safe.py
  - src/ci_workflows/device_lock.py
  - src/ci_workflows/device_plan_contract.py
  - src/ci_workflows/device_primitives.py
  - src/ci_workflows/device_profile_contract.py
  - src/ci_workflows/device_test_lock.py
  - src/ci_workflows/device_types.py
  - src/ci_workflows/devices.py
  - src/ci_workflows/physical_log_policy.py
  assessment: consolidation-required
  reason: Planning/lifecycle/evidence/types are fragmented; exact device fencing is the second security boundary.
  intended_target: device.py + device_lock.py
- domain: flutter
  current_file_count: 5
  files:
  - src/ci_workflows/ciw_flutter.py
  - src/ci_workflows/flutter.py
  - src/ci_workflows/flutter_contract.py
  - src/ci_workflows/flutter_execution.py
  - src/ci_workflows/flutter_types.py
  assessment: consolidation-required
  reason: Contract/execution/types/CLI should converge on the Flutter provider.
  intended_target: flutter.py
- domain: gitops
  current_file_count: 11
  files:
  - src/ci_workflows/ciw_gitops.py
  - src/ci_workflows/gitops.py
  - src/ci_workflows/gitops_composition.py
  - src/ci_workflows/gitops_contract.py
  - src/ci_workflows/gitops_execution.py
  - src/ci_workflows/gitops_plan.py
  - src/ci_workflows/gitops_primitives.py
  - src/ci_workflows/gitops_render.py
  - src/ci_workflows/gitops_runtime.py
  - src/ci_workflows/gitops_source.py
  - src/ci_workflows/gitops_types.py
  assessment: consolidation-required
  reason: Source/plan/render/primitives/types are fragmented; runtime isolation is the second boundary.
  intended_target: gitops.py + gitops_runtime.py
- domain: gradle
  current_file_count: 5
  files:
  - src/ci_workflows/ciw_gradle_seed.py
  - src/ci_workflows/gradle_dependency_warm.py
  - src/ci_workflows/gradle_maven_publish.py
  - src/ci_workflows/gradle_seed.py
  - src/ci_workflows/gradle_seed_internal.py
  assessment: justified
  reason: Dependency warm, Maven publication, and cache-seed transport have independent privilege/lifecycle boundaries; do not combine authority automatically.
  intended_target: owner review before flattening
- domain: helm
  current_file_count: 16
  files:
  - src/ci_workflows/ciw_helm.py
  - src/ci_workflows/helm.py
  - src/ci_workflows/helm_archive.py
  - src/ci_workflows/helm_contract.py
  - src/ci_workflows/helm_dependency_policy.py
  - src/ci_workflows/helm_execution.py
  - src/ci_workflows/helm_manifest.py
  - src/ci_workflows/helm_measurement.py
  - src/ci_workflows/helm_policy.py
  - src/ci_workflows/helm_product_layout.py
  - src/ci_workflows/helm_registry.py
  - src/ci_workflows/helm_release.py
  - src/ci_workflows/helm_runtime.py
  - src/ci_workflows/helm_simple.py
  - src/ci_workflows/helm_types.py
  - src/ci_workflows/helm_upstream_policy.py
  assessment: consolidation-required
  reason: Validation/packaging/policy/runtime/types are fragmented; registry I/O is the second boundary.
  intended_target: helm.py + helm_registry.py
- domain: node
  current_file_count: 5
  files:
  - src/ci_workflows/ciw_node.py
  - src/ci_workflows/node.py
  - src/ci_workflows/node_contract.py
  - src/ci_workflows/node_execution.py
  - src/ci_workflows/node_types.py
  assessment: consolidation-required
  reason: Contract/execution/types/CLI should converge on the Node provider.
  intended_target: node.py
- domain: oci
  current_file_count: 13
  files:
  - src/ci_workflows/ciw_oci.py
  - src/ci_workflows/oci.py
  - src/ci_workflows/oci_base_inspection.py
  - src/ci_workflows/oci_contract.py
  - src/ci_workflows/oci_execution.py
  - src/ci_workflows/oci_execution_safe.py
  - src/ci_workflows/oci_input_contract.py
  - src/ci_workflows/oci_input_download.py
  - src/ci_workflows/oci_publish.py
  - src/ci_workflows/oci_publish_contract.py
  - src/ci_workflows/oci_registry_download.py
  - src/ci_workflows/oci_reproducibility.py
  - src/ci_workflows/oci_types.py
  assessment: consolidation-required
  reason: Supply-chain logic is heavily fragmented; registry acquisition remains the second isolation boundary.
  intended_target: oci.py + oci_registry_download.py
- domain: policy
  current_file_count: 3
  files:
  - src/ci_workflows/evidence.py
  - src/ci_workflows/policy.py
  - src/ci_workflows/readability.py
  assessment: consolidation-required
  reason: Policy/readability/evidence are split; evidence projection is the second boundary.
  intended_target: policy.py + evidence.py
- domain: python
  current_file_count: 7
  files:
  - src/ci_workflows/ciw_python.py
  - src/ci_workflows/python.py
  - src/ci_workflows/python_contract.py
  - src/ci_workflows/python_docker_execution.py
  - src/ci_workflows/python_execution.py
  - src/ci_workflows/python_host_execution.py
  - src/ci_workflows/python_types.py
  assessment: consolidation-required
  reason: Contract/host/docker/types/CLI are fragmented; backend-neutral execution is the second boundary.
  intended_target: python.py + python_execution.py
- domain: release
  current_file_count: 4
  files:
  - src/ci_workflows/private_release_asset.py
  - src/ci_workflows/private_release_asset_action.py
  - src/ci_workflows/release_primitives.py
  - src/ci_workflows/release_tag_authority.py
  assessment: consolidation-required
  reason: Release primitives/assets/action are fragmented; tag authority is the second security boundary.
  intended_target: release.py + release_tag_authority.py
- domain: service
  current_file_count: 3
  files:
  - src/ci_workflows/service_compose_primitives.py
  - src/ci_workflows/service_primitives.py
  - src/ci_workflows/service_runner_smoke.py
  assessment: consolidation-required
  reason: Compose/service/runner-smoke behavior is three modules for one concern.
  intended_target: service.py
- domain: source
  current_file_count: 10
  files:
  - src/ci_workflows/dependencies.py
  - src/ci_workflows/github_app_token.py
  - src/ci_workflows/private_source.py
  - src/ci_workflows/source.py
  - src/ci_workflows/source_admission.py
  - src/ci_workflows/source_checkout.py
  - src/ci_workflows/source_cli.py
  - src/ci_workflows/source_evidence.py
  - src/ci_workflows/source_github.py
  - src/ci_workflows/source_types.py
  assessment: consolidation-required
  reason: Admission/evidence/GitHub/private-source/dependency/token logic is fragmented; exact checkout is the second credential boundary.
  intended_target: source.py + source_checkout.py
- domain: validation
  current_file_count: 7
  files:
  - src/ci_workflows/validation_contracts.py
  - src/ci_workflows/validation_expression_contexts.py
  - src/ci_workflows/validation_graph.py
  - src/ci_workflows/validation_harness.py
  - src/ci_workflows/validation_helpers.py
  - src/ci_workflows/validation_model.py
  - src/ci_workflows/validation_policy.py
  assessment: consolidation-required
  reason: 'The custom digest-locked runtime is retired by #593; remaining validation policy/model/helper modules are existing consolidation debt rather than a dependency-bootstrap boundary.'
  intended_target: validation_harness.py
- domain: runners
  current_file_count: 3
  files:
  - src/ci_workflows/execution_backends.py
  - src/ci_workflows/runner_images.py
  - src/ci_workflows/runners.py
  assessment: consolidation-required
  reason: Selection/backend mapping/image policy span three modules; runner-image policy remains the second boundary.
  intended_target: runners.py + runner_images.py
- domain: public_api
  current_file_count: 3
  files:
  - src/ci_workflows/public_api.py
  - src/ci_workflows/public_api_contract.py
  - src/ci_workflows/public_ci_admission.py
  assessment: consolidation-required
  reason: Registry/contract/admission are three files; fold admission into the contract/policy boundary.
  intended_target: public_api.py + public_api_contract.py
domains:
  android:
    target_module: src/ci_workflows/android.py
    responsibility: Android validation/build/test planning and execution.
    files:
    - path: src/ci_workflows/android.py
      responsibility: Public facade for bounded reusable Android validation.
      status: keep
      classes: []
      functions:
      - name: _project_policy_failure
        signature: '_project_policy_failure(source_policy: Mapping[str, object] | None, error: FoundationError) -> AndroidValidationError'
        purpose: Implements _project_policy_failure for Public facade for bounded reusable Android validation.
      - name: _verify_policy
        signature: '_verify_policy(source_root: Path, request: AndroidValidationRequest, contract_root: Path, phase: str, environment: Mapping[str, str]) -> None'
        purpose: Implements _verify_policy for Public facade for bounded reusable Android validation.
      - name: validate
        signature: 'validate(*, contract_root: Path, source_root: Path | None, state_root: Path | None, request: AndroidValidationRequest, phase: str, environment: Mapping[str, str])
          -> AndroidValidationPlan | AndroidValidationResult'
        purpose: Plan or execute one contract-bounded Android validation request.
      internal_dependencies:
      - android_contract
      - android_execution
      - android_policy
      - android_types
      - foundation_types
    - path: src/ci_workflows/android_contract.py
      responsibility: Checked-in Android validation contract, trust, and plan resolution.
      status: consolidate
      classes: []
      functions:
      - name: require
        signature: 'require(condition: bool, code: str) -> None'
        purpose: Implements require for Checked-in Android validation contract, trust, and plan resolution.
      - name: safe_relative
        signature: 'safe_relative(value: Any, code: str=''invalid_input'', *, allow_dot: bool=False) -> str'
        purpose: Implements safe_relative for Checked-in Android validation contract, trust, and plan resolution.
      - name: bounded_path
        signature: 'bounded_path(root: Path, relative: str) -> Path'
        purpose: Implements bounded_path for Checked-in Android validation contract, trust, and plan resolution.
      - name: file_sha256
        signature: 'file_sha256(path: Path) -> str'
        purpose: Implements file_sha256 for Checked-in Android validation contract, trust, and plan resolution.
      - name: git_blob_sha1
        signature: 'git_blob_sha1(path: Path) -> str'
        purpose: Implements git_blob_sha1 for Checked-in Android validation contract, trust, and plan resolution.
      - name: strings
        signature: 'strings(value: Any, *, nonempty: bool=False, unique: bool=True) -> list[str]'
        purpose: Implements strings for Checked-in Android validation contract, trust, and plan resolution.
      - name: merged_task
        signature: 'merged_task(consumer: Mapping[str, Any], task: Mapping[str, Any]) -> dict[str, Any]'
        purpose: Implements merged_task for Checked-in Android validation contract, trust, and plan resolution.
      - name: validate_wrapper
        signature: 'validate_wrapper(wrapper: Mapping[str, Any]) -> None'
        purpose: Implements validate_wrapper for Checked-in Android validation contract, trust, and plan resolution.
      - name: validate_command
        signature: 'validate_command(command: Mapping[str, Any], forbidden: Sequence[str]) -> None'
        purpose: Implements validate_command for Checked-in Android validation contract, trust, and plan resolution.
      - name: validate_task
        signature: 'validate_task(contract: Mapping[str, Any], repository: str, consumer: Mapping[str, Any], name: str, raw: Mapping[str, Any]) -> None'
        purpose: Implements validate_task for Checked-in Android validation contract, trust, and plan resolution.
      - name: load_android_contract
        signature: 'load_android_contract(root: Path) -> Mapping[str, Any]'
        purpose: Load and validate the complete checked-in Android contract.
      - name: source_trust_from_environment
        signature: 'source_trust_from_environment(environment: Mapping[str, str]) -> str'
        purpose: Implements source_trust_from_environment for Checked-in Android validation contract, trust, and plan resolution.
      - name: optional_input
        signature: 'optional_input(environment: Mapping[str, str], name: str) -> str | None'
        purpose: Implements optional_input for Checked-in Android validation contract, trust, and plan resolution.
      - name: request_from_environment
        signature: 'request_from_environment(environment: Mapping[str, str], contract: Mapping[str, Any] | None=None) -> AndroidValidationRequest'
        purpose: Implements request_from_environment for Checked-in Android validation contract, trust, and plan resolution.
      - name: commands
        signature: 'commands(value: Sequence[Mapping[str, Any]]) -> tuple[AndroidCommand, ...]'
        purpose: Implements commands for Checked-in Android validation contract, trust, and plan resolution.
      - name: resolve_validation_plan
        signature: 'resolve_validation_plan(contract: Mapping[str, Any], request: AndroidValidationRequest) -> AndroidValidationPlan'
        purpose: Implements resolve_validation_plan for Checked-in Android validation contract, trust, and plan resolution.
      internal_dependencies:
      - android_types
    - path: src/ci_workflows/android_execution.py
      responsibility: Hermetic Android/Gradle execution and cleanup.
      status: consolidate
      classes: []
      functions:
      - name: sanitize
        signature: 'sanitize(text: str, roots: Sequence[Path]=()) -> str'
        purpose: Implements sanitize for Hermetic Android/Gradle execution and cleanup.
      - name: run_command
        signature: 'run_command(argv: Sequence[str], *, cwd: Path, environment: Mapping[str, str], timeout_seconds: int, failure_code: str, state_root: Path | None=None, stage: str=''command'',
          check: bool=True, timeout_rule_id: str | None=None, launch_rule_id: str | None=None, nonzero_rule_id: str | None=None, diagnostic_subject: str | None=None) -> subprocess.CompletedProcess[str]'
        purpose: Implements run_command for Hermetic Android/Gradle execution and cleanup.
      - name: isolated_git_environment
        signature: 'isolated_git_environment(inherited: Mapping[str, str] | None=None) -> dict[str, str]'
        purpose: Return the fixed Git environment used at every source-integrity boundary.
      - name: git_output
        signature: 'git_output(root: Path, args: Sequence[str], code: str=''dirty_tree'', inherited: Mapping[str, str] | None=None) -> str'
        purpose: Implements git_output for Hermetic Android/Gradle execution and cleanup.
      - name: pre_execution_status
        signature: 'pre_execution_status(root: Path, inherited: Mapping[str, str] | None=None) -> tuple[str, ...]'
        purpose: Capture the exact bounded status checked before any Android command runs.
      - name: verify_exact_source
        signature: 'verify_exact_source(root: Path, sha: str, inherited: Mapping[str, str] | None=None) -> None'
        purpose: Implements verify_exact_source for Hermetic Android/Gradle execution and cleanup.
      - name: copy_source
        signature: 'copy_source(source: Path, destination: Path) -> None'
        purpose: Implements copy_source for Hermetic Android/Gradle execution and cleanup.
      - name: remove_no_follow
        signature: 'remove_no_follow(path: Path) -> None'
        purpose: Implements remove_no_follow for Hermetic Android/Gradle execution and cleanup.
      - name: _properties
        signature: '_properties(path: Path, code: str) -> dict[str, str]'
        purpose: Implements _properties for Hermetic Android/Gradle execution and cleanup.
      - name: _major
        signature: '_major(output: str) -> int | None'
        purpose: Implements _major for Hermetic Android/Gradle execution and cleanup.
      - name: _has_installed_package
        signature: '_has_installed_package(listing: str, package: str) -> bool'
        purpose: Match a reviewed installed-package identity without weakening package checks.
      - name: _isolated_runtime_directories
        signature: '_isolated_runtime_directories(state: Path) -> dict[str, Path]'
        purpose: Implements _isolated_runtime_directories for Hermetic Android/Gradle execution and cleanup.
      - name: verify_toolchain
        signature: 'verify_toolchain(source: Path, state: Path, plan: AndroidValidationPlan, contract: Mapping[str, Any], inherited: Mapping[str, str]) -> tuple[int, int]'
        purpose: Implements verify_toolchain for Hermetic Android/Gradle execution and cleanup.
      - name: verify_wrapper
        signature: 'verify_wrapper(copy: Path, state: Path, plan: AndroidValidationPlan, environment: Mapping[str, str]) -> str'
        purpose: Implements verify_wrapper for Hermetic Android/Gradle execution and cleanup.
      - name: execution_environment
        signature: 'execution_environment(plan: AndroidValidationPlan, state: Path, inherited: Mapping[str, str], dependency: Path | None) -> dict[str, str]'
        purpose: Implements execution_environment for Hermetic Android/Gradle execution and cleanup.
      - name: verify_private_dependency
        signature: 'verify_private_dependency(state: Path, plan: AndroidValidationPlan, inherited: Mapping[str, str], contract: Mapping[str, Any] | None=None) -> Path | None'
        purpose: Implements verify_private_dependency for Hermetic Android/Gradle execution and cleanup.
      - name: _tree_hash
        signature: '_tree_hash(root: Path) -> str'
        purpose: Implements _tree_hash for Hermetic Android/Gradle execution and cleanup.
      - name: protected_hashes
        signature: 'protected_hashes(root: Path, plan: AndroidValidationPlan) -> dict[str, str]'
        purpose: Implements protected_hashes for Hermetic Android/Gradle execution and cleanup.
      - name: _failure
        signature: '_failure(stage: str, profile: str) -> str'
        purpose: Implements _failure for Hermetic Android/Gradle execution and cleanup.
      - name: _argv
        signature: '_argv(copy: Path, plan: AndroidValidationPlan, original: Sequence[str]) -> list[str]'
        purpose: Implements _argv for Hermetic Android/Gradle execution and cleanup.
      - name: verify_debug_outputs
        signature: 'verify_debug_outputs(copy: Path, plan: AndroidValidationPlan) -> bool'
        purpose: Implements verify_debug_outputs for Hermetic Android/Gradle execution and cleanup.
      - name: execute_android_plan
        signature: 'execute_android_plan(source: Path, state: Path, plan: AndroidValidationPlan, contract: Mapping[str, Any], inherited: Mapping[str, str]) -> AndroidValidationResult'
        purpose: Implements execute_android_plan for Hermetic Android/Gradle execution and cleanup.
      - name: _gradle_daemon_is_active
        signature: '_gradle_daemon_is_active(state: Path) -> bool'
        purpose: Implements _gradle_daemon_is_active for Hermetic Android/Gradle execution and cleanup.
      - name: _wait_for_gradle_daemon_exit
        signature: '_wait_for_gradle_daemon_exit(state: Path) -> None'
        purpose: Implements _wait_for_gradle_daemon_exit for Hermetic Android/Gradle execution and cleanup.
      - name: cleanup_android_state
        signature: 'cleanup_android_state(state: Path, contract: Mapping[str, Any]) -> None'
        purpose: Implements cleanup_android_state for Hermetic Android/Gradle execution and cleanup.
      internal_dependencies:
      - android_contract
      - android_types
    - path: src/ci_workflows/android_policy.py
      responsibility: Reviewed repository-policy projection for Android validation.
      status: keep
      classes:
      - name: AndroidPolicyFinding
        purpose: Policy failure carrying only one stable rule and bounded safe subject.
        methods: []
      functions:
      - name: _strings
        signature: '_strings(value: Any, instruction: str) -> tuple[str, ...]'
        purpose: Implements _strings for Reviewed repository-policy projection for Android validation.
      - name: load_android_source_policy
        signature: 'load_android_source_policy(root: Path) -> Mapping[str, Any]'
        purpose: Load and validate the Android-only source-policy projection contract.
      - name: _active_secret_exceptions
        signature: '_active_secret_exceptions(contract: Mapping[str, Any], request: AndroidValidationRequest) -> dict[str, str]'
        purpose: Implements _active_secret_exceptions for Reviewed repository-policy projection for Android validation.
      - name: _git_blob_sha1
        signature: '_git_blob_sha1(raw: bytes) -> str'
        purpose: Implements _git_blob_sha1 for Reviewed repository-policy projection for Android validation.
      - name: _git_environment
        signature: '_git_environment(environment: Mapping[str, str]) -> dict[str, str]'
        purpose: Implements _git_environment for Reviewed repository-policy projection for Android validation.
      - name: _generated_output_check
        signature: '_generated_output_check(root: Path, *, repository_policy: Mapping[str, Any], environment: Mapping[str, str]) -> None'
        purpose: Implements _generated_output_check for Reviewed repository-policy projection for Android validation.
      - name: _repository_status
        signature: '_repository_status(root: Path, *, environment: Mapping[str, str]) -> tuple[str, ...]'
        purpose: Implements _repository_status for Reviewed repository-policy projection for Android validation.
      - name: _clean_tree_check
        signature: '_clean_tree_check(root: Path, *, environment: Mapping[str, str]) -> None'
        purpose: Implements _clean_tree_check for Reviewed repository-policy projection for Android validation.
      - name: _scan_tracked_source
        signature: '_scan_tracked_source(root: Path, *, request: AndroidValidationRequest, repository_policy: Mapping[str, Any], source_policy: Mapping[str, Any]) -> None'
        purpose: Implements _scan_tracked_source for Reviewed repository-policy projection for Android validation.
      - name: verify_android_repository_policy
        signature: 'verify_android_repository_policy(root: Path, *, request: AndroidValidationRequest, phase: str, contract_root: Path, environment: Mapping[str, str], source_policy:
          Mapping[str, Any]) -> None'
        purpose: Apply the reviewed Android projection without weakening global policy.
      internal_dependencies:
      - android_execution
      - android_types
      - foundation_types
      - policy
    - path: src/ci_workflows/android_resource_metrics.py
      responsibility: Bounded Android execution resource measurements for one existing CI executor.
      status: consolidate
      classes:
      - name: AndroidResourceMetrics
        purpose: Finite, path-free resource facts safe for bounded CI evidence.
        methods: []
      - name: AndroidResourceSampler
        purpose: Sample Linux cgroup-v2 usage without changing the measured child process.
        methods:
        - name: _sample
          signature: _sample(self) -> None
          purpose: Implements AndroidResourceSampler._sample behavior for Bounded Android execution resource measurements for one existing CI executor.
        - name: _poll
          signature: _poll(self) -> None
          purpose: Implements AndroidResourceSampler._poll behavior for Bounded Android execution resource measurements for one existing CI executor.
        - name: start
          signature: start(self) -> 'AndroidResourceSampler'
          purpose: Implements AndroidResourceSampler.start behavior for Bounded Android execution resource measurements for one existing CI executor.
        - name: stop
          signature: stop(self) -> AndroidResourceMetrics
          purpose: Implements AndroidResourceSampler.stop behavior for Bounded Android execution resource measurements for one existing CI executor.
        - name: result
          signature: result(self) -> AndroidResourceMetrics
          purpose: Implements AndroidResourceSampler.result behavior for Bounded Android execution resource measurements for one existing CI executor.
        - name: __enter__
          signature: __enter__(self) -> 'AndroidResourceSampler'
          purpose: Implements AndroidResourceSampler.__enter__ behavior for Bounded Android execution resource measurements for one existing CI executor.
        - name: __exit__
          signature: '__exit__(self, exc_type: object, exc: object, traceback: object) -> bool'
          purpose: Implements AndroidResourceSampler.__exit__ behavior for Bounded Android execution resource measurements for one existing CI executor.
      functions:
      - name: _child_cpu_seconds
        signature: _child_cpu_seconds() -> float | None
        purpose: Implements _child_cpu_seconds for Bounded Android execution resource measurements for one existing CI executor.
      - name: _read_nonnegative_integer
        signature: '_read_nonnegative_integer(path: Path) -> int | None'
        purpose: Implements _read_nonnegative_integer for Bounded Android execution resource measurements for one existing CI executor.
      - name: _cgroup_v2_directory
        signature: '_cgroup_v2_directory(proc_cgroup: Path, cgroup_root: Path) -> Path | None'
        purpose: Resolve only this process' unified cgroup beneath the fixed cgroup root.
      internal_dependencies: []
    - path: src/ci_workflows/android_types.py
      responsibility: Typed models and stable errors for reusable Android validation.
      status: consolidate
      classes:
      - name: AndroidValidationError
        purpose: Fail-closed Android validation error carrying bounded safe diagnostics.
        methods:
        - name: diagnostic_values
          signature: diagnostic_values(self) -> dict[str, str]
          purpose: Implements AndroidValidationError.diagnostic_values behavior for Typed models and stable errors for reusable Android validation.
      - name: AndroidValidationRequest
        purpose: Defines AndroidValidationRequest for Typed models and stable errors for reusable Android validation.
        methods: []
      - name: AndroidCommand
        purpose: Defines AndroidCommand for Typed models and stable errors for reusable Android validation.
        methods: []
      - name: AndroidWrapperContract
        purpose: Defines AndroidWrapperContract for Typed models and stable errors for reusable Android validation.
        methods: []
      - name: AndroidValidationPlan
        purpose: Defines AndroidValidationPlan for Typed models and stable errors for reusable Android validation.
        methods:
        - name: requires_private_dependency
          signature: requires_private_dependency(self) -> bool
          purpose: Implements AndroidValidationPlan.requires_private_dependency behavior for Typed models and stable errors for reusable Android validation.
        - name: is_device_handoff
          signature: is_device_handoff(self) -> bool
          purpose: Implements AndroidValidationPlan.is_device_handoff behavior for Typed models and stable errors for reusable Android validation.
        - name: planning_outputs
          signature: planning_outputs(self) -> dict[str, str]
          purpose: Implements AndroidValidationPlan.planning_outputs behavior for Typed models and stable errors for reusable Android validation.
      - name: AndroidValidationResult
        purpose: Defines AndroidValidationResult for Typed models and stable errors for reusable Android validation.
        methods:
        - name: output_values
          signature: output_values(self) -> dict[str, str]
          purpose: Implements AndroidValidationResult.output_values behavior for Typed models and stable errors for reusable Android validation.
      functions:
      - name: _safe_subject
        signature: '_safe_subject(value: str) -> str'
        purpose: Implements _safe_subject for Typed models and stable errors for reusable Android validation.
      internal_dependencies: []
    - path: src/ci_workflows/ciw_android.py
      responsibility: Primitive-backed product-neutral ``ciw android validate`` adapter.
      status: compatibility
      classes:
      - name: _BoundedGradleRunner
        purpose: Defines _BoundedGradleRunner for Primitive-backed product-neutral ``ciw android validate`` adapter.
        methods:
        - name: run
          signature: 'run(self, argv: Sequence[str], *, cwd: Path, env: Mapping[str, str])'
          purpose: Implements _BoundedGradleRunner.run behavior for Primitive-backed product-neutral ``ciw android validate`` adapter.
      - name: ScriptPlan
        purpose: Defines ScriptPlan for Primitive-backed product-neutral ``ciw android validate`` adapter.
        methods: []
      - name: ProtectedFullPlan
        purpose: Defines ProtectedFullPlan for Primitive-backed product-neutral ``ciw android validate`` adapter.
        methods:
        - name: gradle_tasks
          signature: gradle_tasks(self) -> tuple[str, ...]
          purpose: Implements ProtectedFullPlan.gradle_tasks behavior for Primitive-backed product-neutral ``ciw android validate`` adapter.
        - name: gradle_groups
          signature: gradle_groups(self) -> tuple[tuple[str, tuple[str, ...]], ...]
          purpose: Implements ProtectedFullPlan.gradle_groups behavior for Primitive-backed product-neutral ``ciw android validate`` adapter.
        - name: remainder_gradle_tasks
          signature: remainder_gradle_tasks(self) -> tuple[str, ...]
          purpose: Implements ProtectedFullPlan.remainder_gradle_tasks behavior for Primitive-backed product-neutral ``ciw android validate`` adapter.
      - name: AndroidPrimitiveRequest
        purpose: Defines AndroidPrimitiveRequest for Primitive-backed product-neutral ``ciw android validate`` adapter.
        methods:
        - name: private_dependency_used
          signature: private_dependency_used(self) -> bool
          purpose: Implements AndroidPrimitiveRequest.private_dependency_used behavior for Primitive-backed product-neutral ``ciw android validate`` adapter.
      functions:
      - name: configure_android_validate
        signature: 'configure_android_validate(parser: argparse.ArgumentParser) -> None'
        purpose: Implements configure_android_validate for Primitive-backed product-neutral ``ciw android validate`` adapter.
      - name: _value
        signature: '_value(args: argparse.Namespace, context: CIWContext, name: str, default: str='''') -> str'
        purpose: Implements _value for Primitive-backed product-neutral ``ciw android validate`` adapter.
      - name: _plain
        signature: '_plain(value: object, code: str, *, allow_empty: bool=False, maximum: int=4096) -> str'
        purpose: Implements _plain for Primitive-backed product-neutral ``ciw android validate`` adapter.
      - name: _relative_path
        signature: '_relative_path(value: object, code: str, *, allow_dot: bool) -> str'
        purpose: Implements _relative_path for Primitive-backed product-neutral ``ciw android validate`` adapter.
      - name: _json_object
        signature: '_json_object(raw: str, code: str) -> dict[str, object]'
        purpose: Implements _json_object for Primitive-backed product-neutral ``ciw android validate`` adapter.
      - name: _exact_keys
        signature: '_exact_keys(value: Mapping[str, object], keys: set[str], code: str) -> None'
        purpose: Implements _exact_keys for Primitive-backed product-neutral ``ciw android validate`` adapter.
      - name: _strings
        signature: '_strings(value: object, code: str, *, maximum_items: int, allow_empty: bool=False, maximum_item_bytes: int=2048) -> tuple[str, ...]'
        purpose: Implements _strings for Primitive-backed product-neutral ``ciw android validate`` adapter.
      - name: _tasks
        signature: '_tasks(value: object, code: str, *, maximum_items: int=32) -> tuple[str, ...]'
        purpose: Implements _tasks for Primitive-backed product-neutral ``ciw android validate`` adapter.
      - name: _script_plan
        signature: '_script_plan(value: Mapping[str, object], code: str) -> ScriptPlan'
        purpose: Implements _script_plan for Primitive-backed product-neutral ``ciw android validate`` adapter.
      - name: _protected_full_plan
        signature: '_protected_full_plan(value: Mapping[str, object], code: str, default_execution_mode: str=''grouped'') -> ProtectedFullPlan'
        purpose: Implements _protected_full_plan for Primitive-backed product-neutral ``ciw android validate`` adapter.
      - name: _validation_plan
        signature: '_validation_plan(scope: str, raw: str, default_protected_full_execution_mode: str=''grouped'') -> tuple[tuple[str, ...], str, ScriptPlan | None, ProtectedFullPlan
          | None]'
        purpose: Implements _validation_plan for Primitive-backed product-neutral ``ciw android validate`` adapter.
      - name: _request
        signature: '_request(args: argparse.Namespace, context: CIWContext) -> AndroidPrimitiveRequest'
        purpose: Implements _request for Primitive-backed product-neutral ``ciw android validate`` adapter.
      - name: _state_root
        signature: '_state_root(context: CIWContext) -> Path'
        purpose: Implements _state_root for Primitive-backed product-neutral ``ciw android validate`` adapter.
      - name: _existing_root
        signature: '_existing_root(raw: str, code: str) -> Path'
        purpose: Implements _existing_root for Primitive-backed product-neutral ``ciw android validate`` adapter.
      - name: _runner_directory
        signature: '_runner_directory(raw: str, code: str) -> Path'
        purpose: Implements _runner_directory for Primitive-backed product-neutral ``ciw android validate`` adapter.
      - name: _bounded_existing_directory
        signature: '_bounded_existing_directory(root: Path, relative: str, code: str) -> Path'
        purpose: Implements _bounded_existing_directory for Primitive-backed product-neutral ``ciw android validate`` adapter.
      - name: _bounded_existing_file
        signature: '_bounded_existing_file(root: Path, relative: str, code: str, *, executable: bool) -> Path'
        purpose: Implements _bounded_existing_file for Primitive-backed product-neutral ``ciw android validate`` adapter.
      - name: _source_root
        signature: '_source_root(args: argparse.Namespace, context: CIWContext) -> Path'
        purpose: Implements _source_root for Primitive-backed product-neutral ``ciw android validate`` adapter.
      - name: _dependency_path
        signature: '_dependency_path(request: AndroidPrimitiveRequest, args: argparse.Namespace, context: CIWContext, state_root: Path) -> Path | None'
        purpose: Implements _dependency_path for Primitive-backed product-neutral ``ciw android validate`` adapter.
      - name: _read_only_gradle_cache
        signature: '_read_only_gradle_cache(source: Mapping[str, str]) -> Path | None'
        purpose: Implements _read_only_gradle_cache for Primitive-backed product-neutral ``ciw android validate`` adapter.
      - name: _runtime_environment
        signature: '_runtime_environment(context: CIWContext, dependency: Path | None) -> dict[str, str]'
        purpose: Implements _runtime_environment for Primitive-backed product-neutral ``ciw android validate`` adapter.
      - name: _plan_result
        signature: '_plan_result(request: AndroidPrimitiveRequest) -> CIWResult'
        purpose: Implements _plan_result for Primitive-backed product-neutral ``ciw android validate`` adapter.
      - name: _execute_script
        signature: '_execute_script(plan: ScriptPlan, *, project: Path, environment: Mapping[str, str]) -> None'
        purpose: Implements _execute_script for Primitive-backed product-neutral ``ciw android validate`` adapter.
      - name: _elapsed_ms
        signature: '_elapsed_ms(started_ns: int) -> int'
        purpose: Implements _elapsed_ms for Primitive-backed product-neutral ``ciw android validate`` adapter.
      - name: _run_protected_full_group
        signature: '_run_protected_full_group(wrapper: Path, group_name: str, group_tasks: tuple[str, ...], *, project: Path, environment: Mapping[str, str], context: CIWContext) ->
          int'
        purpose: Implements _run_protected_full_group for Primitive-backed product-neutral ``ciw android validate`` adapter.
      - name: _execute_request
        signature: '_execute_request(request: AndroidPrimitiveRequest, args: argparse.Namespace, context: CIWContext) -> CIWResult'
        purpose: Implements _execute_request for Primitive-backed product-neutral ``ciw android validate`` adapter.
      - name: _cleanup
        signature: '_cleanup(context: CIWContext) -> CIWResult'
        purpose: Implements _cleanup for Primitive-backed product-neutral ``ciw android validate`` adapter.
      - name: _residue
        signature: '_residue(context: CIWContext) -> CIWResult'
        purpose: Implements _residue for Primitive-backed product-neutral ``ciw android validate`` adapter.
      - name: _failure_outputs
        signature: '_failure_outputs(context: CIWContext, error: BaseException) -> CIWError'
        purpose: Implements _failure_outputs for Primitive-backed product-neutral ``ciw android validate`` adapter.
      - name: execute_android_validate
        signature: 'execute_android_validate(args: argparse.Namespace, context: CIWContext) -> CIWResult'
        purpose: Plan, execute, clean, or residue-check one product-neutral Android request.
      internal_dependencies:
      - android_execution
      - android_resource_metrics
      - android_types
      - ciw_types
      - foundation_types
      - language_primitives
      - runtime_primitives
      - workspace
    - path: src/ci_workflows/ciw_android_completion.py
      responsibility: Product-neutral Android live-service and unsigned-release CIW adapters.
      status: compatibility
      classes:
      - name: DependencyPlan
        purpose: Defines DependencyPlan for Product-neutral Android live-service and unsigned-release CIW adapters.
        methods:
        - name: used
          signature: used(self) -> bool
          purpose: Implements DependencyPlan.used behavior for Product-neutral Android live-service and unsigned-release CIW adapters.
      - name: CommandScript
        purpose: Defines CommandScript for Product-neutral Android live-service and unsigned-release CIW adapters.
        methods: []
      - name: LiveServicePlan
        purpose: Defines LiveServicePlan for Product-neutral Android live-service and unsigned-release CIW adapters.
        methods: []
      - name: GradleGroup
        purpose: Defines GradleGroup for Product-neutral Android live-service and unsigned-release CIW adapters.
        methods: []
      - name: SizeBudgetPlan
        purpose: Defines SizeBudgetPlan for Product-neutral Android live-service and unsigned-release CIW adapters.
        methods: []
      - name: ArtifactPlan
        purpose: Defines ArtifactPlan for Product-neutral Android live-service and unsigned-release CIW adapters.
        methods: []
      - name: ReleasePlan
        purpose: Defines ReleasePlan for Product-neutral Android live-service and unsigned-release CIW adapters.
        methods: []
      functions:
      - name: configure_android_live_validate
        signature: 'configure_android_live_validate(parser: argparse.ArgumentParser) -> None'
        purpose: Implements configure_android_live_validate for Product-neutral Android live-service and unsigned-release CIW adapters.
      - name: configure_android_release_validate
        signature: 'configure_android_release_validate(parser: argparse.ArgumentParser) -> None'
        purpose: Implements configure_android_release_validate for Product-neutral Android live-service and unsigned-release CIW adapters.
      - name: _configure
        signature: '_configure(parser: argparse.ArgumentParser) -> None'
        purpose: Implements _configure for Product-neutral Android live-service and unsigned-release CIW adapters.
      - name: _value
        signature: '_value(args: argparse.Namespace, context: CIWContext, name: str, default: str='''') -> str'
        purpose: Implements _value for Product-neutral Android live-service and unsigned-release CIW adapters.
      - name: _json
        signature: '_json(raw: str, domain: str) -> dict[str, object]'
        purpose: Implements _json for Product-neutral Android live-service and unsigned-release CIW adapters.
      - name: _exact
        signature: '_exact(value: Mapping[str, object], keys: set[str], domain: str) -> None'
        purpose: Implements _exact for Product-neutral Android live-service and unsigned-release CIW adapters.
      - name: _strings
        signature: '_strings(value: object, domain: str, *, maximum: int=32) -> tuple[str, ...]'
        purpose: Implements _strings for Product-neutral Android live-service and unsigned-release CIW adapters.
      - name: _script
        signature: '_script(value: object, domain: str) -> CommandScript'
        purpose: Implements _script for Product-neutral Android live-service and unsigned-release CIW adapters.
      - name: _dependency
        signature: '_dependency(args: argparse.Namespace, context: CIWContext, domain: str) -> DependencyPlan'
        purpose: Implements _dependency for Product-neutral Android live-service and unsigned-release CIW adapters.
      - name: _base
        signature: '_base(args: argparse.Namespace, context: CIWContext, domain: str) -> tuple[str, str, DependencyPlan]'
        purpose: Implements _base for Product-neutral Android live-service and unsigned-release CIW adapters.
      - name: _live_plan
        signature: '_live_plan(args: argparse.Namespace, context: CIWContext) -> LiveServicePlan'
        purpose: Implements _live_plan for Product-neutral Android live-service and unsigned-release CIW adapters.
      - name: _artifact_pattern
        signature: '_artifact_pattern(value: object, kind: str, domain: str) -> str'
        purpose: Implements _artifact_pattern for Product-neutral Android live-service and unsigned-release CIW adapters.
      - name: _release_plan
        signature: '_release_plan(args: argparse.Namespace, context: CIWContext) -> ReleasePlan'
        purpose: Implements _release_plan for Product-neutral Android live-service and unsigned-release CIW adapters.
      - name: _plan_outputs
        signature: '_plan_outputs(domain: str, admitted_sha: str, dependency: DependencyPlan, **extra: str) -> CIWResult'
        purpose: Implements _plan_outputs for Product-neutral Android live-service and unsigned-release CIW adapters.
      - name: _dependency_path
        signature: '_dependency_path(dependency: DependencyPlan, args: argparse.Namespace, context: CIWContext, state_root: Path, domain: str) -> Path | None'
        purpose: Implements _dependency_path for Product-neutral Android live-service and unsigned-release CIW adapters.
      - name: _runtime_environment
        signature: '_runtime_environment(context: CIWContext, dependency: Path | None, domain: str, *, credentials: bool=False) -> dict[str, str]'
        purpose: Implements _runtime_environment for Product-neutral Android live-service and unsigned-release CIW adapters.
      - name: _prepare_copy
        signature: '_prepare_copy(admitted_sha: str, working: str, copy_relative: str, args: argparse.Namespace, context: CIWContext, domain: str) -> tuple[Path, Path, Path]'
        purpose: Implements _prepare_copy for Product-neutral Android live-service and unsigned-release CIW adapters.
      - name: _verify_java
        signature: '_verify_java(project: Path, environment: Mapping[str, str]) -> int'
        purpose: Implements _verify_java for Product-neutral Android live-service and unsigned-release CIW adapters.
      - name: _run_script
        signature: '_run_script(plan: CommandScript, project: Path, environment: Mapping[str, str], domain: str) -> None'
        purpose: Implements _run_script for Product-neutral Android live-service and unsigned-release CIW adapters.
      - name: _safe_output_path
        signature: '_safe_output_path(project: Path, relative: str) -> Path'
        purpose: Implements _safe_output_path for Product-neutral Android live-service and unsigned-release CIW adapters.
      - name: _resolve_pattern
        signature: '_resolve_pattern(project: Path, pattern: str, kind: str, *, maximum: int, required: bool) -> tuple[Path, ...]'
        purpose: Implements _resolve_pattern for Product-neutral Android live-service and unsigned-release CIW adapters.
      - name: _execute_live
        signature: '_execute_live(plan: LiveServicePlan, args: argparse.Namespace, context: CIWContext) -> CIWResult'
        purpose: Implements _execute_live for Product-neutral Android live-service and unsigned-release CIW adapters.
      - name: _execute_release
        signature: '_execute_release(plan: ReleasePlan, args: argparse.Namespace, context: CIWContext) -> CIWResult'
        purpose: Implements _execute_release for Product-neutral Android live-service and unsigned-release CIW adapters.
      - name: _cleanup
        signature: '_cleanup(context: CIWContext, domain: str, relative: str) -> CIWResult'
        purpose: Implements _cleanup for Product-neutral Android live-service and unsigned-release CIW adapters.
      - name: _residue
        signature: '_residue(context: CIWContext, domain: str, relative: str) -> CIWResult'
        purpose: Implements _residue for Product-neutral Android live-service and unsigned-release CIW adapters.
      - name: _failure
        signature: '_failure(context: CIWContext, domain: str, error: BaseException) -> CIWError'
        purpose: Implements _failure for Product-neutral Android live-service and unsigned-release CIW adapters.
      - name: execute_android_live_validate
        signature: 'execute_android_live_validate(args: argparse.Namespace, context: CIWContext) -> CIWResult'
        purpose: Implements execute_android_live_validate for Product-neutral Android live-service and unsigned-release CIW adapters.
      - name: execute_android_release_validate
        signature: 'execute_android_release_validate(args: argparse.Namespace, context: CIWContext) -> CIWResult'
        purpose: Implements execute_android_release_validate for Product-neutral Android live-service and unsigned-release CIW adapters.
      internal_dependencies:
      - android_execution
      - ciw_android
      - ciw_types
      - language_primitives
      - runtime_primitives
  apple:
    target_module: src/ci_workflows/apple.py
    responsibility: Apple validation/build/test planning and execution.
    files:
    - path: src/ci_workflows/apple.py
      responsibility: Public façade for reusable Apple validation.
      status: keep
      classes: []
      functions:
      - name: source_trust_from_environment
        signature: 'source_trust_from_environment(environment: Mapping[str, str]) -> str'
        purpose: Implements source_trust_from_environment for Public façade for reusable Apple validation.
      - name: _optional
        signature: '_optional(environment: Mapping[str, str], key: str) -> str | None'
        purpose: Implements _optional for Public façade for reusable Apple validation.
      - name: request_from_environment
        signature: 'request_from_environment(environment: Mapping[str, str], contract: Mapping[str, object]) -> AppleValidationRequest'
        purpose: Implements request_from_environment for Public façade for reusable Apple validation.
      - name: resolve_plan
        signature: 'resolve_plan(contract: Mapping[str, object], request: AppleValidationRequest) -> AppleValidationPlan'
        purpose: Resolve one Apple plan with a collision-safe simulator identity.
      - name: validate
        signature: 'validate(*, contract_root: Path, source_root: Path | None, state_root: Path | None, request: AppleValidationRequest, phase: str, environment: Mapping[str, str] |
          None=None, runner: CommandRunner | None=None) -> AppleValidationPlan | AppleValidationResult'
        purpose: Implements validate for Public façade for reusable Apple validation.
      internal_dependencies:
      - apple_contract
      - apple_execution
      - apple_types
    - path: src/ci_workflows/apple_contract.py
      responsibility: Checked-in Apple validation contract, trust, and plan resolution.
      status: consolidate
      classes: []
      functions:
      - name: fail
        signature: 'fail(code: str) -> None'
        purpose: Implements fail for Checked-in Apple validation contract, trust, and plan resolution.
      - name: require
        signature: 'require(condition: bool, code: str) -> None'
        purpose: Implements require for Checked-in Apple validation contract, trust, and plan resolution.
      - name: _strings
        signature: '_strings(value: Any, *, nonempty: bool=False, unique: bool=True) -> list[str]'
        purpose: Implements _strings for Checked-in Apple validation contract, trust, and plan resolution.
      - name: safe_relative
        signature: 'safe_relative(value: Any, code: str=''invalid_input'', *, allow_dot: bool=False) -> str'
        purpose: Implements safe_relative for Checked-in Apple validation contract, trust, and plan resolution.
      - name: bounded_path
        signature: 'bounded_path(root: Path, relative: str, *, must_exist: bool=False) -> Path'
        purpose: Implements bounded_path for Checked-in Apple validation contract, trust, and plan resolution.
      - name: regular_path
        signature: 'regular_path(root: Path, relative: str, code: str=''path_rejected'') -> Path'
        purpose: Implements regular_path for Checked-in Apple validation contract, trust, and plan resolution.
      - name: _validate_toolchain
        signature: '_validate_toolchain(raw: Mapping[str, Any]) -> AppleToolchain'
        purpose: Implements _validate_toolchain for Checked-in Apple validation contract, trust, and plan resolution.
      - name: _validate_container
        signature: '_validate_container(raw: object) -> AppleContainer | None'
        purpose: Implements _validate_container for Checked-in Apple validation contract, trust, and plan resolution.
      - name: _validate_simulator
        signature: '_validate_simulator(identifier: str, raw: Mapping[str, Any]) -> AppleSimulator'
        purpose: Implements _validate_simulator for Checked-in Apple validation contract, trust, and plan resolution.
      - name: _validate_command
        signature: '_validate_command(raw: Mapping[str, Any]) -> AppleCommand'
        purpose: Implements _validate_command for Checked-in Apple validation contract, trust, and plan resolution.
      - name: _validate_task
        signature: '_validate_task(task_id: str, raw: Mapping[str, Any], profiles: Mapping[str, Any], simulators: Mapping[str, AppleSimulator], artifact_exceptions: Mapping[str, Any])
          -> dict[str, Any]'
        purpose: Implements _validate_task for Checked-in Apple validation contract, trust, and plan resolution.
      - name: load_apple_contract
        signature: 'load_apple_contract(root: Path) -> Mapping[str, Any]'
        purpose: Load and validate the complete checked-in Apple contract.
      - name: build_plan
        signature: 'build_plan(contract: Mapping[str, Any], request: AppleValidationRequest) -> AppleValidationPlan'
        purpose: Implements build_plan for Checked-in Apple validation contract, trust, and plan resolution.
      internal_dependencies:
      - apple_types
    - path: src/ci_workflows/apple_contract_fragments.py
      responsibility: Collision-safe extension loading for reviewed Apple consumer contracts.
      status: consolidate
      classes: []
      functions:
      - name: _mapping
        signature: '_mapping(path: Path) -> dict[str, object]'
        purpose: Implements _mapping for Collision-safe extension loading for reviewed Apple consumer contracts.
      - name: _validate_fragment_task
        signature: '_validate_fragment_task(task_id: str, raw: Mapping[str, object], contract: Mapping[str, object]) -> dict[str, object]'
        purpose: Validate an ordinary task or a stricter script-only simulator extension.
      - name: load_apple_contract
        signature: 'load_apple_contract(root: Path) -> Mapping[str, object]'
        purpose: Load the base contract and validate additive task/consumer fragments.
      internal_dependencies:
      - apple_contract
      - apple_types
    - path: src/ci_workflows/apple_execution.py
      responsibility: Hermetic Apple toolchain, simulator, build, and cleanup execution.
      status: consolidate
      classes:
      - name: CommandOutcome
        purpose: Defines CommandOutcome for Hermetic Apple toolchain, simulator, build, and cleanup execution.
        methods: []
      - name: CommandRunner
        purpose: Defines CommandRunner for Hermetic Apple toolchain, simulator, build, and cleanup execution.
        methods:
        - name: run
          signature: 'run(self, argv: Sequence[str], *, cwd: Path, env: Mapping[str, str], timeout_seconds: int) -> CommandOutcome'
          purpose: Implements CommandRunner.run behavior for Hermetic Apple toolchain, simulator, build, and cleanup execution.
      - name: SubprocessCommandRunner
        purpose: Defines SubprocessCommandRunner for Hermetic Apple toolchain, simulator, build, and cleanup execution.
        methods:
        - name: run
          signature: 'run(self, argv: Sequence[str], *, cwd: Path, env: Mapping[str, str], timeout_seconds: int) -> CommandOutcome'
          purpose: Implements SubprocessCommandRunner.run behavior for Hermetic Apple toolchain, simulator, build, and cleanup execution.
      - name: SimulatorLease
        purpose: Defines SimulatorLease for Hermetic Apple toolchain, simulator, build, and cleanup execution.
        methods: []
      - name: SimulatorOwnership
        purpose: One host-user simulator lock and its validated ownership registry.
        methods:
        - name: row
          signature: 'row(self, owner_key: str) -> dict[str, str] | None'
          purpose: Implements SimulatorOwnership.row behavior for Hermetic Apple toolchain, simulator, build, and cleanup execution.
        - name: replace
          signature: 'replace(self, owner_key: str, row: Mapping[str, str] | None) -> None'
          purpose: Implements SimulatorOwnership.replace behavior for Hermetic Apple toolchain, simulator, build, and cleanup execution.
      functions:
      - name: sanitize
        signature: 'sanitize(text: str, roots: Sequence[Path]=()) -> str'
        purpose: Implements sanitize for Hermetic Apple toolchain, simulator, build, and cleanup execution.
      - name: _run
        signature: '_run(runner: CommandRunner, argv: Sequence[str], *, cwd: Path, env: Mapping[str, str], timeout_seconds: int, failure_code: str, state_root: Path | None=None, stage:
          str=''command'', check: bool=True) -> CommandOutcome'
        purpose: Implements _run for Hermetic Apple toolchain, simulator, build, and cleanup execution.
      - name: _state_directory
        signature: '_state_directory(state_root: Path) -> Path'
        purpose: Implements _state_directory for Hermetic Apple toolchain, simulator, build, and cleanup execution.
      - name: isolated_environment
        signature: 'isolated_environment(state_root: Path, base: Mapping[str, str] | None=None) -> tuple[dict[str, str], dict[str, Path]]'
        purpose: Implements isolated_environment for Hermetic Apple toolchain, simulator, build, and cleanup execution.
      - name: parse_xcode_identity
        signature: 'parse_xcode_identity(output: str) -> tuple[str, str]'
        purpose: Implements parse_xcode_identity for Hermetic Apple toolchain, simulator, build, and cleanup execution.
      - name: parse_swift_identity
        signature: 'parse_swift_identity(output: str) -> str'
        purpose: Implements parse_swift_identity for Hermetic Apple toolchain, simulator, build, and cleanup execution.
      - name: verify_toolchain
        signature: 'verify_toolchain(plan: AppleValidationPlan, source_root: Path, state_root: Path, runner: CommandRunner, env: Mapping[str, str]) -> tuple[str, str, str, dict[str,
          str], dict[str, object] | None]'
        purpose: Implements verify_toolchain for Hermetic Apple toolchain, simulator, build, and cleanup execution.
      - name: _lstat
        signature: '_lstat(path: Path) -> os.stat_result | None'
        purpose: Implements _lstat for Hermetic Apple toolchain, simulator, build, and cleanup execution.
      - name: _verify_absolute_directory_no_follow
        signature: '_verify_absolute_directory_no_follow(path: Path, code: str) -> Path'
        purpose: Implements _verify_absolute_directory_no_follow for Hermetic Apple toolchain, simulator, build, and cleanup execution.
      - name: _ownership_base
        signature: '_ownership_base(environment: Mapping[str, str], state_root: Path | None=None) -> Path'
        purpose: Implements _ownership_base for Hermetic Apple toolchain, simulator, build, and cleanup execution.
      - name: _ownership_root
        signature: '_ownership_root(environment: Mapping[str, str], state_root: Path | None=None) -> Path'
        purpose: Implements _ownership_root for Hermetic Apple toolchain, simulator, build, and cleanup execution.
      - name: _read_regular_no_follow
        signature: '_read_regular_no_follow(path: Path, code: str, maximum_bytes: int) -> bytes'
        purpose: Implements _read_regular_no_follow for Hermetic Apple toolchain, simulator, build, and cleanup execution.
      - name: _validate_ownership_row
        signature: '_validate_ownership_row(raw: object) -> dict[str, str]'
        purpose: Implements _validate_ownership_row for Hermetic Apple toolchain, simulator, build, and cleanup execution.
      - name: _read_ownership_registry
        signature: '_read_ownership_registry(root: Path) -> list[dict[str, str]]'
        purpose: Implements _read_ownership_registry for Hermetic Apple toolchain, simulator, build, and cleanup execution.
      - name: _write_ownership_registry
        signature: '_write_ownership_registry(root: Path, rows: Sequence[Mapping[str, str]]) -> None'
        purpose: Implements _write_ownership_registry for Hermetic Apple toolchain, simulator, build, and cleanup execution.
      - name: _simulator_ownership
        signature: '_simulator_ownership(environment: Mapping[str, str], state_root: Path | None=None) -> Iterator[SimulatorOwnership]'
        purpose: Implements _simulator_ownership for Hermetic Apple toolchain, simulator, build, and cleanup execution.
      - name: _simulator_owner_key
        signature: '_simulator_owner_key(plan: AppleValidationPlan) -> str'
        purpose: Implements _simulator_owner_key for Hermetic Apple toolchain, simulator, build, and cleanup execution.
      - name: _simulator_device_name
        signature: '_simulator_device_name(plan: AppleValidationPlan, state_root: Path | None=None) -> str'
        purpose: Return the contract-owned name; ``state_root`` is ignored for compatibility.
      - name: _ownership_record
        signature: '_ownership_record(plan: AppleValidationPlan, *, status: str, udid: str='''') -> dict[str, str]'
        purpose: Implements _ownership_record for Hermetic Apple toolchain, simulator, build, and cleanup execution.
      - name: _require_record_identity
        signature: '_require_record_identity(plan: AppleValidationPlan, row: Mapping[str, str]) -> None'
        purpose: Implements _require_record_identity for Hermetic Apple toolchain, simulator, build, and cleanup execution.
      - name: _external_display_name
        signature: '_external_display_name(row: Mapping[str, str]) -> str'
        purpose: Implements _external_display_name for Hermetic Apple toolchain, simulator, build, and cleanup execution.
      - name: _recorded_owned_companion_candidates
        signature: '_recorded_owned_companion_candidates(row: Mapping[str, str], payload: object, *, require_available: bool=False) -> list[dict[str, str]]'
        purpose: Find only the exact External Display companion derived from one CIW row.
      - name: _exact_owned_candidates
        signature: '_exact_owned_candidates(plan: AppleValidationPlan, payload: object, *, require_available: bool=True) -> list[dict[str, str]]'
        purpose: Implements _exact_owned_candidates for Hermetic Apple toolchain, simulator, build, and cleanup execution.
      - name: _recorded_owned_candidates
        signature: '_recorded_owned_candidates(row: Mapping[str, str], payload: object) -> list[dict[str, str]]'
        purpose: Find only devices described by one validated CIW ownership row.
      - name: _device_inventory
        signature: '_device_inventory(runner: CommandRunner, *, source_root: Path, state_root: Path, env: Mapping[str, str], available_only: bool, failure_code: str, record_log: bool=True)
          -> object'
        purpose: Implements _device_inventory for Hermetic Apple toolchain, simulator, build, and cleanup execution.
      - name: _delete_owned_simulator_with_retry
        signature: '_delete_owned_simulator_with_retry(runner: CommandRunner, *, source_root: Path, state_root: Path, env: Mapping[str, str], udid: str, failure_code: str, stage_prefix:
          str, candidates_for: Callable[[object], list[dict[str, str]]]) -> None'
        purpose: Delete one proven simulator with bounded read-back retry/backoff.
      - name: _delete_exact_owned_simulator
        signature: '_delete_exact_owned_simulator(runner: CommandRunner, *, source_root: Path, state_root: Path, env: Mapping[str, str], udid: str, plan: AppleValidationPlan, failure_code:
          str) -> None'
        purpose: Implements _delete_exact_owned_simulator for Hermetic Apple toolchain, simulator, build, and cleanup execution.
      - name: _delete_recorded_owned_simulator
        signature: '_delete_recorded_owned_simulator(runner: CommandRunner, *, source_root: Path, state_root: Path, env: Mapping[str, str], row: Mapping[str, str], udid: str, failure_code:
          str) -> None'
        purpose: Delete one exact simulator proven by a persisted CIW ownership row.
      - name: _delete_recorded_owned_companion
        signature: '_delete_recorded_owned_companion(runner: CommandRunner, *, source_root: Path, state_root: Path, env: Mapping[str, str], row: Mapping[str, str], udid: str, failure_code:
          str) -> None'
        purpose: Delete one exact External Display companion derived from a persisted row.
      - name: _delete_recorded_owned_objects
        signature: '_delete_recorded_owned_objects(runner: CommandRunner, *, source_root: Path, state_root: Path, env: Mapping[str, str], row: Mapping[str, str], failure_code: str) ->
          None'
        purpose: Delete a recorded primary and its exact companion before releasing the row.
      - name: _reconcile_stale_ownership_rows
        signature: '_reconcile_stale_ownership_rows(plan: AppleValidationPlan, source_root: Path, state_root: Path, runner: CommandRunner, env: Mapping[str, str], ownership: SimulatorOwnership)
          -> None'
        purpose: Terminalize stale CIW rows from older exact heads before new selection.
      - name: _recover_stale_owned_simulator
        signature: '_recover_stale_owned_simulator(plan: AppleValidationPlan, source_root: Path, state_root: Path, runner: CommandRunner, env: Mapping[str, str], ownership: SimulatorOwnership)
          -> None'
        purpose: Implements _recover_stale_owned_simulator for Hermetic Apple toolchain, simulator, build, and cleanup execution.
      - name: _select_simulator_locked
        signature: '_select_simulator_locked(plan: AppleValidationPlan, source_root: Path, state_root: Path, runner: CommandRunner, env: Mapping[str, str], ownership: SimulatorOwnership)
          -> SimulatorLease'
        purpose: Implements _select_simulator_locked for Hermetic Apple toolchain, simulator, build, and cleanup execution.
      - name: select_simulator
        signature: 'select_simulator(plan: AppleValidationPlan, source_root: Path, state_root: Path, runner: CommandRunner, env: Mapping[str, str], *, ownership: SimulatorOwnership |
          None=None) -> SimulatorLease'
        purpose: Implements select_simulator for Hermetic Apple toolchain, simulator, build, and cleanup execution.
      - name: _git_output
        signature: '_git_output(root: Path, arguments: Sequence[str], code: str) -> str'
        purpose: Implements _git_output for Hermetic Apple toolchain, simulator, build, and cleanup execution.
      - name: _has_git
        signature: '_has_git(source_root: Path) -> bool'
        purpose: Implements _has_git for Hermetic Apple toolchain, simulator, build, and cleanup execution.
      - name: verify_exact_source
        signature: 'verify_exact_source(source_root: Path, admitted_sha: str) -> None'
        purpose: Implements verify_exact_source for Hermetic Apple toolchain, simulator, build, and cleanup execution.
      - name: _assert_tracked_regular
        signature: '_assert_tracked_regular(source_root: Path, relative: str) -> Path'
        purpose: Implements _assert_tracked_regular for Hermetic Apple toolchain, simulator, build, and cleanup execution.
      - name: _tree_hash
        signature: '_tree_hash(path: Path) -> str'
        purpose: Implements _tree_hash for Hermetic Apple toolchain, simulator, build, and cleanup execution.
      - name: protected_hashes
        signature: 'protected_hashes(source_root: Path, plan: AppleValidationPlan) -> dict[str, str]'
        purpose: Implements protected_hashes for Hermetic Apple toolchain, simulator, build, and cleanup execution.
      - name: _validate_container_files
        signature: '_validate_container_files(source_root: Path, plan: AppleValidationPlan) -> None'
        purpose: Implements _validate_container_files for Hermetic Apple toolchain, simulator, build, and cleanup execution.
      - name: _verify_outputs
        signature: '_verify_outputs(source_root: Path, command: AppleCommand) -> bool'
        purpose: Implements _verify_outputs for Hermetic Apple toolchain, simulator, build, and cleanup execution.
      - name: _destination
        signature: '_destination(plan: AppleValidationPlan, lease: SimulatorLease | None) -> str'
        purpose: Implements _destination for Hermetic Apple toolchain, simulator, build, and cleanup execution.
      - name: _xcodebuild_argv
        signature: '_xcodebuild_argv(plan: AppleValidationPlan, command: AppleCommand, source_root: Path, directories: Mapping[str, Path], lease: SimulatorLease | None) -> tuple[str,
          ...]'
        purpose: Implements _xcodebuild_argv for Hermetic Apple toolchain, simulator, build, and cleanup execution.
      - name: _swift_argv
        signature: '_swift_argv(plan: AppleValidationPlan, command: AppleCommand, source_root: Path, directories: Mapping[str, Path]) -> tuple[str, ...]'
        purpose: Implements _swift_argv for Hermetic Apple toolchain, simulator, build, and cleanup execution.
      - name: _execute_command
        signature: '_execute_command(plan: AppleValidationPlan, command: AppleCommand, source_root: Path, state_root: Path, runner: CommandRunner, env: Mapping[str, str], directories:
          Mapping[str, Path], lease: SimulatorLease | None) -> bool'
        purpose: Implements _execute_command for Hermetic Apple toolchain, simulator, build, and cleanup execution.
      - name: _lexical_target
        signature: '_lexical_target(root: Path, relative: str) -> Path'
        purpose: Implements _lexical_target for Hermetic Apple toolchain, simulator, build, and cleanup execution.
      - name: _remove_no_follow
        signature: '_remove_no_follow(path: Path) -> None'
        purpose: Implements _remove_no_follow for Hermetic Apple toolchain, simulator, build, and cleanup execution.
      - name: _cleanup_targets
        signature: '_cleanup_targets(source_root: Path, state_root: Path, plan: AppleValidationPlan) -> tuple[Path, ...]'
        purpose: Implements _cleanup_targets for Hermetic Apple toolchain, simulator, build, and cleanup execution.
      - name: _cleanup_simulator_locked
        signature: '_cleanup_simulator_locked(source_root: Path, state_root: Path, plan: AppleValidationPlan, *, runner: CommandRunner, environment: Mapping[str, str], ownership: SimulatorOwnership)
          -> None'
        purpose: Implements _cleanup_simulator_locked for Hermetic Apple toolchain, simulator, build, and cleanup execution.
      - name: _cleanup_apple_state_locked
        signature: '_cleanup_apple_state_locked(source_root: Path, state_root: Path, plan: AppleValidationPlan, *, runner: CommandRunner, environment: Mapping[str, str], ownership: SimulatorOwnership
          | None) -> None'
        purpose: Implements _cleanup_apple_state_locked for Hermetic Apple toolchain, simulator, build, and cleanup execution.
      - name: cleanup_apple_state
        signature: 'cleanup_apple_state(source_root: Path, state_root: Path, plan: AppleValidationPlan, *, runner: CommandRunner | None=None, environment: Mapping[str, str] | None=None,
          ownership: SimulatorOwnership | None=None) -> None'
        purpose: Implements cleanup_apple_state for Hermetic Apple toolchain, simulator, build, and cleanup execution.
      - name: _assert_zero_apple_residue_locked
        signature: '_assert_zero_apple_residue_locked(source_root: Path, state_root: Path, plan: AppleValidationPlan, *, runner: CommandRunner, environment: Mapping[str, str], ownership:
          SimulatorOwnership | None) -> None'
        purpose: Implements _assert_zero_apple_residue_locked for Hermetic Apple toolchain, simulator, build, and cleanup execution.
      - name: assert_zero_apple_residue
        signature: 'assert_zero_apple_residue(source_root: Path, state_root: Path, plan: AppleValidationPlan, *, runner: CommandRunner | None=None, environment: Mapping[str, str] | None=None,
          ownership: SimulatorOwnership | None=None) -> None'
        purpose: Implements assert_zero_apple_residue for Hermetic Apple toolchain, simulator, build, and cleanup execution.
      - name: _execute_apple_plan_locked
        signature: '_execute_apple_plan_locked(*, plan: AppleValidationPlan, source_root: Path, state_root: Path, runner: CommandRunner, environment: Mapping[str, str], ownership: SimulatorOwnership
          | None) -> AppleValidationResult'
        purpose: Implements _execute_apple_plan_locked for Hermetic Apple toolchain, simulator, build, and cleanup execution.
      - name: execute_apple_plan
        signature: 'execute_apple_plan(*, plan: AppleValidationPlan, source_root: Path, state_root: Path, runner: CommandRunner | None=None, environment: Mapping[str, str] | None=None)
          -> AppleValidationResult'
        purpose: Implements execute_apple_plan for Hermetic Apple toolchain, simulator, build, and cleanup execution.
      internal_dependencies:
      - apple_contract
      - apple_types
    - path: src/ci_workflows/apple_multistage.py
      responsibility: Caller-owned Apple protected-full planning and single-executor execution.
      status: consolidate
      classes:
      - name: ProtectedAppleStage
        purpose: Defines ProtectedAppleStage for Caller-owned Apple protected-full planning and single-executor execution.
        methods:
        - name: needs_booted_simulator
          signature: needs_booted_simulator(self) -> bool
          purpose: Implements ProtectedAppleStage.needs_booted_simulator behavior for Caller-owned Apple protected-full planning and single-executor execution.
      - name: ProtectedApplePlan
        purpose: Defines ProtectedApplePlan for Caller-owned Apple protected-full planning and single-executor execution.
        methods:
        - name: private_dependency_used
          signature: private_dependency_used(self) -> bool
          purpose: Implements ProtectedApplePlan.private_dependency_used behavior for Caller-owned Apple protected-full planning and single-executor execution.
        - name: simulator_plans
          signature: simulator_plans(self) -> tuple[AppleValidationPlan, ...]
          purpose: Implements ProtectedApplePlan.simulator_plans behavior for Caller-owned Apple protected-full planning and single-executor execution.
        - name: planning_outputs
          signature: planning_outputs(self) -> dict[str, str]
          purpose: Implements ProtectedApplePlan.planning_outputs behavior for Caller-owned Apple protected-full planning and single-executor execution.
      functions:
      - name: _fail
        signature: '_fail(code: str) -> None'
        purpose: Implements _fail for Caller-owned Apple protected-full planning and single-executor execution.
      - name: _plain
        signature: '_plain(value: object, code: str, *, allow_empty: bool=False, maximum: int=4096) -> str'
        purpose: Implements _plain for Caller-owned Apple protected-full planning and single-executor execution.
      - name: _strings
        signature: '_strings(value: object, code: str, *, maximum_items: int, unique: bool=True) -> tuple[str, ...]'
        purpose: Implements _strings for Caller-owned Apple protected-full planning and single-executor execution.
      - name: _relative
        signature: '_relative(value: object, code: str, *, allow_dot: bool=False) -> str'
        purpose: Implements _relative for Caller-owned Apple protected-full planning and single-executor execution.
      - name: _json_object
        signature: '_json_object(raw: str) -> dict[str, object]'
        purpose: Implements _json_object for Caller-owned Apple protected-full planning and single-executor execution.
      - name: _arguments
        signature: '_arguments(value: object) -> tuple[str, ...]'
        purpose: Implements _arguments for Caller-owned Apple protected-full planning and single-executor execution.
      - name: _paths
        signature: '_paths(value: object, code: str, maximum_items: int=16) -> tuple[str, ...]'
        purpose: Implements _paths for Caller-owned Apple protected-full planning and single-executor execution.
      - name: _simulator_for
        signature: '_simulator_for(contract: Mapping[str, Any], platform: str)'
        purpose: Implements _simulator_for for Caller-owned Apple protected-full planning and single-executor execution.
      - name: _profile
        signature: '_profile(platform: str, operation: str) -> AppleProfile'
        purpose: Implements _profile for Caller-owned Apple protected-full planning and single-executor execution.
      - name: _request
        signature: '_request(*, repository: str, admitted_sha: str, source_trust: str, profile: AppleProfile, working_directory: str, scheme: str | None, platform: str, destination_profile:
          str | None) -> AppleValidationRequest'
        purpose: Implements _request for Caller-owned Apple protected-full planning and single-executor execution.
      - name: _script_stage
        signature: '_script_stage(raw: Mapping[str, object], *, identifier: str, repository: str, admitted_sha: str, source_trust: str, working_directory: str, cleanup_paths: tuple[str,
          ...], expected_outputs: tuple[str, ...], contract: Mapping[str, Any]) -> ProtectedAppleStage'
        purpose: Implements _script_stage for Caller-owned Apple protected-full planning and single-executor execution.
      - name: _xcode_stage
        signature: '_xcode_stage(raw: Mapping[str, object], *, identifier: str, platform: str, operation: str, repository: str, admitted_sha: str, source_trust: str, working_directory:
          str, cleanup_paths: tuple[str, ...], expected_outputs: tuple[str, ...], contract: Mapping[str, Any]) -> ProtectedAppleStage'
        purpose: Implements _xcode_stage for Caller-owned Apple protected-full planning and single-executor execution.
      - name: _stage_plan
        signature: '_stage_plan(raw: Mapping[str, object], *, repository: str, admitted_sha: str, source_trust: str, contract: Mapping[str, Any]) -> ProtectedAppleStage'
        purpose: Implements _stage_plan for Caller-owned Apple protected-full planning and single-executor execution.
      - name: build_protected_full_plan
        signature: 'build_protected_full_plan(raw_json: str, *, repository: str, admitted_sha: str, source_trust: str, contract: Mapping[str, Any], private_dependency_repository: str='''',
          private_dependency_sha: str='''', private_dependency_subdirectory: str=''.'', private_dependency_id: str='''') -> ProtectedApplePlan'
        purpose: Implements build_protected_full_plan for Caller-owned Apple protected-full planning and single-executor execution.
      - name: verify_private_dependency
        signature: 'verify_private_dependency(plan: ProtectedApplePlan, *, workflow_state_root: Path, environment: Mapping[str, str]) -> Path | None'
        purpose: Implements verify_private_dependency for Caller-owned Apple protected-full planning and single-executor execution.
      - name: _tracked_container_hash
        signature: '_tracked_container_hash(source_root: Path, relative: str) -> str'
        purpose: Implements _tracked_container_hash for Caller-owned Apple protected-full planning and single-executor execution.
      - name: _stage_protected_hashes
        signature: '_stage_protected_hashes(stage: ProtectedAppleStage, source_root: Path) -> dict[str, str]'
        purpose: Implements _stage_protected_hashes for Caller-owned Apple protected-full planning and single-executor execution.
      - name: _protected_hashes
        signature: '_protected_hashes(plan: ProtectedApplePlan, source_root: Path) -> tuple[dict[str, str], ...]'
        purpose: Implements _protected_hashes for Caller-owned Apple protected-full planning and single-executor execution.
      - name: _require_protected_unchanged
        signature: '_require_protected_unchanged(plan: ProtectedApplePlan, source_root: Path, before: Sequence[Mapping[str, str]]) -> None'
        purpose: Implements _require_protected_unchanged for Caller-owned Apple protected-full planning and single-executor execution.
      - name: _stage_directories
        signature: '_stage_directories(shared: Mapping[str, Path], stage: ProtectedAppleStage) -> dict[str, Path]'
        purpose: Implements _stage_directories for Caller-owned Apple protected-full planning and single-executor execution.
      - name: execute_protected_full
        signature: 'execute_protected_full(plan: ProtectedApplePlan, *, source_root: Path, state_root: Path, runner: apple_execution.CommandRunner | None=None, environment: Mapping[str,
          str] | None=None) -> dict[str, str]'
        purpose: Implements execute_protected_full for Caller-owned Apple protected-full planning and single-executor execution.
      - name: _cleanup_targets
        signature: '_cleanup_targets(plan: ProtectedApplePlan, source_root: Path, state_root: Path) -> tuple[Path, ...]'
        purpose: Implements _cleanup_targets for Caller-owned Apple protected-full planning and single-executor execution.
      - name: cleanup_protected_full
        signature: 'cleanup_protected_full(plan: ProtectedApplePlan, *, source_root: Path, state_root: Path, runner: apple_execution.CommandRunner | None=None, environment: Mapping[str,
          str] | None=None) -> None'
        purpose: Implements cleanup_protected_full for Caller-owned Apple protected-full planning and single-executor execution.
      - name: assert_zero_protected_full_residue
        signature: 'assert_zero_protected_full_residue(plan: ProtectedApplePlan, *, source_root: Path, state_root: Path, runner: apple_execution.CommandRunner | None=None, environment:
          Mapping[str, str] | None=None) -> None'
        purpose: Implements assert_zero_protected_full_residue for Caller-owned Apple protected-full planning and single-executor execution.
      internal_dependencies:
      - apple_contract
      - apple_execution
      - apple_types
    - path: src/ci_workflows/apple_plan_guard.py
      responsibility: Security guard for caller-owned Apple protected-full plan channels.
      status: consolidate
      classes: []
      functions:
      - name: _fail
        signature: '_fail(code: str) -> None'
        purpose: Implements _fail for Security guard for caller-owned Apple protected-full plan channels.
      - name: _safe_relative
        signature: '_safe_relative(value: object, code: str) -> str'
        purpose: Implements _safe_relative for Security guard for caller-owned Apple protected-full plan channels.
      - name: _guard_xcode_arguments
        signature: '_guard_xcode_arguments(value: object) -> None'
        purpose: Implements _guard_xcode_arguments for Security guard for caller-owned Apple protected-full plan channels.
      - name: _guard_cleanup_paths
        signature: '_guard_cleanup_paths(value: object) -> None'
        purpose: Implements _guard_cleanup_paths for Security guard for caller-owned Apple protected-full plan channels.
      - name: _guard_runtime_operation
        signature: '_guard_runtime_operation(stage: dict[object, object]) -> None'
        purpose: Keep routine protected-full planning free of simulator runtime execution.
      - name: validate_protected_full_plan_json
        signature: 'validate_protected_full_plan_json(raw: str) -> None'
        purpose: Reject unsafe protected-full requests before Apple runner allocation.
      internal_dependencies:
      - apple_types
    - path: src/ci_workflows/apple_primitives.py
      responsibility: Product-neutral Apple build, test, simulator, packaging, and cleanup primitives.
      status: keep
      classes:
      - name: ApplePrimitiveError
        purpose: Stable failure raised by the standalone primitive layer.
        methods: []
      - name: ApplePlatform
        purpose: Defines ApplePlatform for Product-neutral Apple build, test, simulator, packaging, and cleanup primitives.
        methods: []
      - name: XcodeAction
        purpose: Defines XcodeAction for Product-neutral Apple build, test, simulator, packaging, and cleanup primitives.
        methods: []
      - name: CommandOutcome
        purpose: Defines CommandOutcome for Product-neutral Apple build, test, simulator, packaging, and cleanup primitives.
        methods: []
      - name: CommandRunner
        purpose: Defines CommandRunner for Product-neutral Apple build, test, simulator, packaging, and cleanup primitives.
        methods:
        - name: run
          signature: 'run(self, argv: Sequence[str], *, cwd: Path, env: Mapping[str, str], timeout_seconds: int) -> CommandOutcome'
          purpose: Implements CommandRunner.run behavior for Product-neutral Apple build, test, simulator, packaging, and cleanup primitives.
      - name: CommandSpec
        purpose: Defines CommandSpec for Product-neutral Apple build, test, simulator, packaging, and cleanup primitives.
        methods: []
      - name: ToolchainIdentity
        purpose: Defines ToolchainIdentity for Product-neutral Apple build, test, simulator, packaging, and cleanup primitives.
        methods: []
      - name: XcodeBuildRequest
        purpose: Defines XcodeBuildRequest for Product-neutral Apple build, test, simulator, packaging, and cleanup primitives.
        methods: []
      - name: SimulatorRequest
        purpose: Defines SimulatorRequest for Product-neutral Apple build, test, simulator, packaging, and cleanup primitives.
        methods: []
      - name: SimulatorLease
        purpose: Defines SimulatorLease for Product-neutral Apple build, test, simulator, packaging, and cleanup primitives.
        methods: []
      functions:
      - name: _require
        signature: '_require(condition: bool, code: str) -> None'
        purpose: Implements _require for Product-neutral Apple build, test, simulator, packaging, and cleanup primitives.
      - name: _safe_text
        signature: '_safe_text(value: str, code: str) -> str'
        purpose: Implements _safe_text for Product-neutral Apple build, test, simulator, packaging, and cleanup primitives.
      - name: _relative_source_path
        signature: '_relative_source_path(root: Path, relative: str, *, suffix: str | None=None, must_exist: bool=True) -> Path'
        purpose: Implements _relative_source_path for Product-neutral Apple build, test, simulator, packaging, and cleanup primitives.
      - name: _state_path
        signature: '_state_path(root: Path, relative: str) -> Path'
        purpose: Implements _state_path for Product-neutral Apple build, test, simulator, packaging, and cleanup primitives.
      - name: _parse_xcode
        signature: '_parse_xcode(output: str) -> tuple[str, str]'
        purpose: Implements _parse_xcode for Product-neutral Apple build, test, simulator, packaging, and cleanup primitives.
      - name: _parse_swift
        signature: '_parse_swift(output: str) -> str'
        purpose: Implements _parse_swift for Product-neutral Apple build, test, simulator, packaging, and cleanup primitives.
      - name: run_command
        signature: 'run_command(spec: CommandSpec, runner: CommandRunner, *, environment: Mapping[str, str], check: bool=True) -> CommandOutcome'
        purpose: Implements run_command for Product-neutral Apple build, test, simulator, packaging, and cleanup primitives.
      - name: inspect_toolchain
        signature: 'inspect_toolchain(runner: CommandRunner, *, cwd: Path, environment: Mapping[str, str], sdks: Sequence[str]=(''iphoneos'', ''iphonesimulator'', ''appletvos'', ''appletvsimulator'',
          ''macosx'')) -> ToolchainIdentity'
        purpose: Implements inspect_toolchain for Product-neutral Apple build, test, simulator, packaging, and cleanup primitives.
      - name: select_xcode
        signature: 'select_xcode(candidates: Sequence[Path], *, expected_version: str, runner: CommandRunner, cwd: Path, environment: Mapping[str, str], expected_build: str | None=None)
          -> Path'
        purpose: Implements select_xcode for Product-neutral Apple build, test, simulator, packaging, and cleanup primitives.
      - name: plan_xcodebuild
        signature: 'plan_xcodebuild(request: XcodeBuildRequest, *, source_root: Path, state_root: Path) -> CommandSpec'
        purpose: Implements plan_xcodebuild for Product-neutral Apple build, test, simulator, packaging, and cleanup primitives.
      - name: plan_unsigned_package
        signature: 'plan_unsigned_package(source_path: str, output_path: str, *, source_root: Path, state_root: Path) -> CommandSpec'
        purpose: Implements plan_unsigned_package for Product-neutral Apple build, test, simulator, packaging, and cleanup primitives.
      - name: plan_export_archive
        signature: 'plan_export_archive(*, archive_path: str, export_path: str, export_options_plist: str, source_root: Path, state_root: Path, signing_authorized: bool) -> CommandSpec'
        purpose: Implements plan_export_archive for Product-neutral Apple build, test, simulator, packaging, and cleanup primitives.
      - name: create_boot_simulator
        signature: 'create_boot_simulator(request: SimulatorRequest, runner: CommandRunner, *, cwd: Path, environment: Mapping[str, str]) -> SimulatorLease'
        purpose: Implements create_boot_simulator for Product-neutral Apple build, test, simulator, packaging, and cleanup primitives.
      - name: cleanup_simulator
        signature: 'cleanup_simulator(lease: SimulatorLease, runner: CommandRunner, *, cwd: Path, environment: Mapping[str, str]) -> None'
        purpose: Implements cleanup_simulator for Product-neutral Apple build, test, simulator, packaging, and cleanup primitives.
      - name: _remove_no_follow
        signature: '_remove_no_follow(path: Path) -> None'
        purpose: Implements _remove_no_follow for Product-neutral Apple build, test, simulator, packaging, and cleanup primitives.
      - name: cleanup_output_paths
        signature: 'cleanup_output_paths(state_root: Path, relative_paths: Sequence[str]) -> None'
        purpose: Implements cleanup_output_paths for Product-neutral Apple build, test, simulator, packaging, and cleanup primitives.
      internal_dependencies: []
    - path: src/ci_workflows/apple_simulator_confidence.py
      responsibility: Strict product-neutral one-simulator confidence planning for validation.apple.
      status: consolidate
      classes:
      - name: SimulatorConfidencePacket
        purpose: Defines SimulatorConfidencePacket for Strict product-neutral one-simulator confidence planning for validation.apple.
        methods:
        - name: planning_outputs
          signature: planning_outputs(self) -> dict[str, str]
          purpose: Implements SimulatorConfidencePacket.planning_outputs behavior for Strict product-neutral one-simulator confidence planning for validation.apple.
      functions:
      - name: _arguments
        signature: '_arguments(value: object) -> tuple[str, ...]'
        purpose: Implements _arguments for Strict product-neutral one-simulator confidence planning for validation.apple.
      - name: build_simulator_confidence_packet
        signature: 'build_simulator_confidence_packet(raw_json: str, *, repository: str, admitted_sha: str, source_trust: str, contract: Mapping[str, object]) -> SimulatorConfidencePacket'
        purpose: Parse one caller-owned packet and bind it to Central's existing simulator executor.
      - name: confidence_outputs
        signature: 'confidence_outputs(values: Mapping[str, str], packet: SimulatorConfidencePacket) -> dict[str, str]'
        purpose: Mark ordinary Apple result evidence as simulator confidence only.
      internal_dependencies:
      - apple_contract
      - apple_simulator_script
      - apple_types
    - path: src/ci_workflows/apple_simulator_script.py
      responsibility: Internal exact simulator-lease argument handoff for Apple script tasks.
      status: consolidate
      classes:
      - name: SimulatorLeaseArgumentRunner
        purpose: Replace one reserved argument only after Central creates its simulator.
        methods:
        - name: _arguments
          signature: '_arguments(self, argv: Sequence[str]) -> tuple[str, ...]'
          purpose: Implements SimulatorLeaseArgumentRunner._arguments behavior for Internal exact simulator-lease argument handoff for Apple script tasks.
        - name: run
          signature: 'run(self, argv: Sequence[str], *, cwd: Path, env: Mapping[str, str], timeout_seconds: int) -> CommandOutcome'
          purpose: Implements SimulatorLeaseArgumentRunner.run behavior for Internal exact simulator-lease argument handoff for Apple script tasks.
      functions:
      - name: _stream_text
        signature: '_stream_text(value: str | bytes | None) -> str'
        purpose: Implements _stream_text for Internal exact simulator-lease argument handoff for Apple script tasks.
      - name: _private_log_path
        signature: '_private_log_path(environment: Mapping[str, str]) -> Path | None'
        purpose: Return one validated runner-local private log target when explicitly enabled.
      - name: _append_private_output
        signature: '_append_private_output(path: Path | None, stdout: str | bytes | None, stderr: str | bytes | None) -> None'
        purpose: Implements _append_private_output for Internal exact simulator-lease argument handoff for Apple script tasks.
      internal_dependencies:
      - apple_execution
      - apple_types
    - path: src/ci_workflows/apple_types.py
      responsibility: Typed models and stable errors for reusable Apple validation.
      status: consolidate
      classes:
      - name: AppleValidationError
        purpose: Fail-closed Apple validation error carrying one stable code.
        methods: []
      - name: AppleProfile
        purpose: Defines AppleProfile for Typed models and stable errors for reusable Apple validation.
        methods: []
      - name: AppleRunnerCapability
        purpose: Defines AppleRunnerCapability for Typed models and stable errors for reusable Apple validation.
        methods: []
      - name: AppleStage
        purpose: Defines AppleStage for Typed models and stable errors for reusable Apple validation.
        methods: []
      - name: AppleToolchain
        purpose: Defines AppleToolchain for Typed models and stable errors for reusable Apple validation.
        methods: []
      - name: AppleContainer
        purpose: Defines AppleContainer for Typed models and stable errors for reusable Apple validation.
        methods: []
      - name: AppleSimulator
        purpose: Defines AppleSimulator for Typed models and stable errors for reusable Apple validation.
        methods: []
      - name: AppleCommand
        purpose: Defines AppleCommand for Typed models and stable errors for reusable Apple validation.
        methods: []
      - name: AppleValidationRequest
        purpose: Defines AppleValidationRequest for Typed models and stable errors for reusable Apple validation.
        methods: []
      - name: AppleValidationPlan
        purpose: Defines AppleValidationPlan for Typed models and stable errors for reusable Apple validation.
        methods:
        - name: requires_simulator
          signature: requires_simulator(self) -> bool
          purpose: Implements AppleValidationPlan.requires_simulator behavior for Typed models and stable errors for reusable Apple validation.
        - name: planning_outputs
          signature: planning_outputs(self) -> dict[str, str]
          purpose: Implements AppleValidationPlan.planning_outputs behavior for Typed models and stable errors for reusable Apple validation.
      - name: AppleValidationResult
        purpose: Defines AppleValidationResult for Typed models and stable errors for reusable Apple validation.
        methods:
        - name: output_values
          signature: output_values(self) -> dict[str, str]
          purpose: Implements AppleValidationResult.output_values behavior for Typed models and stable errors for reusable Apple validation.
      functions: []
      internal_dependencies: []
    - path: src/ci_workflows/ciw_apple.py
      responsibility: Bounded ``ciw apple validate`` adapter and standalone compatibility CLI.
      status: compatibility
      classes:
      - name: _FailureDiagnosticRunner
        purpose: Emit only failing command diagnostics before Apple cleanup removes state.
        methods:
        - name: _eligible
          signature: '_eligible(argv: Sequence[str]) -> bool'
          purpose: Implements _FailureDiagnosticRunner._eligible behavior for Bounded ``ciw apple validate`` adapter and standalone compatibility CLI.
        - name: _emit
          signature: '_emit(self, summary: str, text: str, cwd: Path) -> None'
          purpose: Implements _FailureDiagnosticRunner._emit behavior for Bounded ``ciw apple validate`` adapter and standalone compatibility CLI.
        - name: _failure_text
          signature: '_failure_text(self, argv: Sequence[str], ordinary: str, cwd: Path) -> str'
          purpose: Implements _FailureDiagnosticRunner._failure_text behavior for Bounded ``ciw apple validate`` adapter and standalone compatibility CLI.
        - name: run
          signature: 'run(self, argv: Sequence[str], *, cwd: Path, env: Mapping[str, str], timeout_seconds: int) -> apple_execution.CommandOutcome'
          purpose: Implements _FailureDiagnosticRunner.run behavior for Bounded ``ciw apple validate`` adapter and standalone compatibility CLI.
      functions:
      - name: _stream_text
        signature: '_stream_text(value: str | bytes | None) -> str'
        purpose: Implements _stream_text for Bounded ``ciw apple validate`` adapter and standalone compatibility CLI.
      - name: _bounded_failure_diagnostic
        signature: '_bounded_failure_diagnostic(text: str, roots: Sequence[Path]) -> str'
        purpose: Return one bounded fail-closed diagnostic safe for ordinary job logs.
      - name: _has_concrete_error
        signature: '_has_concrete_error(text: str) -> bool'
        purpose: Return whether ordinary command output already carries an actionable error.
      - name: _owned_result_bundle
        signature: '_owned_result_bundle(argv: Sequence[str], state_root: Path | None) -> Path | None'
        purpose: Return the exact Central-owned result bundle selected by xcodebuild.
      - name: _read_xcresult_build_results
        signature: '_read_xcresult_build_results(bundle: Path) -> str'
        purpose: Read bounded Xcode build-result JSON from one already-owned result bundle.
      - name: _xcresult_source_location
        signature: '_xcresult_source_location(value: object) -> str'
        purpose: Implements _xcresult_source_location for Bounded ``ciw apple validate`` adapter and standalone compatibility CLI.
      - name: _xcresult_compiler_diagnostic
        signature: '_xcresult_compiler_diagnostic(raw: str, roots: Sequence[Path]) -> str'
        purpose: Project one deterministic compiler error from bounded xcresult JSON.
      - name: _diagnostic_runner
        signature: '_diagnostic_runner(source: Path, state: Path) -> _FailureDiagnosticRunner'
        purpose: Implements _diagnostic_runner for Bounded ``ciw apple validate`` adapter and standalone compatibility CLI.
      - name: configure_apple_validate
        signature: 'configure_apple_validate(parser: argparse.ArgumentParser) -> None'
        purpose: Implements configure_apple_validate for Bounded ``ciw apple validate`` adapter and standalone compatibility CLI.
      - name: _workflow_state_root
        signature: '_workflow_state_root(root: Path, environment: Mapping[str, str]) -> Path'
        purpose: Implements _workflow_state_root for Bounded ``ciw apple validate`` adapter and standalone compatibility CLI.
      - name: _resolved_state_root
        signature: '_resolved_state_root(root: Path, environment: Mapping[str, str]) -> Path'
        purpose: Implements _resolved_state_root for Bounded ``ciw apple validate`` adapter and standalone compatibility CLI.
      - name: _standalone_parser
        signature: _standalone_parser() -> argparse.ArgumentParser
        purpose: Implements _standalone_parser for Bounded ``ciw apple validate`` adapter and standalone compatibility CLI.
      - name: _write_outputs
        signature: '_write_outputs(values: Mapping[str, str], target: str | None) -> None'
        purpose: Implements _write_outputs for Bounded ``ciw apple validate`` adapter and standalone compatibility CLI.
      - name: _planning_outputs
        signature: '_planning_outputs(root: Path, plan: apple_validation.AppleValidationPlan, request: apple_validation.AppleValidationRequest) -> dict[str, str]'
        purpose: Implements _planning_outputs for Bounded ``ciw apple validate`` adapter and standalone compatibility CLI.
      - name: _protected_planning_outputs
        signature: '_protected_planning_outputs(root: Path, plan: apple_multistage.ProtectedApplePlan) -> dict[str, str]'
        purpose: Implements _protected_planning_outputs for Bounded ``ciw apple validate`` adapter and standalone compatibility CLI.
      - name: _source_path
        signature: '_source_path(root: Path, source_root: str, environment: Mapping[str, str]) -> Path'
        purpose: Implements _source_path for Bounded ``ciw apple validate`` adapter and standalone compatibility CLI.
      - name: _run_plan
        signature: '_run_plan(*, plan: apple_validation.AppleValidationPlan, source: Path | None, state: Path | None, environment: Mapping[str, str]) -> apple_validation.AppleValidationPlan
          | apple_validation.AppleValidationResult'
        purpose: Implements _run_plan for Bounded ``ciw apple validate`` adapter and standalone compatibility CLI.
      - name: _source_trust
        signature: '_source_trust(environment: Mapping[str, str]) -> str'
        purpose: Implements _source_trust for Bounded ``ciw apple validate`` adapter and standalone compatibility CLI.
      - name: _protected_plan
        signature: '_protected_plan(context: ''CIWContext'', contract: Mapping[str, object]) -> apple_multistage.ProtectedApplePlan'
        purpose: Implements _protected_plan for Bounded ``ciw apple validate`` adapter and standalone compatibility CLI.
      - name: _private_dependency_execution_environment
        signature: '_private_dependency_execution_environment(environment: Mapping[str, str], plan: apple_multistage.ProtectedApplePlan, dependency: Path | None) -> dict[str, str]'
        purpose: Bind one verified private repository URL to its credential-free local checkout.
      - name: _execute_protected_apple_validate
        signature: '_execute_protected_apple_validate(args: argparse.Namespace, context: ''CIWContext'', contract: Mapping[str, object]) -> ''CIWResult'''
        purpose: Implements _execute_protected_apple_validate for Bounded ``ciw apple validate`` adapter and standalone compatibility CLI.
      - name: standalone_main
        signature: 'standalone_main(argv: Sequence[str] | None=None) -> int'
        purpose: Implements standalone_main for Bounded ``ciw apple validate`` adapter and standalone compatibility CLI.
      - name: execute_apple_validate
        signature: 'execute_apple_validate(args: argparse.Namespace, context: ''CIWContext'') -> ''CIWResult'''
        purpose: Implements execute_apple_validate for Bounded ``ciw apple validate`` adapter and standalone compatibility CLI.
      - name: main
        signature: 'main(argv: Sequence[str] | None=None) -> int'
        purpose: Implements main for Bounded ``ciw apple validate`` adapter and standalone compatibility CLI.
      internal_dependencies:
      - apple
      - apple_contract_fragments
      - apple_execution
      - apple_multistage
      - apple_plan_guard
      - apple_simulator_script
      - ciw_types
      - runners
      - workspace
  core:
    target_module: src/ci_workflows/ciw.py
    responsibility: Product-neutral CIW command registry, shared types, language primitives, and generated foundation documentation.
    files:
    - path: src/ci_workflows/__init__.py
      responsibility: Shared named CI workflow function library.
      status: keep
      classes: []
      functions: []
      internal_dependencies:
      - ciw_types
    - path: src/ci_workflows/ciw.py
      responsibility: Stable typed command registry and dispatcher for shared CI workflow functions.
      status: keep
      classes:
      - name: CommandSpec
        purpose: Defines CommandSpec for Stable typed command registry and dispatcher for shared CI workflow functions.
        methods:
        - name: key
          signature: key(self) -> str
          purpose: Implements CommandSpec.key behavior for Stable typed command registry and dispatcher for shared CI workflow functions.
        - name: qualified_handler
          signature: qualified_handler(self) -> str
          purpose: Implements CommandSpec.qualified_handler behavior for Stable typed command registry and dispatcher for shared CI workflow functions.
      functions:
      - name: _noop
        signature: '_noop(_parser: argparse.ArgumentParser) -> None'
        purpose: Implements _noop for Stable typed command registry and dispatcher for shared CI workflow functions.
      - name: _add_source_revalidate
        signature: '_add_source_revalidate(parser: argparse.ArgumentParser) -> None'
        purpose: Implements _add_source_revalidate for Stable typed command registry and dispatcher for shared CI workflow functions.
      - name: _add_source_checkout
        signature: '_add_source_checkout(parser: argparse.ArgumentParser) -> None'
        purpose: Implements _add_source_checkout for Stable typed command registry and dispatcher for shared CI workflow functions.
      - name: _add_runner_generate
        signature: '_add_runner_generate(parser: argparse.ArgumentParser) -> None'
        purpose: Implements _add_runner_generate for Stable typed command registry and dispatcher for shared CI workflow functions.
      - name: _add_runner_resolve
        signature: '_add_runner_resolve(parser: argparse.ArgumentParser) -> None'
        purpose: Implements _add_runner_resolve for Stable typed command registry and dispatcher for shared CI workflow functions.
      - name: _add_runner_selector
        signature: '_add_runner_selector(parser: argparse.ArgumentParser) -> None'
        purpose: Implements _add_runner_selector for Stable typed command registry and dispatcher for shared CI workflow functions.
      - name: _add_runner_tier
        signature: '_add_runner_tier(parser: argparse.ArgumentParser) -> None'
        purpose: Implements _add_runner_tier for Stable typed command registry and dispatcher for shared CI workflow functions.
      - name: _add_android_validate
        signature: '_add_android_validate(parser: argparse.ArgumentParser) -> None'
        purpose: Implements _add_android_validate for Stable typed command registry and dispatcher for shared CI workflow functions.
      - name: _add_apple_validate
        signature: '_add_apple_validate(parser: argparse.ArgumentParser) -> None'
        purpose: Implements _add_apple_validate for Stable typed command registry and dispatcher for shared CI workflow functions.
      - name: _add_flutter_validate
        signature: '_add_flutter_validate(parser: argparse.ArgumentParser) -> None'
        purpose: Implements _add_flutter_validate for Stable typed command registry and dispatcher for shared CI workflow functions.
      - name: _add_python_validate
        signature: '_add_python_validate(parser: argparse.ArgumentParser) -> None'
        purpose: Implements _add_python_validate for Stable typed command registry and dispatcher for shared CI workflow functions.
      - name: _add_node_validate
        signature: '_add_node_validate(parser: argparse.ArgumentParser) -> None'
        purpose: Implements _add_node_validate for Stable typed command registry and dispatcher for shared CI workflow functions.
      - name: _add_package_publish
        signature: '_add_package_publish(parser: argparse.ArgumentParser) -> None'
        purpose: Implements _add_package_publish for Stable typed command registry and dispatcher for shared CI workflow functions.
      - name: _add_network
        signature: '_add_network(parser: argparse.ArgumentParser) -> None'
        purpose: Implements _add_network for Stable typed command registry and dispatcher for shared CI workflow functions.
      - name: _add_helm_validate
        signature: '_add_helm_validate(parser: argparse.ArgumentParser) -> None'
        purpose: Implements _add_helm_validate for Stable typed command registry and dispatcher for shared CI workflow functions.
      - name: _add_helm_publish
        signature: '_add_helm_publish(parser: argparse.ArgumentParser) -> None'
        purpose: Implements _add_helm_publish for Stable typed command registry and dispatcher for shared CI workflow functions.
      - name: _add_gitops_validate
        signature: '_add_gitops_validate(parser: argparse.ArgumentParser) -> None'
        purpose: Implements _add_gitops_validate for Stable typed command registry and dispatcher for shared CI workflow functions.
      - name: _add_oci_publish
        signature: '_add_oci_publish(parser: argparse.ArgumentParser) -> None'
        purpose: Implements _add_oci_publish for Stable typed command registry and dispatcher for shared CI workflow functions.
      - name: _add_oci_validate
        signature: '_add_oci_validate(parser: argparse.ArgumentParser) -> None'
        purpose: Implements _add_oci_validate for Stable typed command registry and dispatcher for shared CI workflow functions.
      - name: _add_device_validate
        signature: '_add_device_validate(parser: argparse.ArgumentParser) -> None'
        purpose: Implements _add_device_validate for Stable typed command registry and dispatcher for shared CI workflow functions.
      - name: _add_device_lock
        signature: '_add_device_lock(parser: argparse.ArgumentParser) -> None'
        purpose: Implements _add_device_lock for Stable typed command registry and dispatcher for shared CI workflow functions.
      - name: _add_workspace_prepare
        signature: '_add_workspace_prepare(parser: argparse.ArgumentParser) -> None'
        purpose: Implements _add_workspace_prepare for Stable typed command registry and dispatcher for shared CI workflow functions.
      - name: _add_tooling_verify
        signature: '_add_tooling_verify(parser: argparse.ArgumentParser) -> None'
        purpose: Implements _add_tooling_verify for Stable typed command registry and dispatcher for shared CI workflow functions.
      - name: _add_tooling_install
        signature: '_add_tooling_install(parser: argparse.ArgumentParser) -> None'
        purpose: Implements _add_tooling_install for Stable typed command registry and dispatcher for shared CI workflow functions.
      - name: _add_dependency_checkout
        signature: '_add_dependency_checkout(parser: argparse.ArgumentParser) -> None'
        purpose: Implements _add_dependency_checkout for Stable typed command registry and dispatcher for shared CI workflow functions.
      - name: _add_policy_verify
        signature: '_add_policy_verify(parser: argparse.ArgumentParser) -> None'
        purpose: Implements _add_policy_verify for Stable typed command registry and dispatcher for shared CI workflow functions.
      - name: _add_cache_validate
        signature: '_add_cache_validate(parser: argparse.ArgumentParser) -> None'
        purpose: Implements _add_cache_validate for Stable typed command registry and dispatcher for shared CI workflow functions.
      - name: _add_evidence_render
        signature: '_add_evidence_render(parser: argparse.ArgumentParser) -> None'
        purpose: Implements _add_evidence_render for Stable typed command registry and dispatcher for shared CI workflow functions.
      - name: _argument_or_input
        signature: '_argument_or_input(args: argparse.Namespace, context: CIWContext, name: str, default: str='''') -> str'
        purpose: Implements _argument_or_input for Stable typed command registry and dispatcher for shared CI workflow functions.
      - name: _required_argument_or_input
        signature: '_required_argument_or_input(args: argparse.Namespace, context: CIWContext, name: str, *, domain: str, code: str) -> str'
        purpose: Implements _required_argument_or_input for Stable typed command registry and dispatcher for shared CI workflow functions.
      - name: _require_output
        signature: '_require_output(context: CIWContext, *, domain: str) -> None'
        purpose: Implements _require_output for Stable typed command registry and dispatcher for shared CI workflow functions.
      - name: _source_summary
        signature: '_source_summary(result: AdmissionResult) -> str'
        purpose: Implements _source_summary for Stable typed command registry and dispatcher for shared CI workflow functions.
      - name: handle_source_resolve
        signature: 'handle_source_resolve(_args: argparse.Namespace, context: CIWContext) -> CIWResult'
        purpose: Implements handle_source_resolve for Stable typed command registry and dispatcher for shared CI workflow functions.
      - name: _admission_from_json
        signature: '_admission_from_json(raw: str) -> AdmissionResult'
        purpose: Implements _admission_from_json for Stable typed command registry and dispatcher for shared CI workflow functions.
      - name: handle_source_revalidate
        signature: 'handle_source_revalidate(args: argparse.Namespace, context: CIWContext) -> CIWResult'
        purpose: Implements handle_source_revalidate for Stable typed command registry and dispatcher for shared CI workflow functions.
      - name: handle_source_exact_checkout
        signature: 'handle_source_exact_checkout(args: argparse.Namespace, context: CIWContext) -> CIWResult'
        purpose: Implements handle_source_exact_checkout for Stable typed command registry and dispatcher for shared CI workflow functions.
      - name: handle_runners_validate
        signature: 'handle_runners_validate(_args: argparse.Namespace, context: CIWContext) -> CIWResult'
        purpose: Implements handle_runners_validate for Stable typed command registry and dispatcher for shared CI workflow functions.
      - name: handle_runners_generate
        signature: 'handle_runners_generate(args: argparse.Namespace, context: CIWContext) -> CIWResult'
        purpose: Implements handle_runners_generate for Stable typed command registry and dispatcher for shared CI workflow functions.
      - name: _runner_object
        signature: '_runner_object(value: str | None) -> Mapping[str, Any] | None'
        purpose: Implements _runner_object for Stable typed command registry and dispatcher for shared CI workflow functions.
      - name: handle_runners_resolve
        signature: 'handle_runners_resolve(args: argparse.Namespace, context: CIWContext) -> CIWResult'
        purpose: Implements handle_runners_resolve for Stable typed command registry and dispatcher for shared CI workflow functions.
      - name: handle_runners_validate_selector
        signature: 'handle_runners_validate_selector(args: argparse.Namespace, context: CIWContext) -> CIWResult'
        purpose: Implements handle_runners_validate_selector for Stable typed command registry and dispatcher for shared CI workflow functions.
      - name: handle_runners_select_buildah_tier
        signature: 'handle_runners_select_buildah_tier(args: argparse.Namespace, context: CIWContext) -> CIWResult'
        purpose: Implements handle_runners_select_buildah_tier for Stable typed command registry and dispatcher for shared CI workflow functions.
      - name: handle_android_validate
        signature: 'handle_android_validate(args: argparse.Namespace, context: CIWContext) -> CIWResult'
        purpose: Implements handle_android_validate for Stable typed command registry and dispatcher for shared CI workflow functions.
      - name: handle_apple_validate
        signature: 'handle_apple_validate(args: argparse.Namespace, context: CIWContext) -> CIWResult'
        purpose: Implements handle_apple_validate for Stable typed command registry and dispatcher for shared CI workflow functions.
      - name: handle_flutter_validate
        signature: 'handle_flutter_validate(args: argparse.Namespace, context: CIWContext) -> CIWResult'
        purpose: Implements handle_flutter_validate for Stable typed command registry and dispatcher for shared CI workflow functions.
      - name: handle_python_validate
        signature: 'handle_python_validate(args: argparse.Namespace, context: CIWContext) -> CIWResult'
        purpose: Implements handle_python_validate for Stable typed command registry and dispatcher for shared CI workflow functions.
      - name: handle_node_validate
        signature: 'handle_node_validate(args: argparse.Namespace, context: CIWContext) -> CIWResult'
        purpose: Implements handle_node_validate for Stable typed command registry and dispatcher for shared CI workflow functions.
      - name: handle_package_publish
        signature: 'handle_package_publish(args: argparse.Namespace, context: CIWContext) -> CIWResult'
        purpose: Implements handle_package_publish for Stable typed command registry and dispatcher for shared CI workflow functions.
      - name: handle_native_validate
        signature: 'handle_native_validate(_args: argparse.Namespace, context: CIWContext) -> CIWResult'
        purpose: Implements handle_native_validate for Stable typed command registry and dispatcher for shared CI workflow functions.
      - name: handle_static_web_validate
        signature: 'handle_static_web_validate(args: argparse.Namespace, context: CIWContext) -> CIWResult'
        purpose: Implements handle_static_web_validate for Stable typed command registry and dispatcher for shared CI workflow functions.
      - name: handle_network
        signature: 'handle_network(args: argparse.Namespace, context: CIWContext) -> CIWResult'
        purpose: Implements handle_network for Stable typed command registry and dispatcher for shared CI workflow functions.
      - name: handle_helm_validate
        signature: 'handle_helm_validate(args: argparse.Namespace, context: CIWContext) -> CIWResult'
        purpose: Implements handle_helm_validate for Stable typed command registry and dispatcher for shared CI workflow functions.
      - name: handle_helm_publish
        signature: 'handle_helm_publish(args: argparse.Namespace, context: CIWContext) -> CIWResult'
        purpose: Implements handle_helm_publish for Stable typed command registry and dispatcher for shared CI workflow functions.
      - name: handle_gitops_validate
        signature: 'handle_gitops_validate(args: argparse.Namespace, context: CIWContext) -> CIWResult'
        purpose: Implements handle_gitops_validate for Stable typed command registry and dispatcher for shared CI workflow functions.
      - name: handle_oci_publish
        signature: 'handle_oci_publish(args: argparse.Namespace, context: CIWContext) -> CIWResult'
        purpose: Implements handle_oci_publish for Stable typed command registry and dispatcher for shared CI workflow functions.
      - name: handle_oci_validate
        signature: 'handle_oci_validate(args: argparse.Namespace, context: CIWContext) -> CIWResult'
        purpose: Implements handle_oci_validate for Stable typed command registry and dispatcher for shared CI workflow functions.
      - name: handle_device_validate
        signature: 'handle_device_validate(args: argparse.Namespace, context: CIWContext) -> CIWResult'
        purpose: Implements handle_device_validate for Stable typed command registry and dispatcher for shared CI workflow functions.
      - name: handle_device_lock
        signature: 'handle_device_lock(args: argparse.Namespace, context: CIWContext) -> CIWResult'
        purpose: Implements handle_device_lock for Stable typed command registry and dispatcher for shared CI workflow functions.
      - name: _foundation_environment
        signature: '_foundation_environment(context: CIWContext, name: str, *, required: bool=True, default: str='''') -> str'
        purpose: Implements _foundation_environment for Stable typed command registry and dispatcher for shared CI workflow functions.
      - name: _state_root
        signature: '_state_root(context: CIWContext) -> Path'
        purpose: Implements _state_root for Stable typed command registry and dispatcher for shared CI workflow functions.
      - name: handle_workspace_prepare
        signature: 'handle_workspace_prepare(args: argparse.Namespace, context: CIWContext) -> CIWResult'
        purpose: Implements handle_workspace_prepare for Stable typed command registry and dispatcher for shared CI workflow functions.
      - name: handle_workspace_cleanup
        signature: 'handle_workspace_cleanup(_args: argparse.Namespace, context: CIWContext) -> CIWResult'
        purpose: Implements handle_workspace_cleanup for Stable typed command registry and dispatcher for shared CI workflow functions.
      - name: handle_tooling_verify
        signature: 'handle_tooling_verify(args: argparse.Namespace, context: CIWContext) -> CIWResult'
        purpose: Implements handle_tooling_verify for Stable typed command registry and dispatcher for shared CI workflow functions.
      - name: handle_tooling_install_asset
        signature: 'handle_tooling_install_asset(args: argparse.Namespace, context: CIWContext) -> CIWResult'
        purpose: Implements handle_tooling_install_asset for Stable typed command registry and dispatcher for shared CI workflow functions.
      - name: handle_dependencies_checkout_private
        signature: 'handle_dependencies_checkout_private(args: argparse.Namespace, context: CIWContext) -> CIWResult'
        purpose: Implements handle_dependencies_checkout_private for Stable typed command registry and dispatcher for shared CI workflow functions.
      - name: handle_policy_verify_repository
        signature: 'handle_policy_verify_repository(args: argparse.Namespace, context: CIWContext) -> CIWResult'
        purpose: Implements handle_policy_verify_repository for Stable typed command registry and dispatcher for shared CI workflow functions.
      - name: handle_policy_validate_cache
        signature: 'handle_policy_validate_cache(args: argparse.Namespace, context: CIWContext) -> CIWResult'
        purpose: Implements handle_policy_validate_cache for Stable typed command registry and dispatcher for shared CI workflow functions.
      - name: handle_evidence_render
        signature: 'handle_evidence_render(args: argparse.Namespace, context: CIWContext) -> CIWResult'
        purpose: Implements handle_evidence_render for Stable typed command registry and dispatcher for shared CI workflow functions.
      - name: _release_provider
        signature: '_release_provider(context: CIWContext) -> GitHubTagProvider'
        purpose: Implements _release_provider for Stable typed command registry and dispatcher for shared CI workflow functions.
      - name: _release_outputs
        signature: '_release_outputs(authority: Any) -> dict[str, str]'
        purpose: Implements _release_outputs for Stable typed command registry and dispatcher for shared CI workflow functions.
      - name: handle_release_tag_resolve
        signature: 'handle_release_tag_resolve(_args: argparse.Namespace, context: CIWContext) -> CIWResult'
        purpose: Implements handle_release_tag_resolve for Stable typed command registry and dispatcher for shared CI workflow functions.
      - name: handle_release_tag_revalidate
        signature: 'handle_release_tag_revalidate(_args: argparse.Namespace, context: CIWContext) -> CIWResult'
        purpose: Implements handle_release_tag_revalidate for Stable typed command registry and dispatcher for shared CI workflow functions.
      - name: command_specs
        signature: command_specs() -> tuple[CommandSpec, ...]
        purpose: Implements command_specs for Stable typed command registry and dispatcher for shared CI workflow functions.
      - name: runtime_command_index
        signature: runtime_command_index() -> dict[str, CommandSpec]
        purpose: Implements runtime_command_index for Stable typed command registry and dispatcher for shared CI workflow functions.
      - name: validate_runtime_contract
        signature: 'validate_runtime_contract(root: Path) -> None'
        purpose: Implements validate_runtime_contract for Stable typed command registry and dispatcher for shared CI workflow functions.
      - name: parser
        signature: parser() -> argparse.ArgumentParser
        purpose: Implements parser for Stable typed command registry and dispatcher for shared CI workflow functions.
      - name: main
        signature: 'main(argv: Sequence[str] | None=None, *, environment: Mapping[str, str] | None=None, stdout: Any | None=None, stderr: Any | None=None) -> int'
        purpose: Implements main for Stable typed command registry and dispatcher for shared CI workflow functions.
      internal_dependencies:
      - ciw_android
      - ciw_apple
      - ciw_device
      - ciw_device_lock
      - ciw_docs
      - ciw_flutter
      - ciw_gitops
      - ciw_helm
      - ciw_native
      - ciw_network
      - ciw_node
      - ciw_oci
      - ciw_packages
      - ciw_python
      - ciw_types
      - ciw_web
      - dependencies
      - evidence
      - foundation_types
      - policy
      - release_tag_authority
      - runners
      - source_admission
      - source_checkout
      - source_cli
      - source_github
      - source_types
      - tooling
      - workspace
    - path: src/ci_workflows/ciw_docs.py
      responsibility: Deterministic contract validation and documentation for the ``ciw`` command tree.
      status: consolidate
      classes: []
      functions:
      - name: _require
        signature: '_require(condition: bool, code: str) -> None'
        purpose: Implements _require for Deterministic contract validation and documentation for the ``ciw`` command tree.
      - name: _safe_path
        signature: '_safe_path(value: Any, code: str) -> str'
        purpose: Implements _safe_path for Deterministic contract validation and documentation for the ``ciw`` command tree.
      - name: _strings
        signature: '_strings(value: Any, code: str, *, allow_empty: bool=True) -> list[str]'
        purpose: Implements _strings for Deterministic contract validation and documentation for the ``ciw`` command tree.
      - name: _command_key
        signature: '_command_key(command: Mapping[str, Any]) -> str'
        purpose: Implements _command_key for Deterministic contract validation and documentation for the ``ciw`` command tree.
      - name: _validate_aliases
        signature: '_validate_aliases(aliases: Mapping[str, str], commands: set[str]) -> None'
        purpose: Implements _validate_aliases for Deterministic contract validation and documentation for the ``ciw`` command tree.
      - name: validate_command_contract
        signature: 'validate_command_contract(payload: Mapping[str, Any]) -> tuple[str, ...]'
        purpose: Validate the checked-in command registry and return stable command keys.
      - name: load_command_contract
        signature: 'load_command_contract(root: Path) -> Mapping[str, Any]'
        purpose: Implements load_command_contract for Deterministic contract validation and documentation for the ``ciw`` command tree.
      - name: _inline
        signature: '_inline(values: Sequence[str]) -> str'
        purpose: Implements _inline for Deterministic contract validation and documentation for the ``ciw`` command tree.
      - name: render_ciw_docs
        signature: 'render_ciw_docs(*, contract_root: Path) -> str'
        purpose: Implements render_ciw_docs for Deterministic contract validation and documentation for the ``ciw`` command tree.
      - name: write_ciw_docs
        signature: 'write_ciw_docs(*, contract_root: Path, check: bool=False) -> Path'
        purpose: Implements write_ciw_docs for Deterministic contract validation and documentation for the ``ciw`` command tree.
      internal_dependencies:
      - ciw_types
    - path: src/ci_workflows/ciw_types.py
      responsibility: Typed result, error, context, and GitHub command-file primitives for ``ciw``.
      status: consolidate
      classes:
      - name: CIWError
        purpose: Fail-closed projected error containing only a stable non-secret code.
        methods: []
      - name: CIWContext
        purpose: Immutable command execution context.
        methods: []
      - name: CIWResult
        purpose: Stable typed result emitted by one registered command.
        methods:
        - name: emit
          signature: 'emit(self, context: CIWContext) -> None'
          purpose: Implements CIWResult.emit behavior for Typed result, error, context, and GitHub command-file primitives for ``ciw``.
      functions:
      - name: _validated_values
        signature: '_validated_values(values: Mapping[str, str]) -> dict[str, str]'
        purpose: Implements _validated_values for Typed result, error, context, and GitHub command-file primitives for ``ciw``.
      - name: write_command_file
        signature: 'write_command_file(path: Path, values: Mapping[str, str]) -> None'
        purpose: Append validated single-line values to one GitHub command file.
      - name: required_environment
        signature: 'required_environment(environment: Mapping[str, str], name: str, *, domain: str, code: str | None=None) -> str'
        purpose: Implements required_environment for Typed result, error, context, and GitHub command-file primitives for ``ciw``.
      - name: input_value
        signature: 'input_value(environment: Mapping[str, str], name: str, default: str='''') -> str'
        purpose: Implements input_value for Typed result, error, context, and GitHub command-file primitives for ``ciw``.
      - name: project_error
        signature: 'project_error(error: BaseException, *, domain: str) -> CIWError'
        purpose: Project domain failures without leaking arbitrary exception text.
      - name: default_context
        signature: 'default_context(root: Path, *, stdout: TextIO, stderr: TextIO) -> CIWContext'
        purpose: Implements default_context for Typed result, error, context, and GitHub command-file primitives for ``ciw``.
      internal_dependencies: []
    - path: src/ci_workflows/foundation_cli.py
      responsibility: Compatibility adapter for shared non-language foundation commands.
      status: compatibility
      classes: []
      functions:
      - name: parser
        signature: parser() -> argparse.ArgumentParser
        purpose: Implements parser for Compatibility adapter for shared non-language foundation commands.
      - name: _ciw_arguments
        signature: '_ciw_arguments(arguments: argparse.Namespace) -> list[str] | None'
        purpose: Implements _ciw_arguments for Compatibility adapter for shared non-language foundation commands.
      - name: main
        signature: 'main(argv: Sequence[str] | None=None) -> int'
        purpose: Preserve the existing flat CLI while delegating to ``ciw``.
      internal_dependencies:
      - ciw
    - path: src/ci_workflows/foundation_docs.py
      responsibility: Deterministic documentation renderer for shared foundation primitives.
      status: consolidate
      classes: []
      functions:
      - name: _inline
        signature: '_inline(values: Any) -> str'
        purpose: Implements _inline for Deterministic documentation renderer for shared foundation primitives.
      - name: render_foundation_docs
        signature: 'render_foundation_docs(*, contract_root: Path) -> str'
        purpose: Implements render_foundation_docs for Deterministic documentation renderer for shared foundation primitives.
      - name: write_foundation_docs
        signature: 'write_foundation_docs(*, contract_root: Path, check: bool=False) -> Path'
        purpose: Implements write_foundation_docs for Deterministic documentation renderer for shared foundation primitives.
      internal_dependencies:
      - foundation_types
    - path: src/ci_workflows/foundation_types.py
      responsibility: Shared typed validation helpers for non-language CI foundation primitives.
      status: consolidate
      classes:
      - name: FoundationError
        purpose: Fail-closed error carrying only a stable non-secret instruction code.
        methods: []
      - name: ContractLocation
        purpose: Defines ContractLocation for Shared typed validation helpers for non-language CI foundation primitives.
        methods:
        - name: path
          signature: path(self) -> Path
          purpose: Implements ContractLocation.path behavior for Shared typed validation helpers for non-language CI foundation primitives.
      functions:
      - name: require
        signature: 'require(condition: bool, instruction: str) -> None'
        purpose: Implements require for Shared typed validation helpers for non-language CI foundation primitives.
      - name: full_sha
        signature: 'full_sha(value: Any, instruction: str=''exact_sha_required'') -> str'
        purpose: Implements full_sha for Shared typed validation helpers for non-language CI foundation primitives.
      - name: sha256_hex
        signature: 'sha256_hex(value: Any, instruction: str=''sha256_required'') -> str'
        purpose: Implements sha256_hex for Shared typed validation helpers for non-language CI foundation primitives.
      - name: safe_id
        signature: 'safe_id(value: Any, instruction: str=''invalid_safe_id'') -> str'
        purpose: Implements safe_id for Shared typed validation helpers for non-language CI foundation primitives.
      - name: safe_name
        signature: 'safe_name(value: Any, instruction: str=''invalid_safe_name'') -> str'
        purpose: Implements safe_name for Shared typed validation helpers for non-language CI foundation primitives.
      - name: repository_name
        signature: 'repository_name(value: Any, instruction: str=''invalid_repository'') -> str'
        purpose: Implements repository_name for Shared typed validation helpers for non-language CI foundation primitives.
      - name: bounded_int
        signature: 'bounded_int(value: Any, *, minimum: int, maximum: int, instruction: str) -> int'
        purpose: Implements bounded_int for Shared typed validation helpers for non-language CI foundation primitives.
      - name: safe_relative_path
        signature: 'safe_relative_path(value: Any, instruction: str=''invalid_relative_path'') -> str'
        purpose: Implements safe_relative_path for Shared typed validation helpers for non-language CI foundation primitives.
      - name: bounded_path
        signature: 'bounded_path(root: Path, relative: str, instruction: str=''path_outside_root'') -> Path'
        purpose: Implements bounded_path for Shared typed validation helpers for non-language CI foundation primitives.
      - name: ensure_no_symlink_escape
        signature: 'ensure_no_symlink_escape(root: Path, target: Path) -> None'
        purpose: Implements ensure_no_symlink_escape for Shared typed validation helpers for non-language CI foundation primitives.
      - name: load_contract
        signature: 'load_contract(root: Path, relative_path: str) -> Mapping[str, Any]'
        purpose: Implements load_contract for Shared typed validation helpers for non-language CI foundation primitives.
      - name: canonical_json
        signature: 'canonical_json(value: Any) -> str'
        purpose: Implements canonical_json for Shared typed validation helpers for non-language CI foundation primitives.
      - name: stable_identifier
        signature: 'stable_identifier(prefix: str, value: Any, *, length: int=20) -> str'
        purpose: Implements stable_identifier for Shared typed validation helpers for non-language CI foundation primitives.
      - name: atomic_write_text
        signature: 'atomic_write_text(path: Path, content: str, *, mode: int=384) -> None'
        purpose: Implements atomic_write_text for Shared typed validation helpers for non-language CI foundation primitives.
      - name: atomic_write_json
        signature: 'atomic_write_json(path: Path, value: Any, *, mode: int=384) -> None'
        purpose: Implements atomic_write_json for Shared typed validation helpers for non-language CI foundation primitives.
      internal_dependencies: []
    - path: src/ci_workflows/language_primitives.py
      responsibility: Product-neutral language and build primitives for shared CI functions.
      status: keep
      classes:
      - name: LanguagePrimitiveError
        purpose: Stable redacted language/build failure.
        methods: []
      - name: CommandOutcome
        purpose: Compatibility result for injected test/process boundaries.
        methods: []
      - name: OperationResult
        purpose: Defines OperationResult for Product-neutral language and build primitives for shared CI functions.
        methods: []
      - name: PythonVenv
        purpose: Defines PythonVenv for Product-neutral language and build primitives for shared CI functions.
        methods: []
      - name: NodeRuntime
        purpose: Defines NodeRuntime for Product-neutral language and build primitives for shared CI functions.
        methods: []
      - name: JavaRuntime
        purpose: Defines JavaRuntime for Product-neutral language and build primitives for shared CI functions.
        methods: []
      - name: CommandRunner
        purpose: Defines CommandRunner for Product-neutral language and build primitives for shared CI functions.
        methods:
        - name: run
          signature: 'run(self, argv: Sequence[str], *, cwd: Path, env: Mapping[str, str]) -> ProcessResult | CommandOutcome'
          purpose: Implements CommandRunner.run behavior for Product-neutral language and build primitives for shared CI functions.
      - name: RuntimeCommandRunner
        purpose: Adapt the generic runtime process primitive to this module's test boundary.
        methods:
        - name: run
          signature: 'run(self, argv: Sequence[str], *, cwd: Path, env: Mapping[str, str]) -> ProcessResult'
          purpose: Implements RuntimeCommandRunner.run behavior for Product-neutral language and build primitives for shared CI functions.
      functions:
      - name: _fail
        signature: '_fail(code: str, operation: str, *, returncode: int | None=None) -> None'
        purpose: Implements _fail for Product-neutral language and build primitives for shared CI functions.
      - name: _argument
        signature: '_argument(value: str, field: str) -> str'
        purpose: Implements _argument for Product-neutral language and build primitives for shared CI functions.
      - name: _arguments
        signature: '_arguments(values: Sequence[str], field: str) -> tuple[str, ...]'
        purpose: Implements _arguments for Product-neutral language and build primitives for shared CI functions.
      - name: _environment
        signature: '_environment(value: Mapping[str, str] | None) -> dict[str, str]'
        purpose: Implements _environment for Product-neutral language and build primitives for shared CI functions.
      - name: _directory
        signature: '_directory(path: Path, operation: str) -> Path'
        purpose: Implements _directory for Product-neutral language and build primitives for shared CI functions.
      - name: _project_file
        signature: '_project_file(project_directory: Path, path: Path, operation: str, *, executable: bool=False) -> Path'
        purpose: Implements _project_file for Product-neutral language and build primitives for shared CI functions.
      - name: _tool
        signature: '_tool(path: Path, operation: str) -> Path'
        purpose: Implements _tool for Product-neutral language and build primitives for shared CI functions.
      - name: _resolve
        signature: '_resolve(candidates: Sequence[str], *, operation: str, search_path: str | None) -> Path'
        purpose: Implements _resolve for Product-neutral language and build primitives for shared CI functions.
      - name: _android_failure_diagnostic
        signature: '_android_failure_diagnostic(operation: str, outcome: ProcessResult | CommandOutcome, cwd: Path) -> None'
        purpose: Implements _android_failure_diagnostic for Product-neutral language and build primitives for shared CI functions.
      - name: _execute
        signature: '_execute(operation: str, argv: Sequence[str], *, cwd: Path, environment: Mapping[str, str] | None, runner: CommandRunner | None) -> OperationResult'
        purpose: Implements _execute for Product-neutral language and build primitives for shared CI functions.
      - name: resolve_python_interpreter
        signature: 'resolve_python_interpreter(candidates: Sequence[str]=(''python3.12'', ''python3''), *, search_path: str | None=None) -> Path'
        purpose: Implements resolve_python_interpreter for Product-neutral language and build primitives for shared CI functions.
      - name: create_python_venv
        signature: 'create_python_venv(interpreter: Path, venv_directory: Path, *, working_directory: Path, clear: bool=False, system_site_packages: bool=False, upgrade_deps: bool=False,
          environment: Mapping[str, str] | None=None, runner: CommandRunner | None=None) -> PythonVenv'
        purpose: Implements create_python_venv for Product-neutral language and build primitives for shared CI functions.
      - name: install_python_dependencies
        signature: 'install_python_dependencies(interpreter: Path, *, project_directory: Path, requirement_files: Sequence[Path]=(), install_project: bool=False, editable_project: bool=False,
          options: Sequence[str]=(), environment: Mapping[str, str] | None=None, runner: CommandRunner | None=None) -> OperationResult'
        purpose: Implements install_python_dependencies for Product-neutral language and build primitives for shared CI functions.
      - name: run_python_module
        signature: 'run_python_module(interpreter: Path, module: str, *, project_directory: Path, arguments: Sequence[str]=(), environment: Mapping[str, str] | None=None, runner: CommandRunner
          | None=None, operation: str=''python.module'') -> OperationResult'
        purpose: Implements run_python_module for Product-neutral language and build primitives for shared CI functions.
      - name: run_python_script
        signature: 'run_python_script(interpreter: Path, script: Path, *, project_directory: Path, arguments: Sequence[str]=(), environment: Mapping[str, str] | None=None, runner: CommandRunner
          | None=None) -> OperationResult'
        purpose: Implements run_python_script for Product-neutral language and build primitives for shared CI functions.
      - name: run_python_tests
        signature: 'run_python_tests(interpreter: Path, *, project_directory: Path, arguments: Sequence[str]=(), test_module: str=''pytest'', environment: Mapping[str, str] | None=None,
          runner: CommandRunner | None=None) -> OperationResult'
        purpose: Implements run_python_tests for Product-neutral language and build primitives for shared CI functions.
      - name: resolve_node_runtime
        signature: 'resolve_node_runtime(node_candidates: Sequence[str]=(''node'',), package_manager_candidates: Sequence[str]=(''npm'',), *, search_path: str | None=None) -> NodeRuntime'
        purpose: Implements resolve_node_runtime for Product-neutral language and build primitives for shared CI functions.
      - name: install_node_dependencies
        signature: 'install_node_dependencies(package_manager: Path, *, project_directory: Path, mode: Literal[''ci'', ''install'']=''ci'', options: Sequence[str]=(), environment: Mapping[str,
          str] | None=None, runner: CommandRunner | None=None) -> OperationResult'
        purpose: Implements install_node_dependencies for Product-neutral language and build primitives for shared CI functions.
      - name: run_node_package_script
        signature: 'run_node_package_script(package_manager: Path, script: str, *, project_directory: Path, arguments: Sequence[str]=(), environment: Mapping[str, str] | None=None, runner:
          CommandRunner | None=None) -> OperationResult'
        purpose: Implements run_node_package_script for Product-neutral language and build primitives for shared CI functions.
      - name: resolve_java_executable
        signature: 'resolve_java_executable(candidates: Sequence[str]=(''java'',), *, search_path: str | None=None) -> Path'
        purpose: Implements resolve_java_executable for Product-neutral language and build primitives for shared CI functions.
      - name: _java_major
        signature: '_java_major(version: str) -> int'
        purpose: Implements _java_major for Product-neutral language and build primitives for shared CI functions.
      - name: inspect_java_runtime
        signature: 'inspect_java_runtime(executable: Path, *, working_directory: Path, environment: Mapping[str, str] | None=None, runner: CommandRunner | None=None) -> JavaRuntime'
        purpose: Implements inspect_java_runtime for Product-neutral language and build primitives for shared CI functions.
      - name: validate_java_runtime
        signature: 'validate_java_runtime(runtime: JavaRuntime, *, expected_major: int, exact_version: str | None=None) -> JavaRuntime'
        purpose: Implements validate_java_runtime for Product-neutral language and build primitives for shared CI functions.
      - name: _gradle_task
        signature: '_gradle_task(value: str, operation: str) -> str'
        purpose: Implements _gradle_task for Product-neutral language and build primitives for shared CI functions.
      - name: run_gradle_tasks
        signature: 'run_gradle_tasks(wrapper_path: Path, tasks: Sequence[str], *, project_directory: Path, options: Sequence[str]=(), environment: Mapping[str, str] | None=None, runner:
          CommandRunner | None=None, operation: str=''gradle.tasks'') -> OperationResult'
        purpose: Implements run_gradle_tasks for Product-neutral language and build primitives for shared CI functions.
      - name: _android
        signature: '_android(operation: str, wrapper_path: Path, task: str, *, project_directory: Path, options: Sequence[str], environment: Mapping[str, str] | None, runner: CommandRunner
          | None) -> OperationResult'
        purpose: Implements _android for Product-neutral language and build primitives for shared CI functions.
      - name: android_build
        signature: 'android_build(wrapper_path: Path, task: str, *, project_directory: Path, options: Sequence[str]=(), environment: Mapping[str, str] | None=None, runner: CommandRunner
          | None=None) -> OperationResult'
        purpose: Implements android_build for Product-neutral language and build primitives for shared CI functions.
      - name: android_assemble
        signature: 'android_assemble(wrapper_path: Path, task: str, *, project_directory: Path, options: Sequence[str]=(), environment: Mapping[str, str] | None=None, runner: CommandRunner
          | None=None) -> OperationResult'
        purpose: Implements android_assemble for Product-neutral language and build primitives for shared CI functions.
      - name: android_unit_test
        signature: 'android_unit_test(wrapper_path: Path, task: str, *, project_directory: Path, options: Sequence[str]=(), environment: Mapping[str, str] | None=None, runner: CommandRunner
          | None=None) -> OperationResult'
        purpose: Implements android_unit_test for Product-neutral language and build primitives for shared CI functions.
      - name: android_lint
        signature: 'android_lint(wrapper_path: Path, task: str, *, project_directory: Path, options: Sequence[str]=(), environment: Mapping[str, str] | None=None, runner: CommandRunner
          | None=None) -> OperationResult'
        purpose: Implements android_lint for Product-neutral language and build primitives for shared CI functions.
      - name: android_targeted_test
        signature: 'android_targeted_test(wrapper_path: Path, task: str, test_selector: str, *, project_directory: Path, options: Sequence[str]=(), environment: Mapping[str, str] | None=None,
          runner: CommandRunner | None=None) -> OperationResult'
        purpose: Implements android_targeted_test for Product-neutral language and build primitives for shared CI functions.
      - name: _pub_restore
        signature: '_pub_restore(operation: str, executable: Path, *, project_directory: Path, environment: Mapping[str, str] | None, runner: CommandRunner | None) -> OperationResult'
        purpose: Implements _pub_restore for Product-neutral language and build primitives for shared CI functions.
      - name: flutter_restore
        signature: 'flutter_restore(executable: Path, *, project_directory: Path, environment: Mapping[str, str] | None=None, runner: CommandRunner | None=None) -> OperationResult'
        purpose: Implements flutter_restore for Product-neutral language and build primitives for shared CI functions.
      - name: dart_restore
        signature: 'dart_restore(executable: Path, *, project_directory: Path, environment: Mapping[str, str] | None=None, runner: CommandRunner | None=None) -> OperationResult'
        purpose: Implements dart_restore for Product-neutral language and build primitives for shared CI functions.
      - name: run_flutter_operation
        signature: 'run_flutter_operation(executable: Path, operation: Literal[''build'', ''test'', ''analyze''], *, project_directory: Path, arguments: Sequence[str]=(), environment:
          Mapping[str, str] | None=None, runner: CommandRunner | None=None) -> OperationResult'
        purpose: Implements run_flutter_operation for Product-neutral language and build primitives for shared CI functions.
      - name: run_dart_operation
        signature: 'run_dart_operation(executable: Path, operation: Literal[''test'', ''analyze''], *, project_directory: Path, arguments: Sequence[str]=(), environment: Mapping[str,
          str] | None=None, runner: CommandRunner | None=None) -> OperationResult'
        purpose: Implements run_dart_operation for Product-neutral language and build primitives for shared CI functions.
      internal_dependencies:
      - runtime_primitives
  device:
    target_module: src/ci_workflows/device.py
    responsibility: Physical/synthetic device admission, planning, lifecycle, execution, evidence, and fencing.
    files:
    - path: src/ci_workflows/ciw_device.py
      responsibility: Bounded ``ciw device validate`` adapter and standalone compatibility CLI.
      status: compatibility
      classes: []
      functions:
      - name: configure_device_validate
        signature: 'configure_device_validate(parser: argparse.ArgumentParser) -> None'
        purpose: Implements configure_device_validate for Bounded ``ciw device validate`` adapter and standalone compatibility CLI.
      - name: _standalone_parser
        signature: _standalone_parser() -> argparse.ArgumentParser
        purpose: Implements _standalone_parser for Bounded ``ciw device validate`` adapter and standalone compatibility CLI.
      - name: _write_outputs
        signature: '_write_outputs(values: Mapping[str, str], target: str | None) -> None'
        purpose: Implements _write_outputs for Bounded ``ciw device validate`` adapter and standalone compatibility CLI.
      - name: _resolved_state_root
        signature: '_resolved_state_root(root: Path, environment: Mapping[str, str]) -> Path'
        purpose: Implements _resolved_state_root for Bounded ``ciw device validate`` adapter and standalone compatibility CLI.
      - name: _bounded_relative_file
        signature: '_bounded_relative_file(root: Path, value: str) -> Path'
        purpose: Implements _bounded_relative_file for Bounded ``ciw device validate`` adapter and standalone compatibility CLI.
      - name: _source_path
        signature: '_source_path(root: Path, relative: str, environment: Mapping[str, str]) -> Path'
        purpose: Implements _source_path for Bounded ``ciw device validate`` adapter and standalone compatibility CLI.
      - name: _approved_base_runs_on_json
        signature: '_approved_base_runs_on_json(runner_contract: Mapping[str, object], device_contract: Mapping[str, object], plan: device_validation.DevicePlan) -> str'
        purpose: Implements _approved_base_runs_on_json for Bounded ``ciw device validate`` adapter and standalone compatibility CLI.
      - name: _runs_on_json
        signature: '_runs_on_json(root: Path, plan: device_validation.DevicePlan, device_contract: Mapping[str, object] | None=None) -> str'
        purpose: Implements _runs_on_json for Bounded ``ciw device validate`` adapter and standalone compatibility CLI.
      - name: _unique_json_object
        signature: '_unique_json_object(pairs: list[tuple[str, object]]) -> dict[str, object]'
        purpose: Implements _unique_json_object for Bounded ``ciw device validate`` adapter and standalone compatibility CLI.
      - name: _authorization_receipt
        signature: '_authorization_receipt(environment: Mapping[str, str]) -> str'
        purpose: Canonicalize the transported JSON secret before exact authorization checks.
      - name: _execute_command
        signature: '_execute_command(*, root: Path, command: str, source_root: str, inventory_fixture: str, environment: Mapping[str, str]) -> dict[str, str]'
        purpose: Implements _execute_command for Bounded ``ciw device validate`` adapter and standalone compatibility CLI.
      - name: standalone_main
        signature: 'standalone_main(argv: Sequence[str] | None=None) -> int'
        purpose: Implements standalone_main for Bounded ``ciw device validate`` adapter and standalone compatibility CLI.
      - name: execute_device_validate
        signature: 'execute_device_validate(args: argparse.Namespace, context: ''CIWContext'') -> ''CIWResult'''
        purpose: Implements execute_device_validate for Bounded ``ciw device validate`` adapter and standalone compatibility CLI.
      - name: main
        signature: 'main(argv: Sequence[str] | None=None) -> int'
        purpose: Implements main for Bounded ``ciw device validate`` adapter and standalone compatibility CLI.
      internal_dependencies:
      - ciw_types
      - device
      - runners
      - workspace
    - path: src/ci_workflows/ciw_device_lock.py
      responsibility: Thin command adapter for the production device-lock contract.
      status: compatibility
      classes: []
      functions:
      - name: configure_device_lock
        signature: 'configure_device_lock(parser: argparse.ArgumentParser) -> None'
        purpose: Register the one bounded device-lock phase selector.
      - name: _parser
        signature: _parser() -> argparse.ArgumentParser
        purpose: Implements _parser for Thin command adapter for the production device-lock contract.
      - name: _write_outputs
        signature: '_write_outputs(values: Mapping[str, str], target: str | None) -> None'
        purpose: Implements _write_outputs for Thin command adapter for the production device-lock contract.
      - name: execute_phase
        signature: 'execute_phase(phase: str, *, contract_root: Path, environment: Mapping[str, str]) -> dict[str, str]'
        purpose: Implements execute_phase for Thin command adapter for the production device-lock contract.
      - name: execute_device_lock
        signature: 'execute_device_lock(args: argparse.Namespace, context: CIWContext) -> CIWResult'
        purpose: Execute one registered ``ciw device lock`` phase.
      - name: main
        signature: 'main(argv: Sequence[str] | None=None, *, environment: Mapping[str, str] | None=None, stdout: TextIO | None=None, stderr: TextIO | None=None) -> int'
        purpose: Implements main for Thin command adapter for the production device-lock contract.
      internal_dependencies:
      - ciw_types
      - device_lock
    - path: src/ci_workflows/device.py
      responsibility: Public façade for reusable physical-device validation.
      status: keep
      classes: []
      functions:
      - name: plan_from_environment
        signature: 'plan_from_environment(contract_root: Path, environment: Mapping[str, str]) -> DevicePlan'
        purpose: Implements plan_from_environment for Public façade for reusable physical-device validation.
      - name: synthetic_validate
        signature: 'synthetic_validate(*, contract_root: Path, environment: Mapping[str, str], inventory_text: str, now_values: Sequence[int]=(1000, 1001, 1002)) -> DeviceResult'
        purpose: Implements synthetic_validate for Public façade for reusable physical-device validation.
      internal_dependencies:
      - device_contract
      - device_execution
      - device_types
    - path: src/ci_workflows/device_admission.py
      responsibility: GitHub-derived device source admission and generic request parsing.
      status: consolidate
      classes: []
      functions:
      - name: _boolean_text
        signature: '_boolean_text(value: str, code: str) -> bool'
        purpose: Implements _boolean_text for GitHub-derived device source admission and generic request parsing.
      - name: source_trust_from_environment
        signature: 'source_trust_from_environment(environment: Mapping[str, str], admitted_sha: str) -> str'
        purpose: Derive trust only from fixed GitHub event metadata, never from inputs.
      - name: optional_input
        signature: 'optional_input(environment: Mapping[str, str], key: str) -> str | None'
        purpose: Implements optional_input for GitHub-derived device source admission and generic request parsing.
      - name: _positive_int
        signature: '_positive_int(value: str, code: str) -> int'
        purpose: Implements _positive_int for GitHub-derived device source admission and generic request parsing.
      - name: _arguments
        signature: '_arguments(raw: str) -> tuple[str, ...]'
        purpose: Implements _arguments for GitHub-derived device source admission and generic request parsing.
      - name: _caller_environment
        signature: '_caller_environment(raw: str) -> Mapping[str, str]'
        purpose: Implements _caller_environment for GitHub-derived device source admission and generic request parsing.
      - name: request_from_environment
        signature: 'request_from_environment(environment: Mapping[str, str], contract: Mapping[str, Any]) -> DeviceRequest'
        purpose: Implements request_from_environment for GitHub-derived device source admission and generic request parsing.
      internal_dependencies:
      - device_contract_common
      - device_types
    - path: src/ci_workflows/device_cleanup.py
      responsibility: No-follow checkout and device-state cleanup with exact checkout validation.
      status: consolidate
      classes: []
      functions:
      - name: _lstat
        signature: '_lstat(path: Path) -> os.stat_result | None'
        purpose: Implements _lstat for No-follow checkout and device-state cleanup with exact checkout validation.
      - name: remove_no_follow
        signature: 'remove_no_follow(path: Path) -> None'
        purpose: Implements remove_no_follow for No-follow checkout and device-state cleanup with exact checkout validation.
      - name: registered_state_paths
        signature: 'registered_state_paths(state_root: Path) -> tuple[Path, ...]'
        purpose: Implements registered_state_paths for No-follow checkout and device-state cleanup with exact checkout validation.
      - name: cleanup_device_state
        signature: 'cleanup_device_state(state_root: Path) -> None'
        purpose: Implements cleanup_device_state for No-follow checkout and device-state cleanup with exact checkout validation.
      - name: assert_zero_device_residue
        signature: 'assert_zero_device_residue(state_root: Path) -> None'
        purpose: Implements assert_zero_device_residue for No-follow checkout and device-state cleanup with exact checkout validation.
      - name: cleanup_checkout_path
        signature: 'cleanup_checkout_path(workspace: Path, relative: str) -> None'
        purpose: Remove only the fixed `.ciw` or `source` checkout without following links.
      - name: validate_exact_checkout
        signature: 'validate_exact_checkout(source_root: Path, expected_sha: str) -> None'
        purpose: Implements validate_exact_checkout for No-follow checkout and device-state cleanup with exact checkout validation.
      internal_dependencies:
      - device_contract_common
      - device_types
    - path: src/ci_workflows/device_contract.py
      responsibility: Checked-in physical-device contract, source admission, and plan resolution.
      status: consolidate
      classes: []
      functions: []
      internal_dependencies:
      - device_admission
      - device_contract_common
      - device_plan_contract
      - device_profile_contract
    - path: src/ci_workflows/device_contract_common.py
      responsibility: Shared bounded helpers for the device contract modules.
      status: consolidate
      classes: []
      functions:
      - name: fail
        signature: 'fail(code: str) -> None'
        purpose: Implements fail for Shared bounded helpers for the device contract modules.
      - name: require
        signature: 'require(condition: bool, code: str) -> None'
        purpose: Implements require for Shared bounded helpers for the device contract modules.
      - name: _read_json
        signature: '_read_json(path: Path, code: str=''invalid_input'') -> Mapping[str, Any]'
        purpose: Implements _read_json for Shared bounded helpers for the device contract modules.
      - name: strings
        signature: 'strings(value: Any, *, nonempty: bool=False, unique: bool=True, code: str=''invalid_input'') -> list[str]'
        purpose: Implements strings for Shared bounded helpers for the device contract modules.
      - name: safe_relative
        signature: 'safe_relative(value: Any, code: str=''invalid_input'') -> str'
        purpose: Implements safe_relative for Shared bounded helpers for the device contract modules.
      - name: version_tuple
        signature: 'version_tuple(value: str) -> tuple[int, ...]'
        purpose: Implements version_tuple for Shared bounded helpers for the device contract modules.
      - name: parse_request_id
        signature: 'parse_request_id(value: str) -> int'
        purpose: Implements parse_request_id for Shared bounded helpers for the device contract modules.
      internal_dependencies:
      - device_types
    - path: src/ci_workflows/device_evidence.py
      responsibility: Deterministic redacted device evidence construction and validation.
      status: consolidate
      classes: []
      functions:
      - name: _timestamp
        signature: '_timestamp(value: int) -> str'
        purpose: Implements _timestamp for Deterministic redacted device evidence construction and validation.
      - name: _artifact_inventory
        signature: '_artifact_inventory(retained: Sequence[Mapping[str, object]]) -> list[dict[str, object]]'
        purpose: Implements _artifact_inventory for Deterministic redacted device evidence construction and validation.
      - name: _scope_map
        signature: '_scope_map(evidence_contract: Mapping[str, object], *, synthetic: bool) -> Mapping[str, object]'
        purpose: Implements _scope_map for Deterministic redacted device evidence construction and validation.
      - name: _limitations
        signature: '_limitations(evidence_contract: Mapping[str, object], *, synthetic: bool) -> list[str]'
        purpose: Implements _limitations for Deterministic redacted device evidence construction and validation.
      - name: build_evidence_packet
        signature: 'build_evidence_packet(*, plan: DevicePlan, selected: SelectedDevice, lock_receipt: LockReceipt, release_receipt: LockReleaseReceipt | None, evidence_contract: Mapping[str,
          object], started_at: int, ended_at: int, result: str, failure_code: str, assertions: Sequence[str], restoration: str, cleanup: str, retained_evidence: Sequence[Mapping[str,
          object]]=(), certification_scope: str | None=None, synthetic: bool=False) -> dict[str, object]'
        purpose: Implements build_evidence_packet for Deterministic redacted device evidence construction and validation.
      - name: _walk_keys
        signature: '_walk_keys(value: object) -> Sequence[str]'
        purpose: Implements _walk_keys for Deterministic redacted device evidence construction and validation.
      - name: validate_evidence_packet
        signature: 'validate_evidence_packet(packet: Mapping[str, object], evidence_contract: Mapping[str, object], *, raw_identifier: str) -> None'
        purpose: Implements validate_evidence_packet for Deterministic redacted device evidence construction and validation.
      - name: build_stable_physical_evidence
        signature: 'build_stable_physical_evidence(*, contract_root: Path, repository: str, source_sha: str, workflow_run_id: int, job_id: int, device_family: str, request_id: str, result:
          str, cleanup_result: str, evidence_id: str, validation_profile: str | None=None, toolchain_profile: str | None=None) -> dict[str, object]'
        purpose: 'Route any durable physical proof through the shared #137 allowlist.'
      - name: evidence_id
        signature: 'evidence_id(packet: Mapping[str, object]) -> str'
        purpose: Implements evidence_id for Deterministic redacted device evidence construction and validation.
      internal_dependencies:
      - device_contract_common
      - device_types
      - physical_log_policy
    - path: src/ci_workflows/device_execution.py
      responsibility: Compatibility façade for issue-owned device execution primitives.
      status: compatibility
      classes: []
      functions: []
      internal_dependencies:
      - device_cleanup
      - device_evidence
      - device_inventory
      - device_lifecycle
      - device_live
      - device_live_safe
      - device_test_lock
    - path: src/ci_workflows/device_inventory.py
      responsibility: Bounded synthetic inventory parsing and opaque device selection.
      status: consolidate
      classes: []
      functions:
      - name: _boolean
        signature: '_boolean(value: object) -> bool'
        purpose: Implements _boolean for Bounded synthetic inventory parsing and opaque device selection.
      - name: parse_android_inventory
        signature: 'parse_android_inventory(raw: str) -> tuple[DeviceRecord, ...]'
        purpose: Implements parse_android_inventory for Bounded synthetic inventory parsing and opaque device selection.
      - name: parse_apple_inventory
        signature: 'parse_apple_inventory(raw: str, family: DeviceFamily) -> tuple[DeviceRecord, ...]'
        purpose: Implements parse_apple_inventory for Bounded synthetic inventory parsing and opaque device selection.
      - name: parse_inventory
        signature: 'parse_inventory(family: DeviceFamily, raw: str) -> tuple[DeviceRecord, ...]'
        purpose: Implements parse_inventory for Bounded synthetic inventory parsing and opaque device selection.
      - name: stable_identity_hash
        signature: 'stable_identity_hash(profile_id: str, family: DeviceFamily, raw_identifier: str) -> str'
        purpose: Implements stable_identity_hash for Bounded synthetic inventory parsing and opaque device selection.
      - name: _version_allowed
        signature: '_version_allowed(plan: DevicePlan, record: DeviceRecord) -> bool'
        purpose: Implements _version_allowed for Bounded synthetic inventory parsing and opaque device selection.
      - name: _record_allowed
        signature: '_record_allowed(plan: DevicePlan, record: DeviceRecord) -> bool'
        purpose: Implements _record_allowed for Bounded synthetic inventory parsing and opaque device selection.
      - name: select_device
        signature: 'select_device(plan: DevicePlan, records: Sequence[DeviceRecord]) -> SelectedDevice'
        purpose: Implements select_device for Bounded synthetic inventory parsing and opaque device selection.
      internal_dependencies:
      - device_contract_common
      - device_types
    - path: src/ci_workflows/device_lifecycle.py
      responsibility: Restoration-first synthetic device lifecycle orchestration.
      status: consolidate
      classes:
      - name: DeviceRuntime
        purpose: Defines DeviceRuntime for Restoration-first synthetic device lifecycle orchestration.
        methods:
        - name: snapshot
          signature: 'snapshot(self, selected: SelectedDevice) -> Mapping[str, object]'
          purpose: Implements DeviceRuntime.snapshot behavior for Restoration-first synthetic device lifecycle orchestration.
        - name: prepare
          signature: 'prepare(self, plan: DevicePlan, selected: SelectedDevice) -> None'
          purpose: Implements DeviceRuntime.prepare behavior for Restoration-first synthetic device lifecycle orchestration.
        - name: test
          signature: 'test(self, plan: DevicePlan, selected: SelectedDevice) -> Sequence[str]'
          purpose: Implements DeviceRuntime.test behavior for Restoration-first synthetic device lifecycle orchestration.
        - name: collect_evidence
          signature: 'collect_evidence(self, plan: DevicePlan, selected: SelectedDevice) -> Sequence[str]'
          purpose: Implements DeviceRuntime.collect_evidence behavior for Restoration-first synthetic device lifecycle orchestration.
        - name: connected
          signature: 'connected(self, selected: SelectedDevice) -> bool'
          purpose: Implements DeviceRuntime.connected behavior for Restoration-first synthetic device lifecycle orchestration.
        - name: restore
          signature: 'restore(self, selected: SelectedDevice, snapshot: Mapping[str, object]) -> None'
          purpose: Implements DeviceRuntime.restore behavior for Restoration-first synthetic device lifecycle orchestration.
        - name: cleanup
          signature: 'cleanup(self, plan: DevicePlan, selected: SelectedDevice) -> None'
          purpose: Implements DeviceRuntime.cleanup behavior for Restoration-first synthetic device lifecycle orchestration.
        - name: residue
          signature: 'residue(self, plan: DevicePlan, selected: SelectedDevice) -> Sequence[str]'
          purpose: Implements DeviceRuntime.residue behavior for Restoration-first synthetic device lifecycle orchestration.
      - name: SyntheticDeviceRuntime
        purpose: Defines SyntheticDeviceRuntime for Restoration-first synthetic device lifecycle orchestration.
        methods:
        - name: _maybe_fail
          signature: '_maybe_fail(self, stage: str, code: str) -> None'
          purpose: Implements SyntheticDeviceRuntime._maybe_fail behavior for Restoration-first synthetic device lifecycle orchestration.
        - name: snapshot
          signature: 'snapshot(self, selected: SelectedDevice) -> Mapping[str, object]'
          purpose: Implements SyntheticDeviceRuntime.snapshot behavior for Restoration-first synthetic device lifecycle orchestration.
        - name: prepare
          signature: 'prepare(self, plan: DevicePlan, selected: SelectedDevice) -> None'
          purpose: Implements SyntheticDeviceRuntime.prepare behavior for Restoration-first synthetic device lifecycle orchestration.
        - name: test
          signature: 'test(self, plan: DevicePlan, selected: SelectedDevice) -> Sequence[str]'
          purpose: Implements SyntheticDeviceRuntime.test behavior for Restoration-first synthetic device lifecycle orchestration.
        - name: collect_evidence
          signature: 'collect_evidence(self, plan: DevicePlan, selected: SelectedDevice) -> Sequence[str]'
          purpose: Implements SyntheticDeviceRuntime.collect_evidence behavior for Restoration-first synthetic device lifecycle orchestration.
        - name: connected
          signature: 'connected(self, selected: SelectedDevice) -> bool'
          purpose: Implements SyntheticDeviceRuntime.connected behavior for Restoration-first synthetic device lifecycle orchestration.
        - name: restore
          signature: 'restore(self, selected: SelectedDevice, snapshot: Mapping[str, object]) -> None'
          purpose: Implements SyntheticDeviceRuntime.restore behavior for Restoration-first synthetic device lifecycle orchestration.
        - name: cleanup
          signature: 'cleanup(self, plan: DevicePlan, selected: SelectedDevice) -> None'
          purpose: Implements SyntheticDeviceRuntime.cleanup behavior for Restoration-first synthetic device lifecycle orchestration.
        - name: residue
          signature: 'residue(self, plan: DevicePlan, selected: SelectedDevice) -> Sequence[str]'
          purpose: Implements SyntheticDeviceRuntime.residue behavior for Restoration-first synthetic device lifecycle orchestration.
      functions:
      - name: _initial_assertions
        signature: '_initial_assertions(*, synthetic: bool) -> list[str]'
        purpose: Implements _initial_assertions for Restoration-first synthetic device lifecycle orchestration.
      - name: execute_device_plan
        signature: 'execute_device_plan(*, plan: DevicePlan, records: Sequence[DeviceRecord], lock_adapter: DeviceLockAdapter, runtime: DeviceRuntime, evidence_contract: Mapping[str,
          object], now: Callable[[], int], synthetic_authorized: bool=False) -> DeviceResult'
        purpose: Implements execute_device_plan for Restoration-first synthetic device lifecycle orchestration.
      internal_dependencies:
      - device_contract_common
      - device_evidence
      - device_inventory
      - device_test_lock
      - device_types
    - path: src/ci_workflows/device_live.py
      responsibility: Production physical-device discovery and bounded execution helpers.
      status: consolidate
      classes: []
      functions:
      - name: validate_authorization_receipt
        signature: 'validate_authorization_receipt(raw: str, *, plan: DevicePlan, now_epoch: int | None=None) -> str'
        purpose: Validate one owner-provisioned exact-request authorization receipt.
      - name: _private_command
        signature: '_private_command(argv: Sequence[str], *, cwd: Path, environment: Mapping[str, str], timeout_seconds: int=30, failure_code: str=''device_inventory_malformed'') ->
          str'
        purpose: Run a command while intentionally retaining no stdout/stderr in CI logs.
      - name: _android_record
        signature: '_android_record(plan: DevicePlan, *, cwd: Path, environment: Mapping[str, str]) -> DeviceRecord'
        purpose: Implements _android_record for Production physical-device discovery and bounded execution helpers.
      - name: _apple_record
        signature: '_apple_record(plan: DevicePlan, *, cwd: Path, private_root: Path, environment: Mapping[str, str]) -> DeviceRecord'
        purpose: Implements _apple_record for Production physical-device discovery and bounded execution helpers.
      - name: _private_root
        signature: '_private_root(state_root: Path) -> Path'
        purpose: Implements _private_root for Production physical-device discovery and bounded execution helpers.
      - name: discover_live_device
        signature: 'discover_live_device(plan: DevicePlan, *, source_root: Path, state_root: Path, environment: Mapping[str, str]) -> SelectedDevice'
        purpose: Discover exactly one eligible physical device without logging its identity.
      - name: load_selected_device
        signature: 'load_selected_device(*, state_root: Path, plan: DevicePlan, expected_identity_hash: str) -> SelectedDevice'
        purpose: Implements load_selected_device for Production physical-device discovery and bounded execution helpers.
      - name: verify_production_lock
        signature: 'verify_production_lock(*, contract_root: Path, plan: DevicePlan, selected: SelectedDevice, authorization_receipt: str, resource_lock_receipt: str, environment: Mapping[str,
          str], minimum_remaining_seconds: int=1) -> None'
        purpose: Implements verify_production_lock for Production physical-device discovery and bounded execution helpers.
      - name: _bounded_script
        signature: '_bounded_script(source_root: Path, relative: str) -> Path'
        purpose: Implements _bounded_script for Production physical-device discovery and bounded execution helpers.
      - name: _run_product_stage
        signature: '_run_product_stage(source_root: Path, script_path: str, *, args: Sequence[str], environment: Mapping[str, str], timeout_seconds: int, failure_code: str) -> None'
        purpose: Implements _run_product_stage for Production physical-device discovery and bounded execution helpers.
      internal_dependencies:
      - device_contract_common
      - device_inventory
      - device_lock
      - device_types
    - path: src/ci_workflows/device_live_safe.py
      responsibility: Secret-minimized generic live-device execution wrappers.
      status: consolidate
      classes: []
      functions:
      - name: product_environment
        signature: 'product_environment(*, environment: Mapping[str, str], plan: DevicePlan, selected: SelectedDevice) -> dict[str, str]'
        purpose: Return the bounded product environment without Central authority material.
      - name: cleanup_live_device
        signature: 'cleanup_live_device(*, contract_root: Path, plan: DevicePlan, source_root: Path, state_root: Path, selected_identity_hash: str, authorization_receipt: str, resource_lock_receipt:
          str, environment: Mapping[str, str]) -> None'
        purpose: Run the caller's one restoration/cleanup stage while the lock is valid.
      - name: execute_live_device
        signature: 'execute_live_device(*, contract_root: Path, plan: DevicePlan, source_root: Path, state_root: Path, selected_identity_hash: str, authorization_receipt: str, resource_lock_receipt:
          str, environment: Mapping[str, str]) -> DeviceResult'
        purpose: Run prepare/test/evidence only; workflow restoration performs cleanup once.
      internal_dependencies:
      - device_contract_common
      - device_live
      - device_types
    - path: src/ci_workflows/device_lock.py
      responsibility: Production-capable cross-run fencing for physical-device validation.
      status: keep
      classes:
      - name: DeviceLockError
        purpose: Fail closed with one stable, non-sensitive public code.
        methods: []
      - name: DeviceLockContract
        purpose: Defines DeviceLockContract for Production-capable cross-run fencing for physical-device validation.
        methods: []
      - name: DeviceLockOwner
        purpose: Defines DeviceLockOwner for Production-capable cross-run fencing for physical-device validation.
        methods:
        - name: from_environment
          signature: 'from_environment(cls, environment: Mapping[str, str]) -> ''DeviceLockOwner'''
          purpose: Implements DeviceLockOwner.from_environment behavior for Production-capable cross-run fencing for physical-device validation.
        - name: identity_hash
          signature: identity_hash(self) -> str
          purpose: Implements DeviceLockOwner.identity_hash behavior for Production-capable cross-run fencing for physical-device validation.
      - name: DeviceLockRequest
        purpose: Defines DeviceLockRequest for Production-capable cross-run fencing for physical-device validation.
        methods:
        - name: create
          signature: 'create(cls, contract: DeviceLockContract, *, device_family: str, device_capability: str, device_identity_hash: str, tested_source_sha: str, authorization_receipt:
            str, owner: DeviceLockOwner, request_id: str, lease_seconds: int) -> ''DeviceLockRequest'''
          purpose: Implements DeviceLockRequest.create behavior for Production-capable cross-run fencing for physical-device validation.
        - name: resource_key_hash
          signature: resource_key_hash(self) -> str
          purpose: Hash one exact physical device, independent of requested capability.
      - name: DeviceLockReceipt
        purpose: Defines DeviceLockReceipt for Production-capable cross-run fencing for physical-device validation.
        methods:
        - name: payload
          signature: payload(self) -> dict[str, object]
          purpose: Implements DeviceLockReceipt.payload behavior for Production-capable cross-run fencing for physical-device validation.
        - name: encode
          signature: encode(self) -> str
          purpose: Implements DeviceLockReceipt.encode behavior for Production-capable cross-run fencing for physical-device validation.
        - name: receipt_id
          signature: receipt_id(self) -> str
          purpose: Implements DeviceLockReceipt.receipt_id behavior for Production-capable cross-run fencing for physical-device validation.
        - name: from_payload
          signature: 'from_payload(cls, contract: DeviceLockContract, payload: Mapping[str, object]) -> ''DeviceLockReceipt'''
          purpose: Implements DeviceLockReceipt.from_payload behavior for Production-capable cross-run fencing for physical-device validation.
        - name: decode
          signature: 'decode(cls, contract: DeviceLockContract, value: str) -> ''DeviceLockReceipt'''
          purpose: Implements DeviceLockReceipt.decode behavior for Production-capable cross-run fencing for physical-device validation.
      - name: DeviceLockReleaseEvidence
        purpose: Defines DeviceLockReleaseEvidence for Production-capable cross-run fencing for physical-device validation.
        methods:
        - name: marker_payload
          signature: marker_payload(self) -> dict[str, str]
          purpose: Implements DeviceLockReleaseEvidence.marker_payload behavior for Production-capable cross-run fencing for physical-device validation.
      - name: PosixDeviceLockBackend
        purpose: Cross-process lease backend over a pre-provisioned shared POSIX root.
        methods:
        - name: _lease_path
          signature: '_lease_path(self, resource_key_hash: str) -> Path'
          purpose: Implements PosixDeviceLockBackend._lease_path behavior for Production-capable cross-run fencing for physical-device validation.
        - name: _release_path
          signature: '_release_path(self, resource_key_hash: str) -> Path'
          purpose: Implements PosixDeviceLockBackend._release_path behavior for Production-capable cross-run fencing for physical-device validation.
        - name: _current
          signature: '_current(self, resource_key_hash: str) -> DeviceLockReceipt | None'
          purpose: Implements PosixDeviceLockBackend._current behavior for Production-capable cross-run fencing for physical-device validation.
        - name: _last_release
          signature: '_last_release(self, resource_key_hash: str) -> DeviceLockReleaseEvidence | None'
          purpose: Implements PosixDeviceLockBackend._last_release behavior for Production-capable cross-run fencing for physical-device validation.
        - name: acquire
          signature: 'acquire(self, request: DeviceLockRequest) -> DeviceLockReceipt'
          purpose: Implements PosixDeviceLockBackend.acquire behavior for Production-capable cross-run fencing for physical-device validation.
        - name: verify
          signature: 'verify(self, encoded_receipt: str, request: DeviceLockRequest, *, minimum_remaining_seconds: int=1) -> DeviceLockReceipt'
          purpose: Implements PosixDeviceLockBackend.verify behavior for Production-capable cross-run fencing for physical-device validation.
        - name: release
          signature: 'release(self, encoded_receipt: str, request: DeviceLockRequest) -> DeviceLockReleaseEvidence'
          purpose: Implements PosixDeviceLockBackend.release behavior for Production-capable cross-run fencing for physical-device validation.
        - name: assert_released
          signature: 'assert_released(self, encoded_receipt: str, request: DeviceLockRequest) -> DeviceLockReleaseEvidence'
          purpose: Implements PosixDeviceLockBackend.assert_released behavior for Production-capable cross-run fencing for physical-device validation.
      functions:
      - name: _require
        signature: '_require(condition: bool, code: str) -> None'
        purpose: Implements _require for Production-capable cross-run fencing for physical-device validation.
      - name: _canonical
        signature: '_canonical(value: object) -> str'
        purpose: Implements _canonical for Production-capable cross-run fencing for physical-device validation.
      - name: _sha256_text
        signature: '_sha256_text(value: str) -> str'
        purpose: Implements _sha256_text for Production-capable cross-run fencing for physical-device validation.
      - name: _opaque_text
        signature: '_opaque_text(value: object, *, minimum: int=8, maximum: int=4096) -> str'
        purpose: Implements _opaque_text for Production-capable cross-run fencing for physical-device validation.
      - name: load_device_lock_contract
        signature: 'load_device_lock_contract(root: Path) -> DeviceLockContract'
        purpose: Implements load_device_lock_contract for Production-capable cross-run fencing for physical-device validation.
      - name: _release_evidence
        signature: '_release_evidence(receipt: DeviceLockReceipt) -> DeviceLockReleaseEvidence'
        purpose: Implements _release_evidence for Production-capable cross-run fencing for physical-device validation.
      - name: _validate_release_marker
        signature: '_validate_release_marker(contract: DeviceLockContract, payload: Mapping[str, object]) -> DeviceLockReleaseEvidence'
        purpose: Implements _validate_release_marker for Production-capable cross-run fencing for physical-device validation.
      - name: _mode
        signature: '_mode(info: os.stat_result) -> int'
        purpose: Implements _mode for Production-capable cross-run fencing for physical-device validation.
      - name: _safe_control_directory
        signature: '_safe_control_directory(path: Path, *, create: bool) -> None'
        purpose: Implements _safe_control_directory for Production-capable cross-run fencing for physical-device validation.
      - name: resolve_backend_root
        signature: 'resolve_backend_root(contract: DeviceLockContract, environment: Mapping[str, str] | None=None) -> Path'
        purpose: Implements resolve_backend_root for Production-capable cross-run fencing for physical-device validation.
      - name: _open_flags
        signature: '_open_flags(base: int) -> int'
        purpose: Implements _open_flags for Production-capable cross-run fencing for physical-device validation.
      - name: _fsync_directory
        signature: '_fsync_directory(path: Path) -> None'
        purpose: Implements _fsync_directory for Production-capable cross-run fencing for physical-device validation.
      - name: _read_json_file
        signature: '_read_json_file(path: Path) -> Mapping[str, object] | None'
        purpose: Implements _read_json_file for Production-capable cross-run fencing for physical-device validation.
      - name: _write_json_file
        signature: '_write_json_file(path: Path, payload: Mapping[str, object]) -> None'
        purpose: Implements _write_json_file for Production-capable cross-run fencing for physical-device validation.
      - name: _unlink_regular
        signature: '_unlink_regular(path: Path) -> None'
        purpose: Implements _unlink_regular for Production-capable cross-run fencing for physical-device validation.
      - name: _transaction
        signature: '_transaction(root: Path, contract: DeviceLockContract) -> Iterator[None]'
        purpose: Implements _transaction for Production-capable cross-run fencing for physical-device validation.
      - name: _matches_request
        signature: '_matches_request(receipt: DeviceLockReceipt, request: DeviceLockRequest) -> bool'
        purpose: Implements _matches_request for Production-capable cross-run fencing for physical-device validation.
      - name: locked_device_session
        signature: 'locked_device_session(backend: PosixDeviceLockBackend, request: DeviceLockRequest) -> Iterator[DeviceLockReceipt]'
        purpose: Acquire one lease and expected-state release it on every Python exit path.
      - name: request_from_environment
        signature: 'request_from_environment(*, contract_root: Path, environment: Mapping[str, str]) -> tuple[DeviceLockRequest, DeviceLockOwner]'
        purpose: Implements request_from_environment for Production-capable cross-run fencing for physical-device validation.
      internal_dependencies: []
    - path: src/ci_workflows/device_plan_contract.py
      responsibility: Contract-owned generic device plan construction and typed-plan verification.
      status: consolidate
      classes: []
      functions:
      - name: build_plan
        signature: 'build_plan(contract: Mapping[str, Any], request: DeviceRequest) -> DevicePlan'
        purpose: Implements build_plan for Contract-owned generic device plan construction and typed-plan verification.
      - name: validate_typed_plan
        signature: 'validate_typed_plan(raw_plan: str, expected_hash: str, *, contract: Mapping[str, Any], environment: Mapping[str, str]) -> Mapping[str, Any]'
        purpose: Implements validate_typed_plan for Contract-owned generic device plan construction and typed-plan verification.
      internal_dependencies:
      - device_admission
      - device_contract_common
      - device_profile_contract
      - device_types
    - path: src/ci_workflows/device_primitives.py
      responsibility: Product-neutral device discovery, selection, lease, execution, and cleanup primitives.
      status: consolidate
      classes:
      - name: DevicePrimitiveError
        purpose: Fail closed with one stable, non-sensitive public error code.
        methods: []
      - name: DeviceDescriptor
        purpose: Normalized device metadata with a runner-private identifier held only in memory.
        methods:
        - name: public_projection
          signature: public_projection(self) -> dict[str, object]
          purpose: Implements DeviceDescriptor.public_projection behavior for Product-neutral device discovery, selection, lease, execution, and cleanup primitives.
      - name: DeviceQuery
        purpose: Defines DeviceQuery for Product-neutral device discovery, selection, lease, execution, and cleanup primitives.
        methods: []
      - name: CommandOutcome
        purpose: Defines CommandOutcome for Product-neutral device discovery, selection, lease, execution, and cleanup primitives.
        methods: []
      - name: DeviceLease
        purpose: Defines DeviceLease for Product-neutral device discovery, selection, lease, execution, and cleanup primitives.
        methods:
        - name: public_projection
          signature: public_projection(self) -> dict[str, str]
          purpose: Implements DeviceLease.public_projection behavior for Product-neutral device discovery, selection, lease, execution, and cleanup primitives.
      - name: DeviceOperationResult
        purpose: Defines DeviceOperationResult for Product-neutral device discovery, selection, lease, execution, and cleanup primitives.
        methods:
        - name: public_projection
          signature: public_projection(self) -> dict[str, object]
          purpose: Implements DeviceOperationResult.public_projection behavior for Product-neutral device discovery, selection, lease, execution, and cleanup primitives.
      - name: DeviceDiscoveryBackend
        purpose: Defines DeviceDiscoveryBackend for Product-neutral device discovery, selection, lease, execution, and cleanup primitives.
        methods:
        - name: discover
          signature: 'discover(self, *, environment: Mapping[str, str]) -> Sequence[Mapping[str, object]]'
          purpose: Implements DeviceDiscoveryBackend.discover behavior for Product-neutral device discovery, selection, lease, execution, and cleanup primitives.
      - name: DeviceLeaseBackend
        purpose: Defines DeviceLeaseBackend for Product-neutral device discovery, selection, lease, execution, and cleanup primitives.
        methods:
        - name: acquire
          signature: 'acquire(self, *, resource_hash: str, owner_hash: str, ttl_seconds: int, environment: Mapping[str, str]) -> str'
          purpose: Implements DeviceLeaseBackend.acquire behavior for Product-neutral device discovery, selection, lease, execution, and cleanup primitives.
        - name: release
          signature: 'release(self, token: str, *, environment: Mapping[str, str]) -> None'
          purpose: Implements DeviceLeaseBackend.release behavior for Product-neutral device discovery, selection, lease, execution, and cleanup primitives.
      - name: DeviceProcessBackend
        purpose: Defines DeviceProcessBackend for Product-neutral device discovery, selection, lease, execution, and cleanup primitives.
        methods:
        - name: run
          signature: 'run(self, argv: Sequence[str], *, cwd: Path, environment: Mapping[str, str], timeout_seconds: int) -> CommandOutcome'
          purpose: Implements DeviceProcessBackend.run behavior for Product-neutral device discovery, selection, lease, execution, and cleanup primitives.
      - name: DeviceStateBackend
        purpose: Defines DeviceStateBackend for Product-neutral device discovery, selection, lease, execution, and cleanup primitives.
        methods:
        - name: snapshot
          signature: 'snapshot(self, device: DeviceDescriptor, *, environment: Mapping[str, str]) -> object'
          purpose: Implements DeviceStateBackend.snapshot behavior for Product-neutral device discovery, selection, lease, execution, and cleanup primitives.
        - name: restore
          signature: 'restore(self, device: DeviceDescriptor, snapshot: object, *, environment: Mapping[str, str]) -> None'
          purpose: Implements DeviceStateBackend.restore behavior for Product-neutral device discovery, selection, lease, execution, and cleanup primitives.
        - name: cleanup
          signature: 'cleanup(self, device: DeviceDescriptor, *, environment: Mapping[str, str]) -> None'
          purpose: Implements DeviceStateBackend.cleanup behavior for Product-neutral device discovery, selection, lease, execution, and cleanup primitives.
        - name: residue
          signature: 'residue(self, device: DeviceDescriptor, *, environment: Mapping[str, str]) -> Sequence[str]'
          purpose: Implements DeviceStateBackend.residue behavior for Product-neutral device discovery, selection, lease, execution, and cleanup primitives.
      functions:
      - name: _require
        signature: '_require(condition: bool, code: str) -> None'
        purpose: Implements _require for Product-neutral device discovery, selection, lease, execution, and cleanup primitives.
      - name: _slug
        signature: '_slug(value: object, code: str) -> str'
        purpose: Implements _slug for Product-neutral device discovery, selection, lease, execution, and cleanup primitives.
      - name: _runtime
        signature: '_runtime(value: object, code: str) -> str'
        purpose: Implements _runtime for Product-neutral device discovery, selection, lease, execution, and cleanup primitives.
      - name: _capabilities
        signature: '_capabilities(values: object, code: str) -> tuple[str, ...]'
        purpose: Implements _capabilities for Product-neutral device discovery, selection, lease, execution, and cleanup primitives.
      - name: _state
        signature: '_state(value: object) -> str'
        purpose: Implements _state for Product-neutral device discovery, selection, lease, execution, and cleanup primitives.
      - name: _private_identifier
        signature: '_private_identifier(value: object) -> str'
        purpose: Implements _private_identifier for Product-neutral device discovery, selection, lease, execution, and cleanup primitives.
      - name: normalize_device_record
        signature: 'normalize_device_record(raw: Mapping[str, object]) -> DeviceDescriptor'
        purpose: Normalize one backend record without exposing its private identifier.
      - name: discover_devices
        signature: 'discover_devices(backend: DeviceDiscoveryBackend, *, environment: Mapping[str, str]) -> tuple[DeviceDescriptor, ...]'
        purpose: Discover and normalize a bounded physical/simulator inventory.
      - name: select_device
        signature: 'select_device(devices: Sequence[DeviceDescriptor], query: DeviceQuery) -> DeviceDescriptor'
        purpose: Select one ready device deterministically by capability metadata.
      - name: lease_owner_hash
        signature: 'lease_owner_hash(environment: Mapping[str, str]) -> str'
        purpose: Derive an opaque lease owner from runner environment.
      - name: acquire_device_lease
        signature: 'acquire_device_lease(device: DeviceDescriptor, backend: DeviceLeaseBackend, *, environment: Mapping[str, str], ttl_seconds: int) -> DeviceLease'
        purpose: Fence shared hardware using only hashed device/run identities.
      - name: release_device_lease
        signature: 'release_device_lease(lease: DeviceLease, backend: DeviceLeaseBackend, *, environment: Mapping[str, str]) -> None'
        purpose: Implements release_device_lease for Product-neutral device discovery, selection, lease, execution, and cleanup primitives.
      - name: _checked_in_command
        signature: '_checked_in_command(source_root: Path, command_path: str) -> Path'
        purpose: Implements _checked_in_command for Product-neutral device discovery, selection, lease, execution, and cleanup primitives.
      - name: _arguments
        signature: '_arguments(values: Sequence[str]) -> tuple[str, ...]'
        purpose: Implements _arguments for Product-neutral device discovery, selection, lease, execution, and cleanup primitives.
      - name: execute_checked_in_command
        signature: 'execute_checked_in_command(device: DeviceDescriptor, *, source_root: Path, command_path: str, arguments: Sequence[str], process: DeviceProcessBackend, environment:
          Mapping[str, str], timeout_seconds: int) -> CommandOutcome'
        purpose: Run a checked-in command; the private device ID is environment-only.
      - name: execute_device_operation
        signature: 'execute_device_operation(*, discovery: DeviceDiscoveryBackend, query: DeviceQuery, source_root: Path, command_path: str, arguments: Sequence[str], process: DeviceProcessBackend,
          state: DeviceStateBackend, environment: Mapping[str, str], timeout_seconds: int, lease_backend: DeviceLeaseBackend | None=None, lease_ttl_seconds: int=3600) -> DeviceOperationResult'
        purpose: Execute with restoration/cleanup and never mutate shared hardware before fencing.
      internal_dependencies: []
    - path: src/ci_workflows/device_profile_contract.py
      responsibility: Validation and typed loading for the generic physical-device contract.
      status: consolidate
      classes: []
      functions:
      - name: _validate_family_policy
        signature: '_validate_family_policy(family: DeviceFamily, raw: Mapping[str, Any], contract: Mapping[str, Any]) -> None'
        purpose: Implements _validate_family_policy for Validation and typed loading for the generic physical-device contract.
      - name: load_device_contract
        signature: 'load_device_contract(root: Path) -> Mapping[str, Any]'
        purpose: Implements load_device_contract for Validation and typed loading for the generic physical-device contract.
      - name: load_evidence_contract
        signature: 'load_evidence_contract(root: Path) -> Mapping[str, Any]'
        purpose: Implements load_evidence_contract for Validation and typed loading for the generic physical-device contract.
      - name: profile_for_request
        signature: 'profile_for_request(contract: Mapping[str, Any], request: DeviceRequest) -> DeviceProfile'
        purpose: Combine generic production family policy with caller-owned bounded commands.
      internal_dependencies:
      - device_contract_common
      - device_types
    - path: src/ci_workflows/device_test_lock.py
      responsibility: Single-process test-only device lock adapter interfaces.
      status: consolidate
      classes:
      - name: DeviceLockAdapter
        purpose: Defines DeviceLockAdapter for Single-process test-only device lock adapter interfaces.
        methods:
        - name: acquire
          signature: 'acquire(self, *, plan: DevicePlan, selected: SelectedDevice, now: int) -> LockReceipt'
          purpose: Implements DeviceLockAdapter.acquire behavior for Single-process test-only device lock adapter interfaces.
        - name: release
          signature: 'release(self, receipt: LockReceipt, *, now: int) -> LockReleaseReceipt'
          purpose: Implements DeviceLockAdapter.release behavior for Single-process test-only device lock adapter interfaces.
      - name: InMemoryDeviceLockAdapter
        purpose: Single-process test double only; it is not cross-run fencing authority.
        methods:
        - name: resource_key
          signature: 'resource_key(plan: DevicePlan, selected: SelectedDevice) -> str'
          purpose: Implements InMemoryDeviceLockAdapter.resource_key behavior for Single-process test-only device lock adapter interfaces.
        - name: owner_hash
          signature: 'owner_hash(plan: DevicePlan) -> str'
          purpose: Implements InMemoryDeviceLockAdapter.owner_hash behavior for Single-process test-only device lock adapter interfaces.
        - name: acquire
          signature: 'acquire(self, *, plan: DevicePlan, selected: SelectedDevice, now: int) -> LockReceipt'
          purpose: Implements InMemoryDeviceLockAdapter.acquire behavior for Single-process test-only device lock adapter interfaces.
        - name: release
          signature: 'release(self, receipt: LockReceipt, *, now: int) -> LockReleaseReceipt'
          purpose: Implements InMemoryDeviceLockAdapter.release behavior for Single-process test-only device lock adapter interfaces.
        - name: active_count
          signature: active_count(self) -> int
          purpose: Implements InMemoryDeviceLockAdapter.active_count behavior for Single-process test-only device lock adapter interfaces.
      functions: []
      internal_dependencies:
      - device_types
    - path: src/ci_workflows/device_types.py
      responsibility: Typed models and stable failures for bounded physical-device validation.
      status: consolidate
      classes:
      - name: DeviceValidationError
        purpose: Fail closed with one stable, non-sensitive public error code.
        methods: []
      - name: DeviceFamily
        purpose: Defines DeviceFamily for Typed models and stable failures for bounded physical-device validation.
        methods: []
      - name: DeviceRequest
        purpose: Defines DeviceRequest for Typed models and stable failures for bounded physical-device validation.
        methods: []
      - name: DeviceCommandProfile
        purpose: Validated caller-owned checked-in command plan.
        methods: []
      - name: DeviceProfile
        purpose: Generic family policy combined with one semantic host-capacity request.
        methods: []
      - name: DeviceRecord
        purpose: Defines DeviceRecord for Typed models and stable failures for bounded physical-device validation.
        methods: []
      - name: SelectedDevice
        purpose: Defines SelectedDevice for Typed models and stable failures for bounded physical-device validation.
        methods:
        - name: public_projection
          signature: public_projection(self) -> dict[str, object]
          purpose: Implements SelectedDevice.public_projection behavior for Typed models and stable failures for bounded physical-device validation.
      - name: DevicePlan
        purpose: Defines DevicePlan for Typed models and stable failures for bounded physical-device validation.
        methods:
        - name: packet
          signature: 'packet(self, *, runs_on_json: str) -> dict[str, object]'
          purpose: Return the complete bounded plan passed from planner to executor.
        - name: planning_outputs
          signature: 'planning_outputs(self, *, runs_on_json: str) -> dict[str, str]'
          purpose: Implements DevicePlan.planning_outputs behavior for Typed models and stable failures for bounded physical-device validation.
      - name: LockReceipt
        purpose: Defines LockReceipt for Typed models and stable failures for bounded physical-device validation.
        methods: []
      - name: LockReleaseReceipt
        purpose: Defines LockReleaseReceipt for Typed models and stable failures for bounded physical-device validation.
        methods: []
      - name: DeviceResult
        purpose: Defines DeviceResult for Typed models and stable failures for bounded physical-device validation.
        methods:
        - name: output_values
          signature: output_values(self) -> dict[str, str]
          purpose: Implements DeviceResult.output_values behavior for Typed models and stable failures for bounded physical-device validation.
      functions:
      - name: canonical_json
        signature: 'canonical_json(value: object) -> str'
        purpose: Implements canonical_json for Typed models and stable failures for bounded physical-device validation.
      - name: stable_failure
        signature: 'stable_failure(code: str) -> DeviceValidationError'
        purpose: Implements stable_failure for Typed models and stable failures for bounded physical-device validation.
      - name: sorted_unique
        signature: 'sorted_unique(values: Sequence[str]) -> tuple[str, ...]'
        purpose: Implements sorted_unique for Typed models and stable failures for bounded physical-device validation.
      internal_dependencies: []
    - path: src/ci_workflows/devices.py
      responsibility: Compatibility facade for the original physical-device component API.
      status: compatibility
      classes: []
      functions:
      - name: lock
        signature: 'lock(*, adapter: Any, plan: Any, selected: Any, now: int) -> Any'
        purpose: Acquire one lock through the supplied reviewed adapter.
      - name: validate
        signature: 'validate(**kwargs: Any) -> Any'
        purpose: Delegate bounded lifecycle execution to the typed implementation.
      - name: cleanup
        signature: 'cleanup(*, state_root: Path) -> None'
        purpose: Remove registered device state and fail closed on residue.
      internal_dependencies:
      - device_cleanup
      - device_lifecycle
    - path: src/ci_workflows/physical_log_policy.py
      responsibility: Fail-closed durable evidence policy for physical-device CI.
      status: consolidate
      classes: []
      functions:
      - name: load_physical_log_policy
        signature: 'load_physical_log_policy(root: Path) -> Mapping[str, Any]'
        purpose: Implements load_physical_log_policy for Fail-closed durable evidence policy for physical-device CI.
      - name: _policy_list
        signature: '_policy_list(value: Any) -> tuple[str, ...]'
        purpose: Implements _policy_list for Fail-closed durable evidence policy for physical-device CI.
      - name: _validate_durable_text
        signature: '_validate_durable_text(text: str, stable: Mapping[str, Any]) -> None'
        purpose: Implements _validate_durable_text for Fail-closed durable evidence policy for physical-device CI.
      - name: _durable_id
        signature: '_durable_id(value: Any, stable: Mapping[str, Any]) -> str'
        purpose: Implements _durable_id for Fail-closed durable evidence policy for physical-device CI.
      - name: validate_durable_text
        signature: 'validate_durable_text(text: str, *, contract_root: Path) -> str'
        purpose: Implements validate_durable_text for Fail-closed durable evidence policy for physical-device CI.
      - name: validate_stable_evidence
        signature: 'validate_stable_evidence(payload: Mapping[str, Any], *, contract_root: Path) -> dict[str, Any]'
        purpose: Validate the only durable product evidence shape allowed for physical CI.
      - name: render_stable_evidence
        signature: 'render_stable_evidence(payload: Mapping[str, Any], *, contract_root: Path) -> str'
        purpose: Implements render_stable_evidence for Fail-closed durable evidence policy for physical-device CI.
      - name: platform_log_boundary
        signature: 'platform_log_boundary(*, contract_root: Path) -> Mapping[str, str | bool]'
        purpose: Implements platform_log_boundary for Fail-closed durable evidence policy for physical-device CI.
      internal_dependencies:
      - foundation_types
  distribution:
    target_module: src/ci_workflows/distribution_primitives.py
    responsibility: Signing and store-distribution primitives.
    files:
    - path: src/ci_workflows/distribution_primitives.py
      responsibility: Product-neutral mobile signing and distribution primitives.
      status: keep
      classes:
      - name: DistributionPrimitiveError
        purpose: Defines DistributionPrimitiveError for Product-neutral mobile signing and distribution primitives.
        methods: []
      - name: CommandOutcome
        purpose: Defines CommandOutcome for Product-neutral mobile signing and distribution primitives.
        methods: []
      - name: CommandRunner
        purpose: Defines CommandRunner for Product-neutral mobile signing and distribution primitives.
        methods:
        - name: run
          signature: 'run(self, argv: Sequence[str], *, cwd: Path, env: Mapping[str, str]) -> ProcessResult | CommandOutcome'
          purpose: Implements CommandRunner.run behavior for Product-neutral mobile signing and distribution primitives.
      - name: RuntimeCommandRunner
        purpose: Defines RuntimeCommandRunner for Product-neutral mobile signing and distribution primitives.
        methods:
        - name: run
          signature: 'run(self, argv: Sequence[str], *, cwd: Path, env: Mapping[str, str]) -> ProcessResult'
          purpose: Implements RuntimeCommandRunner.run behavior for Product-neutral mobile signing and distribution primitives.
      - name: DistributionState
        purpose: Defines DistributionState for Product-neutral mobile signing and distribution primitives.
        methods: []
      - name: CredentialFile
        purpose: Defines CredentialFile for Product-neutral mobile signing and distribution primitives.
        methods: []
      - name: AppleCredentials
        purpose: Defines AppleCredentials for Product-neutral mobile signing and distribution primitives.
        methods: []
      - name: AppleKeychain
        purpose: Defines AppleKeychain for Product-neutral mobile signing and distribution primitives.
        methods: []
      - name: PackageResult
        purpose: Defines PackageResult for Product-neutral mobile signing and distribution primitives.
        methods: []
      - name: StoreAuth
        purpose: Defines StoreAuth for Product-neutral mobile signing and distribution primitives.
        methods: []
      - name: UploadRequest
        purpose: Defines UploadRequest for Product-neutral mobile signing and distribution primitives.
        methods: []
      - name: UploadResult
        purpose: Defines UploadResult for Product-neutral mobile signing and distribution primitives.
        methods: []
      functions:
      - name: _fail
        signature: '_fail(code: str, operation: str='''') -> None'
        purpose: Implements _fail for Product-neutral mobile signing and distribution primitives.
      - name: _line
        signature: '_line(value: str, code: str, *, maximum: int=4096) -> str'
        purpose: Implements _line for Product-neutral mobile signing and distribution primitives.
      - name: _safe
        signature: '_safe(value: str, code: str) -> str'
        purpose: Implements _safe for Product-neutral mobile signing and distribution primitives.
      - name: _env
        signature: '_env(environment: Mapping[str, str] | None) -> dict[str, str]'
        purpose: Implements _env for Product-neutral mobile signing and distribution primitives.
      - name: _secret
        signature: '_secret(environment: Mapping[str, str], name: str) -> str'
        purpose: Implements _secret for Product-neutral mobile signing and distribution primitives.
      - name: _path
        signature: '_path(path: Path, *, directory: bool | None=None, code: str=''path_invalid'') -> Path'
        purpose: Implements _path for Product-neutral mobile signing and distribution primitives.
      - name: _tool
        signature: '_tool(path: Path, operation: str) -> Path'
        purpose: Implements _tool for Product-neutral mobile signing and distribution primitives.
      - name: create_distribution_state
        signature: 'create_distribution_state(parent: Path) -> DistributionState'
        purpose: Implements create_distribution_state for Product-neutral mobile signing and distribution primitives.
      - name: _state
        signature: '_state(state: DistributionState) -> Path'
        purpose: Implements _state for Product-neutral mobile signing and distribution primitives.
      - name: _write_secret
        signature: '_write_secret(state: DistributionState, environment: Mapping[str, str], env_name: str, filename: str, kind: str, *, json_secret: bool=False) -> CredentialFile'
        purpose: Implements _write_secret for Product-neutral mobile signing and distribution primitives.
      - name: materialize_apple_credentials
        signature: 'materialize_apple_credentials(state: DistributionState, *, environment: Mapping[str, str], include_profile: bool=True) -> AppleCredentials'
        purpose: Implements materialize_apple_credentials for Product-neutral mobile signing and distribution primitives.
      - name: materialize_android_keystore
        signature: 'materialize_android_keystore(state: DistributionState, *, environment: Mapping[str, str]) -> CredentialFile'
        purpose: Implements materialize_android_keystore for Product-neutral mobile signing and distribution primitives.
      - name: materialize_store_auth
        signature: 'materialize_store_auth(state: DistributionState, provider: Literal[''app-store-connect'', ''google-play''], *, environment: Mapping[str, str]) -> StoreAuth'
        purpose: Implements materialize_store_auth for Product-neutral mobile signing and distribution primitives.
      - name: _run
        signature: '_run(operation: str, argv: Sequence[str], cwd: Path, environment: Mapping[str, str] | None, runner: CommandRunner | None) -> None'
        purpose: Implements _run for Product-neutral mobile signing and distribution primitives.
      - name: prepare_apple_keychain
        signature: 'prepare_apple_keychain(security: Path, credentials: AppleCredentials, state: DistributionState, *, environment: Mapping[str, str], runner: CommandRunner | None=None)
          -> AppleKeychain'
        purpose: Implements prepare_apple_keychain for Product-neutral mobile signing and distribution primitives.
      - name: apple_archive
        signature: 'apple_archive(xcodebuild: Path, *, project_directory: Path, container: Path, container_kind: Literal[''project'', ''workspace''], scheme: str, configuration: str,
          archive_path: Path, destination: str | None=None, environment: Mapping[str, str] | None=None, runner: CommandRunner | None=None) -> PackageResult'
        purpose: Implements apple_archive for Product-neutral mobile signing and distribution primitives.
      - name: apple_export
        signature: 'apple_export(xcodebuild: Path, *, project_directory: Path, archive: Path, export_options: Path, export_path: Path, environment: Mapping[str, str] | None=None, runner:
          CommandRunner | None=None) -> PackageResult'
        purpose: Implements apple_export for Product-neutral mobile signing and distribution primitives.
      - name: apple_sign
        signature: 'apple_sign(codesign: Path, package: Path, *, project_directory: Path, identity: str, keychain: AppleKeychain | None=None, environment: Mapping[str, str] | None=None,
          runner: CommandRunner | None=None) -> PackageResult'
        purpose: Implements apple_sign for Product-neutral mobile signing and distribution primitives.
      - name: apple_verify
        signature: 'apple_verify(codesign: Path, package: Path, *, project_directory: Path, environment: Mapping[str, str] | None=None, runner: CommandRunner | None=None) -> PackageResult'
        purpose: Implements apple_verify for Product-neutral mobile signing and distribution primitives.
      - name: android_sign
        signature: 'android_sign(kind: Literal[''apk'', ''aab''], tool: Path, package: Path, keystore: CredentialFile, *, project_directory: Path, key_alias: str, environment: Mapping[str,
          str], output_path: Path | None=None, runner: CommandRunner | None=None) -> PackageResult'
        purpose: Implements android_sign for Product-neutral mobile signing and distribution primitives.
      - name: android_verify
        signature: 'android_verify(kind: Literal[''apk'', ''aab''], tool: Path, package: Path, *, project_directory: Path, environment: Mapping[str, str] | None=None, runner: CommandRunner
          | None=None) -> PackageResult'
        purpose: Implements android_verify for Product-neutral mobile signing and distribution primitives.
      - name: app_store_upload_request
        signature: 'app_store_upload_request(xcrun: Path, package: Path, auth: StoreAuth, *, project_directory: Path, platform: Literal[''ios'', ''tvos'']) -> UploadRequest'
        purpose: Implements app_store_upload_request for Product-neutral mobile signing and distribution primitives.
      - name: google_play_upload_request
        signature: 'google_play_upload_request(fastlane: Path, package: Path, auth: StoreAuth, *, project_directory: Path, package_name: str, track: str, kind: Literal[''apk'', ''aab''],
          release_status: str | None=None) -> UploadRequest'
        purpose: Implements google_play_upload_request for Product-neutral mobile signing and distribution primitives.
      - name: execute_upload
        signature: 'execute_upload(request: UploadRequest, *, environment: Mapping[str, str] | None=None, runner: CommandRunner | None=None) -> UploadResult'
        purpose: Implements execute_upload for Product-neutral mobile signing and distribution primitives.
      - name: cleanup_distribution_state
        signature: 'cleanup_distribution_state(state: DistributionState, *, security: Path | None=None, keychain: AppleKeychain | None=None, environment: Mapping[str, str] | None=None,
          runner: CommandRunner | None=None) -> None'
        purpose: Implements cleanup_distribution_state for Product-neutral mobile signing and distribution primitives.
      internal_dependencies:
      - runtime_primitives
  flutter:
    target_module: src/ci_workflows/flutter.py
    responsibility: Flutter validation planning and execution.
    files:
    - path: src/ci_workflows/ciw_flutter.py
      responsibility: Bounded ``ciw flutter validate`` adapter and standalone compatibility CLI.
      status: compatibility
      classes: []
      functions:
      - name: configure_flutter_validate
        signature: 'configure_flutter_validate(parser: argparse.ArgumentParser) -> None'
        purpose: Implements configure_flutter_validate for Bounded ``ciw flutter validate`` adapter and standalone compatibility CLI.
      - name: _resolved_state_root
        signature: '_resolved_state_root(root: Path, environment: Mapping[str, str]) -> Path'
        purpose: Implements _resolved_state_root for Bounded ``ciw flutter validate`` adapter and standalone compatibility CLI.
      - name: _standalone_parser
        signature: _standalone_parser() -> argparse.ArgumentParser
        purpose: Implements _standalone_parser for Bounded ``ciw flutter validate`` adapter and standalone compatibility CLI.
      - name: _write_outputs
        signature: '_write_outputs(values: Mapping[str, str], target: str | None) -> None'
        purpose: Implements _write_outputs for Bounded ``ciw flutter validate`` adapter and standalone compatibility CLI.
      - name: _planning_outputs
        signature: '_planning_outputs(root: Path, plan: flutter_validation.FlutterPlan, request: flutter_validation.FlutterRequest) -> dict[str, str]'
        purpose: Implements _planning_outputs for Bounded ``ciw flutter validate`` adapter and standalone compatibility CLI.
      - name: _source_path
        signature: '_source_path(root: Path, source_root: str, environment: Mapping[str, str]) -> Path'
        purpose: Implements _source_path for Bounded ``ciw flutter validate`` adapter and standalone compatibility CLI.
      - name: _request_plan
        signature: '_request_plan(root: Path, source: Path, environment: Mapping[str, str], *, source_required: bool) -> tuple[flutter_validation.FlutterRequest, flutter_validation.FlutterPlan]'
        purpose: Implements _request_plan for Bounded ``ciw flutter validate`` adapter and standalone compatibility CLI.
      - name: _phase_outputs
        signature: '_phase_outputs(*, root: Path, command: str, source: Path, state: Path | None, environment: Mapping[str, str], check: bool) -> dict[str, str]'
        purpose: Implements _phase_outputs for Bounded ``ciw flutter validate`` adapter and standalone compatibility CLI.
      - name: standalone_main
        signature: 'standalone_main(argv: Sequence[str] | None=None) -> int'
        purpose: Implements standalone_main for Bounded ``ciw flutter validate`` adapter and standalone compatibility CLI.
      - name: execute_flutter_validate
        signature: 'execute_flutter_validate(args: argparse.Namespace, context: ''CIWContext'') -> ''CIWResult'''
        purpose: Implements execute_flutter_validate for Bounded ``ciw flutter validate`` adapter and standalone compatibility CLI.
      - name: main
        signature: 'main(argv: Sequence[str] | None=None) -> int'
        purpose: Implements main for Bounded ``ciw flutter validate`` adapter and standalone compatibility CLI.
      internal_dependencies:
      - ciw_types
      - flutter
      - runners
      - workspace
    - path: src/ci_workflows/flutter.py
      responsibility: Public façade for reusable Flutter validation.
      status: keep
      classes: []
      functions:
      - name: source_trust_from_environment
        signature: 'source_trust_from_environment(environment: Mapping[str, str]) -> str'
        purpose: Implements source_trust_from_environment for Public façade for reusable Flutter validation.
      - name: _match_optional
        signature: '_match_optional(environment: Mapping[str, str], key: str, expected: str) -> None'
        purpose: Implements _match_optional for Public façade for reusable Flutter validation.
      - name: request_from_environment
        signature: 'request_from_environment(environment: Mapping[str, str], contract: Mapping[str, object]) -> FlutterRequest'
        purpose: Implements request_from_environment for Public façade for reusable Flutter validation.
      - name: validate
        signature: 'validate(*, contract_root: Path, source_root: Path | None, state_root: Path | None, request: FlutterRequest, phase: str, environment: Mapping[str, str] | None=None,
          runner: CommandRunner | None=None) -> FlutterPlan | FlutterResult'
        purpose: Implements validate for Public façade for reusable Flutter validation.
      internal_dependencies:
      - flutter_contract
      - flutter_execution
      - flutter_types
    - path: src/ci_workflows/flutter_contract.py
      responsibility: Deterministic Flutter contract parsing and source authority checks.
      status: consolidate
      classes:
      - name: FlutterValidationError
        purpose: Stable bounded validation failure with deterministic terminal projection.
        methods:
        - name: output_values
          signature: output_values(self) -> dict[str, str]
          purpose: Implements FlutterValidationError.output_values behavior for Deterministic Flutter contract parsing and source authority checks.
      functions:
      - name: fail
        signature: 'fail(code: str) -> None'
        purpose: Implements fail for Deterministic Flutter contract parsing and source authority checks.
      - name: _exact_dict
        signature: '_exact_dict(value: object, keys: set[str], code: str) -> dict[str, Any]'
        purpose: Implements _exact_dict for Deterministic Flutter contract parsing and source authority checks.
      - name: canonical_json_bytes
        signature: 'canonical_json_bytes(value: object) -> bytes'
        purpose: Implements canonical_json_bytes for Deterministic Flutter contract parsing and source authority checks.
      - name: _canonicalize_contract
        signature: '_canonicalize_contract(raw: Mapping[str, Any]) -> dict[str, Any]'
        purpose: Implements _canonicalize_contract for Deterministic Flutter contract parsing and source authority checks.
      - name: runtime_fixture
        signature: 'runtime_fixture(toolchain: Mapping[str, Any], version: str) -> dict[str, str]'
        purpose: Implements runtime_fixture for Deterministic Flutter contract parsing and source authority checks.
      - name: generated_flutter_files
        signature: 'generated_flutter_files(root: Path) -> dict[Path, bytes]'
        purpose: Implements generated_flutter_files for Deterministic Flutter contract parsing and source authority checks.
      - name: generate_flutter_contract_files
        signature: 'generate_flutter_contract_files(root: Path, *, check: bool) -> tuple[str, ...]'
        purpose: Implements generate_flutter_contract_files for Deterministic Flutter contract parsing and source authority checks.
      - name: load_flutter_contract
        signature: 'load_flutter_contract(root: Path) -> dict[str, Any]'
        purpose: Implements load_flutter_contract for Deterministic Flutter contract parsing and source authority checks.
      - name: exact_version
        signature: 'exact_version(value: object) -> str'
        purpose: Implements exact_version for Deterministic Flutter contract parsing and source authority checks.
      - name: safe_relative
        signature: 'safe_relative(value: object, *, allow_dot: bool=True) -> Path'
        purpose: Implements safe_relative for Deterministic Flutter contract parsing and source authority checks.
      - name: _lstat
        signature: '_lstat(path: Path) -> os.stat_result | None'
        purpose: Implements _lstat for Deterministic Flutter contract parsing and source authority checks.
      - name: bounded_path
        signature: 'bounded_path(root: Path, relative: Path, *, must_exist: bool=False) -> Path'
        purpose: Validate a lexical child without following any existing symlink.
      - name: regular_file
        signature: 'regular_file(root: Path, relative: str, code: str) -> Path'
        purpose: Implements regular_file for Deterministic Flutter contract parsing and source authority checks.
      - name: _read_plain_pin
        signature: '_read_plain_pin(path: Path) -> str'
        purpose: Implements _read_plain_pin for Deterministic Flutter contract parsing and source authority checks.
      - name: _read_fvm_pin
        signature: '_read_fvm_pin(path: Path) -> str'
        purpose: Implements _read_fvm_pin for Deterministic Flutter contract parsing and source authority checks.
      - name: discover_flutter_pin
        signature: 'discover_flutter_pin(source_root: Path, consumer: Mapping[str, Any]) -> FlutterPin'
        purpose: Implements discover_flutter_pin for Deterministic Flutter contract parsing and source authority checks.
      - name: file_sha256
        signature: 'file_sha256(path: Path) -> str'
        purpose: Implements file_sha256 for Deterministic Flutter contract parsing and source authority checks.
      - name: source_authority_hashes
        signature: 'source_authority_hashes(source_root: Path) -> dict[str, str]'
        purpose: Implements source_authority_hashes for Deterministic Flutter contract parsing and source authority checks.
      - name: checked_in_script
        signature: 'checked_in_script(source_root: Path, value: object) -> Path'
        purpose: Implements checked_in_script for Deterministic Flutter contract parsing and source authority checks.
      - name: parse_runtime_identity
        signature: 'parse_runtime_identity(output: str, toolchain: FlutterToolchain) -> dict[str, str]'
        purpose: Implements parse_runtime_identity for Deterministic Flutter contract parsing and source authority checks.
      - name: parse_jdk_identity
        signature: 'parse_jdk_identity(java_output: str, javac_output: str, toolchain: FlutterToolchain) -> dict[str, str]'
        purpose: Implements parse_jdk_identity for Deterministic Flutter contract parsing and source authority checks.
      - name: _toolchain
        signature: '_toolchain(contract: Mapping[str, Any], version: str) -> FlutterToolchain'
        purpose: Implements _toolchain for Deterministic Flutter contract parsing and source authority checks.
      - name: _command
        signature: '_command(raw: Mapping[str, Any]) -> FlutterCommand'
        purpose: Implements _command for Deterministic Flutter contract parsing and source authority checks.
      - name: build_plan
        signature: 'build_plan(contract: Mapping[str, Any], request: FlutterRequest, source_root: Path | None) -> FlutterPlan'
        purpose: Implements build_plan for Deterministic Flutter contract parsing and source authority checks.
      - name: validate_contract
        signature: 'validate_contract(raw: object) -> None'
        purpose: Implements validate_contract for Deterministic Flutter contract parsing and source authority checks.
      internal_dependencies:
      - flutter_types
    - path: src/ci_workflows/flutter_execution.py
      responsibility: Execution engine for the bounded Flutter validation contract.
      status: consolidate
      classes:
      - name: CommandOutcome
        purpose: Defines CommandOutcome for Execution engine for the bounded Flutter validation contract.
        methods: []
      - name: CommandRunner
        purpose: Defines CommandRunner for Execution engine for the bounded Flutter validation contract.
        methods:
        - name: run
          signature: 'run(self, argv: Sequence[str], *, cwd: Path, env: Mapping[str, str]) -> CommandOutcome'
          purpose: Implements CommandRunner.run behavior for Execution engine for the bounded Flutter validation contract.
      - name: SubprocessCommandRunner
        purpose: Defines SubprocessCommandRunner for Execution engine for the bounded Flutter validation contract.
        methods:
        - name: run
          signature: 'run(self, argv: Sequence[str], *, cwd: Path, env: Mapping[str, str]) -> CommandOutcome'
          purpose: Implements SubprocessCommandRunner.run behavior for Execution engine for the bounded Flutter validation contract.
      functions:
      - name: _lstat
        signature: '_lstat(path: Path, code: str=''cleanup_failed'') -> os.stat_result | None'
        purpose: Implements _lstat for Execution engine for the bounded Flutter validation contract.
      - name: _state_directory
        signature: '_state_directory(state_root: Path, *, create: bool=True) -> Path'
        purpose: Implements _state_directory for Execution engine for the bounded Flutter validation contract.
      - name: expected_pub_cache_path
        signature: 'expected_pub_cache_path(state_root: Path) -> Path'
        purpose: Implements expected_pub_cache_path for Execution engine for the bounded Flutter validation contract.
      - name: _path_is_within
        signature: '_path_is_within(path: Path, root: Path) -> bool'
        purpose: Implements _path_is_within for Execution engine for the bounded Flutter validation contract.
      - name: _assert_exact_pub_cache
        signature: '_assert_exact_pub_cache(state_root: Path, environment: Mapping[str, str], *, require_exists: bool) -> Path'
        purpose: Implements _assert_exact_pub_cache for Execution engine for the bounded Flutter validation contract.
      - name: isolated_environment
        signature: 'isolated_environment(state_root: Path, base: Mapping[str, str] | None=None) -> tuple[dict[str, str], dict[str, Path]]'
        purpose: Implements isolated_environment for Execution engine for the bounded Flutter validation contract.
      - name: _snapshot_entry
        signature: '_snapshot_entry(path: Path, root: Path, digest: ''hashlib._Hash'', count: list[int]) -> None'
        purpose: Implements _snapshot_entry for Execution engine for the bounded Flutter validation contract.
      - name: fingerprint_no_follow
        signature: 'fingerprint_no_follow(path: Path) -> str'
        purpose: Implements fingerprint_no_follow for Execution engine for the bounded Flutter validation contract.
      - name: _snapshot_file
        signature: '_snapshot_file(state_root: Path) -> Path'
        purpose: Implements _snapshot_file for Execution engine for the bounded Flutter validation contract.
      - name: snapshot_persistent_pub_cache
        signature: 'snapshot_persistent_pub_cache(state_root: Path, environment: Mapping[str, str]) -> dict[str, str]'
        purpose: Implements snapshot_persistent_pub_cache for Execution engine for the bounded Flutter validation contract.
      - name: bind_pub_cache
        signature: 'bind_pub_cache(state_root: Path, environment: Mapping[str, str]) -> dict[str, str]'
        purpose: Implements bind_pub_cache for Execution engine for the bounded Flutter validation contract.
      - name: verify_persistent_pub_cache
        signature: 'verify_persistent_pub_cache(state_root: Path) -> dict[str, str]'
        purpose: Implements verify_persistent_pub_cache for Execution engine for the bounded Flutter validation contract.
      - name: _render_argv
        signature: '_render_argv(argv: Sequence[str], values: Mapping[str, str]) -> tuple[str, ...]'
        purpose: Implements _render_argv for Execution engine for the bounded Flutter validation contract.
      - name: _run_checked
        signature: '_run_checked(runner: CommandRunner, argv: Sequence[str], *, cwd: Path, env: Mapping[str, str], failure_code: str, state_root: Path | None=None) -> CommandOutcome'
        purpose: Implements _run_checked for Execution engine for the bounded Flutter validation contract.
      - name: _verify_authority
        signature: '_verify_authority(source_root: Path, before: Mapping[str, str]) -> str'
        purpose: Implements _verify_authority for Execution engine for the bounded Flutter validation contract.
      - name: _git_marker_exists
        signature: '_git_marker_exists(source_root: Path) -> bool'
        purpose: Implements _git_marker_exists for Execution engine for the bounded Flutter validation contract.
      - name: _git_command
        signature: '_git_command(source_root: Path, arguments: Sequence[str]) -> subprocess.CompletedProcess[str]'
        purpose: Implements _git_command for Execution engine for the bounded Flutter validation contract.
      - name: _assert_clean_source
        signature: '_assert_clean_source(source_root: Path) -> None'
        purpose: Implements _assert_clean_source for Execution engine for the bounded Flutter validation contract.
      - name: _assert_tracked_regular
        signature: '_assert_tracked_regular(source_root: Path, relative: str) -> None'
        purpose: Implements _assert_tracked_regular for Execution engine for the bounded Flutter validation contract.
      - name: _verify_expected_outputs
        signature: '_verify_expected_outputs(source_root: Path, expected: Sequence[str]) -> bool'
        purpose: Implements _verify_expected_outputs for Execution engine for the bounded Flutter validation contract.
      - name: _verify_gradle_wrapper
        signature: '_verify_gradle_wrapper(source_root: Path, expected_version: str) -> str'
        purpose: Implements _verify_gradle_wrapper for Execution engine for the bounded Flutter validation contract.
      - name: _compose_node
        signature: '_compose_node(plan: FlutterPlan, contract_root: Path, source_root: Path, state_root: Path, runner: CommandRunner, env: Mapping[str, str]) -> None'
        purpose: Implements _compose_node for Execution engine for the bounded Flutter validation contract.
      - name: verify_toolchain_identity
        signature: 'verify_toolchain_identity(*, plan: FlutterPlan, source_root: Path, state_root: Path, runner: CommandRunner | None=None, environment: Mapping[str, str] | None=None)
          -> dict[str, str]'
        purpose: Implements verify_toolchain_identity for Execution engine for the bounded Flutter validation contract.
      - name: execute_flutter_plan
        signature: 'execute_flutter_plan(*, plan: FlutterPlan, contract_root: Path, source_root: Path, state_root: Path, runner: CommandRunner | None=None, environment: Mapping[str,
          str] | None=None) -> FlutterResult'
        purpose: Implements execute_flutter_plan for Execution engine for the bounded Flutter validation contract.
      - name: _lexical_target
        signature: '_lexical_target(root: Path, relative: str) -> Path'
        purpose: Implements _lexical_target for Execution engine for the bounded Flutter validation contract.
      - name: _remove_no_follow
        signature: '_remove_no_follow(path: Path) -> None'
        purpose: Implements _remove_no_follow for Execution engine for the bounded Flutter validation contract.
      - name: _cleanup_targets
        signature: '_cleanup_targets(source_root: Path, state_root: Path) -> tuple[Path, ...]'
        purpose: Implements _cleanup_targets for Execution engine for the bounded Flutter validation contract.
      - name: _procfs_state_gradle_daemon_pids
        signature: '_procfs_state_gradle_daemon_pids(state: Path, proc_root: Path=Path(''/proc'')) -> tuple[int, ...]'
        purpose: Implements _procfs_state_gradle_daemon_pids for Execution engine for the bounded Flutter validation contract.
      - name: _state_gradle_daemon_pids
        signature: '_state_gradle_daemon_pids(state: Path) -> tuple[int, ...]'
        purpose: Implements _state_gradle_daemon_pids for Execution engine for the bounded Flutter validation contract.
      - name: _stop_state_gradle_daemons
        signature: '_stop_state_gradle_daemons(state: Path) -> None'
        purpose: Implements _stop_state_gradle_daemons for Execution engine for the bounded Flutter validation contract.
      - name: cleanup_flutter_state
        signature: 'cleanup_flutter_state(source_root: Path, state_root: Path) -> None'
        purpose: Implements cleanup_flutter_state for Execution engine for the bounded Flutter validation contract.
      - name: terminal_cleanup_flutter_state
        signature: 'terminal_cleanup_flutter_state(source_root: Path, state_root: Path, *, primary_failure_code: str='''') -> dict[str, str]'
        purpose: Implements terminal_cleanup_flutter_state for Execution engine for the bounded Flutter validation contract.
      - name: assert_zero_flutter_residue
        signature: 'assert_zero_flutter_residue(source_root: Path, state_root: Path) -> None'
        purpose: Implements assert_zero_flutter_residue for Execution engine for the bounded Flutter validation contract.
      internal_dependencies:
      - flutter_contract
      - flutter_types
    - path: src/ci_workflows/flutter_types.py
      responsibility: Typed contracts for bounded Flutter validation.
      status: consolidate
      classes:
      - name: FlutterProfile
        purpose: Defines FlutterProfile for Typed contracts for bounded Flutter validation.
        methods: []
      - name: RunnerCapability
        purpose: Defines RunnerCapability for Typed contracts for bounded Flutter validation.
        methods: []
      - name: FlutterStage
        purpose: Defines FlutterStage for Typed contracts for bounded Flutter validation.
        methods: []
      - name: FlutterToolchain
        purpose: Defines FlutterToolchain for Typed contracts for bounded Flutter validation.
        methods: []
      - name: FlutterPin
        purpose: Defines FlutterPin for Typed contracts for bounded Flutter validation.
        methods: []
      - name: FlutterCommand
        purpose: Defines FlutterCommand for Typed contracts for bounded Flutter validation.
        methods: []
      - name: FlutterRequest
        purpose: Defines FlutterRequest for Typed contracts for bounded Flutter validation.
        methods: []
      - name: FlutterPlan
        purpose: Defines FlutterPlan for Typed contracts for bounded Flutter validation.
        methods:
        - name: planning_outputs
          signature: planning_outputs(self) -> dict[str, str]
          purpose: Implements FlutterPlan.planning_outputs behavior for Typed contracts for bounded Flutter validation.
      - name: FlutterResult
        purpose: Defines FlutterResult for Typed contracts for bounded Flutter validation.
        methods:
        - name: output_values
          signature: output_values(self) -> dict[str, str]
          purpose: Implements FlutterResult.output_values behavior for Typed contracts for bounded Flutter validation.
      functions:
      - name: _compact_json
        signature: '_compact_json(value: object) -> str'
        purpose: Implements _compact_json for Typed contracts for bounded Flutter validation.
      - name: command_ids
        signature: 'command_ids(commands: Sequence[FlutterCommand]) -> tuple[str, ...]'
        purpose: Implements command_ids for Typed contracts for bounded Flutter validation.
      internal_dependencies: []
  gitops:
    target_module: src/ci_workflows/gitops.py
    responsibility: GitOps source/render/SOPS/Kustomize/Helm validation.
    files:
    - path: src/ci_workflows/ciw_gitops.py
      responsibility: Bounded ``ciw gitops validate`` adapter and standalone compatibility CLI.
      status: compatibility
      classes: []
      functions:
      - name: configure_gitops_validate
        signature: 'configure_gitops_validate(parser: argparse.ArgumentParser) -> None'
        purpose: Implements configure_gitops_validate for Bounded ``ciw gitops validate`` adapter and standalone compatibility CLI.
      - name: _resolved_state_root
        signature: '_resolved_state_root(root: Path, environment: Mapping[str, str]) -> Path'
        purpose: Implements _resolved_state_root for Bounded ``ciw gitops validate`` adapter and standalone compatibility CLI.
      - name: _write_outputs
        signature: '_write_outputs(values: Mapping[str, str], target: str | None) -> None'
        purpose: Implements _write_outputs for Bounded ``ciw gitops validate`` adapter and standalone compatibility CLI.
      - name: _failure_outputs
        signature: '_failure_outputs(environment: Mapping[str, str], code: str) -> dict[str, str]'
        purpose: Implements _failure_outputs for Bounded ``ciw gitops validate`` adapter and standalone compatibility CLI.
      - name: _plan_outputs
        signature: '_plan_outputs(root: Path, plan: gitops_validation.GitOpsPlan, request: gitops_validation.GitOpsRequest) -> dict[str, str]'
        purpose: Implements _plan_outputs for Bounded ``ciw gitops validate`` adapter and standalone compatibility CLI.
      - name: _run
        signature: '_run(*, root: Path, command: str, source_relative: str, environment: Mapping[str, str]) -> dict[str, str]'
        purpose: Implements _run for Bounded ``ciw gitops validate`` adapter and standalone compatibility CLI.
      - name: standalone_main
        signature: 'standalone_main(argv: Sequence[str] | None=None) -> int'
        purpose: Implements standalone_main for Bounded ``ciw gitops validate`` adapter and standalone compatibility CLI.
      - name: execute_gitops_validate
        signature: 'execute_gitops_validate(args: argparse.Namespace, context: ''CIWContext'') -> ''CIWResult'''
        purpose: Implements execute_gitops_validate for Bounded ``ciw gitops validate`` adapter and standalone compatibility CLI.
      - name: main
        signature: 'main(argv: Sequence[str] | None=None) -> int'
        purpose: Implements main for Bounded ``ciw gitops validate`` adapter and standalone compatibility CLI.
      internal_dependencies:
      - ciw_types
      - gitops
      - runners
      - workspace
    - path: src/ci_workflows/gitops.py
      responsibility: Public facade for bounded source-only GitOps validation.
      status: keep
      classes: []
      functions:
      - name: validate
        signature: 'validate(*, contract_root: Path, source_root: Path | None, state_root: Path | None, request: GitOpsRequest, phase: str, environment: Mapping[str, str], tools: GitOpsTools
          | None=None) -> GitOpsPlan | GitOpsResult | None'
        purpose: Plan, execute, clean, or inspect one checked-in validation request.
      internal_dependencies:
      - gitops_contract
      - gitops_execution
      - gitops_plan
      - gitops_types
    - path: src/ci_workflows/gitops_composition.py
      responsibility: Composition helpers for bounded GitOps source and render validation.
      status: consolidate
      classes: []
      functions:
      - name: _target_matches
        signature: '_target_matches(target: GitOpsTarget, changed: Sequence[str]) -> bool'
        purpose: Return whether a repository-relative changed path falls under a target root.
      - name: selected_targets
        signature: 'selected_targets(plan: GitOpsPlan, source_root: Path) -> tuple[GitOpsTarget, ...]'
        purpose: Select changed-tree targets without assuming Git paths start with ``./``.
      - name: style_paths_for_plan
        signature: 'style_paths_for_plan(plan: GitOpsPlan, source_root: Path) -> frozenset[Path] | None'
        purpose: Resolve the reviewed YAML style scope for one profile.
      - name: _yaml_documents
        signature: '_yaml_documents(path: Path, yaml: Any, *, enforce_style: bool, display_path: str) -> list[Any]'
        purpose: Parse YAML semantically and apply formatting only when the profile requires it.
      - name: yaml_target
        signature: 'yaml_target(target: GitOpsTarget, source_root: Path, yaml: Any, *, style_paths: frozenset[Path] | None) -> tuple[list[tuple[Any, Path]], int]'
        purpose: Validate YAML and retain each document's exact bounded source path.
      - name: source_render_overlap_allowed
        signature: 'source_render_overlap_allowed(previous_target: GitOpsTarget, previous_source_path: Path | None, current_target: GitOpsTarget, current_source_path: Path | None, source_root:
          Path) -> bool'
        purpose: Allow only a raw document that physically belongs to one render root.
      internal_dependencies:
      - gitops_contract
      - gitops_render
      - gitops_runtime
      - gitops_source
      - gitops_types
    - path: src/ci_workflows/gitops_contract.py
      responsibility: Contract loading, source authority, and plan construction for GitOps validation.
      status: consolidate
      classes: []
      functions:
      - name: fail
        signature: 'fail(code: str, detail: str='''') -> None'
        purpose: Implements fail for Contract loading, source authority, and plan construction for GitOps validation.
      - name: require
        signature: 'require(condition: bool, code: str, detail: str='''') -> None'
        purpose: Implements require for Contract loading, source authority, and plan construction for GitOps validation.
      - name: _mapping
        signature: '_mapping(value: object, code: str=''contract_invalid'') -> dict[str, Any]'
        purpose: Implements _mapping for Contract loading, source authority, and plan construction for GitOps validation.
      - name: _exact_mapping
        signature: '_exact_mapping(value: object, keys: set[str], code: str=''contract_invalid'') -> dict[str, Any]'
        purpose: Implements _exact_mapping for Contract loading, source authority, and plan construction for GitOps validation.
      - name: _strings
        signature: '_strings(value: object, *, empty: bool=True) -> tuple[str, ...]'
        purpose: Implements _strings for Contract loading, source authority, and plan construction for GitOps validation.
      - name: safe_relative
        signature: 'safe_relative(value: object, *, allow_dot: bool=True) -> str'
        purpose: Return one normalized relative path without traversal or backslashes.
      - name: bounded_path
        signature: 'bounded_path(root: Path, relative: str, *, must_exist: bool=False, kind: str | None=None) -> Path'
        purpose: Resolve a source path while rejecting every symlink component.
      - name: file_sha256
        signature: 'file_sha256(path: Path) -> str'
        purpose: Implements file_sha256 for Contract loading, source authority, and plan construction for GitOps validation.
      - name: source_trust_from_environment
        signature: 'source_trust_from_environment(environment: Mapping[str, str]) -> str'
        purpose: Derive immutable source trust from the GitHub event, never caller input.
      - name: _tool
        signature: '_tool(name: str, raw: object) -> GitOpsToolPin'
        purpose: Implements _tool for Contract loading, source authority, and plan construction for GitOps validation.
      - name: _target
        signature: '_target(identifier: str, raw: object) -> GitOpsTarget'
        purpose: Implements _target for Contract loading, source authority, and plan construction for GitOps validation.
      - name: _policy
        signature: '_policy(identifier: str, raw: object) -> GitOpsPolicyScript'
        purpose: Implements _policy for Contract loading, source authority, and plan construction for GitOps validation.
      - name: validate_contract
        signature: 'validate_contract(payload: Mapping[str, Any]) -> None'
        purpose: Implements validate_contract for Contract loading, source authority, and plan construction for GitOps validation.
      - name: load_gitops_contract
        signature: 'load_gitops_contract(root: Path) -> dict[str, Any]'
        purpose: Implements load_gitops_contract for Contract loading, source authority, and plan construction for GitOps validation.
      - name: request_from_environment
        signature: 'request_from_environment(environment: Mapping[str, str], contract: Mapping[str, Any]) -> GitOpsRequest'
        purpose: Implements request_from_environment for Contract loading, source authority, and plan construction for GitOps validation.
      - name: build_plan
        signature: 'build_plan(contract: Mapping[str, Any], request: GitOpsRequest, source_root: Path | None) -> GitOpsPlan'
        purpose: Implements build_plan for Contract loading, source authority, and plan construction for GitOps validation.
      internal_dependencies:
      - gitops_types
    - path: src/ci_workflows/gitops_execution.py
      responsibility: Hermetic orchestration for source-only GitOps validation.
      status: consolidate
      classes: []
      functions:
      - name: _execute
        signature: '_execute(plan: GitOpsPlan, source_root: Path, state_root: Path, tools: GitOpsTools) -> GitOpsResult'
        purpose: Implements _execute for Hermetic orchestration for source-only GitOps validation.
      - name: execute_gitops_plan
        signature: 'execute_gitops_plan(plan: GitOpsPlan, source_root: Path, state_root: Path, *, tools: GitOpsTools | None=None) -> GitOpsResult'
        purpose: Execute one exact plan and preserve primary plus cleanup failure.
      internal_dependencies:
      - gitops_composition
      - gitops_render
      - gitops_runtime
      - gitops_source
      - gitops_types
    - path: src/ci_workflows/gitops_plan.py
      responsibility: Source-bound plan validation for checked-in GitOps consumer contracts.
      status: consolidate
      classes: []
      functions:
      - name: _is_glob
        signature: '_is_glob(value: str) -> bool'
        purpose: Implements _is_glob for Source-bound plan validation for checked-in GitOps consumer contracts.
      - name: build_gitops_plan
        signature: 'build_gitops_plan(contract: Mapping[str, Any], request: GitOpsRequest, source_root: Path | None) -> GitOpsPlan'
        purpose: Build one contract plan and validate its source-bound paths when present.
      internal_dependencies:
      - gitops_contract
      - gitops_runtime
      - gitops_source
      - gitops_types
    - path: src/ci_workflows/gitops_primitives.py
      responsibility: Product-neutral GitOps, YAML, JSON, Kustomize and client validation primitives.
      status: consolidate
      classes:
      - name: GitOpsPrimitiveError
        purpose: Fail closed with one stable non-secret GitOps primitive code.
        methods: []
      - name: ResourceMetadata
        purpose: Defines ResourceMetadata for Product-neutral GitOps, YAML, JSON, Kustomize and client validation primitives.
        methods: []
      - name: DocumentValidationResult
        purpose: Defines DocumentValidationResult for Product-neutral GitOps, YAML, JSON, Kustomize and client validation primitives.
        methods: []
      - name: DirectoryValidationResult
        purpose: Defines DirectoryValidationResult for Product-neutral GitOps, YAML, JSON, Kustomize and client validation primitives.
        methods: []
      - name: RenderResult
        purpose: Defines RenderResult for Product-neutral GitOps, YAML, JSON, Kustomize and client validation primitives.
        methods: []
      - name: ValidationResult
        purpose: Defines ValidationResult for Product-neutral GitOps, YAML, JSON, Kustomize and client validation primitives.
        methods: []
      - name: SourceInspectionResult
        purpose: Defines SourceInspectionResult for Product-neutral GitOps, YAML, JSON, Kustomize and client validation primitives.
        methods: []
      - name: CommandRunner
        purpose: Defines CommandRunner for Product-neutral GitOps, YAML, JSON, Kustomize and client validation primitives.
        methods:
        - name: run
          signature: 'run(self, arguments: Sequence[str], *, cwd: Path, environment: Mapping[str, str], stdin: str='''', timeout_seconds: float | None=None) -> ProcessResult'
          purpose: Implements CommandRunner.run behavior for Product-neutral GitOps, YAML, JSON, Kustomize and client validation primitives.
      - name: RuntimeCommandRunner
        purpose: Defines RuntimeCommandRunner for Product-neutral GitOps, YAML, JSON, Kustomize and client validation primitives.
        methods:
        - name: run
          signature: 'run(self, arguments: Sequence[str], *, cwd: Path, environment: Mapping[str, str], stdin: str='''', timeout_seconds: float | None=None) -> ProcessResult'
          purpose: Implements RuntimeCommandRunner.run behavior for Product-neutral GitOps, YAML, JSON, Kustomize and client validation primitives.
      functions:
      - name: _fail
        signature: '_fail(code: str, operation: str, *, returncode: int | None=None) -> None'
        purpose: Implements _fail for Product-neutral GitOps, YAML, JSON, Kustomize and client validation primitives.
      - name: _text
        signature: '_text(value: str, field: str) -> str'
        purpose: Implements _text for Product-neutral GitOps, YAML, JSON, Kustomize and client validation primitives.
      - name: _environment
        signature: '_environment(value: Mapping[str, str] | None) -> dict[str, str]'
        purpose: Implements _environment for Product-neutral GitOps, YAML, JSON, Kustomize and client validation primitives.
      - name: _real_file
        signature: '_real_file(path: Path, operation: str) -> Path'
        purpose: Implements _real_file for Product-neutral GitOps, YAML, JSON, Kustomize and client validation primitives.
      - name: _real_directory
        signature: '_real_directory(path: Path, operation: str) -> Path'
        purpose: Implements _real_directory for Product-neutral GitOps, YAML, JSON, Kustomize and client validation primitives.
      - name: _tool
        signature: '_tool(value: str, allowed: frozenset[str], operation: str) -> str'
        purpose: Implements _tool for Product-neutral GitOps, YAML, JSON, Kustomize and client validation primitives.
      - name: _run
        signature: '_run(arguments: Sequence[str], *, cwd: Path, environment: Mapping[str, str] | None, operation: str, stdin: str='''', timeout_seconds: float=300, runner: CommandRunner
          | None=None) -> ProcessResult'
        purpose: Implements _run for Product-neutral GitOps, YAML, JSON, Kustomize and client validation primitives.
      - name: _resource_metadata
        signature: '_resource_metadata(value: Any, *, source: str, document_index: int) -> ResourceMetadata | None'
        purpose: Implements _resource_metadata for Product-neutral GitOps, YAML, JSON, Kustomize and client validation primitives.
      - name: _parse_yaml_text
        signature: '_parse_yaml_text(text: str, *, source: str) -> tuple[Any, ...]'
        purpose: Implements _parse_yaml_text for Product-neutral GitOps, YAML, JSON, Kustomize and client validation primitives.
      - name: _resources
        signature: '_resources(values: Sequence[Any], *, source: str) -> tuple[ResourceMetadata, ...]'
        purpose: Implements _resources for Product-neutral GitOps, YAML, JSON, Kustomize and client validation primitives.
      - name: validate_yaml_file
        signature: 'validate_yaml_file(path: Path) -> DocumentValidationResult'
        purpose: Parse one real YAML file and return document/resource metadata.
      - name: _reject_json_constant
        signature: '_reject_json_constant(value: str) -> None'
        purpose: Implements _reject_json_constant for Product-neutral GitOps, YAML, JSON, Kustomize and client validation primitives.
      - name: _unique_json_object
        signature: '_unique_json_object(pairs: Sequence[tuple[str, Any]]) -> dict[str, Any]'
        purpose: Implements _unique_json_object for Product-neutral GitOps, YAML, JSON, Kustomize and client validation primitives.
      - name: validate_json_file
        signature: 'validate_json_file(path: Path) -> DocumentValidationResult'
        purpose: Parse one strict JSON file and return resource metadata when applicable.
      - name: validate_config_directory
        signature: 'validate_config_directory(root: Path, *, recursive: bool=True, require_files: bool=True) -> DirectoryValidationResult'
        purpose: Validate every YAML/JSON file in one caller-owned directory.
      - name: render_kustomize
        signature: 'render_kustomize(root: Path, *, environment: Mapping[str, str] | None=None, tool: str=''kustomize'', runner: CommandRunner | None=None) -> RenderResult'
        purpose: Run a local Kustomize build and parse the rendered resource metadata.
      - name: render_helm_chart
        signature: 'render_helm_chart(chart: Path, *, values: Sequence[Path]=(), release_name: str=''release'', namespace: str | None=None, environment: Mapping[str, str] | None=None)
          -> RenderResult'
        purpose: Render a chart through the shared packaging primitive and inspect resources.
      - name: validate_kubernetes_client_dry_run
        signature: 'validate_kubernetes_client_dry_run(rendered: str, *, cwd: Path, environment: Mapping[str, str] | None=None, tool: str=''kubectl'', runner: CommandRunner | None=None)
          -> ValidationResult'
        purpose: Run kubectl client dry-run without kubeconfig or service-account authority.
      - name: validate_kubernetes_schema
        signature: 'validate_kubernetes_schema(rendered: str, *, cwd: Path, environment: Mapping[str, str] | None=None, tool: str=''kubeconform'', schema_locations: Sequence[str]=(),
          strict: bool=True, ignore_missing_schemas: bool=False, runner: CommandRunner | None=None) -> ValidationResult'
        purpose: Validate rendered Kubernetes YAML with caller-supplied schema tooling.
      - name: _local_directory_argument
        signature: '_local_directory_argument(value: str, *, cwd: Path, operation: str) -> Path'
        purpose: Implements _local_directory_argument for Product-neutral GitOps, YAML, JSON, Kustomize and client validation primitives.
      - name: run_read_only_source_validation
        signature: 'run_read_only_source_validation(arguments: Sequence[str], *, cwd: Path, environment: Mapping[str, str] | None=None, stdin: str='''', runner: CommandRunner | None=None)
          -> ValidationResult'
        purpose: Run one bounded caller-selected read-only source validation command.
      - name: inspect_gitops_sources
        signature: 'inspect_gitops_sources(paths: Sequence[Path]) -> SourceInspectionResult'
        purpose: Inspect caller-selected YAML/JSON sources and return Flux/Kustomization objects.
      - name: cleanup_gitops_state
        signature: 'cleanup_gitops_state(paths: Sequence[Path], *, root: Path) -> int'
        purpose: Idempotently remove bounded temporary render state without deleting the root.
      internal_dependencies:
      - packaging_primitives
      - runtime_primitives
    - path: src/ci_workflows/gitops_render.py
      responsibility: Deterministic YAML, Helm, Kustomize, and policy rendering.
      status: consolidate
      classes: []
      functions:
      - name: _is_helm_template_source
        signature: '_is_helm_template_source(path: Path, root: Path) -> bool'
        purpose: Classify only template source beneath an actual checked-in Helm chart.
      - name: _yaml_target
        signature: '_yaml_target(target: GitOpsTarget, source_root: Path, yaml: Any) -> tuple[list[Any], int]'
        purpose: Implements _yaml_target for Deterministic YAML, Helm, Kustomize, and policy rendering.
      - name: _nested_value
        signature: '_nested_value(value: Any, dotted: str) -> Any'
        purpose: Implements _nested_value for Deterministic YAML, Helm, Kustomize, and policy rendering.
      - name: _canonical_dependency_digest
        signature: '_canonical_dependency_digest(dependencies: Any) -> str'
        purpose: Implements _canonical_dependency_digest for Deterministic YAML, Helm, Kustomize, and policy rendering.
      - name: _helm_target
        signature: '_helm_target(target: GitOpsTarget, source_root: Path, state_root: Path, tools: GitOpsTools) -> tuple[list[Any], int]'
        purpose: Implements _helm_target for Deterministic YAML, Helm, Kustomize, and policy rendering.
      - name: _kustomization_path
        signature: '_kustomization_path(root: Path) -> Path'
        purpose: Implements _kustomization_path for Deterministic YAML, Helm, Kustomize, and policy rendering.
      - name: _validate_kustomization
        signature: '_validate_kustomization(root: Path, yaml: Any, visited: set[Path]) -> None'
        purpose: Implements _validate_kustomization for Deterministic YAML, Helm, Kustomize, and policy rendering.
      - name: _kustomize_target
        signature: '_kustomize_target(target: GitOpsTarget, source_root: Path, state_root: Path, tools: GitOpsTools) -> tuple[list[Any], int]'
        purpose: Implements _kustomize_target for Deterministic YAML, Helm, Kustomize, and policy rendering.
      - name: _compare_expected
        signature: '_compare_expected(target: GitOpsTarget, source_root: Path, documents: Sequence[Any], yaml: Any) -> None'
        purpose: Implements _compare_expected for Deterministic YAML, Helm, Kustomize, and policy rendering.
      - name: _policy
        signature: '_policy(plan: GitOpsPlan, source_root: Path, state_root: Path) -> str'
        purpose: Implements _policy for Deterministic YAML, Helm, Kustomize, and policy rendering.
      internal_dependencies:
      - gitops_contract
      - gitops_runtime
      - gitops_source
      - gitops_types
    - path: src/ci_workflows/gitops_runtime.py
      responsibility: Verified tools, bounded subprocesses, and cleanup for GitOps validation.
      status: keep
      classes:
      - name: GitOpsTools
        purpose: Exact runtime identities used by one execution.
        methods: []
      functions:
      - name: _fail
        signature: '_fail(code: str, detail: str='''') -> None'
        purpose: Implements _fail for Verified tools, bounded subprocesses, and cleanup for GitOps validation.
      - name: _require
        signature: '_require(condition: bool, code: str, detail: str='''') -> None'
        purpose: Implements _require for Verified tools, bounded subprocesses, and cleanup for GitOps validation.
      - name: _bounded_output
        signature: '_bounded_output(value: bytes, maximum: int, code: str) -> str'
        purpose: Implements _bounded_output for Verified tools, bounded subprocesses, and cleanup for GitOps validation.
      - name: _run
        signature: '_run(argv: Sequence[str], *, cwd: Path, environment: Mapping[str, str], timeout: int, max_output: int, code: str) -> str'
        purpose: Implements _run for Verified tools, bounded subprocesses, and cleanup for GitOps validation.
      - name: _state_marker
        signature: '_state_marker(state_root: Path) -> Path'
        purpose: Implements _state_marker for Verified tools, bounded subprocesses, and cleanup for GitOps validation.
      - name: _ensure_no_follow_directory
        signature: '_ensure_no_follow_directory(path: Path, *, code: str, detail: str) -> None'
        purpose: Create a state directory only through verified directory ancestors.
      - name: _open_new_file_no_follow
        signature: '_open_new_file_no_follow(path: Path, *, code: str, detail: str)'
        purpose: Open a new regular state file without following an existing link.
      - name: initialize_gitops_state
        signature: 'initialize_gitops_state(state_root: Path) -> None'
        purpose: Create one marker-bound runtime root without following a symlink.
      - name: _validate_marker
        signature: '_validate_marker(state_root: Path) -> None'
        purpose: Implements _validate_marker for Verified tools, bounded subprocesses, and cleanup for GitOps validation.
      - name: _remove_no_follow
        signature: '_remove_no_follow(path: Path) -> None'
        purpose: Delete a registered tree with lstat and no symlink traversal.
      - name: cleanup_gitops_state
        signature: 'cleanup_gitops_state(state_root: Path) -> None'
        purpose: Remove exactly one marker-bound issue-owned state root.
      - name: assert_zero_gitops_residue
        signature: 'assert_zero_gitops_residue(state_root: Path) -> None'
        purpose: Implements assert_zero_gitops_residue for Verified tools, bounded subprocesses, and cleanup for GitOps validation.
      - name: _download
        signature: '_download(pin: GitOpsToolPin, destination: Path) -> None'
        purpose: Implements _download for Verified tools, bounded subprocesses, and cleanup for GitOps validation.
      - name: _safe_tar_member
        signature: '_safe_tar_member(archive: Path, pin: GitOpsToolPin, destination: Path) -> Path'
        purpose: Implements _safe_tar_member for Verified tools, bounded subprocesses, and cleanup for GitOps validation.
      - name: _safe_wheel
        signature: '_safe_wheel(archive: Path, pin: GitOpsToolPin, destination: Path) -> None'
        purpose: Implements _safe_wheel for Verified tools, bounded subprocesses, and cleanup for GitOps validation.
      - name: _load_pinned_yaml
        signature: '_load_pinned_yaml(root: Path, expected_version: str) -> Any'
        purpose: Implements _load_pinned_yaml for Verified tools, bounded subprocesses, and cleanup for GitOps validation.
      - name: prepare_gitops_tools
        signature: 'prepare_gitops_tools(plan: GitOpsPlan, state_root: Path) -> GitOpsTools'
        purpose: Download, verify, safely install, and identify exact pinned tools.
      - name: _tool_environment
        signature: '_tool_environment(state_root: Path) -> dict[str, str]'
        purpose: Implements _tool_environment for Verified tools, bounded subprocesses, and cleanup for GitOps validation.
      internal_dependencies:
      - gitops_types
    - path: src/ci_workflows/gitops_source.py
      responsibility: Exact source snapshots, YAML, schema, and SOPS validation.
      status: consolidate
      classes: []
      functions:
      - name: _tree_digest
        signature: '_tree_digest(root: Path) -> str'
        purpose: Implements _tree_digest for Exact source snapshots, YAML, schema, and SOPS validation.
      - name: _source_snapshot
        signature: '_source_snapshot(source_root: Path, admitted_sha: str) -> str'
        purpose: Implements _source_snapshot for Exact source snapshots, YAML, schema, and SOPS validation.
      - name: _changed_paths
        signature: '_changed_paths(source_root: Path, base_sha: str, admitted_sha: str) -> tuple[str, ...]'
        purpose: Implements _changed_paths for Exact source snapshots, YAML, schema, and SOPS validation.
      - name: _target_matches
        signature: '_target_matches(target: GitOpsTarget, changed: Sequence[str]) -> bool'
        purpose: Implements _target_matches for Exact source snapshots, YAML, schema, and SOPS validation.
      - name: _selected_targets
        signature: '_selected_targets(plan: GitOpsPlan, source_root: Path) -> tuple[GitOpsTarget, ...]'
        purpose: Implements _selected_targets for Exact source snapshots, YAML, schema, and SOPS validation.
      - name: _unique_loader
        signature: '_unique_loader(yaml: Any) -> Any'
        purpose: Implements _unique_loader for Exact source snapshots, YAML, schema, and SOPS validation.
      - name: _yaml_documents
        signature: '_yaml_documents(path: Path, yaml: Any) -> list[Any]'
        purpose: Implements _yaml_documents for Exact source snapshots, YAML, schema, and SOPS validation.
      - name: _schema_type
        signature: '_schema_type(value: Any) -> str'
        purpose: Implements _schema_type for Exact source snapshots, YAML, schema, and SOPS validation.
      - name: _validate_schema
        signature: '_validate_schema(value: Any, schema: Mapping[str, Any], location: str=''$'') -> None'
        purpose: Implements _validate_schema for Exact source snapshots, YAML, schema, and SOPS validation.
      - name: _load_schema
        signature: '_load_schema(source_root: Path, relative: str, yaml: Any) -> Mapping[str, Any]'
        purpose: Implements _load_schema for Exact source snapshots, YAML, schema, and SOPS validation.
      - name: _object_identity
        signature: '_object_identity(value: Any) -> ObjectIdentity | None'
        purpose: Implements _object_identity for Exact source snapshots, YAML, schema, and SOPS validation.
      - name: _canonical_documents
        signature: '_canonical_documents(documents: Iterable[Any]) -> bytes'
        purpose: Implements _canonical_documents for Exact source snapshots, YAML, schema, and SOPS validation.
      - name: _validate_sops
        signature: '_validate_sops(document: Any, relative: str) -> None'
        purpose: Implements _validate_sops for Exact source snapshots, YAML, schema, and SOPS validation.
      - name: _glob_files
        signature: '_glob_files(root: Path, patterns: Sequence[str]) -> tuple[Path, ...]'
        purpose: Implements _glob_files for Exact source snapshots, YAML, schema, and SOPS validation.
      internal_dependencies:
      - gitops_contract
      - gitops_runtime
      - gitops_types
    - path: src/ci_workflows/gitops_types.py
      responsibility: Typed models and stable failures for source-only GitOps validation.
      status: consolidate
      classes:
      - name: GitOpsValidationError
        purpose: Fail-closed validation error carrying one stable public code.
        methods: []
      - name: GitOpsProfile
        purpose: Defines GitOpsProfile for Typed models and stable failures for source-only GitOps validation.
        methods: []
      - name: GitOpsTargetKind
        purpose: Defines GitOpsTargetKind for Typed models and stable failures for source-only GitOps validation.
        methods: []
      - name: GitOpsToolPin
        purpose: Defines GitOpsToolPin for Typed models and stable failures for source-only GitOps validation.
        methods: []
      - name: VendoredDependency
        purpose: Defines VendoredDependency for Typed models and stable failures for source-only GitOps validation.
        methods: []
      - name: GitOpsTarget
        purpose: Defines GitOpsTarget for Typed models and stable failures for source-only GitOps validation.
        methods: []
      - name: GitOpsPolicyScript
        purpose: Defines GitOpsPolicyScript for Typed models and stable failures for source-only GitOps validation.
        methods: []
      - name: GitOpsRequest
        purpose: Defines GitOpsRequest for Typed models and stable failures for source-only GitOps validation.
        methods: []
      - name: GitOpsPlan
        purpose: Defines GitOpsPlan for Typed models and stable failures for source-only GitOps validation.
        methods:
        - name: planning_outputs
          signature: planning_outputs(self) -> dict[str, str]
          purpose: Implements GitOpsPlan.planning_outputs behavior for Typed models and stable failures for source-only GitOps validation.
      - name: ObjectIdentity
        purpose: Defines ObjectIdentity for Typed models and stable failures for source-only GitOps validation.
        methods:
        - name: label
          signature: label(self) -> str
          purpose: Implements ObjectIdentity.label behavior for Typed models and stable failures for source-only GitOps validation.
      - name: GitOpsResult
        purpose: Defines GitOpsResult for Typed models and stable failures for source-only GitOps validation.
        methods:
        - name: output_values
          signature: output_values(self) -> dict[str, str]
          purpose: Implements GitOpsResult.output_values behavior for Typed models and stable failures for source-only GitOps validation.
      functions:
      - name: compact_json
        signature: 'compact_json(value: object) -> str'
        purpose: Return the canonical compact JSON used by stable outputs.
      - name: _compact_json
        signature: '_compact_json(value: object) -> str'
        purpose: Implements _compact_json for Typed models and stable failures for source-only GitOps validation.
      internal_dependencies: []
  gradle:
    target_module: src/ci_workflows/gradle_seed.py
    responsibility: Gradle dependency warming, Maven publication, and cache-seed synchronization.
    files:
    - path: src/ci_workflows/ciw_gradle_seed.py
      responsibility: Thin CLI adapter for internal Gradle dependency-cache synchronization.
      status: compatibility
      classes: []
      functions:
      - name: configure_gradle_seed_upload
        signature: 'configure_gradle_seed_upload(parser: argparse.ArgumentParser) -> None'
        purpose: Implements configure_gradle_seed_upload for Thin CLI adapter for internal Gradle dependency-cache synchronization.
      - name: _require_registered_gradle_home
        signature: '_require_registered_gradle_home(environment: Mapping[str, str]) -> None'
        purpose: 'Bind synchronization to ``prepare-workspace profile: gradle`` state.'
      - name: execute_gradle_seed_upload
        signature: 'execute_gradle_seed_upload(args: argparse.Namespace, context: CIWContext) -> CIWResult'
        purpose: Implements execute_gradle_seed_upload for Thin CLI adapter for internal Gradle dependency-cache synchronization.
      - name: parser
        signature: parser() -> argparse.ArgumentParser
        purpose: Implements parser for Thin CLI adapter for internal Gradle dependency-cache synchronization.
      - name: main
        signature: 'main(argv: Sequence[str] | None=None, *, environment: Mapping[str, str] | None=None, stdout: Any | None=None, stderr: Any | None=None) -> int'
        purpose: Implements main for Thin CLI adapter for internal Gradle dependency-cache synchronization.
      internal_dependencies:
      - ciw_types
      - gradle_seed
      - gradle_seed_internal
    - path: src/ci_workflows/gradle_dependency_warm.py
      responsibility: Resolve a Gradle project's dependency graph without compiling product code.
      status: keep
      classes:
      - name: GradleDependencyWarmResult
        purpose: Defines GradleDependencyWarmResult for Resolve a Gradle project's dependency graph without compiling product code.
        methods:
        - name: output_values
          signature: output_values(self) -> dict[str, str]
          purpose: Implements GradleDependencyWarmResult.output_values behavior for Resolve a Gradle project's dependency graph without compiling product code.
      functions:
      - name: _plain
        signature: '_plain(value: object, code: str, *, maximum: int=32768) -> str'
        purpose: Implements _plain for Resolve a Gradle project's dependency graph without compiling product code.
      - name: _relative
        signature: '_relative(value: object, code: str, *, allow_dot: bool) -> str'
        purpose: Implements _relative for Resolve a Gradle project's dependency graph without compiling product code.
      - name: _existing_directory
        signature: '_existing_directory(raw: object, code: str) -> Path'
        purpose: Implements _existing_directory for Resolve a Gradle project's dependency graph without compiling product code.
      - name: _runner_directory
        signature: '_runner_directory(raw: object, code: str) -> Path'
        purpose: Resolve a runner-owned toolchain directory without weakening state boundaries.
      - name: _bounded_directory
        signature: '_bounded_directory(root: Path, relative: str, code: str) -> Path'
        purpose: Implements _bounded_directory for Resolve a Gradle project's dependency graph without compiling product code.
      - name: _bounded_file
        signature: '_bounded_file(root: Path, relative: str, code: str) -> Path'
        purpose: Implements _bounded_file for Resolve a Gradle project's dependency graph without compiling product code.
      - name: _git_environment
        signature: '_git_environment(environment: Mapping[str, str]) -> dict[str, str]'
        purpose: Implements _git_environment for Resolve a Gradle project's dependency graph without compiling product code.
      - name: _git_output
        signature: '_git_output(root: Path, arguments: Sequence[str], environment: Mapping[str, str]) -> str'
        purpose: Implements _git_output for Resolve a Gradle project's dependency graph without compiling product code.
      - name: _verify_exact_source
        signature: '_verify_exact_source(root: Path, sha: str, environment: Mapping[str, str]) -> None'
        purpose: Implements _verify_exact_source for Resolve a Gradle project's dependency graph without compiling product code.
      - name: _copy_source
        signature: '_copy_source(source: Path, destination: Path) -> None'
        purpose: Implements _copy_source for Resolve a Gradle project's dependency graph without compiling product code.
      - name: _remove_no_follow
        signature: '_remove_no_follow(path: Path) -> None'
        purpose: Implements _remove_no_follow for Resolve a Gradle project's dependency graph without compiling product code.
      - name: _read_only_cache
        signature: '_read_only_cache(environment: Mapping[str, str]) -> Path | None'
        purpose: Implements _read_only_cache for Resolve a Gradle project's dependency graph without compiling product code.
      - name: _runtime_environment
        signature: '_runtime_environment(environment: Mapping[str, str], state_root: Path, private_dependency_subdirectory: str) -> dict[str, str]'
        purpose: Implements _runtime_environment for Resolve a Gradle project's dependency graph without compiling product code.
      - name: _diagnostic
        signature: '_diagnostic(result_stdout: str, result_stderr: str, roots: Sequence[Path]) -> None'
        purpose: Implements _diagnostic for Resolve a Gradle project's dependency graph without compiling product code.
      - name: warm_gradle_dependencies
        signature: 'warm_gradle_dependencies(*, admitted_sha: str, working_directory: str, gradle_wrapper_path: str, private_dependency_subdirectory: str, environment: Mapping[str, str])
          -> GradleDependencyWarmResult'
        purpose: Implements warm_gradle_dependencies for Resolve a Gradle project's dependency graph without compiling product code.
      - name: parser
        signature: parser() -> argparse.ArgumentParser
        purpose: Implements parser for Resolve a Gradle project's dependency graph without compiling product code.
      - name: main
        signature: 'main(argv: Sequence[str] | None=None) -> int'
        purpose: Implements main for Resolve a Gradle project's dependency graph without compiling product code.
      internal_dependencies:
      - ciw_types
      - runtime_primitives
    - path: src/ci_workflows/gradle_maven_publish.py
      responsibility: Bounded Gradle-to-Maven publication mechanics for reusable CI.
      status: keep
      classes:
      - name: GradleMavenPublishError
        purpose: Stable, non-secret Gradle Maven publication error.
        methods: []
      - name: GradleMavenPublishPlan
        purpose: Defines GradleMavenPublishPlan for Bounded Gradle-to-Maven publication mechanics for reusable CI.
        methods: []
      functions:
      - name: _relative
        signature: '_relative(root: Path, value: str, *, allow_dot: bool) -> Path'
        purpose: Implements _relative for Bounded Gradle-to-Maven publication mechanics for reusable CI.
      - name: _require_sha
        signature: '_require_sha(admitted_sha: str) -> str'
        purpose: Implements _require_sha for Bounded Gradle-to-Maven publication mechanics for reusable CI.
      - name: parse_publication_tasks
        signature: 'parse_publication_tasks(raw: str) -> tuple[str, ...]'
        purpose: Accept only bounded Gradle tasks that publish to one Maven repository.
      - name: derive_release_version
        signature: 'derive_release_version(base_version: str, *, github_ref: str, expected_branch: str, admitted_sha: str) -> str'
        purpose: Derive the only supported Maven version from the exact caller ref.
      - name: resolve_plan
        signature: 'resolve_plan(*, source_root: Path, admitted_sha: str, github_ref: str, expected_branch: str, working_directory: str, gradle_wrapper_path: str, version_file: str,
          arguments_json: str) -> GradleMavenPublishPlan'
        purpose: Validate caller-owned hooks without accepting commands or registry endpoints.
      - name: publish
        signature: 'publish(plan: GradleMavenPublishPlan, *, environment: Mapping[str, str], registry_username: str, registry_token: str) -> int'
        purpose: Run exactly one bounded Gradle publication process.
      - name: _git
        signature: '_git(source_root: Path, *arguments: str) -> str'
        purpose: Implements _git for Bounded Gradle-to-Maven publication mechanics for reusable CI.
      - name: _verify_admitted_source
        signature: '_verify_admitted_source(source_root: Path, admitted_sha: str) -> None'
        purpose: Implements _verify_admitted_source for Bounded Gradle-to-Maven publication mechanics for reusable CI.
      - name: cleanup_source
        signature: 'cleanup_source(source_root: Path, *, admitted_sha: str) -> None'
        purpose: Remove ignored Gradle build state from the disposable exact checkout.
      - name: verify_source_clean
        signature: 'verify_source_clean(source_root: Path, *, admitted_sha: str) -> None'
        purpose: Require exact HEAD and no tracked, untracked, or ignored build residue.
      - name: append_github_outputs
        signature: 'append_github_outputs(path: str | None, **values: str | int) -> None'
        purpose: Implements append_github_outputs for Bounded Gradle-to-Maven publication mechanics for reusable CI.
      - name: _plan_from_environment
        signature: '_plan_from_environment(environment: Mapping[str, str]) -> GradleMavenPublishPlan'
        purpose: Implements _plan_from_environment for Bounded Gradle-to-Maven publication mechanics for reusable CI.
      - name: main
        signature: 'main(environment: Mapping[str, str] | None=None) -> int'
        purpose: Dispatch one internal action phase without logging credential material.
      internal_dependencies: []
    - path: src/ci_workflows/gradle_seed.py
      responsibility: Portable Gradle dependency-module cache selection and framing primitives.
      status: keep
      classes:
      - name: GradleSeedError
        purpose: Fail-closed Gradle seed error with a stable non-secret code.
        methods: []
      - name: SeedFile
        purpose: Defines SeedFile for Portable Gradle dependency-module cache selection and framing primitives.
        methods:
        - name: header_bytes
          signature: header_bytes(self) -> bytes
          purpose: Implements SeedFile.header_bytes behavior for Portable Gradle dependency-module cache selection and framing primitives.
      - name: UploadResponse
        purpose: Defines UploadResponse for Portable Gradle dependency-module cache selection and framing primitives.
        methods: []
      - name: GradleSeedResult
        purpose: Defines GradleSeedResult for Portable Gradle dependency-module cache selection and framing primitives.
        methods:
        - name: output_values
          signature: output_values(self) -> dict[str, str]
          purpose: Implements GradleSeedResult.output_values behavior for Portable Gradle dependency-module cache selection and framing primitives.
      functions:
      - name: _safe_relative_path
        signature: '_safe_relative_path(parts: tuple[str, ...]) -> str'
        purpose: Implements _safe_relative_path for Portable Gradle dependency-module cache selection and framing primitives.
      - name: _directory_flags
        signature: _directory_flags() -> int
        purpose: Implements _directory_flags for Portable Gradle dependency-module cache selection and framing primitives.
      - name: _regular_flags
        signature: _regular_flags() -> int
        purpose: Implements _regular_flags for Portable Gradle dependency-module cache selection and framing primitives.
      - name: _open_absolute_directory_nofollow
        signature: '_open_absolute_directory_nofollow(path: Path, *, code: str=''gradle_seed_path_rejected'') -> tuple[int, os.stat_result]'
        purpose: Implements _open_absolute_directory_nofollow for Portable Gradle dependency-module cache selection and framing primitives.
      - name: _open_child_directory
        signature: '_open_child_directory(parent_fd: int, name: str, *, expected: os.stat_result | None=None, code: str=''gradle_seed_path_rejected'') -> tuple[int, os.stat_result]'
        purpose: Implements _open_child_directory for Portable Gradle dependency-module cache selection and framing primitives.
      - name: _directory_entries
        signature: '_directory_entries(directory_fd: int) -> tuple[tuple[str, os.stat_result], ...]'
        purpose: Implements _directory_entries for Portable Gradle dependency-module cache selection and framing primitives.
      - name: _require_regular_identity
        signature: '_require_regular_identity(current: os.stat_result, expected: os.stat_result) -> None'
        purpose: Implements _require_regular_identity for Portable Gradle dependency-module cache selection and framing primitives.
      - name: _open_regular_at
        signature: '_open_regular_at(parent_fd: int, name: str, expected: os.stat_result) -> tuple[int, os.stat_result]'
        purpose: Implements _open_regular_at for Portable Gradle dependency-module cache selection and framing primitives.
      - name: _hash_open_regular_file
        signature: '_hash_open_regular_file(parent_fd: int, name: str, expected: os.stat_result) -> str'
        purpose: Implements _hash_open_regular_file for Portable Gradle dependency-module cache selection and framing primitives.
      - name: collect_seed_files
        signature: 'collect_seed_files(gradle_user_home: Path) -> tuple[SeedFile, ...]'
        purpose: Collect deterministic writable ``modules-*`` files without following links.
      - name: _open_selected_file
        signature: '_open_selected_file(seed_file: SeedFile) -> int'
        purpose: Implements _open_selected_file for Portable Gradle dependency-module cache selection and framing primitives.
      - name: framed_seed_stream
        signature: 'framed_seed_stream(files: tuple[SeedFile, ...]) -> Iterator[bytes]'
        purpose: Yield the protocol without materializing an archive or candidate copy.
      internal_dependencies: []
    - path: src/ci_workflows/gradle_seed_internal.py
      responsibility: Internal best-effort Gradle dependency-seed sync transport.
      status: keep
      classes:
      - name: SeedUploader
        purpose: Defines SeedUploader for Internal best-effort Gradle dependency-seed sync transport.
        methods:
        - name: upload
          signature: 'upload(self, *, source_sha: str, body: Iterable[bytes]) -> UploadResponse'
          purpose: Implements SeedUploader.upload behavior for Internal best-effort Gradle dependency-seed sync transport.
      - name: InternalFluxSeedUploader
        purpose: Stream one framed delta only to the fixed cluster-local Flux service.
        methods:
        - name: upload
          signature: 'upload(self, *, source_sha: str, body: Iterable[bytes]) -> UploadResponse'
          purpose: Implements InternalFluxSeedUploader.upload behavior for Internal best-effort Gradle dependency-seed sync transport.
      functions:
      - name: _manifest_sha256
        signature: '_manifest_sha256(files: tuple[SeedFile, ...]) -> str'
        purpose: Implements _manifest_sha256 for Internal best-effort Gradle dependency-seed sync transport.
      - name: _rejection_code
        signature: '_rejection_code(status: int) -> str'
        purpose: Implements _rejection_code for Internal best-effort Gradle dependency-seed sync transport.
      - name: _validated_response
        signature: '_validated_response(response: UploadResponse, *, source_sha: str, files: tuple[SeedFile, ...]) -> tuple[str, int]'
        purpose: Implements _validated_response for Internal best-effort Gradle dependency-seed sync transport.
      - name: sync_gradle_seed
        signature: 'sync_gradle_seed(*, source_sha: str, environment: Mapping[str, str], uploader: SeedUploader | None=None, report_selection: SelectionReporter | None=None) -> GradleSeedResult'
        purpose: Stream one job-private dependency delta without running Gradle again.
      internal_dependencies:
      - gradle_seed
  helm:
    target_module: src/ci_workflows/helm.py
    responsibility: Helm validation, packaging, dependency policy, registry publication, and read-back.
    files:
    - path: src/ci_workflows/ciw_helm.py
      responsibility: Thin, bounded Helm command adapter registered through shared CIW.
      status: compatibility
      classes: []
      functions:
      - name: _state_root
        signature: '_state_root(root: Path, environment: Mapping[str, str]) -> Path'
        purpose: Implements _state_root for Thin, bounded Helm command adapter registered through shared CIW.
      - name: _source_root
        signature: '_source_root(root: Path, environment: Mapping[str, str], relative: str) -> Path'
        purpose: Implements _source_root for Thin, bounded Helm command adapter registered through shared CIW.
      - name: _require_operation_trust
        signature: '_require_operation_trust(request: HelmRequest, operation: str) -> None'
        purpose: Implements _require_operation_trust for Thin, bounded Helm command adapter registered through shared CIW.
      - name: _runner_profile
        signature: '_runner_profile(operation: str) -> str'
        purpose: Implements _runner_profile for Thin, bounded Helm command adapter registered through shared CIW.
      - name: _failure_outputs
        signature: '_failure_outputs(environment: Mapping[str, str], code: str, operation: str) -> None'
        purpose: Implements _failure_outputs for Thin, bounded Helm command adapter registered through shared CIW.
      - name: _plan
        signature: '_plan(root: Path, environment: Mapping[str, str], operation: str) -> dict[str, str]'
        purpose: Resolve only shared runner mechanics; product metadata stays in caller source.
      - name: _caller_manifest_contract
        signature: '_caller_manifest_contract(request: HelmRequest) -> dict[str, object]'
        purpose: Compatibility shape for the existing manifest parser without a central allowlist.
      - name: execute
        signature: 'execute(root: Path, environment: Mapping[str, str], *, operation: str, phase: str, source_relative: str) -> dict[str, str]'
        purpose: Implements execute for Thin, bounded Helm command adapter registered through shared CIW.
      - name: _configure
        signature: '_configure(parser: argparse.ArgumentParser) -> None'
        purpose: Implements _configure for Thin, bounded Helm command adapter registered through shared CIW.
      - name: configure_helm_validate
        signature: 'configure_helm_validate(parser: argparse.ArgumentParser) -> None'
        purpose: Implements configure_helm_validate for Thin, bounded Helm command adapter registered through shared CIW.
      - name: configure_helm_publish
        signature: 'configure_helm_publish(parser: argparse.ArgumentParser) -> None'
        purpose: Implements configure_helm_publish for Thin, bounded Helm command adapter registered through shared CIW.
      - name: execute_helm_validate
        signature: 'execute_helm_validate(args: argparse.Namespace, context: CIWContext) -> CIWResult'
        purpose: Implements execute_helm_validate for Thin, bounded Helm command adapter registered through shared CIW.
      - name: execute_helm_publish
        signature: 'execute_helm_publish(args: argparse.Namespace, context: CIWContext) -> CIWResult'
        purpose: Implements execute_helm_publish for Thin, bounded Helm command adapter registered through shared CIW.
      - name: main
        signature: 'main(argv: Sequence[str] | None=None) -> int'
        purpose: Implements main for Thin, bounded Helm command adapter registered through shared CIW.
      internal_dependencies:
      - ciw_types
      - helm_archive
      - helm_contract
      - helm_execution
      - helm_measurement
      - helm_policy
      - helm_simple
      - helm_types
      - runners
      - workspace
    - path: src/ci_workflows/helm.py
      responsibility: Public façade for bounded Helm validation and OCI chart publication.
      status: keep
      classes: []
      functions:
      - name: validate
        signature: 'validate(source_root: Path, state_root: Path, plan: HelmPlan, admitted_sha: str, environment: Mapping[str, str]) -> HelmValidationResult'
        purpose: Validate one resolved product chart and emit its deterministic digest.
      - name: package
        signature: 'package(source_root: Path, state_root: Path, plan: HelmPlan, admitted_sha: str, environment: Mapping[str, str]) -> HelmValidationResult'
        purpose: Package via the same validation boundary; standalone packaging is forbidden.
      - name: _remote_publication_result
        signature: '_remote_publication_result(source_root: Path, state_root: Path, plan: HelmPlan, validation: HelmValidationResult, environment: Mapping[str, str]) -> HelmPublicationResult'
        purpose: Implements _remote_publication_result for Public façade for bounded Helm validation and OCI chart publication.
      - name: publish
        signature: 'publish(source_root: Path, state_root: Path, plan: HelmPlan, validation: HelmValidationResult, environment: Mapping[str, str]) -> HelmPublicationResult'
        purpose: Publish on tag authority, then return exact remote manifest evidence.
      - name: read_back
        signature: 'read_back(source_root: Path, state_root: Path, plan: HelmPlan, validation: HelmValidationResult, environment: Mapping[str, str]) -> HelmPublicationResult'
        purpose: Verify existing immutable content without permitting publication.
      internal_dependencies:
      - helm_archive
      - helm_contract
      - helm_dependency_policy
      - helm_execution
      - helm_manifest
      - helm_policy
      - helm_registry
      - helm_types
    - path: src/ci_workflows/helm_archive.py
      responsibility: Recursive deterministic Helm chart archive canonicalization.
      status: consolidate
      classes: []
      functions:
      - name: _member_name
        signature: '_member_name(name: str, root_name: str) -> str'
        purpose: Implements _member_name for Recursive deterministic Helm chart archive canonicalization.
      - name: _infer_root
        signature: '_infer_root(members: list[tarfile.TarInfo]) -> str'
        purpose: Implements _infer_root for Recursive deterministic Helm chart archive canonicalization.
      - name: _scan_text
        signature: '_scan_text(content: bytes) -> None'
        purpose: Implements _scan_text for Recursive deterministic Helm chart archive canonicalization.
      - name: _write_archive
        signature: '_write_archive(members: list[tuple[str, bool, bytes]]) -> bytes'
        purpose: Implements _write_archive for Recursive deterministic Helm chart archive canonicalization.
      - name: canonicalize_chart_archive_bytes
        signature: 'canonicalize_chart_archive_bytes(content: bytes, *, expected_root: str | None=None, depth: int=0, _budget: dict[str, int] | None=None) -> bytes'
        purpose: Canonicalize an outer chart and nested subcharts under one total budget.
      - name: canonicalize_chart_archive
        signature: 'canonicalize_chart_archive(source: Path, destination: Path, chart_name: str) -> str'
        purpose: Implements canonicalize_chart_archive for Recursive deterministic Helm chart archive canonicalization.
      - name: finalize_validation_archive
        signature: 'finalize_validation_archive(validation: HelmValidationResult, chart_name: str) -> HelmValidationResult'
        purpose: Replace one preliminary package with its recursively canonical package.
      internal_dependencies:
      - helm_contract
      - helm_types
    - path: src/ci_workflows/helm_contract.py
      responsibility: 'Strict product-manifest and Helm chart admission for issue #18.'
      status: consolidate
      classes: []
      functions:
      - name: require
        signature: 'require(condition: bool, code: str) -> None'
        purpose: 'Implements require for Strict product-manifest and Helm chart admission for issue #18.'
      - name: safe_relative
        signature: 'safe_relative(value: Any, code: str=''invalid_input'', *, allow_dot: bool=False) -> str'
        purpose: 'Implements safe_relative for Strict product-manifest and Helm chart admission for issue #18.'
      - name: bounded_path
        signature: 'bounded_path(root: Path, relative: str, code: str=''invalid_input'') -> Path'
        purpose: Return an in-root ordinary path without following a symlink component.
      - name: _json
        signature: '_json(path: Path, code: str) -> Mapping[str, Any]'
        purpose: 'Implements _json for Strict product-manifest and Helm chart admission for issue #18.'
      - name: _yaml
        signature: '_yaml(path: Path, code: str) -> Mapping[str, Any]'
        purpose: 'Implements _yaml for Strict product-manifest and Helm chart admission for issue #18.'
      - name: _expected_upstream_assets
        signature: '_expected_upstream_assets(value: Any) -> tuple[tuple[str, str], ...]'
        purpose: 'Implements _expected_upstream_assets for Strict product-manifest and Helm chart admission for issue #18.'
      - name: load_helm_contract
        signature: 'load_helm_contract(root: Path) -> Mapping[str, Any]'
        purpose: 'Implements load_helm_contract for Strict product-manifest and Helm chart admission for issue #18.'
      - name: load_helm_publication_contract
        signature: 'load_helm_publication_contract(root: Path) -> Mapping[str, Any]'
        purpose: 'Implements load_helm_publication_contract for Strict product-manifest and Helm chart admission for issue #18.'
      - name: request_from_environment
        signature: 'request_from_environment(environment: Mapping[str, str]) -> HelmRequest'
        purpose: 'Implements request_from_environment for Strict product-manifest and Helm chart admission for issue #18.'
      - name: _dependency_repository
        signature: '_dependency_repository(value: Any) -> str'
        purpose: 'Implements _dependency_repository for Strict product-manifest and Helm chart admission for issue #18.'
      - name: _locked_dependencies
        signature: '_locked_dependencies(value: Any) -> tuple[tuple[str, str, str], ...]'
        purpose: 'Implements _locked_dependencies for Strict product-manifest and Helm chart admission for issue #18.'
      - name: _upstream_repository
        signature: '_upstream_repository(value: Any) -> str'
        purpose: 'Implements _upstream_repository for Strict product-manifest and Helm chart admission for issue #18.'
      - name: _upstream_assets
        signature: '_upstream_assets(value: Any, expected: Any, source_root: Path, chart_root: str) -> None'
        purpose: 'Implements _upstream_assets for Strict product-manifest and Helm chart admission for issue #18.'
      - name: load_product_manifest
        signature: 'load_product_manifest(source_root: Path, contract: Mapping[str, Any], request: HelmRequest) -> HelmProduct'
        purpose: 'Implements load_product_manifest for Strict product-manifest and Helm chart admission for issue #18.'
      - name: resolve_validation_plan
        signature: 'resolve_validation_plan(source_root: Path, contract: Mapping[str, Any], request: HelmRequest) -> HelmPlan'
        purpose: 'Implements resolve_validation_plan for Strict product-manifest and Helm chart admission for issue #18.'
      - name: validate_chart_metadata
        signature: 'validate_chart_metadata(chart_root: Path, product: HelmProduct) -> Mapping[str, Any]'
        purpose: 'Implements validate_chart_metadata for Strict product-manifest and Helm chart admission for issue #18.'
      - name: validate_chart_lock
        signature: 'validate_chart_lock(chart_root: Path, product: HelmProduct) -> None'
        purpose: 'Implements validate_chart_lock for Strict product-manifest and Helm chart admission for issue #18.'
      - name: validate_chart_layout
        signature: 'validate_chart_layout(source_root: Path, plan: HelmPlan) -> tuple[Path, Path]'
        purpose: 'Implements validate_chart_layout for Strict product-manifest and Helm chart admission for issue #18.'
      internal_dependencies:
      - helm_types
    - path: src/ci_workflows/helm_dependency_policy.py
      responsibility: Central product admission policy for Helm planning.
      status: consolidate
      classes: []
      functions:
      - name: _repository
        signature: '_repository(value: Any) -> str'
        purpose: Implements _repository for Central product admission policy for Helm planning.
      - name: _rows
        signature: '_rows(value: Any) -> tuple[tuple[str, str, str], ...]'
        purpose: Implements _rows for Central product admission policy for Helm planning.
      - name: load_dependency_policy
        signature: 'load_dependency_policy(root: Path=CENTRAL_ROOT) -> Mapping[str, Any]'
        purpose: Implements load_dependency_policy for Central product admission policy for Helm planning.
      - name: enforce_dependency_policy
        signature: 'enforce_dependency_policy(plan: HelmPlan, policy: Mapping[str, Any]) -> HelmPlan'
        purpose: Implements enforce_dependency_policy for Central product admission policy for Helm planning.
      - name: resolve_validation_plan
        signature: 'resolve_validation_plan(source_root: Path, contract: Mapping[str, Any], request: HelmRequest, *, contract_root: Path=CENTRAL_ROOT) -> HelmPlan'
        purpose: Resolve caller intent only after every central product policy matches.
      internal_dependencies:
      - helm_contract
      - helm_policy
      - helm_product_layout
      - helm_types
      - helm_upstream_policy
    - path: src/ci_workflows/helm_execution.py
      responsibility: Hermetic Helm validation, deterministic packaging, and OCI read-back.
      status: consolidate
      classes: []
      functions:
      - name: sanitize
        signature: 'sanitize(value: str, roots: Sequence[Path]=()) -> str'
        purpose: Implements sanitize for Hermetic Helm validation, deterministic packaging, and OCI read-back.
      - name: _run
        signature: '_run(argv: Sequence[str], *, cwd: Path, environment: Mapping[str, str], timeout: int, code: str, stdin: str | None=None, check: bool=True) -> subprocess.CompletedProcess[str]'
        purpose: Implements _run for Hermetic Helm validation, deterministic packaging, and OCI read-back.
      - name: _remove_no_follow
        signature: '_remove_no_follow(path: Path) -> None'
        purpose: Implements _remove_no_follow for Hermetic Helm validation, deterministic packaging, and OCI read-back.
      - name: cleanup_helm_state
        signature: 'cleanup_helm_state(state_root: Path) -> None'
        purpose: Implements cleanup_helm_state for Hermetic Helm validation, deterministic packaging, and OCI read-back.
      - name: verify_no_helm_residue
        signature: 'verify_no_helm_residue(state_root: Path) -> None'
        purpose: Implements verify_no_helm_residue for Hermetic Helm validation, deterministic packaging, and OCI read-back.
      - name: _runtime_environment
        signature: '_runtime_environment(inherited: Mapping[str, str], state_root: Path) -> dict[str, str]'
        purpose: Implements _runtime_environment for Hermetic Helm validation, deterministic packaging, and OCI read-back.
      - name: verify_helm_toolchain
        signature: 'verify_helm_toolchain(source_root: Path, environment: Mapping[str, str]) -> None'
        purpose: Implements verify_helm_toolchain for Hermetic Helm validation, deterministic packaging, and OCI read-back.
      - name: verify_exact_source
        signature: 'verify_exact_source(source_root: Path, admitted_sha: str, environment: Mapping[str, str]) -> None'
        purpose: Implements verify_exact_source for Hermetic Helm validation, deterministic packaging, and OCI read-back.
      - name: _archive_member_name
        signature: '_archive_member_name(name: str, chart_name: str) -> str'
        purpose: Implements _archive_member_name for Hermetic Helm validation, deterministic packaging, and OCI read-back.
      - name: normalize_chart_archive
        signature: 'normalize_chart_archive(source: Path, destination: Path, chart_name: str) -> str'
        purpose: Rewrite one ordinary chart archive to stable order, modes, and mtimes.
      - name: _image_reference_assertions
        signature: '_image_reference_assertions(rendered: str, expected: Sequence[str]) -> None'
        purpose: Implements _image_reference_assertions for Hermetic Helm validation, deterministic packaging, and OCI read-back.
      - name: _chart_version
        signature: '_chart_version(chart_root: Path) -> str'
        purpose: Implements _chart_version for Hermetic Helm validation, deterministic packaging, and OCI read-back.
      - name: _copy_chart_for_build
        signature: '_copy_chart_for_build(chart_root: Path, values_path: Path, state_root: Path, chart_name: str) -> tuple[Path, Path]'
        purpose: Implements _copy_chart_for_build for Hermetic Helm validation, deterministic packaging, and OCI read-back.
      - name: validate_and_package
        signature: 'validate_and_package(source_root: Path, state_root: Path, plan: HelmPlan, admitted_sha: str, inherited: Mapping[str, str]) -> HelmValidationResult'
        purpose: Implements validate_and_package for Hermetic Helm validation, deterministic packaging, and OCI read-back.
      - name: _registry_host
        signature: '_registry_host(repository: str) -> str'
        purpose: Implements _registry_host for Hermetic Helm validation, deterministic packaging, and OCI read-back.
      - name: _verify_no_kubernetes_authority
        signature: '_verify_no_kubernetes_authority(inherited: Mapping[str, str]) -> None'
        purpose: Implements _verify_no_kubernetes_authority for Hermetic Helm validation, deterministic packaging, and OCI read-back.
      - name: publish_and_read_back
        signature: 'publish_and_read_back(source_root: Path, state_root: Path, plan: HelmPlan, validation: HelmValidationResult, inherited: Mapping[str, str], *, allow_publish: bool=True)
          -> HelmPublicationResult'
        purpose: Push once when authorized or prove byte-identical prior publication.
      internal_dependencies:
      - helm_contract
      - helm_runtime
      - helm_types
    - path: src/ci_workflows/helm_manifest.py
      responsibility: Independent Helm OCI raw-manifest digest and layer read-back proof.
      status: consolidate
      classes: []
      functions:
      - name: remote_chart_manifest_digest
        signature: 'remote_chart_manifest_digest(source_root: Path, state_root: Path, chart_reference: str, release_version: str, expected_package_sha256: str, inherited: Mapping[str,
          str]) -> str'
        purpose: Hash the exact raw remote manifest bytes and verify the Helm content layer.
      internal_dependencies:
      - helm_contract
      - helm_execution
      - helm_types
    - path: src/ci_workflows/helm_measurement.py
      responsibility: Bounded peak resource evidence for trusted Helm publication jobs.
      status: consolidate
      classes: []
      functions:
      - name: _json
        signature: '_json(path: Path, code: str) -> Mapping[str, Any]'
        purpose: Implements _json for Bounded peak resource evidence for trusted Helm publication jobs.
      - name: _write_json
        signature: '_write_json(path: Path, value: Mapping[str, Any]) -> None'
        purpose: Implements _write_json for Bounded peak resource evidence for trusted Helm publication jobs.
      - name: _allocated_bytes
        signature: '_allocated_bytes(root: Path) -> int'
        purpose: Count allocated bytes below one root without following symlinks.
      - name: _read_positive_integer
        signature: '_read_positive_integer(path: Path) -> int | None'
        purpose: Implements _read_positive_integer for Bounded peak resource evidence for trusted Helm publication jobs.
      - name: _memory_bytes
        signature: _memory_bytes() -> int
        purpose: Implements _memory_bytes for Bounded peak resource evidence for trusted Helm publication jobs.
      - name: _roots
        signature: '_roots(environment: Mapping[str, str]) -> tuple[Path, Path, Path]'
        purpose: Implements _roots for Bounded peak resource evidence for trusted Helm publication jobs.
      - name: _policy
        signature: '_policy(root: Path) -> Mapping[str, Any]'
        purpose: Implements _policy for Bounded peak resource evidence for trusted Helm publication jobs.
      - name: _finalize_evidence
        signature: '_finalize_evidence(root: Path, config: Mapping[str, Any], record: Mapping[str, Any]) -> dict[str, str]'
        purpose: Implements _finalize_evidence for Bounded peak resource evidence for trusted Helm publication jobs.
      - name: start
        signature: 'start(root: Path, environment: Mapping[str, str]) -> dict[str, str]'
        purpose: Implements start for Bounded peak resource evidence for trusted Helm publication jobs.
      - name: monitor
        signature: 'monitor(state: Path) -> int'
        purpose: Implements monitor for Bounded peak resource evidence for trusted Helm publication jobs.
      - name: _read_pid
        signature: '_read_pid(state: Path) -> int'
        purpose: Implements _read_pid for Bounded peak resource evidence for trusted Helm publication jobs.
      - name: _terminate
        signature: '_terminate(pid: int | None) -> None'
        purpose: Implements _terminate for Bounded peak resource evidence for trusted Helm publication jobs.
      - name: _cleanup_state
        signature: '_cleanup_state(state: Path, pid: int | None) -> None'
        purpose: Implements _cleanup_state for Bounded peak resource evidence for trusted Helm publication jobs.
      - name: stop
        signature: 'stop(root: Path, environment: Mapping[str, str]) -> dict[str, str]'
        purpose: Implements stop for Bounded peak resource evidence for trusted Helm publication jobs.
      internal_dependencies:
      - helm_contract
      - helm_types
      - runners
    - path: src/ci_workflows/helm_policy.py
      responsibility: Central product-owned Helm policy hook admission and execution.
      status: consolidate
      classes: []
      functions:
      - name: _policy_path
        signature: '_policy_path(value: Any) -> str | None'
        purpose: Implements _policy_path for Central product-owned Helm policy hook admission and execution.
      - name: _tree_fingerprint
        signature: '_tree_fingerprint(root: Path) -> str'
        purpose: Hash the isolated chart tree without following links.
      - name: load_policy_hook_contract
        signature: 'load_policy_hook_contract(root: Path=CENTRAL_ROOT) -> Mapping[str, Any]'
        purpose: Implements load_policy_hook_contract for Central product-owned Helm policy hook admission and execution.
      - name: enforce_policy_hook
        signature: 'enforce_policy_hook(source_root: Path, product_id: str, policy: Mapping[str, Any]) -> None'
        purpose: Implements enforce_policy_hook for Central product-owned Helm policy hook admission and execution.
      - name: run_policy_hook
        signature: 'run_policy_hook(source_root: Path, state_root: Path, plan: HelmPlan, admitted_sha: str, inherited: Mapping[str, str]) -> int'
        purpose: Run one centrally approved checked-in shell hook as a read-only validator.
      internal_dependencies:
      - helm_contract
      - helm_execution
      - helm_types
    - path: src/ci_workflows/helm_product_layout.py
      responsibility: Central Helm product layout policy for chart roots and render profiles.
      status: consolidate
      classes: []
      functions:
      - name: _profiles
        signature: '_profiles(value: Any, code: str) -> dict[str, str]'
        purpose: Implements _profiles for Central Helm product layout policy for chart roots and render profiles.
      - name: _repositories
        signature: '_repositories(value: Any) -> tuple[str, ...]'
        purpose: Implements _repositories for Central Helm product layout policy for chart roots and render profiles.
      - name: _product
        signature: '_product(value: Any) -> tuple[str, dict[str, str], int, tuple[str, ...]]'
        purpose: Implements _product for Central Helm product layout policy for chart roots and render profiles.
      - name: load_product_layout
        signature: 'load_product_layout(root: Path=CENTRAL_ROOT) -> Mapping[str, Any]'
        purpose: Implements load_product_layout for Central Helm product layout policy for chart roots and render profiles.
      - name: enforce_product_layout
        signature: 'enforce_product_layout(source_root: Path, product_id: str, policy: Mapping[str, Any]) -> None'
        purpose: Implements enforce_product_layout for Central Helm product layout policy for chart roots and render profiles.
      internal_dependencies:
      - helm_contract
      - helm_types
    - path: src/ci_workflows/helm_registry.py
      responsibility: Fail-closed Helm registry publication and exact immutable read-back.
      status: keep
      classes: []
      functions:
      - name: _release_mode
        signature: '_release_mode(environment: Mapping[str, str]) -> str'
        purpose: Implements _release_mode for Fail-closed Helm registry publication and exact immutable read-back.
      - name: _definite_remote_absence
        signature: '_definite_remote_absence(stdout: str, stderr: str, *, source_root: Path, state_root: Path) -> bool'
        purpose: Accept only registry-standard unknown-manifest/name signals as absence.
      - name: _assert_remote_archive
        signature: '_assert_remote_archive(remote_root: Path, remote_archive: Path) -> None'
        purpose: Implements _assert_remote_archive for Fail-closed Helm registry publication and exact immutable read-back.
      - name: publish_and_read_back
        signature: 'publish_and_read_back(source_root: Path, state_root: Path, plan: HelmPlan, validation: HelmValidationResult, inherited: Mapping[str, str]) -> HelmPublicationResult'
        purpose: Publish only on trusted tag-push or verify one existing immutable chart.
      internal_dependencies:
      - helm_contract
      - helm_execution
      - helm_types
    - path: src/ci_workflows/helm_release.py
      responsibility: 'Exact-tag Helm release binding and remote OCI manifest evidence for issue #18.'
      status: consolidate
      classes: []
      functions:
      - name: _mapping
        signature: '_mapping(value: Any, code: str) -> Mapping[str, Any]'
        purpose: 'Implements _mapping for Exact-tag Helm release binding and remote OCI manifest evidence for issue #18.'
      - name: _repository
        signature: '_repository(value: Any, code: str) -> str'
        purpose: 'Implements _repository for Exact-tag Helm release binding and remote OCI manifest evidence for issue #18.'
      - name: load_release_bindings
        signature: 'load_release_bindings(root: Path) -> Mapping[str, Any]'
        purpose: 'Implements load_release_bindings for Exact-tag Helm release binding and remote OCI manifest evidence for issue #18.'
      - name: parse_oci_publication_evidence
        signature: 'parse_oci_publication_evidence(image_digest_raw: str, immutable_references_raw: str, product_id: str, contract: Mapping[str, Any], admitted_sha: str, release_version:
          str) -> tuple[str, ...]'
        purpose: 'Implements parse_oci_publication_evidence for Exact-tag Helm release binding and remote OCI manifest evidence for issue #18.'
      - name: _yaml_scalar_node
        signature: '_yaml_scalar_node(text: str, key_path: Sequence[str])'
        purpose: 'Implements _yaml_scalar_node for Exact-tag Helm release binding and remote OCI manifest evidence for issue #18.'
      - name: apply_release_image_bindings
        signature: 'apply_release_image_bindings(chart_root: Path, product_contract: Mapping[str, Any], references: tuple[str, ...]) -> None'
        purpose: 'Implements apply_release_image_bindings for Exact-tag Helm release binding and remote OCI manifest evidence for issue #18.'
      - name: validate_and_package_release
        signature: 'validate_and_package_release(source_root: Path, state_root: Path, plan: HelmPlan, admitted_sha: str, references: tuple[str, ...], product_contract: Mapping[str, Any],
          inherited: Mapping[str, str]) -> HelmValidationResult'
        purpose: 'Implements validate_and_package_release for Exact-tag Helm release binding and remote OCI manifest evidence for issue #18.'
      - name: remote_chart_manifest_digest
        signature: 'remote_chart_manifest_digest(source_root: Path, state_root: Path, chart_reference: str, release_version: str, expected_package_sha256: str, inherited: Mapping[str,
          str]) -> str'
        purpose: 'Implements remote_chart_manifest_digest for Exact-tag Helm release binding and remote OCI manifest evidence for issue #18.'
      internal_dependencies:
      - helm_contract
      - helm_execution
      - helm_types
    - path: src/ci_workflows/helm_runtime.py
      responsibility: Reviewed fixed runtime identity for the portable Helm workflow family.
      status: consolidate
      classes: []
      functions: []
      internal_dependencies: []
    - path: src/ci_workflows/helm_simple.py
      responsibility: Simple Helm validation/package/publish path for the core reusable workflow.
      status: consolidate
      classes: []
      functions:
      - name: _string_list
        signature: '_string_list(value: Any, code: str) -> list[str]'
        purpose: Implements _string_list for Simple Helm validation/package/publish path for the core reusable workflow.
      - name: resolve_plan
        signature: 'resolve_plan(source_root: Path, contract: Mapping[str, Any], request: HelmRequest) -> HelmPlan'
        purpose: Resolve the product-owned chart manifest with only repository admission central.
      - name: _reject_latest_images
        signature: '_reject_latest_images(rendered: str) -> None'
        purpose: Keep one generic image hygiene rule without owning product digest policy.
      - name: validate_and_package
        signature: 'validate_and_package(source_root: Path, state_root: Path, plan: HelmPlan, admitted_sha: str, inherited: Mapping[str, str]) -> HelmValidationResult'
        purpose: Lint, render, and package one caller-owned product chart.
      - name: _registry_host
        signature: '_registry_host(repository: str) -> str'
        purpose: Implements _registry_host for Simple Helm validation/package/publish path for the core reusable workflow.
      - name: publish
        signature: 'publish(source_root: Path, state_root: Path, plan: HelmPlan, validation: HelmValidationResult, inherited: Mapping[str, str]) -> HelmPublicationResult'
        purpose: Authenticate and push one already-validated chart without mandatory read-back.
      internal_dependencies:
      - helm_contract
      - helm_execution
      - helm_types
    - path: src/ci_workflows/helm_types.py
      responsibility: Typed data and stable failures for the bounded Helm workflow family.
      status: consolidate
      classes:
      - name: HelmValidationError
        purpose: A stable, non-sensitive Helm validation failure.
        methods: []
      - name: HelmRequest
        purpose: Defines HelmRequest for Typed data and stable failures for the bounded Helm workflow family.
        methods: []
      - name: HelmProduct
        purpose: Defines HelmProduct for Typed data and stable failures for the bounded Helm workflow family.
        methods: []
      - name: HelmPlan
        purpose: Defines HelmPlan for Typed data and stable failures for the bounded Helm workflow family.
        methods:
        - name: planning_outputs
          signature: planning_outputs(self) -> dict[str, str]
          purpose: Implements HelmPlan.planning_outputs behavior for Typed data and stable failures for the bounded Helm workflow family.
      - name: HelmValidationResult
        purpose: Defines HelmValidationResult for Typed data and stable failures for the bounded Helm workflow family.
        methods:
        - name: output_values
          signature: output_values(self) -> dict[str, str]
          purpose: Implements HelmValidationResult.output_values behavior for Typed data and stable failures for the bounded Helm workflow family.
      - name: HelmPublicationResult
        purpose: Defines HelmPublicationResult for Typed data and stable failures for the bounded Helm workflow family.
        methods:
        - name: output_values
          signature: output_values(self) -> dict[str, str]
          purpose: Implements HelmPublicationResult.output_values behavior for Typed data and stable failures for the bounded Helm workflow family.
      functions: []
      internal_dependencies: []
    - path: src/ci_workflows/helm_upstream_policy.py
      responsibility: Central stable-origin policy for mirrored Helm upstream assets.
      status: consolidate
      classes: []
      functions:
      - name: _asset_rows
        signature: '_asset_rows(value: Any) -> tuple[tuple[str, str, str, str, str, str], ...]'
        purpose: Implements _asset_rows for Central stable-origin policy for mirrored Helm upstream assets.
      - name: load_upstream_policy
        signature: 'load_upstream_policy(root: Path=CENTRAL_ROOT) -> Mapping[str, Any]'
        purpose: Implements load_upstream_policy for Central stable-origin policy for mirrored Helm upstream assets.
      - name: enforce_upstream_policy
        signature: 'enforce_upstream_policy(source_root: Path, product_id: str, policy: Mapping[str, Any]) -> None'
        purpose: Implements enforce_upstream_policy for Central stable-origin policy for mirrored Helm upstream assets.
      internal_dependencies:
      - helm_contract
      - helm_types
  native:
    target_module: src/ci_workflows/native.py
    responsibility: Native/CMake validation primitives and adapter.
    files:
    - path: src/ci_workflows/ciw_native.py
      responsibility: Bounded product-neutral native/CMake validation adapter.
      status: compatibility
      classes:
      - name: NativeValidationError
        purpose: Fail closed with one stable native-validation error code.
        methods: []
      functions:
      - name: _require
        signature: '_require(condition: bool, code: str) -> None'
        purpose: Implements _require for Bounded product-neutral native/CMake validation adapter.
      - name: safe_relative
        signature: 'safe_relative(value: str, *, code: str=''invalid_working_directory'') -> Path'
        purpose: Implements safe_relative for Bounded product-neutral native/CMake validation adapter.
      - name: bounded_path
        signature: 'bounded_path(root: Path, relative: Path, *, code: str) -> Path'
        purpose: Implements bounded_path for Bounded product-neutral native/CMake validation adapter.
      - name: _bounded_text
        signature: '_bounded_text(value: object, *, code: str, allow_empty: bool=True) -> str'
        purpose: Implements _bounded_text for Bounded product-neutral native/CMake validation adapter.
      - name: _string_map
        signature: '_string_map(value: object) -> dict[str, str]'
        purpose: Implements _string_map for Bounded product-neutral native/CMake validation adapter.
      - name: _string_list
        signature: '_string_list(value: object, *, code: str) -> tuple[str, ...]'
        purpose: Implements _string_list for Bounded product-neutral native/CMake validation adapter.
      - name: _jobs
        signature: '_jobs(value: object) -> int'
        purpose: Implements _jobs for Bounded product-neutral native/CMake validation adapter.
      - name: _plan
        signature: '_plan(raw: str) -> dict[str, object]'
        purpose: Implements _plan for Bounded product-neutral native/CMake validation adapter.
      - name: _state_temp
        signature: '_state_temp(environment: Mapping[str, str], contract_root: Path) -> Path'
        purpose: Implements _state_temp for Bounded product-neutral native/CMake validation adapter.
      - name: request_from_environment
        signature: 'request_from_environment(environment: Mapping[str, str]) -> dict[str, object]'
        purpose: Implements request_from_environment for Bounded product-neutral native/CMake validation adapter.
      - name: _emit_outputs
        signature: '_emit_outputs(environment: Mapping[str, str], values: Mapping[str, str]) -> None'
        purpose: Implements _emit_outputs for Bounded product-neutral native/CMake validation adapter.
      - name: execute_native_validate
        signature: 'execute_native_validate(*, contract_root: Path, workspace: Path, environment: Mapping[str, str], configure: Callable[..., native_primitives.NativeCommandResult]=native_primitives.cmake_configure,
          build: Callable[..., native_primitives.NativeCommandResult]=native_primitives.cmake_build) -> dict[str, str]'
        purpose: Run CMake configure, build and test-target work in one isolated build tree.
      - name: _failure_outputs
        signature: '_failure_outputs(environment: Mapping[str, str], code: str) -> None'
        purpose: Implements _failure_outputs for Bounded product-neutral native/CMake validation adapter.
      - name: parser
        signature: parser() -> argparse.ArgumentParser
        purpose: Implements parser for Bounded product-neutral native/CMake validation adapter.
      - name: main
        signature: 'main(argv: Sequence[str] | None=None, *, environment: Mapping[str, str] | None=None) -> int'
        purpose: Implements main for Bounded product-neutral native/CMake validation adapter.
      internal_dependencies:
      - ciw_types
      - native_primitives
      - workspace
    - path: src/ci_workflows/native_primitives.py
      responsibility: Product-neutral native configure, build, archive, inspection, and cleanup primitives.
      status: consolidate
      classes:
      - name: NativePrimitiveError
        purpose: Fail closed with one stable, non-secret native primitive code.
        methods: []
      - name: ConfigureStep
        purpose: One shell-free configure/autoconf-style process step.
        methods: []
      - name: NativeCommandResult
        purpose: Stable successful native command result without environment or host identity.
        methods: []
      - name: NativeOutput
        purpose: One inspected native output file.
        methods: []
      - name: NativeArchive
        purpose: One deterministic archive result.
        methods: []
      functions:
      - name: _require
        signature: '_require(condition: bool, code: str) -> None'
        purpose: Implements _require for Product-neutral native configure, build, archive, inspection, and cleanup primitives.
      - name: _text
        signature: '_text(value: object, *, code: str, allow_empty: bool=False) -> str'
        purpose: Implements _text for Product-neutral native configure, build, archive, inspection, and cleanup primitives.
      - name: _arguments
        signature: '_arguments(values: Sequence[str], *, code: str) -> tuple[str, ...]'
        purpose: Implements _arguments for Product-neutral native configure, build, archive, inspection, and cleanup primitives.
      - name: _tool
        signature: '_tool(value: str, *, code: str=''native_tool_invalid'') -> str'
        purpose: Implements _tool for Product-neutral native configure, build, archive, inspection, and cleanup primitives.
      - name: _existing_directory
        signature: '_existing_directory(path: Path, *, code: str) -> Path'
        purpose: Implements _existing_directory for Product-neutral native configure, build, archive, inspection, and cleanup primitives.
      - name: _future_directory
        signature: '_future_directory(path: Path, *, code: str) -> Path'
        purpose: Implements _future_directory for Product-neutral native configure, build, archive, inspection, and cleanup primitives.
      - name: _runner_environment
        signature: '_runner_environment(environment: Mapping[str, str]) -> Mapping[str, str]'
        purpose: Implements _runner_environment for Product-neutral native configure, build, archive, inspection, and cleanup primitives.
      - name: _timeout
        signature: '_timeout(value: float | int) -> float'
        purpose: Implements _timeout for Product-neutral native configure, build, archive, inspection, and cleanup primitives.
      - name: _run
        signature: '_run(operation: str, argv: Sequence[str], *, cwd: Path, environment: Mapping[str, str], timeout_seconds: float | int, runner: ProcessRunner) -> NativeCommandResult'
        purpose: Implements _run for Product-neutral native configure, build, archive, inspection, and cleanup primitives.
      - name: run_configure_steps
        signature: 'run_configure_steps(steps: Sequence[ConfigureStep], *, environment: Mapping[str, str], timeout_seconds: float | int=_DEFAULT_TIMEOUT_SECONDS, runner: ProcessRunner=run_process)
          -> tuple[NativeCommandResult, ...]'
        purpose: Run caller-owned configure/autoconf-style steps without invoking a shell.
      - name: cmake_configure
        signature: 'cmake_configure(*, source_dir: Path, build_dir: Path, definitions: Mapping[str, str] | None=None, generator: str='''', options: Sequence[str]=(), environment: Mapping[str,
          str], cmake: str=''cmake'', timeout_seconds: float | int=_DEFAULT_TIMEOUT_SECONDS, runner: ProcessRunner=run_process) -> NativeCommandResult'
        purpose: Configure one CMake build tree from typed caller-owned paths and definitions.
      - name: _jobs
        signature: '_jobs(value: int) -> int'
        purpose: Implements _jobs for Product-neutral native configure, build, archive, inspection, and cleanup primitives.
      - name: _target
        signature: '_target(value: str, *, code: str) -> str'
        purpose: Implements _target for Product-neutral native configure, build, archive, inspection, and cleanup primitives.
      - name: cmake_build
        signature: 'cmake_build(*, build_dir: Path, jobs: int, target: str='''', configuration: str='''', options: Sequence[str]=(), environment: Mapping[str, str], cmake: str=''cmake'',
          timeout_seconds: float | int=_DEFAULT_TIMEOUT_SECONDS, runner: ProcessRunner=run_process) -> NativeCommandResult'
        purpose: Build one CMake tree with bounded parallelism and optional target/configuration.
      - name: cmake_install
        signature: 'cmake_install(*, build_dir: Path, install_dir: Path, configuration: str='''', component: str='''', options: Sequence[str]=(), environment: Mapping[str, str], cmake:
          str=''cmake'', timeout_seconds: float | int=_DEFAULT_TIMEOUT_SECONDS, runner: ProcessRunner=run_process) -> NativeCommandResult'
        purpose: Install one CMake tree into a caller-owned install directory.
      - name: _make_like
        signature: '_make_like(operation: str, tool: str, *, cwd: Path, targets: Sequence[str], jobs: int, options: Sequence[str], environment: Mapping[str, str], timeout_seconds: float
          | int, runner: ProcessRunner) -> NativeCommandResult'
        purpose: Implements _make_like for Product-neutral native configure, build, archive, inspection, and cleanup primitives.
      - name: run_make
        signature: 'run_make(*, cwd: Path, targets: Sequence[str], jobs: int, options: Sequence[str]=(), environment: Mapping[str, str], make: str=''make'', timeout_seconds: float |
          int=_DEFAULT_TIMEOUT_SECONDS, runner: ProcessRunner=run_process) -> NativeCommandResult'
        purpose: Run explicit Make targets with caller-owned options and bounded jobs.
      - name: run_ninja
        signature: 'run_ninja(*, cwd: Path, targets: Sequence[str], jobs: int, options: Sequence[str]=(), environment: Mapping[str, str], ninja: str=''ninja'', timeout_seconds: float
          | int=_DEFAULT_TIMEOUT_SECONDS, runner: ProcessRunner=run_process) -> NativeCommandResult'
        purpose: Run explicit Ninja targets with caller-owned options and bounded jobs.
      - name: sha256_file
        signature: 'sha256_file(path: Path) -> str'
        purpose: Hash one regular non-symlink file.
      - name: _relative_member
        signature: '_relative_member(value: str, *, code: str) -> Path'
        purpose: Implements _relative_member for Product-neutral native configure, build, archive, inspection, and cleanup primitives.
      - name: _contained_path
        signature: '_contained_path(root: Path, relative: Path, *, code: str, symlink_code: str | None=None) -> Path'
        purpose: Implements _contained_path for Product-neutral native configure, build, archive, inspection, and cleanup primitives.
      - name: _archive_entries
        signature: '_archive_entries(root: Path, members: Sequence[str]) -> tuple[tuple[Path, str], ...]'
        purpose: Implements _archive_entries for Product-neutral native configure, build, archive, inspection, and cleanup primitives.
      - name: _normalized_tarinfo
        signature: '_normalized_tarinfo(path: Path, arcname: str) -> tarfile.TarInfo'
        purpose: Implements _normalized_tarinfo for Product-neutral native configure, build, archive, inspection, and cleanup primitives.
      - name: create_deterministic_archive
        signature: 'create_deterministic_archive(*, root: Path, members: Sequence[str], output_path: Path, format: str=''tar.gz'') -> NativeArchive'
        purpose: Create a deterministic tar or tar.gz archive from bounded caller outputs.
      - name: _output_kind
        signature: '_output_kind(path: Path) -> str'
        purpose: Implements _output_kind for Product-neutral native configure, build, archive, inspection, and cleanup primitives.
      - name: inspect_native_outputs
        signature: 'inspect_native_outputs(*, root: Path, outputs: Sequence[str]) -> tuple[NativeOutput, ...]'
        purpose: Inspect expected native files/libraries without following symlinks.
      - name: cleanup_native_state
        signature: 'cleanup_native_state(*, root: Path, paths: Sequence[Path]) -> int'
        purpose: Idempotently remove only caller-declared temporary native state beneath root.
      internal_dependencies:
      - runtime_primitives
  network:
    target_module: src/ci_workflows/network.py
    responsibility: Bounded HTTP/download/archive network primitives and adapter.
    files:
    - path: src/ci_workflows/ciw_network.py
      responsibility: Thin typed CIW adapter for verified HTTP downloads and safe extraction.
      status: compatibility
      classes: []
      functions:
      - name: configure_network
        signature: 'configure_network(parser: argparse.ArgumentParser) -> None'
        purpose: Implements configure_network for Thin typed CIW adapter for verified HTTP downloads and safe extraction.
      - name: _value
        signature: '_value(args: argparse.Namespace, context: CIWContext, name: str, default: str='''') -> str'
        purpose: Implements _value for Thin typed CIW adapter for verified HTTP downloads and safe extraction.
      - name: _text
        signature: '_text(value: object, code: str, *, allow_empty: bool=False, maximum: int=8192) -> str'
        purpose: Implements _text for Thin typed CIW adapter for verified HTTP downloads and safe extraction.
      - name: _fixed_root
        signature: '_fixed_root(context: CIWContext, name: str) -> Path'
        purpose: Implements _fixed_root for Thin typed CIW adapter for verified HTTP downloads and safe extraction.
      - name: _relative_file
        signature: '_relative_file(root: Path, raw: str, code: str) -> Path'
        purpose: Implements _relative_file for Thin typed CIW adapter for verified HTTP downloads and safe extraction.
      - name: _integer
        signature: '_integer(args: argparse.Namespace, context: CIWContext, name: str, default: int, maximum: int) -> int'
        purpose: Implements _integer for Thin typed CIW adapter for verified HTTP downloads and safe extraction.
      - name: _result
        signature: '_result(operation: str, *, local_path: Path, **payload: Any) -> CIWResult'
        purpose: Implements _result for Thin typed CIW adapter for verified HTTP downloads and safe extraction.
      - name: execute_network
        signature: 'execute_network(args: argparse.Namespace, context: CIWContext) -> CIWResult'
        purpose: Implements execute_network for Thin typed CIW adapter for verified HTTP downloads and safe extraction.
      internal_dependencies:
      - ciw_types
      - network_primitives
    - path: src/ci_workflows/network_primitives.py
      responsibility: Product-neutral HTTP, verified download, and safe archive extraction primitives.
      status: consolidate
      classes:
      - name: NetworkPrimitiveError
        purpose: Fail closed with one stable non-secret network primitive code.
        methods: []
      - name: NetworkTransportError
        purpose: Sanitized injected/default transport failure.
        methods: []
      - name: ResponseStream
        purpose: Defines ResponseStream for Product-neutral HTTP, verified download, and safe archive extraction primitives.
        methods:
        - name: read
          signature: 'read(self, size: int=-1) -> bytes'
          purpose: Implements ResponseStream.read behavior for Product-neutral HTTP, verified download, and safe archive extraction primitives.
        - name: close
          signature: close(self) -> None
          purpose: Implements ResponseStream.close behavior for Product-neutral HTTP, verified download, and safe archive extraction primitives.
      - name: HttpTransport
        purpose: Defines HttpTransport for Product-neutral HTTP, verified download, and safe archive extraction primitives.
        methods:
        - name: open
          signature: 'open(self, method: str, url: str, headers: Mapping[str, str], timeout_seconds: float) -> ResponseStream'
          purpose: Implements HttpTransport.open behavior for Product-neutral HTTP, verified download, and safe archive extraction primitives.
      - name: HttpResult
        purpose: Defines HttpResult for Product-neutral HTTP, verified download, and safe archive extraction primitives.
        methods: []
      - name: JsonResult
        purpose: Defines JsonResult for Product-neutral HTTP, verified download, and safe archive extraction primitives.
        methods: []
      - name: FileVerification
        purpose: Defines FileVerification for Product-neutral HTTP, verified download, and safe archive extraction primitives.
        methods: []
      - name: DownloadResult
        purpose: Defines DownloadResult for Product-neutral HTTP, verified download, and safe archive extraction primitives.
        methods: []
      - name: ExtractionResult
        purpose: Defines ExtractionResult for Product-neutral HTTP, verified download, and safe archive extraction primitives.
        methods: []
      - name: _Opened
        purpose: Defines _Opened for Product-neutral HTTP, verified download, and safe archive extraction primitives.
        methods: []
      - name: _Member
        purpose: Defines _Member for Product-neutral HTTP, verified download, and safe archive extraction primitives.
        methods: []
      - name: _UrllibStream
        purpose: Defines _UrllibStream for Product-neutral HTTP, verified download, and safe archive extraction primitives.
        methods:
        - name: read
          signature: 'read(self, size: int=-1) -> bytes'
          purpose: Implements _UrllibStream.read behavior for Product-neutral HTTP, verified download, and safe archive extraction primitives.
        - name: close
          signature: close(self) -> None
          purpose: Implements _UrllibStream.close behavior for Product-neutral HTTP, verified download, and safe archive extraction primitives.
      - name: _NoRedirect
        purpose: Defines _NoRedirect for Product-neutral HTTP, verified download, and safe archive extraction primitives.
        methods:
        - name: redirect_request
          signature: redirect_request(self, req, fp, code, msg, headers, newurl)
          purpose: Implements _NoRedirect.redirect_request behavior for Product-neutral HTTP, verified download, and safe archive extraction primitives.
      - name: UrllibTransport
        purpose: Default redirect-disabled urllib transport.
        methods:
        - name: open
          signature: 'open(self, method: str, url: str, headers: Mapping[str, str], timeout_seconds: float) -> ResponseStream'
          purpose: Implements UrllibTransport.open behavior for Product-neutral HTTP, verified download, and safe archive extraction primitives.
      functions:
      - name: _require
        signature: '_require(condition: bool, code: str) -> None'
        purpose: Implements _require for Product-neutral HTTP, verified download, and safe archive extraction primitives.
      - name: _bounded_number
        signature: '_bounded_number(value: int | float, *, minimum: float, maximum: float, code: str) -> float'
        purpose: Implements _bounded_number for Product-neutral HTTP, verified download, and safe archive extraction primitives.
      - name: _bounded_integer
        signature: '_bounded_integer(value: int, *, minimum: int, maximum: int, code: str) -> int'
        purpose: Implements _bounded_integer for Product-neutral HTTP, verified download, and safe archive extraction primitives.
      - name: _canonical_url
        signature: '_canonical_url(url: str, *, allow_http: bool) -> str'
        purpose: Implements _canonical_url for Product-neutral HTTP, verified download, and safe archive extraction primitives.
      - name: _origin
        signature: '_origin(url: str) -> tuple[str, str, int]'
        purpose: Implements _origin for Product-neutral HTTP, verified download, and safe archive extraction primitives.
      - name: _validate_header
        signature: '_validate_header(name: str, value: str, *, secret: bool) -> tuple[str, str]'
        purpose: Implements _validate_header for Product-neutral HTTP, verified download, and safe archive extraction primitives.
      - name: _request_headers
        signature: '_request_headers(headers: Mapping[str, str] | None, *, environment: Mapping[str, str], secret_header_name: str | None) -> tuple[dict[str, str], str | None]'
        purpose: Implements _request_headers for Product-neutral HTTP, verified download, and safe archive extraction primitives.
      - name: _header
        signature: '_header(headers: Mapping[str, str], name: str) -> str | None'
        purpose: Implements _header for Product-neutral HTTP, verified download, and safe archive extraction primitives.
      - name: _content_type
        signature: '_content_type(headers: Mapping[str, str]) -> str | None'
        purpose: Implements _content_type for Product-neutral HTTP, verified download, and safe archive extraction primitives.
      - name: _content_length
        signature: '_content_length(headers: Mapping[str, str]) -> int | None'
        purpose: Implements _content_length for Product-neutral HTTP, verified download, and safe archive extraction primitives.
      - name: _safe_metadata
        signature: '_safe_metadata(headers: Mapping[str, str], name: str) -> str | None'
        purpose: Implements _safe_metadata for Product-neutral HTTP, verified download, and safe archive extraction primitives.
      - name: _retry_delay
        signature: '_retry_delay(attempt: int, *, initial: float, maximum: float) -> float'
        purpose: Implements _retry_delay for Product-neutral HTTP, verified download, and safe archive extraction primitives.
      - name: _sleep
        signature: '_sleep(sleeper: Callable[[float], None], delay: float) -> None'
        purpose: Implements _sleep for Product-neutral HTTP, verified download, and safe archive extraction primitives.
      - name: _close_response
        signature: '_close_response(stream: ResponseStream) -> None'
        purpose: Implements _close_response for Product-neutral HTTP, verified download, and safe archive extraction primitives.
      - name: _open_with_policy
        signature: '_open_with_policy(method: str, url: str, *, transport: HttpTransport, headers: Mapping[str, str] | None, environment: Mapping[str, str], secret_header_name: str |
          None, timeout_seconds: float, max_attempts: int, initial_backoff_seconds: float, maximum_backoff_seconds: float, maximum_redirects: int, allow_http: bool, allow_cross_origin_redirects:
          bool, sleeper: Callable[[float], None]) -> _Opened'
        purpose: Implements _open_with_policy for Product-neutral HTTP, verified download, and safe archive extraction primitives.
      - name: _read_bounded
        signature: '_read_bounded(stream: ResponseStream, *, maximum_bytes: int) -> bytes'
        purpose: Implements _read_bounded for Product-neutral HTTP, verified download, and safe archive extraction primitives.
      - name: request_http
        signature: 'request_http(method: str, url: str, *, transport: HttpTransport | None=None, headers: Mapping[str, str] | None=None, environment: Mapping[str, str] | None=None, secret_header_name:
          str | None=None, timeout_seconds: float=30.0, max_attempts: int=3, initial_backoff_seconds: float=0.25, maximum_backoff_seconds: float=2.0, maximum_redirects: int=5, maximum_response_bytes:
          int=8 * 1024 * 1024, allow_http: bool=False, allow_cross_origin_redirects: bool=False, sleeper: Callable[[float], None]=time.sleep) -> HttpResult'
        purpose: Run one bounded GET/HEAD request and return sanitized structured metadata.
      - name: http_get
        signature: 'http_get(url: str, **kwargs: Any) -> HttpResult'
        purpose: Implements http_get for Product-neutral HTTP, verified download, and safe archive extraction primitives.
      - name: http_head
        signature: 'http_head(url: str, **kwargs: Any) -> HttpResult'
        purpose: Implements http_head for Product-neutral HTTP, verified download, and safe archive extraction primitives.
      - name: http_json
        signature: 'http_json(url: str, *, require_json_content_type: bool=True, **kwargs: Any) -> JsonResult'
        purpose: Implements http_json for Product-neutral HTTP, verified download, and safe archive extraction primitives.
      - name: _real_directory
        signature: '_real_directory(path: Path, code: str) -> Path'
        purpose: Implements _real_directory for Product-neutral HTTP, verified download, and safe archive extraction primitives.
      - name: _bounded_relative_target
        signature: '_bounded_relative_target(root: Path, relative_path: str, *, code: str) -> Path'
        purpose: Implements _bounded_relative_target for Product-neutral HTTP, verified download, and safe archive extraction primitives.
      - name: _verify_sha256
        signature: '_verify_sha256(value: str | None) -> str | None'
        purpose: Implements _verify_sha256 for Product-neutral HTTP, verified download, and safe archive extraction primitives.
      - name: _verify_expected_size
        signature: '_verify_expected_size(value: int | None) -> int | None'
        purpose: Implements _verify_expected_size for Product-neutral HTTP, verified download, and safe archive extraction primitives.
      - name: verify_file
        signature: 'verify_file(path: Path, *, expected_sha256: str | None=None, expected_size: int | None=None) -> FileVerification'
        purpose: Verify one existing regular file without following a symlink.
      - name: _partial_file
        signature: '_partial_file(target: Path) -> tuple[int, Path]'
        purpose: Implements _partial_file for Product-neutral HTTP, verified download, and safe archive extraction primitives.
      - name: download_file
        signature: 'download_file(url: str, *, destination_root: Path, relative_path: str, transport: HttpTransport | None=None, headers: Mapping[str, str] | None=None, environment:
          Mapping[str, str] | None=None, secret_header_name: str | None=None, expected_sha256: str | None=None, expected_size: int | None=None, expected_content_type: str | None=None,
          maximum_bytes: int=512 * 1024 * 1024, timeout_seconds: float=60.0, max_attempts: int=3, initial_backoff_seconds: float=0.25, maximum_backoff_seconds: float=2.0, maximum_redirects:
          int=5, allow_http: bool=False, allow_cross_origin_redirects: bool=False, sleeper: Callable[[float], None]=time.sleep) -> DownloadResult'
        purpose: Stream one HTTP object into an exclusive partial file and atomically finalize it.
      - name: _archive_file
        signature: '_archive_file(path: Path, *, maximum_archive_bytes: int) -> FileVerification'
        purpose: Implements _archive_file for Product-neutral HTTP, verified download, and safe archive extraction primitives.
      - name: _safe_member_path
        signature: '_safe_member_path(name: str) -> PurePosixPath'
        purpose: Implements _safe_member_path for Product-neutral HTTP, verified download, and safe archive extraction primitives.
      - name: _validate_member_set
        signature: '_validate_member_set(members: Sequence[_Member], *, maximum_members: int, maximum_total_bytes: int) -> tuple[int, int, int]'
        purpose: Implements _validate_member_set for Product-neutral HTTP, verified download, and safe archive extraction primitives.
      - name: _zip_members
        signature: '_zip_members(archive: zipfile.ZipFile, *, maximum_members: int, maximum_total_bytes: int) -> tuple[tuple[_Member, ...], int, int, int]'
        purpose: Implements _zip_members for Product-neutral HTTP, verified download, and safe archive extraction primitives.
      - name: _tar_members
        signature: '_tar_members(archive: tarfile.TarFile, *, maximum_members: int, maximum_total_bytes: int) -> tuple[tuple[_Member, ...], int, int, int]'
        purpose: Implements _tar_members for Product-neutral HTTP, verified download, and safe archive extraction primitives.
      - name: _open_exclusive
        signature: '_open_exclusive(path: Path, *, executable: bool) -> int'
        purpose: Implements _open_exclusive for Product-neutral HTTP, verified download, and safe archive extraction primitives.
      - name: _copy_member
        signature: '_copy_member(source: Any, target: Path, *, expected_size: int, executable: bool) -> tuple[int, str]'
        purpose: Implements _copy_member for Product-neutral HTTP, verified download, and safe archive extraction primitives.
      - name: _extract_zip
        signature: '_extract_zip(archive_path: Path, stage: Path, *, maximum_members: int, maximum_total_bytes: int) -> tuple[int, int, int, str]'
        purpose: Implements _extract_zip for Product-neutral HTTP, verified download, and safe archive extraction primitives.
      - name: _extract_tar
        signature: '_extract_tar(archive_path: Path, stage: Path, *, maximum_members: int, maximum_total_bytes: int) -> tuple[int, int, int, str]'
        purpose: Implements _extract_tar for Product-neutral HTTP, verified download, and safe archive extraction primitives.
      - name: extract_archive
        signature: 'extract_archive(archive_path: Path, *, archive_format: str, destination_root: Path, relative_destination: str, maximum_archive_bytes: int=2 * 1024 * 1024 * 1024,
          maximum_members: int=10000, maximum_total_bytes: int=2 * 1024 * 1024 * 1024) -> ExtractionResult'
        purpose: Safely extract ZIP/TAR content through a sibling staging directory.
      internal_dependencies:
      - runtime_primitives
  node:
    target_module: src/ci_workflows/node.py
    responsibility: Node.js/npm validation planning and execution.
    files:
    - path: src/ci_workflows/ciw_node.py
      responsibility: Bounded ``ciw node validate`` adapter.
      status: compatibility
      classes: []
      functions:
      - name: configure_node_validate
        signature: 'configure_node_validate(parser: argparse.ArgumentParser) -> None'
        purpose: Implements configure_node_validate for Bounded ``ciw node validate`` adapter.
      - name: _state_root
        signature: '_state_root(context: CIWContext) -> Path'
        purpose: Implements _state_root for Bounded ``ciw node validate`` adapter.
      - name: _failure_outputs
        signature: '_failure_outputs(context: CIWContext, code: str) -> None'
        purpose: Implements _failure_outputs for Bounded ``ciw node validate`` adapter.
      - name: execute_node_validate
        signature: 'execute_node_validate(args: argparse.Namespace, context: CIWContext) -> CIWResult'
        purpose: Plan or execute one checked-in Node validation request.
      internal_dependencies:
      - ciw_types
      - execution_backends
      - node
      - runners
      - workspace
    - path: src/ci_workflows/node.py
      responsibility: Public facade and bounded orchestration for reusable Node validation.
      status: keep
      classes: []
      functions:
      - name: _verify_policy
        signature: '_verify_policy(source_root: Path, request: NodeValidationRequest, contract_root: Path, phase: str) -> None'
        purpose: Implements _verify_policy for Public facade and bounded orchestration for reusable Node validation.
      - name: validate
        signature: 'validate(*, contract_root: Path, source_root: Path | None, state_root: Path | None, request: NodeValidationRequest, phase: str, environment: Mapping[str, str]) ->
          NodeValidationPlan | NodeValidationResult'
        purpose: Plan or execute one contract-bounded Node validation request.
      internal_dependencies:
      - foundation_types
      - node_contract
      - node_execution
      - node_types
      - policy
    - path: src/ci_workflows/node_contract.py
      responsibility: Checked-in Node validation contract, trust, and plan resolution.
      status: consolidate
      classes: []
      functions:
      - name: require
        signature: 'require(condition: bool, code: str) -> None'
        purpose: Implements require for Checked-in Node validation contract, trust, and plan resolution.
      - name: _read_json
        signature: '_read_json(path: Path, code: str=''invalid_input'') -> Mapping[str, Any]'
        purpose: Implements _read_json for Checked-in Node validation contract, trust, and plan resolution.
      - name: _strings
        signature: '_strings(value: Any, *, empty: bool=True) -> list[str]'
        purpose: Implements _strings for Checked-in Node validation contract, trust, and plan resolution.
      - name: safe_relative
        signature: 'safe_relative(value: Any, code: str=''invalid_input'', *, allow_dot: bool=False) -> str'
        purpose: Implements safe_relative for Checked-in Node validation contract, trust, and plan resolution.
      - name: _optional_relative
        signature: '_optional_relative(value: Any) -> str | None'
        purpose: Implements _optional_relative for Checked-in Node validation contract, trust, and plan resolution.
      - name: bounded_path
        signature: 'bounded_path(root: Path, relative: str) -> Path'
        purpose: Resolve an ordinary source path while rejecting every symlink component.
      - name: _validate_profile_contract
        signature: '_validate_profile_contract(payload: Mapping[str, Any]) -> None'
        purpose: Implements _validate_profile_contract for Checked-in Node validation contract, trust, and plan resolution.
      - name: _validate_command_contract
        signature: '_validate_command_contract(payload: Mapping[str, Any]) -> None'
        purpose: Implements _validate_command_contract for Checked-in Node validation contract, trust, and plan resolution.
      - name: _validate_consumer_shape
        signature: '_validate_consumer_shape(payload: Mapping[str, Any], command_name: str, value: Mapping[str, Any]) -> None'
        purpose: Implements _validate_consumer_shape for Checked-in Node validation contract, trust, and plan resolution.
      - name: load_node_contract
        signature: 'load_node_contract(root: Path) -> Mapping[str, Any]'
        purpose: Load and validate the complete checked-in Node contract.
      - name: source_trust_from_environment
        signature: 'source_trust_from_environment(environment: Mapping[str, str]) -> str'
        purpose: Derive trust from immutable GitHub event metadata.
      - name: _public_environment
        signature: '_public_environment(raw: str, contract: Mapping[str, Any]) -> Mapping[str, str]'
        purpose: Implements _public_environment for Checked-in Node validation contract, trust, and plan resolution.
      - name: request_from_environment
        signature: 'request_from_environment(environment: Mapping[str, str], contract: Mapping[str, Any]) -> NodeValidationRequest'
        purpose: Build one typed request from bounded action inputs.
      - name: resolve_validation_plan
        signature: 'resolve_validation_plan(contract: Mapping[str, Any], request: NodeValidationRequest) -> NodeValidationPlan'
        purpose: Resolve one product-neutral plan from checked-in compatibility data.
      - name: resolve_exact_node_version
        signature: 'resolve_exact_node_version(source_root: Path, plan: NodeValidationPlan) -> str'
        purpose: Resolve exactly one canonical version from the selected authority.
      - name: file_sha256
        signature: 'file_sha256(path: Path, code: str) -> str'
        purpose: Implements file_sha256 for Checked-in Node validation contract, trust, and plan resolution.
      - name: load_package_manifest
        signature: 'load_package_manifest(source_root: Path, plan: NodeValidationPlan) -> Mapping[str, Any] | None'
        purpose: Implements load_package_manifest for Checked-in Node validation contract, trust, and plan resolution.
      - name: load_lockfile
        signature: 'load_lockfile(source_root: Path, plan: NodeValidationPlan) -> Mapping[str, Any] | None'
        purpose: Implements load_lockfile for Checked-in Node validation contract, trust, and plan resolution.
      - name: _version_tuple
        signature: '_version_tuple(value: str) -> tuple[int, int, int]'
        purpose: Implements _version_tuple for Checked-in Node validation contract, trust, and plan resolution.
      - name: _bound_tuple
        signature: '_bound_tuple(value: str) -> tuple[int, int, int]'
        purpose: Implements _bound_tuple for Checked-in Node validation contract, trust, and plan resolution.
      - name: version_satisfies
        signature: 'version_satisfies(version: str, expression: str) -> bool'
        purpose: Evaluate the deliberately small reviewed engine-range grammar.
      - name: verify_manifest_engines
        signature: 'verify_manifest_engines(manifest: Mapping[str, Any] | None, node_version: str, npm_version: str) -> None'
        purpose: Implements verify_manifest_engines for Checked-in Node validation contract, trust, and plan resolution.
      internal_dependencies:
      - node_types
    - path: src/ci_workflows/node_execution.py
      responsibility: Isolated npm execution, static-output verification, and cleanup.
      status: consolidate
      classes: []
      functions:
      - name: run_command
        signature: 'run_command(argv: Sequence[str], *, cwd: Path, environment: Mapping[str, str], timeout_seconds: int, code: str) -> subprocess.CompletedProcess[str]'
        purpose: Run one contract-owned argv without a shell or output projection.
      - name: git_output
        signature: 'git_output(root: Path, arguments: Sequence[str], code: str) -> str'
        purpose: Implements git_output for Isolated npm execution, static-output verification, and cleanup.
      - name: verify_exact_source
        signature: 'verify_exact_source(source_root: Path, admitted_sha: str) -> None'
        purpose: Implements verify_exact_source for Isolated npm execution, static-output verification, and cleanup.
      - name: copy_source
        signature: 'copy_source(source_root: Path, destination: Path) -> None'
        purpose: Copy exact source into registered state while rejecting symlinks.
      - name: _version
        signature: '_version(executable: str, argument: str, *, cwd: Path, environment: Mapping[str, str]) -> str'
        purpose: Implements _version for Isolated npm execution, static-output verification, and cleanup.
      - name: _require_public_environment_scope
        signature: '_require_public_environment_scope(plan: NodeValidationPlan) -> None'
        purpose: Implements _require_public_environment_scope for Isolated npm execution, static-output verification, and cleanup.
      - name: _execution_environment
        signature: '_execution_environment(plan: NodeValidationPlan, state_root: Path, inherited: Mapping[str, str]) -> tuple[dict[str, str], tuple[Path, ...]]'
        purpose: Implements _execution_environment for Isolated npm execution, static-output verification, and cleanup.
      - name: _stage_code
        signature: '_stage_code(stage: str) -> str'
        purpose: Implements _stage_code for Isolated npm execution, static-output verification, and cleanup.
      - name: _checked_in_regular
        signature: '_checked_in_regular(source_root: Path, relative: str, code: str) -> Path'
        purpose: Implements _checked_in_regular for Isolated npm execution, static-output verification, and cleanup.
      - name: _verify_command_hooks
        signature: '_verify_command_hooks(source_root: Path, plan: NodeValidationPlan) -> None'
        purpose: Implements _verify_command_hooks for Isolated npm execution, static-output verification, and cleanup.
      - name: _allowed_generated_path
        signature: '_allowed_generated_path(relative: str, plan: NodeValidationPlan, cleanup_names: Sequence[str]) -> bool'
        purpose: Implements _allowed_generated_path for Isolated npm execution, static-output verification, and cleanup.
      - name: _verify_copy_mutations
        signature: '_verify_copy_mutations(copy_root: Path, plan: NodeValidationPlan, cleanup_names: Sequence[str]) -> None'
        purpose: Implements _verify_copy_mutations for Isolated npm execution, static-output verification, and cleanup.
      - name: _absolute_lexical
        signature: '_absolute_lexical(path: Path) -> Path'
        purpose: Implements _absolute_lexical for Isolated npm execution, static-output verification, and cleanup.
      - name: _lstat
        signature: '_lstat(path: Path) -> os.stat_result | None'
        purpose: Implements _lstat for Isolated npm execution, static-output verification, and cleanup.
      - name: _require_real_directory
        signature: '_require_real_directory(path: Path) -> None'
        purpose: Implements _require_real_directory for Isolated npm execution, static-output verification, and cleanup.
      - name: _lexical_cleanup_target
        signature: '_lexical_cleanup_target(root: Path, relative: str) -> Path'
        purpose: Return an anchored target without resolving its final symlink object.
      - name: _remove_no_follow
        signature: '_remove_no_follow(path: Path) -> None'
        purpose: Remove one object tree through lstat without following any symlink.
      - name: _validated_cleanup_roots
        signature: '_validated_cleanup_roots(copy_root: Path, state_root: Path) -> tuple[Path, Path, Path]'
        purpose: Implements _validated_cleanup_roots for Isolated npm execution, static-output verification, and cleanup.
      - name: cleanup_generated
        signature: 'cleanup_generated(copy_root: Path, state_root: Path, plan: NodeValidationPlan, cleanup_names: Sequence[str]) -> None'
        purpose: Remove only registered Node state and generated objects, without follow.
      - name: verify_static_output
        signature: 'verify_static_output(copy_root: Path, plan: NodeValidationPlan, limits: Mapping[str, Any]) -> str | None'
        purpose: Implements verify_static_output for Isolated npm execution, static-output verification, and cleanup.
      - name: execute_node_plan
        signature: 'execute_node_plan(source_root: Path, state_root: Path, plan: NodeValidationPlan, contract: Mapping[str, Any], inherited_environment: Mapping[str, str]) -> NodeValidationResult'
        purpose: Execute one checked-in plan in copied state and clean every terminal path.
      internal_dependencies:
      - foundation_types
      - node_contract
      - node_types
    - path: src/ci_workflows/node_types.py
      responsibility: Typed models and stable errors for reusable Node validation.
      status: consolidate
      classes:
      - name: NodeValidationError
        purpose: Fail-closed Node validation error carrying one stable code.
        methods: []
      - name: NodeValidationRequest
        purpose: Defines NodeValidationRequest for Typed models and stable errors for reusable Node validation.
        methods: []
      - name: NodeCommand
        purpose: Defines NodeCommand for Typed models and stable errors for reusable Node validation.
        methods: []
      - name: NodeValidationPlan
        purpose: Defines NodeValidationPlan for Typed models and stable errors for reusable Node validation.
        methods:
        - name: version_authority
          signature: version_authority(self) -> str
          purpose: Return the single selected runtime authority without exposing source data.
        - name: planning_outputs
          signature: planning_outputs(self) -> dict[str, str]
          purpose: Implements NodeValidationPlan.planning_outputs behavior for Typed models and stable errors for reusable Node validation.
      - name: NodeValidationResult
        purpose: Defines NodeValidationResult for Typed models and stable errors for reusable Node validation.
        methods:
        - name: output_values
          signature: output_values(self) -> dict[str, str]
          purpose: Implements NodeValidationResult.output_values behavior for Typed models and stable errors for reusable Node validation.
      functions: []
      internal_dependencies: []
  oci:
    target_module: src/ci_workflows/oci.py
    responsibility: OCI acquisition, build, inspection, reproducibility, and publication policy.
    files:
    - path: src/ci_workflows/ciw_oci.py
      responsibility: Thin CIW adapters for versioned OCI validation and publication contracts.
      status: compatibility
      classes: []
      functions:
      - name: _request
        signature: '_request(environment: Mapping[str, str]) -> Mapping[str, object]'
        purpose: Implements _request for Thin CIW adapters for versioned OCI validation and publication contracts.
      - name: _write_outputs
        signature: '_write_outputs(values: Mapping[str, str], environment: Mapping[str, str]) -> None'
        purpose: Implements _write_outputs for Thin CIW adapters for versioned OCI validation and publication contracts.
      - name: configure_oci_validate
        signature: 'configure_oci_validate(parser: argparse.ArgumentParser) -> None'
        purpose: Configure the bounded ``ciw oci validate`` command.
      - name: configure_oci_publish
        signature: 'configure_oci_publish(parser: argparse.ArgumentParser) -> None'
        purpose: Configure the bounded ``ciw oci publish`` command.
      - name: _failure_outputs
        signature: '_failure_outputs(context: CIWContext, error: BaseException) -> None'
        purpose: Implements _failure_outputs for Thin CIW adapters for versioned OCI validation and publication contracts.
      - name: execute_oci_validate
        signature: 'execute_oci_validate(args: argparse.Namespace, context: CIWContext) -> CIWResult'
        purpose: Plan, execute, clean, or inspect one OCI validation request.
      - name: execute_oci_publish
        signature: 'execute_oci_publish(args: argparse.Namespace, context: CIWContext) -> CIWResult'
        purpose: Plan, authenticate, publish/verify, read back, or clean one OCI release.
      - name: _phase
        signature: '_phase(phase: str, source_root: Path, environment: Mapping[str, str]) -> int'
        purpose: Implements _phase for Thin CIW adapters for versioned OCI validation and publication contracts.
      - name: generate_mapping
        signature: 'generate_mapping(*, check: bool) -> int'
        purpose: Implements generate_mapping for Thin CIW adapters for versioned OCI validation and publication contracts.
      - name: main
        signature: 'main(argv: Sequence[str] | None=None, environment: Mapping[str, str] | None=None) -> int'
        purpose: Implements main for Thin CIW adapters for versioned OCI validation and publication contracts.
      internal_dependencies:
      - ciw_types
      - oci
      - oci_contract
      - oci_execution_safe
      - oci_publish_contract
      - oci_types
    - path: src/ci_workflows/oci.py
      responsibility: Engine-neutral public OCI build, inspection, publication, and read-back facade.
      status: keep
      classes: []
      functions:
      - name: build
        signature: 'build(repository_root: Path, source_root: Path, plan: OciBuildPlan, environment: Mapping[str, str], secret_files: Mapping[str, Path] | None=None) -> OciBuildResult'
        purpose: Build and validate one contract-owned OCI plan without publication.
      - name: inspect
        signature: 'inspect(layout: Path, target: OciTarget, labels: Mapping[str, str]) -> OciTargetResult'
        purpose: Strictly inspect one local OCI layout against its target contract.
      - name: publish
        signature: 'publish(plan: OciPublishPlan, environment: Mapping[str, str]) -> dict[str, str]'
        purpose: Publish or verify exact immutable OCI identities under the trusted event boundary.
      - name: read_back
        signature: 'read_back(plan: OciPublishPlan, environment: Mapping[str, str]) -> dict[str, str]'
        purpose: Independently pull and verify exact registry bytes for one publication plan.
      internal_dependencies:
      - oci_execution
      - oci_execution_safe
      - oci_publish_contract
      - oci_types
    - path: src/ci_workflows/oci_base_inspection.py
      responsibility: Offline inspection of an immutable OCI base-image layout.
      status: consolidate
      classes:
      - name: OciBaseInspectionError
        purpose: Fail-closed inspection error carrying one stable, non-secret code.
        methods: []
      - name: OciBasePlatformIdentity
        purpose: Exact immutable identities for one selected base-image platform.
        methods:
        - name: to_dict
          signature: to_dict(self) -> dict[str, str]
          purpose: Implements OciBasePlatformIdentity.to_dict behavior for Offline inspection of an immutable OCI base-image layout.
      - name: OciBaseManifestSelection
        purpose: One requested manifest selected from the authenticated root object.
        methods:
        - name: to_dict
          signature: to_dict(self) -> dict[str, object]
          purpose: Implements OciBaseManifestSelection.to_dict behavior for Offline inspection of an immutable OCI base-image layout.
      - name: OciBaseRootInspection
        purpose: Redacted result of authenticating an index-only or direct root layout.
        methods:
        - name: to_dict
          signature: to_dict(self) -> dict[str, object]
          purpose: Implements OciBaseRootInspection.to_dict behavior for Offline inspection of an immutable OCI base-image layout.
      - name: OciBaseInspectionEvidence
        purpose: Canonical evidence with the registry/repository name intentionally removed.
        methods:
        - name: evidence_payload
          signature: evidence_payload(self) -> dict[str, object]
          purpose: Implements OciBaseInspectionEvidence.evidence_payload behavior for Offline inspection of an immutable OCI base-image layout.
        - name: to_dict
          signature: to_dict(self) -> dict[str, object]
          purpose: Implements OciBaseInspectionEvidence.to_dict behavior for Offline inspection of an immutable OCI base-image layout.
        - name: canonical_json
          signature: canonical_json(self) -> str
          purpose: Implements OciBaseInspectionEvidence.canonical_json behavior for Offline inspection of an immutable OCI base-image layout.
      functions:
      - name: _fail
        signature: '_fail(code: str) -> None'
        purpose: Implements _fail for Offline inspection of an immutable OCI base-image layout.
      - name: _canonical_json
        signature: '_canonical_json(value: object) -> str'
        purpose: Implements _canonical_json for Offline inspection of an immutable OCI base-image layout.
      - name: _reject_duplicate_keys
        signature: '_reject_duplicate_keys(pairs: list[tuple[str, object]]) -> dict[str, object]'
        purpose: Implements _reject_duplicate_keys for Offline inspection of an immutable OCI base-image layout.
      - name: _reject_nonfinite
        signature: '_reject_nonfinite(_value: str) -> object'
        purpose: Implements _reject_nonfinite for Offline inspection of an immutable OCI base-image layout.
      - name: _open_regular
        signature: '_open_regular(path: Path) -> int'
        purpose: Implements _open_regular for Offline inspection of an immutable OCI base-image layout.
      - name: _read_regular
        signature: '_read_regular(path: Path, *, maximum: int | None=None, capture: bool=True) -> tuple[bytes, int, str]'
        purpose: Implements _read_regular for Offline inspection of an immutable OCI base-image layout.
      - name: _read_json
        signature: '_read_json(path: Path) -> object'
        purpose: Implements _read_json for Offline inspection of an immutable OCI base-image layout.
      - name: _descriptor_fields
        signature: '_descriptor_fields(descriptor: object, allowed_media_types: frozenset[str]) -> tuple[str, str, int]'
        purpose: Implements _descriptor_fields for Offline inspection of an immutable OCI base-image layout.
      - name: _blob_path
        signature: '_blob_path(layout: Path, digest: str) -> Path'
        purpose: Implements _blob_path for Offline inspection of an immutable OCI base-image layout.
      - name: _verify_descriptor_blob
        signature: '_verify_descriptor_blob(layout: Path, descriptor: object, allowed_media_types: frozenset[str], *, json_blob: bool) -> tuple[str, str, object | None]'
        purpose: Implements _verify_descriptor_blob for Offline inspection of an immutable OCI base-image layout.
      - name: _platform_identity
        signature: '_platform_identity(value: object, *, expected: str | None=None) -> str'
        purpose: Implements _platform_identity for Offline inspection of an immutable OCI base-image layout.
      - name: _validate_document_type
        signature: '_validate_document_type(payload: object, media_type: str) -> Mapping[str, object]'
        purpose: Implements _validate_document_type for Offline inspection of an immutable OCI base-image layout.
      - name: _inspect_manifest
        signature: '_inspect_manifest(layout: Path, descriptor: object, *, expected_platform: str) -> OciBasePlatformIdentity'
        purpose: Implements _inspect_manifest for Offline inspection of an immutable OCI base-image layout.
      - name: _requested_platforms
        signature: '_requested_platforms(values: Sequence[str]) -> tuple[str, ...]'
        purpose: Implements _requested_platforms for Offline inspection of an immutable OCI base-image layout.
      - name: _reference_digest
        signature: '_reference_digest(reference: str) -> str'
        purpose: Implements _reference_digest for Offline inspection of an immutable OCI base-image layout.
      - name: _layout_root
        signature: '_layout_root(layout: Path) -> tuple[Mapping[str, object], object]'
        purpose: Implements _layout_root for Offline inspection of an immutable OCI base-image layout.
      - name: _descriptor_declared_platform
        signature: '_descriptor_declared_platform(descriptor: object) -> str | None'
        purpose: Implements _descriptor_declared_platform for Offline inspection of an immutable OCI base-image layout.
      - name: _inspect_child_layout
        signature: '_inspect_child_layout(child_layout: Path, expected_descriptor: Mapping[str, object], expected_platform: str) -> OciBasePlatformIdentity'
        purpose: Implements _inspect_child_layout for Offline inspection of an immutable OCI base-image layout.
      - name: _inspect_root_layout
        signature: '_inspect_root_layout(root_layout: Path, declared_reference: str, requested: tuple[str, ...]) -> tuple[OciBaseRootInspection, Mapping[str, Mapping[str, object]]]'
        purpose: Implements _inspect_root_layout for Offline inspection of an immutable OCI base-image layout.
      - name: inspect_oci_base_root_layout
        signature: 'inspect_oci_base_root_layout(root_layout: Path, declared_reference: str, requested_platforms: Sequence[str]) -> OciBaseRootInspection'
        purpose: Authenticate a root layout and select exact requested child manifests.
      - name: inspect_oci_base_layout
        signature: 'inspect_oci_base_layout(root_layout: Path, declared_reference: str, requested_platforms: Sequence[str], child_layouts: Mapping[str, Path] | None=None) -> OciBaseInspectionEvidence'
        purpose: Validate a copied OCI layout and return canonical, redacted evidence.
      internal_dependencies: []
    - path: src/ci_workflows/oci_contract.py
      responsibility: Checked-in OCI product contract and deterministic build planning.
      status: consolidate
      classes: []
      functions:
      - name: require
        signature: 'require(condition: bool, code: str) -> None'
        purpose: Implements require for Checked-in OCI product contract and deterministic build planning.
      - name: mapping
        signature: 'mapping(value: Any) -> Mapping[str, Any]'
        purpose: Implements mapping for Checked-in OCI product contract and deterministic build planning.
      - name: strings
        signature: 'strings(value: Any, *, nonempty: bool=False) -> tuple[str, ...]'
        purpose: Implements strings for Checked-in OCI product contract and deterministic build planning.
      - name: central_public_host
        signature: 'central_public_host(value: str) -> bool'
        purpose: Implements central_public_host for Checked-in OCI product contract and deterministic build planning.
      - name: safe_relative
        signature: 'safe_relative(value: Any, *, allow_dot: bool=False) -> str'
        purpose: Implements safe_relative for Checked-in OCI product contract and deterministic build planning.
      - name: bounded_path
        signature: 'bounded_path(root: Path, relative: str, *, allow_root: bool=False) -> Path'
        purpose: Implements bounded_path for Checked-in OCI product contract and deterministic build planning.
      - name: _validate_target
        signature: '_validate_target(value: Mapping[str, Any], platform_sets: Mapping[str, Any]) -> OciTarget'
        purpose: Implements _validate_target for Checked-in OCI product contract and deterministic build planning.
      - name: load_contract
        signature: 'load_contract(root: Path) -> Mapping[str, Any]'
        purpose: Implements load_contract for Checked-in OCI product contract and deterministic build planning.
      - name: source_trust_from_environment
        signature: 'source_trust_from_environment(environment: Mapping[str, str]) -> str'
        purpose: Implements source_trust_from_environment for Checked-in OCI product contract and deterministic build planning.
      - name: request_from_mapping
        signature: 'request_from_mapping(value: Mapping[str, Any], environment: Mapping[str, str]) -> OciBuildRequest'
        purpose: Implements request_from_mapping for Checked-in OCI product contract and deterministic build planning.
      - name: resolve_plan
        signature: 'resolve_plan(root: Path, request: OciBuildRequest) -> OciBuildPlan'
        purpose: Implements resolve_plan for Checked-in OCI product contract and deterministic build planning.
      - name: metadata_labels
        signature: 'metadata_labels(contract: Mapping[str, Any], plan: OciBuildPlan, target: OciTarget, epoch: int) -> Mapping[str, str]'
        purpose: Implements metadata_labels for Checked-in OCI product contract and deterministic build planning.
      - name: render_engine_mapping
        signature: 'render_engine_mapping(contract: Mapping[str, Any]) -> Mapping[str, Any]'
        purpose: Implements render_engine_mapping for Checked-in OCI product contract and deterministic build planning.
      - name: validate_generated_mapping
        signature: 'validate_generated_mapping(root: Path, contract: Mapping[str, Any] | None=None) -> None'
        purpose: Implements validate_generated_mapping for Checked-in OCI product contract and deterministic build planning.
      internal_dependencies:
      - oci_types
    - path: src/ci_workflows/oci_execution.py
      responsibility: Engine-neutral execution with one reviewed daemonless Buildah adapter.
      status: consolidate
      classes:
      - name: MaterializedTargetInputs
        purpose: Defines MaterializedTargetInputs for Engine-neutral execution with one reviewed daemonless Buildah adapter.
        methods: []
      functions:
      - name: execute_command
        signature: 'execute_command(argv: Sequence[str], *, cwd: Path | None=None, capture: bool=False, env: Mapping[str, str] | None=None, preexec_fn: Callable[[], None] | None=None)
          -> subprocess.CompletedProcess[str]'
        purpose: Implements execute_command for Engine-neutral execution with one reviewed daemonless Buildah adapter.
      - name: execute_binary_command
        signature: 'execute_binary_command(argv: Sequence[str], *, cwd: Path | None=None, env: Mapping[str, str] | None=None, preexec_fn: Callable[[], None] | None=None) -> subprocess.CompletedProcess[bytes]'
        purpose: Implements execute_binary_command for Engine-neutral execution with one reviewed daemonless Buildah adapter.
      - name: _validated_engine_operation
        signature: '_validated_engine_operation(argv: Sequence[str], operation: Callable[[], _EngineResult]) -> _EngineResult'
        purpose: Implements _validated_engine_operation for Engine-neutral execution with one reviewed daemonless Buildah adapter.
      - name: execute_engine_command
        signature: 'execute_engine_command(root: Path, argv: Sequence[str], *, cwd: Path | None=None, capture: bool=False, env: Mapping[str, str] | None=None) -> subprocess.CompletedProcess[str]'
        purpose: Implements execute_engine_command for Engine-neutral execution with one reviewed daemonless Buildah adapter.
      - name: capture_engine_bytes
        signature: 'capture_engine_bytes(root: Path, argv: Sequence[str], *, cwd: Path | None=None, env: Mapping[str, str] | None=None) -> bytes'
        purpose: Capture an OCI engine's stdout without text or newline conversion.
      - name: _private_engine_preexec
        signature: '_private_engine_preexec(root: Path, system_containers: Path=Path(''/var/lib/containers'')) -> Callable[[], None]'
        purpose: Confine rootful containers/image implicit state to registered run state.
      - name: sha256_file
        signature: 'sha256_file(path: Path) -> str'
        purpose: Implements sha256_file for Engine-neutral execution with one reviewed daemonless Buildah adapter.
      - name: state_root
        signature: 'state_root(environment: Mapping[str, str]) -> Path'
        purpose: Implements state_root for Engine-neutral execution with one reviewed daemonless Buildah adapter.
      - name: exact_git_head
        signature: 'exact_git_head(source_root: Path) -> str'
        purpose: Implements exact_git_head for Engine-neutral execution with one reviewed daemonless Buildah adapter.
      - name: assert_clean_source
        signature: 'assert_clean_source(source_root: Path, admitted_sha: str) -> None'
        purpose: Implements assert_clean_source for Engine-neutral execution with one reviewed daemonless Buildah adapter.
      - name: source_date_epoch
        signature: 'source_date_epoch(source_root: Path) -> int'
        purpose: Implements source_date_epoch for Engine-neutral execution with one reviewed daemonless Buildah adapter.
      - name: validate_dockerfile_bases
        signature: 'validate_dockerfile_bases(path: Path) -> tuple[str, ...]'
        purpose: Implements validate_dockerfile_bases for Engine-neutral execution with one reviewed daemonless Buildah adapter.
      - name: _tracked_files
        signature: '_tracked_files(source_root: Path, context_path: str) -> tuple[Path, ...]'
        purpose: Implements _tracked_files for Engine-neutral execution with one reviewed daemonless Buildah adapter.
      - name: stage_context
        signature: 'stage_context(source_root: Path, target: OciTarget, destination: Path) -> Path'
        purpose: Implements stage_context for Engine-neutral execution with one reviewed daemonless Buildah adapter.
      - name: _read_json
        signature: '_read_json(path: Path) -> object'
        purpose: Implements _read_json for Engine-neutral execution with one reviewed daemonless Buildah adapter.
      - name: _blob
        signature: '_blob(layout: Path, digest: str) -> Path'
        purpose: Implements _blob for Engine-neutral execution with one reviewed daemonless Buildah adapter.
      - name: _descriptor_blob
        signature: '_descriptor_blob(layout: Path, descriptor: object, media_types: frozenset[str]) -> tuple[Path, str]'
        purpose: Implements _descriptor_blob for Engine-neutral execution with one reviewed daemonless Buildah adapter.
      - name: _platform_name
        signature: '_platform_name(platform: object) -> tuple[str, str, str, str | None]'
        purpose: Implements _platform_name for Engine-neutral execution with one reviewed daemonless Buildah adapter.
      - name: _index_manifests
        signature: '_index_manifests(value: object) -> list[object]'
        purpose: Implements _index_manifests for Engine-neutral execution with one reviewed daemonless Buildah adapter.
      - name: _image_manifest_descriptors
        signature: '_image_manifest_descriptors(layout: Path, index: object, *, nested: bool=False) -> tuple[Mapping[str, object], ...]'
        purpose: Return the platform manifests from an OCI layout's bounded index graph.
      - name: inspect_layout
        signature: 'inspect_layout(layout: Path, target: OciTarget, labels: Mapping[str, str]) -> OciTargetResult'
        purpose: Implements inspect_layout for Engine-neutral execution with one reviewed daemonless Buildah adapter.
      - name: verify_no_secret_leakage
        signature: 'verify_no_secret_leakage(layout: Path, secret_files: Mapping[str, Path]) -> None'
        purpose: Implements verify_no_secret_leakage for Engine-neutral execution with one reviewed daemonless Buildah adapter.
      - name: _buildah_base
        signature: '_buildah_base(root: Path, driver: str) -> list[str]'
        purpose: Implements _buildah_base for Engine-neutral execution with one reviewed daemonless Buildah adapter.
      - name: _credential_free_authfile
        signature: '_credential_free_authfile(root: Path, *, replace_existing: bool=False) -> Path'
        purpose: Implements _credential_free_authfile for Engine-neutral execution with one reviewed daemonless Buildah adapter.
      - name: credential_free_environment
        signature: 'credential_free_environment(authfile: Path, environment: Mapping[str, str] | None=None) -> dict[str, str]'
        purpose: Pin every Buildah operation to the per-run empty auth file.
      - name: _write_private_runtime_files
        signature: '_write_private_runtime_files(root: Path) -> tuple[Path, Path, Path]'
        purpose: Implements _write_private_runtime_files for Engine-neutral execution with one reviewed daemonless Buildah adapter.
      - name: _ensure_cleanup_storage_config_unchecked
        signature: '_ensure_cleanup_storage_config_unchecked(root: Path) -> Path'
        purpose: Implements _ensure_cleanup_storage_config_unchecked for Engine-neutral execution with one reviewed daemonless Buildah adapter.
      - name: _ensure_cleanup_storage_config
        signature: '_ensure_cleanup_storage_config(root: Path) -> Path'
        purpose: Implements _ensure_cleanup_storage_config for Engine-neutral execution with one reviewed daemonless Buildah adapter.
      - name: _private_builder_environment
        signature: '_private_builder_environment(root: Path, authfile: Path, storage_config: Path, environment: Mapping[str, str], registries_config: Path | None=None) -> dict[str, str]'
        purpose: Implements _private_builder_environment for Engine-neutral execution with one reviewed daemonless Buildah adapter.
      - name: _reference_host
        signature: '_reference_host(reference: str) -> str'
        purpose: Implements _reference_host for Engine-neutral execution with one reviewed daemonless Buildah adapter.
      - name: _record_base_image_id
        signature: '_record_base_image_id(image_ids: dict[str, dict[str, str]], build_platforms: Sequence[str], reference: str, image_id: str) -> None'
        purpose: Implements _record_base_image_id for Engine-neutral execution with one reviewed daemonless Buildah adapter.
      - name: _verify_materialized_external_inputs
        signature: '_verify_materialized_external_inputs(context: Path, materialized: MaterializedTargetInputs) -> None'
        purpose: Re-prove immutable input bytes after every Dockerfile instruction ran.
      - name: _import_base_platform
        signature: '_import_base_platform(child_layout: Path, *, root: Path, authfile: Path, policy_file: Path, builder_environment: Mapping[str, str], target: OciTarget, base_lock:
          OciBaseLock, platform: str, manifest_digest: str, config_digest: str) -> str'
        purpose: Implements _import_base_platform for Engine-neutral execution with one reviewed daemonless Buildah adapter.
      - name: _materialize_target_inputs
        signature: '_materialize_target_inputs(source_root: Path, plan: OciBuildPlan, target: OciTarget, staged_root: Path, root: Path, authfile: Path, policy_file: Path, builder_environment:
          Mapping[str, str]) -> MaterializedTargetInputs'
        purpose: Implements _materialize_target_inputs for Engine-neutral execution with one reviewed daemonless Buildah adapter.
      - name: verify_builder_runtime
        signature: 'verify_builder_runtime(root: Path, environment: Mapping[str, str]) -> None'
        purpose: Implements verify_builder_runtime for Engine-neutral execution with one reviewed daemonless Buildah adapter.
      - name: build_target
        signature: 'build_target(plan: OciBuildPlan, target: OciTarget, staged_root: Path, root: Path, labels: Mapping[str, str], epoch: int, secret_files: Mapping[str, Path], authfile:
          Path, builder_environment: Mapping[str, str], materialized_inputs: MaterializedTargetInputs, policy_file: Path | None=None) -> tuple[OciTargetResult, str]'
        purpose: Implements build_target for Engine-neutral execution with one reviewed daemonless Buildah adapter.
      - name: execute_plan
        signature: 'execute_plan(repository_root: Path, source_root: Path, plan: OciBuildPlan, environment: Mapping[str, str], secret_files: Mapping[str, Path] | None=None) -> OciBuildResult'
        purpose: Implements execute_plan for Engine-neutral execution with one reviewed daemonless Buildah adapter.
      - name: cleanup
        signature: 'cleanup(environment: Mapping[str, str], storage_driver: str=''vfs'') -> None'
        purpose: Implements cleanup for Engine-neutral execution with one reviewed daemonless Buildah adapter.
      - name: residue
        signature: 'residue(environment: Mapping[str, str]) -> None'
        purpose: Implements residue for Engine-neutral execution with one reviewed daemonless Buildah adapter.
      internal_dependencies:
      - foundation_types
      - oci_base_inspection
      - oci_contract
      - oci_input_contract
      - oci_input_download
      - oci_registry_download
      - oci_types
    - path: src/ci_workflows/oci_execution_safe.py
      responsibility: Security adapter for central filesystem assertions and isolated OCI smoke.
      status: consolidate
      classes: []
      functions:
      - name: _layer_paths
        signature: '_layer_paths(layout: Path, layer_digests: Sequence[str]) -> set[str]'
        purpose: Implements _layer_paths for Security adapter for central filesystem assertions and isolated OCI smoke.
      - name: _manifest_layer_sets
        signature: '_manifest_layer_sets(layout: Path) -> tuple[tuple[str, ...], ...]'
        purpose: Return verified layer sets from direct or Buildah-named OCI indexes.
      - name: _assert_target_filesystem
        signature: '_assert_target_filesystem(layout: Path, target: OciTarget) -> None'
        purpose: Implements _assert_target_filesystem for Security adapter for central filesystem assertions and isolated OCI smoke.
      - name: _run_isolated_smoke
        signature: '_run_isolated_smoke(root: Path, plan: OciBuildPlan, target: OciTarget, script: Path) -> str'
        purpose: Implements _run_isolated_smoke for Security adapter for central filesystem assertions and isolated OCI smoke.
      - name: _rebuild_result
        signature: '_rebuild_result(result: OciBuildResult, target_results: tuple[OciTargetResult, ...]) -> OciBuildResult'
        purpose: Implements _rebuild_result for Security adapter for central filesystem assertions and isolated OCI smoke.
      - name: execute_plan
        signature: 'execute_plan(repository_root: Path, source_root: Path, plan: OciBuildPlan, environment: Mapping[str, str], secret_files: Mapping[str, Path] | None=None) -> OciBuildResult'
        purpose: Execute the base builder with host smoke disabled, then assert safely.
      internal_dependencies:
      - oci_execution
      - oci_types
    - path: src/ci_workflows/oci_input_contract.py
      responsibility: Closed producer-owned locks for immutable OCI build inputs.
      status: consolidate
      classes:
      - name: OciInputContractError
        purpose: A stable OCI build failure scoped to the input-lock contract.
        methods: []
      - name: OciBasePlatformIdentity
        purpose: Defines OciBasePlatformIdentity for Closed producer-owned locks for immutable OCI build inputs.
        methods:
        - name: to_dict
          signature: to_dict(self) -> dict[str, str]
          purpose: Implements OciBasePlatformIdentity.to_dict behavior for Closed producer-owned locks for immutable OCI build inputs.
      - name: OciBaseLock
        purpose: Defines OciBaseLock for Closed producer-owned locks for immutable OCI build inputs.
        methods:
        - name: to_dict
          signature: to_dict(self) -> dict[str, object]
          purpose: Implements OciBaseLock.to_dict behavior for Closed producer-owned locks for immutable OCI build inputs.
      - name: OciExternalInputLock
        purpose: Defines OciExternalInputLock for Closed producer-owned locks for immutable OCI build inputs.
        methods:
        - name: to_dict
          signature: to_dict(self) -> dict[str, object]
          purpose: Implements OciExternalInputLock.to_dict behavior for Closed producer-owned locks for immutable OCI build inputs.
      - name: OciTargetInputLock
        purpose: Defines OciTargetInputLock for Closed producer-owned locks for immutable OCI build inputs.
        methods:
        - name: canonical_payload
          signature: canonical_payload(self) -> dict[str, object]
          purpose: Implements OciTargetInputLock.canonical_payload behavior for Closed producer-owned locks for immutable OCI build inputs.
      - name: OciBaseEvidence
        purpose: Defines OciBaseEvidence for Closed producer-owned locks for immutable OCI build inputs.
        methods:
        - name: to_dict
          signature: to_dict(self) -> dict[str, object]
          purpose: Implements OciBaseEvidence.to_dict behavior for Closed producer-owned locks for immutable OCI build inputs.
      - name: OciExternalInputEvidence
        purpose: Defines OciExternalInputEvidence for Closed producer-owned locks for immutable OCI build inputs.
        methods:
        - name: to_dict
          signature: to_dict(self) -> dict[str, object]
          purpose: Implements OciExternalInputEvidence.to_dict behavior for Closed producer-owned locks for immutable OCI build inputs.
      - name: OciTargetInputEvidence
        purpose: Defines OciTargetInputEvidence for Closed producer-owned locks for immutable OCI build inputs.
        methods:
        - name: to_dict
          signature: to_dict(self) -> dict[str, object]
          purpose: Implements OciTargetInputEvidence.to_dict behavior for Closed producer-owned locks for immutable OCI build inputs.
      functions:
      - name: _fail
        signature: '_fail(code: str) -> None'
        purpose: Implements _fail for Closed producer-owned locks for immutable OCI build inputs.
      - name: _require
        signature: '_require(condition: bool, code: str) -> None'
        purpose: Implements _require for Closed producer-owned locks for immutable OCI build inputs.
      - name: _mapping
        signature: '_mapping(value: object, code: str=''input_lock_invalid'') -> dict[str, Any]'
        purpose: Implements _mapping for Closed producer-owned locks for immutable OCI build inputs.
      - name: _exact_mapping
        signature: '_exact_mapping(value: object, keys: set[str]) -> dict[str, Any]'
        purpose: Implements _exact_mapping for Closed producer-owned locks for immutable OCI build inputs.
      - name: _safe_id
        signature: '_safe_id(value: object) -> str'
        purpose: Implements _safe_id for Closed producer-owned locks for immutable OCI build inputs.
      - name: _product_id
        signature: '_product_id(value: object) -> str'
        purpose: Implements _product_id for Closed producer-owned locks for immutable OCI build inputs.
      - name: _platforms
        signature: '_platforms(value: object) -> tuple[str, ...]'
        purpose: Implements _platforms for Closed producer-owned locks for immutable OCI build inputs.
      - name: _safe_relative
        signature: '_safe_relative(value: object) -> str'
        purpose: Implements _safe_relative for Closed producer-owned locks for immutable OCI build inputs.
      - name: _registry_host
        signature: '_registry_host(reference: str) -> str'
        purpose: Implements _registry_host for Closed producer-owned locks for immutable OCI build inputs.
      - name: _public_dns_host
        signature: '_public_dns_host(host: str) -> bool'
        purpose: Implements _public_dns_host for Closed producer-owned locks for immutable OCI build inputs.
      - name: _base
        signature: '_base(value: object, target_platforms: tuple[str, ...]) -> OciBaseLock'
        purpose: Implements _base for Closed producer-owned locks for immutable OCI build inputs.
      - name: _external
        signature: '_external(value: object) -> OciExternalInputLock'
        purpose: Implements _external for Closed producer-owned locks for immutable OCI build inputs.
      - name: _canonical_payload
        signature: '_canonical_payload(value: object) -> object'
        purpose: Implements _canonical_payload for Closed producer-owned locks for immutable OCI build inputs.
      - name: canonical_lock_digest
        signature: 'canonical_lock_digest(value: object) -> str'
        purpose: Hash only canonical declared lock data, never derived runtime evidence.
      - name: _bounded_lock_path
        signature: '_bounded_lock_path(source_root: Path, relative_path: str) -> Path'
        purpose: Implements _bounded_lock_path for Closed producer-owned locks for immutable OCI build inputs.
      - name: _unique_object
        signature: '_unique_object(pairs: list[tuple[str, Any]]) -> dict[str, Any]'
        purpose: Implements _unique_object for Closed producer-owned locks for immutable OCI build inputs.
      - name: _read_lock
        signature: '_read_lock(path: Path) -> object'
        purpose: Implements _read_lock for Closed producer-owned locks for immutable OCI build inputs.
      - name: load_input_lock_contract
        signature: 'load_input_lock_contract(source_root: Path, relative_path: str, *, product_id: str, target_id: str, input_policy_id: str, expected_platforms: tuple[str, ...]) ->
          OciTargetInputLock'
        purpose: Read the fixed exact-source lock and bind it to its central target policy.
      - name: _logical_dockerfile_lines
        signature: '_logical_dockerfile_lines(text: str) -> tuple[str, ...]'
        purpose: Implements _logical_dockerfile_lines for Closed producer-owned locks for immutable OCI build inputs.
      - name: _instruction_body
        signature: '_instruction_body(line: str, instruction: str) -> str | None'
        purpose: Implements _instruction_body for Closed producer-owned locks for immutable OCI build inputs.
      - name: _prior_stage_source
        signature: '_prior_stage_source(value: str, prior_aliases: set[str]) -> None'
        purpose: Implements _prior_stage_source for Closed producer-owned locks for immutable OCI build inputs.
      - name: _pop_option
        signature: '_pop_option(body: str) -> tuple[str, str]'
        purpose: Implements _pop_option for Closed producer-owned locks for immutable OCI build inputs.
      - name: _copy_operands
        signature: '_copy_operands(body: str) -> tuple[str, ...]'
        purpose: Implements _copy_operands for Closed producer-owned locks for immutable OCI build inputs.
      - name: _local_copy_source
        signature: '_local_copy_source(value: str) -> None'
        purpose: Implements _local_copy_source for Closed producer-owned locks for immutable OCI build inputs.
      - name: _validate_copy_from
        signature: '_validate_copy_from(line: str, prior_aliases: set[str]) -> None'
        purpose: Implements _validate_copy_from for Closed producer-owned locks for immutable OCI build inputs.
      - name: _validate_run_mount_from
        signature: '_validate_run_mount_from(line: str, prior_aliases: set[str]) -> None'
        purpose: Implements _validate_run_mount_from for Closed producer-owned locks for immutable OCI build inputs.
      - name: validate_target_dockerfile_lock
        signature: 'validate_target_dockerfile_lock(dockerfile_path: Path, target_lock: OciTargetInputLock, platforms: tuple[str, ...]) -> tuple[OciBaseLock, ...]'
        purpose: Require exact ordered agreement between every logical FROM and its lock.
      internal_dependencies:
      - oci_types
    - path: src/ci_workflows/oci_input_download.py
      responsibility: Fail-closed acquisition of contract-owned OCI build inputs.
      status: consolidate
      classes:
      - name: OciInputDownloadRequest
        purpose: One immutable external input selected by the central product contract.
        methods: []
      - name: VerifiedOciInput
        purpose: Redacted input evidence; destination is always state-relative.
        methods:
        - name: to_dict
          signature: to_dict(self) -> dict[str, object]
          purpose: Implements VerifiedOciInput.to_dict behavior for Fail-closed acquisition of contract-owned OCI build inputs.
      - name: OciInputResponse
        purpose: A transport response whose complete redirect path remains inspectable.
        methods: []
      - name: OciInputTransport
        purpose: Narrow transport seam used only to make acquisition tests hermetic.
        methods:
        - name: __call__
          signature: '__call__(self, source_url: str, *, validate_redirect: Callable[[str], None], maximum_redirects: int) -> ContextManager[OciInputResponse]'
          purpose: Implements OciInputTransport.__call__ behavior for Fail-closed acquisition of contract-owned OCI build inputs.
      - name: _PinnedHTTPSConnection
        purpose: HTTPS with DNS performed once and the resulting peer pinned.
        methods:
        - name: connect
          signature: connect(self) -> None
          purpose: Implements _PinnedHTTPSConnection.connect behavior for Fail-closed acquisition of contract-owned OCI build inputs.
      functions:
      - name: _normalized_host
        signature: '_normalized_host(host: str) -> str'
        purpose: Implements _normalized_host for Fail-closed acquisition of contract-owned OCI build inputs.
      - name: _allowed_hosts
        signature: '_allowed_hosts(values: tuple[str, ...]) -> frozenset[str]'
        purpose: Implements _allowed_hosts for Fail-closed acquisition of contract-owned OCI build inputs.
      - name: _validate_url
        signature: '_validate_url(url: str, allowed_hosts: frozenset[str]) -> None'
        purpose: Implements _validate_url for Fail-closed acquisition of contract-owned OCI build inputs.
      - name: _public_address
        signature: '_public_address(value: str) -> str'
        purpose: Implements _public_address for Fail-closed acquisition of contract-owned OCI build inputs.
      - name: _resolve_public_addresses
        signature: '_resolve_public_addresses(hostname: str) -> tuple[str, ...]'
        purpose: Implements _resolve_public_addresses for Fail-closed acquisition of contract-owned OCI build inputs.
      - name: _request_once
        signature: '_request_once(url: str) -> tuple[_PinnedHTTPSConnection, http.client.HTTPResponse]'
        purpose: Implements _request_once for Fail-closed acquisition of contract-owned OCI build inputs.
      - name: _open_https_input
        signature: '_open_https_input(source_url: str, *, validate_redirect: Callable[[str], None], maximum_redirects: int)'
        purpose: Implements _open_https_input for Fail-closed acquisition of contract-owned OCI build inputs.
      - name: _validated_destination
        signature: '_validated_destination(value: str) -> tuple[str, tuple[str, ...]]'
        purpose: Implements _validated_destination for Fail-closed acquisition of contract-owned OCI build inputs.
      - name: _open_directory
        signature: '_open_directory(name: str | Path, *, dir_fd: int | None=None) -> int'
        purpose: Implements _open_directory for Fail-closed acquisition of contract-owned OCI build inputs.
      - name: _open_destination_parent
        signature: '_open_destination_parent(root_descriptor: int, parent_parts: tuple[str, ...]) -> tuple[int, tuple[tuple[str, ...], ...]]'
        purpose: Implements _open_destination_parent for Fail-closed acquisition of contract-owned OCI build inputs.
      - name: _open_existing_parent
        signature: '_open_existing_parent(root_descriptor: int, parts: tuple[str, ...]) -> int'
        purpose: Implements _open_existing_parent for Fail-closed acquisition of contract-owned OCI build inputs.
      - name: _entry_exists
        signature: '_entry_exists(parent_descriptor: int, name: str) -> bool'
        purpose: Implements _entry_exists for Fail-closed acquisition of contract-owned OCI build inputs.
      - name: _remove_entry
        signature: '_remove_entry(parent_descriptor: int, name: str) -> None'
        purpose: Implements _remove_entry for Fail-closed acquisition of contract-owned OCI build inputs.
      - name: _remove_created_directories
        signature: '_remove_created_directories(root_descriptor: int, created: tuple[tuple[str, ...], ...]) -> None'
        purpose: Implements _remove_created_directories for Fail-closed acquisition of contract-owned OCI build inputs.
      - name: _cleanup_failed_download
        signature: '_cleanup_failed_download(root_descriptor: int, parent_descriptor: int, *, temporary_name: str, destination_name: str, temporary_created: bool, final_created: bool,
          created_directories: tuple[tuple[str, ...], ...]) -> None'
        purpose: Implements _cleanup_failed_download for Fail-closed acquisition of contract-owned OCI build inputs.
      - name: _validated_request
        signature: '_validated_request(request: OciInputDownloadRequest) -> tuple[str, str, int, str, tuple[str, ...], frozenset[str], int]'
        purpose: Implements _validated_request for Fail-closed acquisition of contract-owned OCI build inputs.
      - name: download_oci_input
        signature: 'download_oci_input(request: OciInputDownloadRequest, *, registered_state: Path, transport: OciInputTransport | None=None) -> VerifiedOciInput'
        purpose: Verify and atomically materialize one exact external build input.
      internal_dependencies:
      - foundation_types
    - path: src/ci_workflows/oci_publish.py
      responsibility: Trusted immutable OCI publication, replay safety, and registry read-back.
      status: consolidate
      classes:
      - name: OciPublishError
        purpose: Fail-closed publication error carrying one stable safe code.
        methods: []
      - name: PublishRequest
        purpose: Defines PublishRequest for Trusted immutable OCI publication, replay safety, and registry read-back.
        methods: []
      - name: PublishTarget
        purpose: Defines PublishTarget for Trusted immutable OCI publication, replay safety, and registry read-back.
        methods: []
      - name: PublishPlan
        purpose: Defines PublishPlan for Trusted immutable OCI publication, replay safety, and registry read-back.
        methods:
        - name: planning_outputs
          signature: planning_outputs(self) -> dict[str, str]
          purpose: Implements PublishPlan.planning_outputs behavior for Trusted immutable OCI publication, replay safety, and registry read-back.
      functions:
      - name: _require
        signature: '_require(condition: bool, code: str) -> None'
        purpose: Implements _require for Trusted immutable OCI publication, replay safety, and registry read-back.
      - name: _mapping
        signature: '_mapping(value: Any, code: str=''invalid_contract'') -> Mapping[str, Any]'
        purpose: Implements _mapping for Trusted immutable OCI publication, replay safety, and registry read-back.
      - name: _strings
        signature: '_strings(value: Any, code: str=''invalid_contract'') -> tuple[str, ...]'
        purpose: Implements _strings for Trusted immutable OCI publication, replay safety, and registry read-back.
      - name: _source_trust
        signature: '_source_trust(environment: Mapping[str, str]) -> str'
        purpose: Implements _source_trust for Trusted immutable OCI publication, replay safety, and registry read-back.
      - name: request_from_environment
        signature: 'request_from_environment(environment: Mapping[str, str]) -> PublishRequest'
        purpose: Implements request_from_environment for Trusted immutable OCI publication, replay safety, and registry read-back.
      - name: load_product_contract
        signature: 'load_product_contract(repository_root: Path) -> Mapping[str, Any]'
        purpose: Implements load_product_contract for Trusted immutable OCI publication, replay safety, and registry read-back.
      - name: canonical_registry_repository
        signature: 'canonical_registry_repository(source_repository: str, target_id: str, target_count: int) -> str'
        purpose: Implements canonical_registry_repository for Trusted immutable OCI publication, replay safety, and registry read-back.
      - name: resolve_plan
        signature: 'resolve_plan(repository_root: Path, request: PublishRequest) -> PublishPlan'
        purpose: Implements resolve_plan for Trusted immutable OCI publication, replay safety, and registry read-back.
      - name: _tokenized_root
        signature: '_tokenized_root(environment: Mapping[str, str], domain: str, prefix: str) -> Path'
        purpose: Implements _tokenized_root for Trusted immutable OCI publication, replay safety, and registry read-back.
      - name: publication_state_root
        signature: 'publication_state_root(environment: Mapping[str, str]) -> Path'
        purpose: Implements publication_state_root for Trusted immutable OCI publication, replay safety, and registry read-back.
      - name: build_state_root
        signature: 'build_state_root(environment: Mapping[str, str]) -> Path'
        purpose: Implements build_state_root for Trusted immutable OCI publication, replay safety, and registry read-back.
      - name: _run
        signature: '_run(argv: Sequence[str], *, input_bytes: bytes | None=None, capture: bool=True, check: bool=True) -> subprocess.CompletedProcess[bytes]'
        purpose: Implements _run for Trusted immutable OCI publication, replay safety, and registry read-back.
      - name: _authfile
        signature: '_authfile(root: Path) -> Path'
        purpose: Implements _authfile for Trusted immutable OCI publication, replay safety, and registry read-back.
      - name: _secure_existing_authfile
        signature: '_secure_existing_authfile(root: Path) -> Path'
        purpose: Implements _secure_existing_authfile for Trusted immutable OCI publication, replay safety, and registry read-back.
      - name: authenticate
        signature: 'authenticate(plan: PublishPlan, environment: Mapping[str, str], username: str, token: str) -> dict[str, str]'
        purpose: Implements authenticate for Trusted immutable OCI publication, replay safety, and registry read-back.
      - name: _blob_path
        signature: '_blob_path(layout: Path, digest: str) -> Path'
        purpose: Implements _blob_path for Trusted immutable OCI publication, replay safety, and registry read-back.
      - name: _json_blob
        signature: '_json_blob(layout: Path, descriptor: Mapping[str, Any], allowed_media: set[str]) -> Mapping[str, Any]'
        purpose: Implements _json_blob for Trusted immutable OCI publication, replay safety, and registry read-back.
      - name: _root_descriptor
        signature: '_root_descriptor(layout: Path, ref_name: str) -> Mapping[str, Any]'
        purpose: Implements _root_descriptor for Trusted immutable OCI publication, replay safety, and registry read-back.
      - name: _platform_name
        signature: '_platform_name(config: Mapping[str, Any]) -> str'
        purpose: Implements _platform_name for Trusted immutable OCI publication, replay safety, and registry read-back.
      - name: _validate_runtime
        signature: '_validate_runtime(target: PublishTarget, config: Mapping[str, Any], labels: Mapping[str, Any]) -> None'
        purpose: Implements _validate_runtime for Trusted immutable OCI publication, replay safety, and registry read-back.
      - name: inspect_layout
        signature: 'inspect_layout(layout: Path, target: PublishTarget, ref_name: str) -> Mapping[str, Any]'
        purpose: Implements inspect_layout for Trusted immutable OCI publication, replay safety, and registry read-back.
      - name: raw_manifest_digest
        signature: 'raw_manifest_digest(payload: bytes) -> str'
        purpose: Implements raw_manifest_digest for Trusted immutable OCI publication, replay safety, and registry read-back.
      - name: replay_decision
        signature: 'replay_decision(local_digest: str, version_digest: str | None, source_digest: str | None) -> tuple[bool, bool, bool]'
        purpose: Implements replay_decision for Trusted immutable OCI publication, replay safety, and registry read-back.
      - name: _inspect_remote_digest
        signature: '_inspect_remote_digest(reference: str, authfile: Path) -> str | None'
        purpose: Implements _inspect_remote_digest for Trusted immutable OCI publication, replay safety, and registry read-back.
      - name: _copy
        signature: '_copy(source: str, destination: str, authfile: Path) -> None'
        purpose: Implements _copy for Trusted immutable OCI publication, replay safety, and registry read-back.
      - name: _write_state
        signature: '_write_state(path: Path, value: Mapping[str, Any]) -> None'
        purpose: Implements _write_state for Trusted immutable OCI publication, replay safety, and registry read-back.
      - name: publish
        signature: 'publish(plan: PublishPlan, environment: Mapping[str, str]) -> dict[str, str]'
        purpose: Implements publish for Trusted immutable OCI publication, replay safety, and registry read-back.
      - name: read_back
        signature: 'read_back(plan: PublishPlan, environment: Mapping[str, str]) -> dict[str, str]'
        purpose: Implements read_back for Trusted immutable OCI publication, replay safety, and registry read-back.
      - name: verify
        signature: 'verify(plan: PublishPlan, environment: Mapping[str, str]) -> dict[str, str]'
        purpose: Implements verify for Trusted immutable OCI publication, replay safety, and registry read-back.
      - name: cleanup
        signature: 'cleanup(environment: Mapping[str, str]) -> None'
        purpose: Implements cleanup for Trusted immutable OCI publication, replay safety, and registry read-back.
      - name: residue
        signature: 'residue(environment: Mapping[str, str]) -> None'
        purpose: Implements residue for Trusted immutable OCI publication, replay safety, and registry read-back.
      internal_dependencies: []
    - path: src/ci_workflows/oci_publish_contract.py
      responsibility: Public oci.publish contract projection over the issue-#17 registry runtime.
      status: consolidate
      classes:
      - name: PublishRequest
        purpose: Defines PublishRequest for Public oci.publish contract projection over the issue-#17 registry runtime.
        methods: []
      functions:
      - name: request_from_environment
        signature: 'request_from_environment(environment: Mapping[str, str]) -> PublishRequest'
        purpose: Validate the registered public request plus internal release authority.
      - name: _confirm_platform_set
        signature: '_confirm_platform_set(repository_root: Path, request: PublishRequest) -> None'
        purpose: Implements _confirm_platform_set for Public oci.publish contract projection over the issue-#17 registry runtime.
      - name: resolve_plan
        signature: 'resolve_plan(repository_root: Path, request: PublishRequest) -> PublishPlan'
        purpose: Resolve the runtime plan after optional checked-in platform confirmation.
      - name: verify
        signature: 'verify(plan: PublishPlan, environment: Mapping[str, str]) -> dict[str, str]'
        purpose: Project detailed verified evidence onto the registered public outputs.
      internal_dependencies:
      - oci_publish
    - path: src/ci_workflows/oci_registry_download.py
      responsibility: Anonymous, exact-digest OCI Distribution acquisition.
      status: keep
      classes:
      - name: OciRegistryAcquisitionError
        purpose: Fail-closed acquisition error carrying only a stable non-secret code.
        methods: []
      - name: OciRegistryAcquisitionRequest
        purpose: One immutable anonymous base acquisition selected by central policy.
        methods: []
      - name: OciRegistryHttpRequest
        purpose: A fully constrained request consumed by the hermetic transport seam.
        methods: []
      - name: OciRegistryHttpResponse
        purpose: A response whose complete redirect path remains inspectable.
        methods: []
      - name: OciRegistryTransport
        purpose: Defines OciRegistryTransport for Anonymous, exact-digest OCI Distribution acquisition.
        methods:
        - name: __call__
          signature: '__call__(self, request: OciRegistryHttpRequest) -> ContextManager[OciRegistryHttpResponse]'
          purpose: Implements OciRegistryTransport.__call__ behavior for Anonymous, exact-digest OCI Distribution acquisition.
      - name: AcquiredOciBase
        purpose: Registered exact layouts, with child paths keyed by platform.
        methods: []
      - name: _ParsedRequest
        purpose: Defines _ParsedRequest for Anonymous, exact-digest OCI Distribution acquisition.
        methods: []
      - name: _FetchedDocument
        purpose: Defines _FetchedDocument for Anonymous, exact-digest OCI Distribution acquisition.
        methods: []
      - name: _PinnedHTTPSConnection
        purpose: HTTPS with one public DNS answer pinned and the original TLS name kept.
        methods:
        - name: connect
          signature: connect(self) -> None
          purpose: Implements _PinnedHTTPSConnection.connect behavior for Anonymous, exact-digest OCI Distribution acquisition.
      functions:
      - name: _fail
        signature: '_fail(code: str) -> None'
        purpose: Implements _fail for Anonymous, exact-digest OCI Distribution acquisition.
      - name: _require
        signature: '_require(condition: bool, code: str) -> None'
        purpose: Implements _require for Anonymous, exact-digest OCI Distribution acquisition.
      - name: _normalized_host
        signature: '_normalized_host(value: object) -> str'
        purpose: Implements _normalized_host for Anonymous, exact-digest OCI Distribution acquisition.
      - name: _host_set
        signature: '_host_set(values: object, *, allow_empty: bool=False) -> frozenset[str]'
        purpose: Implements _host_set for Anonymous, exact-digest OCI Distribution acquisition.
      - name: _parse_reference
        signature: '_parse_reference(reference: object) -> tuple[str, str, str]'
        purpose: Implements _parse_reference for Anonymous, exact-digest OCI Distribution acquisition.
      - name: _parse_request
        signature: '_parse_request(request: OciRegistryAcquisitionRequest) -> _ParsedRequest'
        purpose: Implements _parse_request for Anonymous, exact-digest OCI Distribution acquisition.
      - name: _validate_url
        signature: '_validate_url(url: object, hosts: frozenset[str]) -> None'
        purpose: Implements _validate_url for Anonymous, exact-digest OCI Distribution acquisition.
      - name: _public_address
        signature: '_public_address(value: str) -> str'
        purpose: Implements _public_address for Anonymous, exact-digest OCI Distribution acquisition.
      - name: _resolve_public_addresses
        signature: '_resolve_public_addresses(hostname: str) -> tuple[str, ...]'
        purpose: Implements _resolve_public_addresses for Anonymous, exact-digest OCI Distribution acquisition.
      - name: _request_once
        signature: '_request_once(url: str, headers: tuple[tuple[str, str], ...]) -> tuple[_PinnedHTTPSConnection, http.client.HTTPResponse]'
        purpose: Implements _request_once for Anonymous, exact-digest OCI Distribution acquisition.
      - name: _open_https
        signature: '_open_https(request: OciRegistryHttpRequest)'
        purpose: Implements _open_https for Anonymous, exact-digest OCI Distribution acquisition.
      - name: _headers
        signature: '_headers(values: Sequence[tuple[str, str]]) -> Mapping[str, str]'
        purpose: Implements _headers for Anonymous, exact-digest OCI Distribution acquisition.
      - name: _read_bounded
        signature: '_read_bounded(stream: BinaryIO, maximum: int, too_large_code: str) -> bytes'
        purpose: Implements _read_bounded for Anonymous, exact-digest OCI Distribution acquisition.
      - name: _validate_response_path
        signature: '_validate_response_path(response: OciRegistryHttpResponse, *, initial_hosts: frozenset[str], redirect_hosts: frozenset[str], maximum_redirects: int) -> None'
        purpose: Implements _validate_response_path for Anonymous, exact-digest OCI Distribution acquisition.
      - name: _open_response
        signature: '_open_response(transport: OciRegistryTransport, *, url: str, headers: tuple[tuple[str, str], ...], initial_hosts: frozenset[str], redirect_hosts: frozenset[str],
          maximum_redirects: int) -> ContextManager[OciRegistryHttpResponse]'
        purpose: Implements _open_response for Anonymous, exact-digest OCI Distribution acquisition.
      - name: _parse_json
        signature: '_parse_json(content: bytes, code: str) -> object'
        purpose: Implements _parse_json for Anonymous, exact-digest OCI Distribution acquisition.
      - name: _parse_bearer_challenge
        signature: '_parse_bearer_challenge(value: object, *, repository: str, token_hosts: frozenset[str]) -> tuple[str, str, str]'
        purpose: Implements _parse_bearer_challenge for Anonymous, exact-digest OCI Distribution acquisition.
      - name: _anonymous_token
        signature: '_anonymous_token(challenge: str, *, parsed: _ParsedRequest, transport: OciRegistryTransport) -> str'
        purpose: Implements _anonymous_token for Anonymous, exact-digest OCI Distribution acquisition.
      - name: _manifest_url
        signature: '_manifest_url(parsed: _ParsedRequest, digest: str) -> str'
        purpose: Implements _manifest_url for Anonymous, exact-digest OCI Distribution acquisition.
      - name: _blob_url
        signature: '_blob_url(parsed: _ParsedRequest, digest: str) -> str'
        purpose: Implements _blob_url for Anonymous, exact-digest OCI Distribution acquisition.
      - name: _fetch_manifest
        signature: '_fetch_manifest(digest: str, *, parsed: _ParsedRequest, transport: OciRegistryTransport, token: str | None) -> tuple[_FetchedDocument | None, str | None]'
        purpose: Implements _fetch_manifest for Anonymous, exact-digest OCI Distribution acquisition.
      - name: _authenticated_manifest
        signature: '_authenticated_manifest(digest: str, *, parsed: _ParsedRequest, transport: OciRegistryTransport, token: str | None) -> tuple[_FetchedDocument, str | None]'
        purpose: Implements _authenticated_manifest for Anonymous, exact-digest OCI Distribution acquisition.
      - name: _descriptor
        signature: '_descriptor(value: object, media_types: frozenset[str]) -> tuple[str, str, int]'
        purpose: Implements _descriptor for Anonymous, exact-digest OCI Distribution acquisition.
      - name: _platform
        signature: '_platform(value: object) -> str'
        purpose: Implements _platform for Anonymous, exact-digest OCI Distribution acquisition.
      - name: _index_descriptors
        signature: '_index_descriptors(document: _FetchedDocument, expected: Mapping[str, str]) -> Mapping[str, Mapping[str, object]]'
        purpose: Implements _index_descriptors for Anonymous, exact-digest OCI Distribution acquisition.
      - name: _manifest_blob_descriptors
        signature: '_manifest_blob_descriptors(document: _FetchedDocument) -> tuple[Mapping[str, object], tuple[Mapping[str, object], ...]]'
        purpose: Implements _manifest_blob_descriptors for Anonymous, exact-digest OCI Distribution acquisition.
      - name: _create_layout
        signature: '_create_layout(path: Path) -> None'
        purpose: Implements _create_layout for Anonymous, exact-digest OCI Distribution acquisition.
      - name: _atomic_bytes
        signature: '_atomic_bytes(path: Path, content: bytes, mode: int=292) -> None'
        purpose: Implements _atomic_bytes for Anonymous, exact-digest OCI Distribution acquisition.
      - name: _blob_path
        signature: '_blob_path(layout: Path, digest: str) -> Path'
        purpose: Implements _blob_path for Anonymous, exact-digest OCI Distribution acquisition.
      - name: _write_blob
        signature: '_write_blob(layout: Path, document: _FetchedDocument) -> None'
        purpose: Implements _write_blob for Anonymous, exact-digest OCI Distribution acquisition.
      - name: _canonical_bytes
        signature: '_canonical_bytes(value: object) -> bytes'
        purpose: Implements _canonical_bytes for Anonymous, exact-digest OCI Distribution acquisition.
      - name: _initialize_layout
        signature: '_initialize_layout(layout: Path, descriptor: Mapping[str, object]) -> None'
        purpose: Implements _initialize_layout for Anonymous, exact-digest OCI Distribution acquisition.
      - name: _stream_blob
        signature: '_stream_blob(layout: Path, descriptor: Mapping[str, object], *, media_types: frozenset[str], maximum_bytes: int, parsed: _ParsedRequest, transport: OciRegistryTransport,
          token: str | None) -> int'
        purpose: Implements _stream_blob for Anonymous, exact-digest OCI Distribution acquisition.
      - name: _materialize_manifest_layout
        signature: '_materialize_manifest_layout(layout: Path, root_descriptor: Mapping[str, object], manifest: _FetchedDocument, *, parsed: _ParsedRequest, transport: OciRegistryTransport,
          token: str | None, total_bytes: list[int]) -> None'
        purpose: Implements _materialize_manifest_layout for Anonymous, exact-digest OCI Distribution acquisition.
      - name: acquire_oci_base
        signature: 'acquire_oci_base(request: OciRegistryAcquisitionRequest, *, registered_state: Path, transport: OciRegistryTransport | None=None) -> AcquiredOciBase'
        purpose: Acquire an exact anonymous OCI root and the requested complete children.
      internal_dependencies: []
    - path: src/ci_workflows/oci_reproducibility.py
      responsibility: Two-clean-build OCI reproducibility proof for the public reusable workflow.
      status: consolidate
      classes:
      - name: ReproducibilityError
        purpose: Raised when the reproducibility contract cannot be proven.
        methods: []
      - name: ProofInputs
        purpose: Defines ProofInputs for Two-clean-build OCI reproducibility proof for the public reusable workflow.
        methods: []
      functions:
      - name: _require
        signature: '_require(condition: bool, message: str) -> None'
        purpose: Implements _require for Two-clean-build OCI reproducibility proof for the public reusable workflow.
      - name: _run
        signature: '_run(argv: Sequence[str], *, cwd: Path | None=None, environment: Mapping[str, str] | None=None, check: bool=True, text: bool=True) -> subprocess.CompletedProcess'
        purpose: Implements _run for Two-clean-build OCI reproducibility proof for the public reusable workflow.
      - name: _safe_relative
        signature: '_safe_relative(value: str, field: str) -> PurePosixPath'
        purpose: Implements _safe_relative for Two-clean-build OCI reproducibility proof for the public reusable workflow.
      - name: _resolve_source_path
        signature: '_resolve_source_path(source_root: Path, value: str, field: str, *, directory: bool) -> Path'
        purpose: Implements _resolve_source_path for Two-clean-build OCI reproducibility proof for the public reusable workflow.
      - name: _git
        signature: '_git(source_root: Path, *arguments: str) -> str'
        purpose: Implements _git for Two-clean-build OCI reproducibility proof for the public reusable workflow.
      - name: verify_exact_source
        signature: 'verify_exact_source(source_root: Path, admitted_sha: str) -> None'
        purpose: Implements verify_exact_source for Two-clean-build OCI reproducibility proof for the public reusable workflow.
      - name: load_inputs
        signature: 'load_inputs(environment: Mapping[str, str] | None=None) -> ProofInputs'
        purpose: Implements load_inputs for Two-clean-build OCI reproducibility proof for the public reusable workflow.
      - name: _buildah_base
        signature: '_buildah_base(build_root: Path) -> list[str]'
        purpose: Implements _buildah_base for Two-clean-build OCI reproducibility proof for the public reusable workflow.
      - name: _isolated_environment
        signature: '_isolated_environment(build_root: Path) -> dict[str, str]'
        purpose: Implements _isolated_environment for Two-clean-build OCI reproducibility proof for the public reusable workflow.
      - name: build_command
        signature: 'build_command(inputs: ProofInputs, build_root: Path, platform: str, image_tag: str) -> list[str]'
        purpose: Implements build_command for Two-clean-build OCI reproducibility proof for the public reusable workflow.
      - name: raw_config_identity
        signature: 'raw_config_identity(layout: Path, expected_architecture: str, environment: Mapping[str, str] | None=None) -> tuple[str, bytes]'
        purpose: Implements raw_config_identity for Two-clean-build OCI reproducibility proof for the public reusable workflow.
      - name: compare_builds
        signature: 'compare_builds(first: Mapping[str, tuple[str, bytes]], second: Mapping[str, tuple[str, bytes]]) -> dict[str, str]'
        purpose: Implements compare_builds for Two-clean-build OCI reproducibility proof for the public reusable workflow.
      - name: _cleanup_build_root
        signature: '_cleanup_build_root(build_root: Path) -> None'
        purpose: Implements _cleanup_build_root for Two-clean-build OCI reproducibility proof for the public reusable workflow.
      - name: cleanup_state
        signature: 'cleanup_state(state_root: Path) -> None'
        purpose: Implements cleanup_state for Two-clean-build OCI reproducibility proof for the public reusable workflow.
      - name: _one_clean_build
        signature: '_one_clean_build(inputs: ProofInputs, build_name: str) -> dict[str, tuple[str, bytes]]'
        purpose: Implements _one_clean_build for Two-clean-build OCI reproducibility proof for the public reusable workflow.
      - name: _write_outputs
        signature: '_write_outputs(inputs: ProofInputs, identities: Mapping[str, str]) -> None'
        purpose: Implements _write_outputs for Two-clean-build OCI reproducibility proof for the public reusable workflow.
      - name: run_proof
        signature: 'run_proof(inputs: ProofInputs) -> dict[str, str]'
        purpose: Implements run_proof for Two-clean-build OCI reproducibility proof for the public reusable workflow.
      - name: _state_root_from_environment
        signature: '_state_root_from_environment(environment: Mapping[str, str] | None=None) -> Path'
        purpose: Implements _state_root_from_environment for Two-clean-build OCI reproducibility proof for the public reusable workflow.
      - name: main
        signature: 'main(argv: Sequence[str] | None=None) -> int'
        purpose: Implements main for Two-clean-build OCI reproducibility proof for the public reusable workflow.
      internal_dependencies: []
    - path: src/ci_workflows/oci_types.py
      responsibility: Typed models and stable errors for non-publishing OCI builds.
      status: consolidate
      classes:
      - name: OciBuildError
        purpose: Fail-closed OCI build error carrying one stable code.
        methods: []
      - name: OciBuildRequest
        purpose: Defines OciBuildRequest for Typed models and stable errors for non-publishing OCI builds.
        methods: []
      - name: OciInputPolicy
        purpose: Defines OciInputPolicy for Typed models and stable errors for non-publishing OCI builds.
        methods: []
      - name: OciTarget
        purpose: Defines OciTarget for Typed models and stable errors for non-publishing OCI builds.
        methods: []
      - name: OciBuildPlan
        purpose: Defines OciBuildPlan for Typed models and stable errors for non-publishing OCI builds.
        methods:
        - name: planning_outputs
          signature: planning_outputs(self) -> dict[str, str]
          purpose: Implements OciBuildPlan.planning_outputs behavior for Typed models and stable errors for non-publishing OCI builds.
      - name: OciPlatformResult
        purpose: Defines OciPlatformResult for Typed models and stable errors for non-publishing OCI builds.
        methods:
        - name: to_dict
          signature: to_dict(self) -> dict[str, object]
          purpose: Implements OciPlatformResult.to_dict behavior for Typed models and stable errors for non-publishing OCI builds.
      - name: OciResolvedBasePlatform
        purpose: Defines OciResolvedBasePlatform for Typed models and stable errors for non-publishing OCI builds.
        methods:
        - name: to_dict
          signature: to_dict(self) -> dict[str, str]
          purpose: Implements OciResolvedBasePlatform.to_dict behavior for Typed models and stable errors for non-publishing OCI builds.
      - name: OciResolvedBase
        purpose: Defines OciResolvedBase for Typed models and stable errors for non-publishing OCI builds.
        methods:
        - name: to_dict
          signature: to_dict(self) -> dict[str, object]
          purpose: Implements OciResolvedBase.to_dict behavior for Typed models and stable errors for non-publishing OCI builds.
      - name: OciResolvedExternalInput
        purpose: Defines OciResolvedExternalInput for Typed models and stable errors for non-publishing OCI builds.
        methods:
        - name: to_dict
          signature: to_dict(self) -> dict[str, object]
          purpose: Implements OciResolvedExternalInput.to_dict behavior for Typed models and stable errors for non-publishing OCI builds.
      - name: OciBuildInputEvidence
        purpose: Defines OciBuildInputEvidence for Typed models and stable errors for non-publishing OCI builds.
        methods:
        - name: empty
          signature: empty(cls) -> OciBuildInputEvidence
          purpose: Implements OciBuildInputEvidence.empty behavior for Typed models and stable errors for non-publishing OCI builds.
        - name: to_dict
          signature: to_dict(self) -> dict[str, object]
          purpose: Implements OciBuildInputEvidence.to_dict behavior for Typed models and stable errors for non-publishing OCI builds.
      - name: OciTargetResult
        purpose: Defines OciTargetResult for Typed models and stable errors for non-publishing OCI builds.
        methods:
        - name: to_dict
          signature: to_dict(self) -> dict[str, object]
          purpose: Implements OciTargetResult.to_dict behavior for Typed models and stable errors for non-publishing OCI builds.
      - name: OciBuildResult
        purpose: Defines OciBuildResult for Typed models and stable errors for non-publishing OCI builds.
        methods:
        - name: output_values
          signature: output_values(self) -> dict[str, str]
          purpose: Implements OciBuildResult.output_values behavior for Typed models and stable errors for non-publishing OCI builds.
      functions: []
      internal_dependencies: []
  package:
    target_module: src/ci_workflows/package_primitives.py
    responsibility: Language package build/inspection/publication primitives.
    files:
    - path: src/ci_workflows/ciw_packages.py
      responsibility: Bounded product-neutral Python, npm, and JVM package publication adapter.
      status: compatibility
      classes:
      - name: PackagePublishError
        purpose: Fail closed with one stable package-publication error code.
        methods: []
      - name: PackagePublishRequest
        purpose: Defines PackagePublishRequest for Bounded product-neutral Python, npm, and JVM package publication adapter.
        methods: []
      functions:
      - name: _require
        signature: '_require(condition: bool, code: str) -> None'
        purpose: Implements _require for Bounded product-neutral Python, npm, and JVM package publication adapter.
      - name: _single_line
        signature: '_single_line(value: object, *, code: str, maximum: int=2048, allow_empty: bool=False) -> str'
        purpose: Implements _single_line for Bounded product-neutral Python, npm, and JVM package publication adapter.
      - name: _relative_path
        signature: '_relative_path(value: object, *, code: str, allow_dot: bool) -> Path'
        purpose: Implements _relative_path for Bounded product-neutral Python, npm, and JVM package publication adapter.
      - name: _string_list
        signature: '_string_list(value: object, *, code: str) -> tuple[str, ...]'
        purpose: Implements _string_list for Bounded product-neutral Python, npm, and JVM package publication adapter.
      - name: _optional_string_list
        signature: '_optional_string_list(value: object, *, code: str) -> tuple[str, ...]'
        purpose: Implements _optional_string_list for Bounded product-neutral Python, npm, and JVM package publication adapter.
      - name: _plan
        signature: '_plan(raw: str, ecosystem: str) -> dict[str, object]'
        purpose: Implements _plan for Bounded product-neutral Python, npm, and JVM package publication adapter.
      - name: _registry_destination
        signature: '_registry_destination(plan: Mapping[str, object], registries: Mapping[str, str]) -> tuple[str | None, Path | None]'
        purpose: Implements _registry_destination for Bounded product-neutral Python, npm, and JVM package publication adapter.
      - name: _request
        signature: '_request(environment: Mapping[str, str]) -> PackagePublishRequest'
        purpose: Implements _request for Bounded product-neutral Python, npm, and JVM package publication adapter.
      - name: _bounded_source
        signature: '_bounded_source(workspace: Path, relative: Path) -> Path'
        purpose: Implements _bounded_source for Bounded product-neutral Python, npm, and JVM package publication adapter.
      - name: _validate_source_symlinks
        signature: '_validate_source_symlinks(source: Path) -> None'
        purpose: Implements _validate_source_symlinks for Bounded product-neutral Python, npm, and JVM package publication adapter.
      - name: _copy_source
        signature: '_copy_source(source: Path, destination: Path) -> None'
        purpose: Implements _copy_source for Bounded product-neutral Python, npm, and JVM package publication adapter.
      - name: _tool_from_path
        signature: '_tool_from_path(name: str, *, code: str) -> Path'
        purpose: Implements _tool_from_path for Bounded product-neutral Python, npm, and JVM package publication adapter.
      - name: _maven_tool
        signature: '_maven_tool(project: Path, value: str) -> Path'
        purpose: Implements _maven_tool for Bounded product-neutral Python, npm, and JVM package publication adapter.
      - name: _registry_from_config
        signature: '_registry_from_config(project: Path, relative: Path) -> str'
        purpose: Implements _registry_from_config for Bounded product-neutral Python, npm, and JVM package publication adapter.
      - name: _registry_url
        signature: '_registry_url(request: PackagePublishRequest, project: Path, registries: Mapping[str, str]) -> str'
        purpose: Implements _registry_url for Bounded product-neutral Python, npm, and JVM package publication adapter.
      - name: _without_package_credentials
        signature: '_without_package_credentials(environment: Mapping[str, str]) -> dict[str, str]'
        purpose: Implements _without_package_credentials for Bounded product-neutral Python, npm, and JVM package publication adapter.
      - name: _isolated_package_environment
        signature: '_isolated_package_environment(environment: Mapping[str, str], run_root: Path) -> dict[str, str]'
        purpose: Implements _isolated_package_environment for Bounded product-neutral Python, npm, and JVM package publication adapter.
      - name: _publication_environment
        signature: '_publication_environment(base: Mapping[str, str], source: Mapping[str, str], ecosystem: str) -> dict[str, str]'
        purpose: Implements _publication_environment for Bounded product-neutral Python, npm, and JVM package publication adapter.
      - name: _bootstrap_python_tools
        signature: '_bootstrap_python_tools(interpreter: Path, run_root: Path, environment: Mapping[str, str]) -> Mapping[str, str]'
        purpose: Implements _bootstrap_python_tools for Bounded product-neutral Python, npm, and JVM package publication adapter.
      - name: _artifact_paths
        signature: '_artifact_paths(project: Path, output: Path, artifacts: Sequence[PackageArtifact]) -> tuple[Path, ...]'
        purpose: Implements _artifact_paths for Bounded product-neutral Python, npm, and JVM package publication adapter.
      - name: plan_outputs
        signature: 'plan_outputs(environment: Mapping[str, str]) -> dict[str, str]'
        purpose: Implements plan_outputs for Bounded product-neutral Python, npm, and JVM package publication adapter.
      - name: execute_package_publish
        signature: 'execute_package_publish(*, workspace: Path, state_root: Path, environment: Mapping[str, str], python_builder: Callable[..., PackageBuildResult]=build_python_packages,
          python_publisher: Callable[..., PublicationResult]=publish_python_packages, npm_packer: Callable[..., PackageBuildResult]=npm_pack, npm_publisher: Callable[..., PublicationResult]=npm_publish,
          jvm_publisher: Callable[..., PackageBuildResult]=run_jvm_publication_tasks, python_tool_bootstrap: PythonToolBootstrap=_bootstrap_python_tools) -> dict[str, str]'
        purpose: Build, validate, and publish one exact package from an isolated source copy.
      - name: _state_root
        signature: '_state_root(environment: Mapping[str, str], contract_root: Path) -> Path'
        purpose: Implements _state_root for Bounded product-neutral Python, npm, and JVM package publication adapter.
      - name: _emit
        signature: '_emit(environment: Mapping[str, str], values: Mapping[str, str]) -> None'
        purpose: Implements _emit for Bounded product-neutral Python, npm, and JVM package publication adapter.
      - name: _failure
        signature: '_failure(environment: Mapping[str, str], code: str) -> None'
        purpose: Implements _failure for Bounded product-neutral Python, npm, and JVM package publication adapter.
      - name: parser
        signature: parser() -> argparse.ArgumentParser
        purpose: Implements parser for Bounded product-neutral Python, npm, and JVM package publication adapter.
      - name: main
        signature: 'main(argv: Sequence[str] | None=None, *, environment: Mapping[str, str] | None=None) -> int'
        purpose: Implements main for Bounded product-neutral Python, npm, and JVM package publication adapter.
      internal_dependencies:
      - ciw_types
      - package_primitives
      - runtime_primitives
      - workspace
    - path: src/ci_workflows/package_primitives.py
      responsibility: Product-neutral package and SDK build/inspection/publication primitives.
      status: keep
      classes:
      - name: PackagePrimitiveError
        purpose: Stable non-secret package primitive failure.
        methods: []
      - name: PackageIdentity
        purpose: Defines PackageIdentity for Product-neutral package and SDK build/inspection/publication primitives.
        methods: []
      - name: PackageArtifact
        purpose: Defines PackageArtifact for Product-neutral package and SDK build/inspection/publication primitives.
        methods: []
      - name: PackageBuildResult
        purpose: Defines PackageBuildResult for Product-neutral package and SDK build/inspection/publication primitives.
        methods: []
      - name: PublicationResult
        purpose: Defines PublicationResult for Product-neutral package and SDK build/inspection/publication primitives.
        methods: []
      - name: ProcessRunner
        purpose: Defines ProcessRunner for Product-neutral package and SDK build/inspection/publication primitives.
        methods:
        - name: __call__
          signature: '__call__(self, arguments: Sequence[str], *, cwd: Path, environment: Mapping[str, str], stdin: str='''', timeout_seconds: float | None=None) -> ProcessResult'
          purpose: Implements ProcessRunner.__call__ behavior for Product-neutral package and SDK build/inspection/publication primitives.
      functions:
      - name: _fail
        signature: '_fail(code: str, operation: str, *, returncode: int | None=None) -> None'
        purpose: Implements _fail for Product-neutral package and SDK build/inspection/publication primitives.
      - name: _single_line
        signature: '_single_line(value: object, operation: str, *, maximum: int=2048) -> str'
        purpose: Implements _single_line for Product-neutral package and SDK build/inspection/publication primitives.
      - name: validate_package_identity
        signature: 'validate_package_identity(ecosystem: Literal[''python'', ''npm'', ''jvm''], name: str, version: str) -> PackageIdentity'
        purpose: Validate a product-neutral package identity.
      - name: _directory
        signature: '_directory(path: Path, operation: str) -> Path'
        purpose: Implements _directory for Product-neutral package and SDK build/inspection/publication primitives.
      - name: _bounded_target
        signature: '_bounded_target(root: Path, requested: Path, operation: str) -> Path'
        purpose: Implements _bounded_target for Product-neutral package and SDK build/inspection/publication primitives.
      - name: _bounded_file
        signature: '_bounded_file(root: Path, requested: Path, operation: str) -> Path'
        purpose: Implements _bounded_file for Product-neutral package and SDK build/inspection/publication primitives.
      - name: _prepare_output_directory
        signature: '_prepare_output_directory(project: Path, requested: Path, operation: str) -> Path'
        purpose: Implements _prepare_output_directory for Product-neutral package and SDK build/inspection/publication primitives.
      - name: _existing_output_directory
        signature: '_existing_output_directory(project: Path, requested: Path, operation: str) -> Path'
        purpose: Implements _existing_output_directory for Product-neutral package and SDK build/inspection/publication primitives.
      - name: _tool
        signature: '_tool(path: Path, operation: str) -> Path'
        purpose: Implements _tool for Product-neutral package and SDK build/inspection/publication primitives.
      - name: _environment
        signature: '_environment(environment: Mapping[str, str] | None) -> dict[str, str]'
        purpose: Implements _environment for Product-neutral package and SDK build/inspection/publication primitives.
      - name: _credentials
        signature: '_credentials(environment: Mapping[str, str]) -> dict[str, str]'
        purpose: Implements _credentials for Product-neutral package and SDK build/inspection/publication primitives.
      - name: _redact
        signature: '_redact(text: str, secret_values: Sequence[str]) -> str'
        purpose: Implements _redact for Product-neutral package and SDK build/inspection/publication primitives.
      - name: _execute
        signature: '_execute(operation: str, arguments: Sequence[str], *, cwd: Path, environment: Mapping[str, str], runner: ProcessRunner | None, stdin: str='''', timeout_seconds: float
          | None=None, secret_values: Sequence[str]=()) -> ProcessResult'
        purpose: Implements _execute for Product-neutral package and SDK build/inspection/publication primitives.
      - name: _registry_url
        signature: '_registry_url(value: str, operation: str) -> str'
        purpose: Implements _registry_url for Product-neutral package and SDK build/inspection/publication primitives.
      - name: _archive_size
        signature: '_archive_size(path: Path, operation: str) -> int'
        purpose: Implements _archive_size for Product-neutral package and SDK build/inspection/publication primitives.
      - name: _safe_archive_member
        signature: '_safe_archive_member(name: str, operation: str) -> None'
        purpose: Implements _safe_archive_member for Product-neutral package and SDK build/inspection/publication primitives.
      - name: _metadata_identity
        signature: '_metadata_identity(text: str, ecosystem: Literal[''python'', ''npm''], operation: str) -> PackageIdentity'
        purpose: Implements _metadata_identity for Product-neutral package and SDK build/inspection/publication primitives.
      - name: _read_wheel_identity
        signature: '_read_wheel_identity(path: Path) -> PackageIdentity'
        purpose: Implements _read_wheel_identity for Product-neutral package and SDK build/inspection/publication primitives.
      - name: _read_sdist_identity
        signature: '_read_sdist_identity(path: Path) -> PackageIdentity'
        purpose: Implements _read_sdist_identity for Product-neutral package and SDK build/inspection/publication primitives.
      - name: inspect_python_packages
        signature: 'inspect_python_packages(project_directory: Path, output_directory: Path, *, expected_name: str | None=None, expected_version: str | None=None) -> tuple[PackageArtifact,
          ...]'
        purpose: Inspect wheel and sdist metadata without extracting either archive.
      - name: build_python_packages
        signature: 'build_python_packages(interpreter: Path, *, project_directory: Path, output_directory: Path, expected_name: str | None=None, expected_version: str | None=None, environment:
          Mapping[str, str] | None=None, runner: ProcessRunner | None=None, timeout_seconds: float | None=None) -> PackageBuildResult'
        purpose: Build a wheel and sdist, then inspect their embedded package metadata.
      - name: publish_python_packages
        signature: 'publish_python_packages(interpreter: Path, artifacts: Sequence[Path], *, project_directory: Path, registry_url: str, package_name: str, package_version: str, environment:
          Mapping[str, str], runner: ProcessRunner | None=None, timeout_seconds: float | None=None) -> PublicationResult'
        purpose: Publish caller-selected Python artifacts using fixed named credentials.
      - name: _read_npm_identity
        signature: '_read_npm_identity(path: Path) -> PackageIdentity'
        purpose: Implements _read_npm_identity for Product-neutral package and SDK build/inspection/publication primitives.
      - name: inspect_npm_package
        signature: 'inspect_npm_package(project_directory: Path, artifact: Path, *, expected_name: str | None=None, expected_version: str | None=None) -> PackageArtifact'
        purpose: Inspect an npm package tarball without extracting it.
      - name: npm_pack
        signature: 'npm_pack(npm_executable: Path, *, project_directory: Path, output_directory: Path, expected_name: str | None=None, expected_version: str | None=None, environment:
          Mapping[str, str] | None=None, runner: ProcessRunner | None=None, timeout_seconds: float | None=None) -> PackageBuildResult'
        purpose: Create one npm tarball with ``npm pack --json`` and inspect package.json.
      - name: _npm_auth_line
        signature: '_npm_auth_line(registry: str, token: str) -> str'
        purpose: Implements _npm_auth_line for Product-neutral package and SDK build/inspection/publication primitives.
      - name: npm_publish
        signature: 'npm_publish(npm_executable: Path, artifact: Path, *, project_directory: Path, temporary_root: Path, registry_url: str, package_name: str, package_version: str, environment:
          Mapping[str, str], runner: ProcessRunner | None=None, timeout_seconds: float | None=None) -> PublicationResult'
        purpose: Publish an npm tarball using a temporary mode-0600 userconfig.
      - name: _non_secret_arguments
        signature: '_non_secret_arguments(values: Sequence[str], operation: str) -> tuple[str, ...]'
        purpose: Implements _non_secret_arguments for Product-neutral package and SDK build/inspection/publication primitives.
      - name: _jvm_action
        signature: '_jvm_action(tool: Literal[''gradle'', ''maven''], value: str, operation: str) -> str'
        purpose: Implements _jvm_action for Product-neutral package and SDK build/inspection/publication primitives.
      - name: _pom_child
        signature: '_pom_child(root: ET.Element, name: str) -> str | None'
        purpose: Implements _pom_child for Product-neutral package and SDK build/inspection/publication primitives.
      - name: _pom_identity
        signature: '_pom_identity(path: Path) -> tuple[str | None, str, str]'
        purpose: Implements _pom_identity for Product-neutral package and SDK build/inspection/publication primitives.
      - name: inspect_jvm_outputs
        signature: 'inspect_jvm_outputs(project_directory: Path, output_directory: Path, *, expected_name: str, expected_version: str, expected_group: str | None=None) -> tuple[PackageArtifact,
          ...]'
        purpose: Inventory bounded AAR/JAR/POM outputs and verify one matching POM.
      - name: run_jvm_publication_tasks
        signature: 'run_jvm_publication_tasks(tool: Literal[''gradle'', ''maven''], executable: Path, actions: Sequence[str], *, project_directory: Path, output_directory: Path, package_name:
          str, package_version: str, package_group: str | None=None, options: Sequence[str]=(), environment: Mapping[str, str] | None=None, runner: ProcessRunner | None=None, timeout_seconds:
          float | None=None) -> PackageBuildResult'
        purpose: Run caller-selected Gradle/Maven publication tasks and inspect outputs.
      internal_dependencies:
      - runtime_primitives
  policy:
    target_module: src/ci_workflows/policy.py
    responsibility: Repository security/policy checks, readability rules, and bounded evidence projection.
    files:
    - path: src/ci_workflows/evidence.py
      responsibility: Deterministic structured evidence with bounded redaction semantics.
      status: keep
      classes:
      - name: EvidenceResult
        purpose: Defines EvidenceResult for Deterministic structured evidence with bounded redaction semantics.
        methods:
        - name: output_values
          signature: output_values(self) -> dict[str, str]
          purpose: Implements EvidenceResult.output_values behavior for Deterministic structured evidence with bounded redaction semantics.
      functions:
      - name: redact_text
        signature: 'redact_text(value: str, *, contract_root: Path) -> str'
        purpose: Redact tokens, private URLs, runner paths, and secret assignments.
      - name: _toolchain
        signature: '_toolchain(raw: Mapping[str, Any], contract: Mapping[str, Any]) -> dict[str, str]'
        purpose: Implements _toolchain for Deterministic structured evidence with bounded redaction semantics.
      - name: parse_toolchain_json
        signature: 'parse_toolchain_json(raw: str) -> Mapping[str, str]'
        purpose: Implements parse_toolchain_json for Deterministic structured evidence with bounded redaction semantics.
      - name: build_evidence
        signature: 'build_evidence(*, source_sha: str, workflow_release: str, runner_profile: str, toolchain: Mapping[str, Any], command_profile: str, result: str, cleanup_state: str,
          cleanup_removed_paths: int, contract_root: Path) -> EvidenceResult'
        purpose: Build one canonical evidence record from bounded reviewed fields.
      - name: write_evidence
        signature: 'write_evidence(state_root: Path, evidence: EvidenceResult) -> Path'
        purpose: Implements write_evidence for Deterministic structured evidence with bounded redaction semantics.
      internal_dependencies:
      - foundation_types
    - path: src/ci_workflows/policy.py
      responsibility: Tracked-secret, forbidden-file, clean-tree, generated, artifact, and cache policy.
      status: keep
      classes:
      - name: ArtifactDeclaration
        purpose: Defines ArtifactDeclaration for Tracked-secret, forbidden-file, clean-tree, generated, artifact, and cache policy.
        methods: []
      - name: CacheDecision
        purpose: Defines CacheDecision for Tracked-secret, forbidden-file, clean-tree, generated, artifact, and cache policy.
        methods: []
      - name: PolicyReport
        purpose: Defines PolicyReport for Tracked-secret, forbidden-file, clean-tree, generated, artifact, and cache policy.
        methods:
        - name: output_values
          signature: output_values(self) -> dict[str, str]
          purpose: Implements PolicyReport.output_values behavior for Tracked-secret, forbidden-file, clean-tree, generated, artifact, and cache policy.
      functions:
      - name: _run_git
        signature: '_run_git(root: Path, arguments: Sequence[str], instruction: str) -> bytes'
        purpose: Implements _run_git for Tracked-secret, forbidden-file, clean-tree, generated, artifact, and cache policy.
      - name: tracked_paths
        signature: 'tracked_paths(root: Path) -> tuple[str, ...]'
        purpose: Implements tracked_paths for Tracked-secret, forbidden-file, clean-tree, generated, artifact, and cache policy.
      - name: _matches
        signature: '_matches(path: str, patterns: Sequence[str]) -> bool'
        purpose: Implements _matches for Tracked-secret, forbidden-file, clean-tree, generated, artifact, and cache policy.
      - name: _repository_exception_paths
        signature: '_repository_exception_paths(contract: Mapping[str, Any], repository: str) -> set[str]'
        purpose: Implements _repository_exception_paths for Tracked-secret, forbidden-file, clean-tree, generated, artifact, and cache policy.
      - name: _forbidden_path
        signature: '_forbidden_path(path: str, contract: Mapping[str, Any], repository: str) -> bool'
        purpose: Implements _forbidden_path for Tracked-secret, forbidden-file, clean-tree, generated, artifact, and cache policy.
      - name: _split_marker_matches
        signature: '_split_marker_matches(content: str, marker: Mapping[str, Any]) -> bool'
        purpose: Implements _split_marker_matches for Tracked-secret, forbidden-file, clean-tree, generated, artifact, and cache policy.
      - name: _contains_token_like
        signature: '_contains_token_like(content: str, contract: Mapping[str, Any]) -> bool'
        purpose: Implements _contains_token_like for Tracked-secret, forbidden-file, clean-tree, generated, artifact, and cache policy.
      - name: scan_tracked_repository
        signature: 'scan_tracked_repository(root: Path, *, repository: str, contract_root: Path) -> tuple[int, int]'
        purpose: Implements scan_tracked_repository for Tracked-secret, forbidden-file, clean-tree, generated, artifact, and cache policy.
      - name: verify_clean_tree
        signature: 'verify_clean_tree(root: Path) -> None'
        purpose: Implements verify_clean_tree for Tracked-secret, forbidden-file, clean-tree, generated, artifact, and cache policy.
      - name: verify_generated_outputs
        signature: 'verify_generated_outputs(root: Path, *, contract_root: Path) -> int'
        purpose: Implements verify_generated_outputs for Tracked-secret, forbidden-file, clean-tree, generated, artifact, and cache policy.
      - name: parse_artifact_declarations
        signature: 'parse_artifact_declarations(raw: str) -> tuple[ArtifactDeclaration, ...]'
        purpose: Implements parse_artifact_declarations for Tracked-secret, forbidden-file, clean-tree, generated, artifact, and cache policy.
      - name: validate_artifacts
        signature: 'validate_artifacts(artifacts: Sequence[ArtifactDeclaration], *, exception_id: str | None, trust_mode: str | None, contract_root: Path) -> str | None'
        purpose: Implements validate_artifacts for Tracked-secret, forbidden-file, clean-tree, generated, artifact, and cache policy.
      - name: validate_cache_request
        signature: 'validate_cache_request(*, mode: str, repository: str, source_sha: str | None, lock_digest: str | None, platform: str, profile: str, trust_mode: str | None, contract_root:
          Path) -> CacheDecision'
        purpose: Implements validate_cache_request for Tracked-secret, forbidden-file, clean-tree, generated, artifact, and cache policy.
      - name: verify_repository_policy
        signature: 'verify_repository_policy(root: Path, *, repository: str, phase: str, artifact_manifest_json: str, artifact_exception_id: str | None, trust_mode: str | None, contract_root:
          Path) -> PolicyReport'
        purpose: Run one deterministic repository and artifact policy gate.
      internal_dependencies:
      - foundation_types
    - path: src/ci_workflows/readability.py
      responsibility: Checked-in readability policy, deterministic docs, and static source-shape checks.
      status: consolidate
      classes:
      - name: ReadabilityError
        purpose: One stable checked-in readability contract failure.
        methods: []
      - name: ReadabilityException
        purpose: Defines ReadabilityException for Checked-in readability policy, deterministic docs, and static source-shape checks.
        methods: []
      - name: ReadabilityPolicy
        purpose: Defines ReadabilityPolicy for Checked-in readability policy, deterministic docs, and static source-shape checks.
        methods:
        - name: excepts
          signature: 'excepts(self, path: str, rule: str) -> bool'
          purpose: Implements ReadabilityPolicy.excepts behavior for Checked-in readability policy, deterministic docs, and static source-shape checks.
      - name: ReadabilityFinding
        purpose: Defines ReadabilityFinding for Checked-in readability policy, deterministic docs, and static source-shape checks.
        methods: []
      functions:
      - name: _require
        signature: '_require(condition: bool, code: str, path: str, message: str) -> None'
        purpose: Implements _require for Checked-in readability policy, deterministic docs, and static source-shape checks.
      - name: _read_json
        signature: '_read_json(root: Path, relative: Path) -> Mapping[str, Any]'
        purpose: Implements _read_json for Checked-in readability policy, deterministic docs, and static source-shape checks.
      - name: _safe_relative
        signature: '_safe_relative(value: Any, *, code: str) -> str'
        purpose: Implements _safe_relative for Checked-in readability policy, deterministic docs, and static source-shape checks.
      - name: _string_list
        signature: '_string_list(value: Any, *, code: str, allow_empty: bool=False) -> list[str]'
        purpose: Implements _string_list for Checked-in readability policy, deterministic docs, and static source-shape checks.
      - name: load_readability_policy
        signature: 'load_readability_policy(root: Path) -> ReadabilityPolicy'
        purpose: Implements load_readability_policy for Checked-in readability policy, deterministic docs, and static source-shape checks.
      - name: _input_names
        signature: '_input_names(document: ParsedDocument) -> set[str]'
        purpose: Implements _input_names for Checked-in readability policy, deterministic docs, and static source-shape checks.
      - name: _add
        signature: '_add(findings: list[ReadabilityFinding], policy: ReadabilityPolicy, rule: str, path: str, message: str, *, line: int=0) -> None'
        purpose: Implements _add for Checked-in readability policy, deterministic docs, and static source-shape checks.
      - name: validate_repository_readability
        signature: 'validate_repository_readability(root: Path, workflow_documents: Mapping[str, ParsedDocument], action_documents: Mapping[str, ParsedDocument]) -> tuple[ReadabilityFinding,
          ...]'
        purpose: 'Run the #31 contract and source-shape checks not duplicated elsewhere.'
      - name: render_readability_docs
        signature: 'render_readability_docs(*, contract_root: Path) -> str'
        purpose: Implements render_readability_docs for Checked-in readability policy, deterministic docs, and static source-shape checks.
      - name: write_readability_docs
        signature: 'write_readability_docs(*, contract_root: Path, check: bool=False) -> Path'
        purpose: Implements write_readability_docs for Checked-in readability policy, deterministic docs, and static source-shape checks.
      internal_dependencies:
      - ciw_docs
      - validation_graph
      - validation_helpers
      - validation_model
  python:
    target_module: src/ci_workflows/python.py
    responsibility: Python validation planning and execution across hosted and organization backends.
    files:
    - path: src/ci_workflows/ciw_python.py
      responsibility: Bounded ``ciw python validate`` adapter.
      status: compatibility
      classes: []
      functions:
      - name: configure_python_validate
        signature: 'configure_python_validate(parser: argparse.ArgumentParser) -> None'
        purpose: Implements configure_python_validate for Bounded ``ciw python validate`` adapter.
      - name: _state_root
        signature: '_state_root(context: CIWContext) -> Path'
        purpose: Implements _state_root for Bounded ``ciw python validate`` adapter.
      - name: _failure_outputs
        signature: '_failure_outputs(context: CIWContext, code: str) -> None'
        purpose: Implements _failure_outputs for Bounded ``ciw python validate`` adapter.
      - name: execute_python_validate
        signature: 'execute_python_validate(args: argparse.Namespace, context: CIWContext) -> CIWResult'
        purpose: Implements execute_python_validate for Bounded ``ciw python validate`` adapter.
      internal_dependencies:
      - ciw_types
      - execution_backends
      - python
      - runners
      - workspace
    - path: src/ci_workflows/python.py
      responsibility: Public facade and bounded orchestration for reusable Python validation.
      status: keep
      classes: []
      functions:
      - name: _verify_policy
        signature: '_verify_policy(source_root: Path, request: PythonValidationRequest, contract_root: Path, phase: str) -> None'
        purpose: Implements _verify_policy for Public facade and bounded orchestration for reusable Python validation.
      - name: validate
        signature: 'validate(*, contract_root: Path, source_root: Path | None, state_root: Path | None, request: PythonValidationRequest, phase: str, environment: Mapping[str, str])
          -> PythonValidationPlan | PythonValidationResult'
        purpose: Implements validate for Public facade and bounded orchestration for reusable Python validation.
      internal_dependencies:
      - foundation_types
      - policy
      - python_contract
      - python_docker_execution
      - python_execution
      - python_host_execution
      - python_types
    - path: src/ci_workflows/python_contract.py
      responsibility: Checked-in product-neutral contract for reusable Python validation.
      status: consolidate
      classes: []
      functions:
      - name: require
        signature: 'require(condition: bool, code: str) -> None'
        purpose: Implements require for Checked-in product-neutral contract for reusable Python validation.
      - name: _read_json
        signature: '_read_json(path: Path) -> Mapping[str, Any]'
        purpose: Implements _read_json for Checked-in product-neutral contract for reusable Python validation.
      - name: _strings
        signature: '_strings(value: Any, *, empty: bool=True) -> tuple[str, ...]'
        purpose: Implements _strings for Checked-in product-neutral contract for reusable Python validation.
      - name: safe_relative
        signature: 'safe_relative(value: str, code: str=''invalid_input'') -> str'
        purpose: Implements safe_relative for Checked-in product-neutral contract for reusable Python validation.
      - name: _optional_relative
        signature: '_optional_relative(value: Any) -> str | None'
        purpose: Implements _optional_relative for Checked-in product-neutral contract for reusable Python validation.
      - name: runtime_reference
        signature: 'runtime_reference(runtime: Mapping[str, Any]) -> str | None'
        purpose: Implements runtime_reference for Checked-in product-neutral contract for reusable Python validation.
      - name: _validate_profiles
        signature: '_validate_profiles(payload: Mapping[str, Any]) -> None'
        purpose: Implements _validate_profiles for Checked-in product-neutral contract for reusable Python validation.
      - name: _validate_runtimes
        signature: '_validate_runtimes(payload: Mapping[str, Any]) -> None'
        purpose: Implements _validate_runtimes for Checked-in product-neutral contract for reusable Python validation.
      - name: load_python_contract
        signature: 'load_python_contract(root: Path) -> Mapping[str, Any]'
        purpose: Implements load_python_contract for Checked-in product-neutral contract for reusable Python validation.
      - name: source_trust_from_environment
        signature: 'source_trust_from_environment(environment: Mapping[str, str]) -> str'
        purpose: Implements source_trust_from_environment for Checked-in product-neutral contract for reusable Python validation.
      - name: request_from_environment
        signature: 'request_from_environment(environment: Mapping[str, str]) -> PythonValidationRequest'
        purpose: Implements request_from_environment for Checked-in product-neutral contract for reusable Python validation.
      - name: _runtime_id
        signature: '_runtime_id(contract: Mapping[str, Any], profile: Mapping[str, Any], python_version: str) -> str'
        purpose: Implements _runtime_id for Checked-in product-neutral contract for reusable Python validation.
      - name: resolve_validation_plan
        signature: 'resolve_validation_plan(contract: Mapping[str, Any], request: PythonValidationRequest) -> PythonValidationPlan'
        purpose: Implements resolve_validation_plan for Checked-in product-neutral contract for reusable Python validation.
      - name: bounded_path
        signature: 'bounded_path(root: Path, relative: str) -> Path'
        purpose: Implements bounded_path for Checked-in product-neutral contract for reusable Python validation.
      - name: validate_script_entrypoint
        signature: 'validate_script_entrypoint(source_root: Path, plan: PythonValidationPlan) -> Path'
        purpose: Implements validate_script_entrypoint for Checked-in product-neutral contract for reusable Python validation.
      - name: resolve_python_version
        signature: 'resolve_python_version(source_root: Path, plan: PythonValidationPlan) -> str'
        purpose: Implements resolve_python_version for Checked-in product-neutral contract for reusable Python validation.
      - name: _lock_material
        signature: '_lock_material(path: Path, source_root: Path, visited: set[Path]) -> str'
        purpose: Implements _lock_material for Checked-in product-neutral contract for reusable Python validation.
      - name: validate_dependency_lock
        signature: 'validate_dependency_lock(source_root: Path, plan: PythonValidationPlan) -> str | None'
        purpose: Implements validate_dependency_lock for Checked-in product-neutral contract for reusable Python validation.
      internal_dependencies:
      - foundation_types
      - python_types
    - path: src/ci_workflows/python_docker_execution.py
      responsibility: Docker-backed execution for reviewed GitHub-hosted Python validation.
      status: consolidate
      classes: []
      functions:
      - name: _report_failure
        signature: '_report_failure(stage: str, code: str) -> None'
        purpose: Implements _report_failure for Docker-backed execution for reviewed GitHub-hosted Python validation.
      - name: _raise_container_execution_failure
        signature: '_raise_container_execution_failure(returncode: int) -> None'
        purpose: Implements _raise_container_execution_failure for Docker-backed execution for reviewed GitHub-hosted Python validation.
      - name: _docker_command
        signature: _docker_command() -> list[str]
        purpose: Implements _docker_command for Docker-backed execution for reviewed GitHub-hosted Python validation.
      - name: _resource_token
        signature: '_resource_token(plan: PythonValidationPlan, environment: Mapping[str, str]) -> str'
        purpose: Implements _resource_token for Docker-backed execution for reviewed GitHub-hosted Python validation.
      - name: _known_absence
        signature: '_known_absence(completed: subprocess.CompletedProcess[str]) -> bool'
        purpose: Implements _known_absence for Docker-backed execution for reviewed GitHub-hosted Python validation.
      - name: _verify_absent
        signature: '_verify_absent(command: Sequence[str], arguments: Sequence[str], *, cwd: Path, environment: Mapping[str, str]) -> bool'
        purpose: Implements _verify_absent for Docker-backed execution for reviewed GitHub-hosted Python validation.
      - name: _cleanup_docker
        signature: '_cleanup_docker(command: Sequence[str], *, containers: Sequence[str], network: str | None, volume: str | None, images: Sequence[str], cwd: Path, environment: Mapping[str,
          str]) -> None'
        purpose: Implements _cleanup_docker for Docker-backed execution for reviewed GitHub-hosted Python validation.
      - name: _start_postgres
        signature: '_start_postgres(docker: Sequence[str], *, source_root: Path, state_root: Path, plan: PythonValidationPlan, contract: Mapping[str, Any], environment: Mapping[str,
          str], container: str, network: str, volume: str) -> str'
        purpose: Implements _start_postgres for Docker-backed execution for reviewed GitHub-hosted Python validation.
      - name: _emit_validation_output
        signature: '_emit_validation_output(completed: subprocess.CompletedProcess[str]) -> None'
        purpose: Implements _emit_validation_output for Docker-backed execution for reviewed GitHub-hosted Python validation.
      - name: execute_docker_plan
        signature: 'execute_docker_plan(source_root: Path, state_root: Path, plan: PythonValidationPlan, contract: Mapping[str, Any], environment: Mapping[str, str]) -> int'
        purpose: Execute the existing container validation plan on fixed hosted Docker.
      internal_dependencies:
      - foundation_types
      - python_contract
      - python_execution
      - python_types
    - path: src/ci_workflows/python_execution.py
      responsibility: Isolated Podman execution and shared helpers for product-neutral Python validation.
      status: keep
      classes: []
      functions:
      - name: _report_failure
        signature: '_report_failure(stage: str, code: str) -> None'
        purpose: Implements _report_failure for Isolated Podman execution and shared helpers for product-neutral Python validation.
      - name: _classify_script_failure
        signature: '_classify_script_failure(output: str) -> str'
        purpose: Implements _classify_script_failure for Isolated Podman execution and shared helpers for product-neutral Python validation.
      - name: run_command
        signature: 'run_command(argv: Sequence[str], *, cwd: Path, environment: Mapping[str, str], timeout_seconds: int, code: str=''command_failed'', allow_failure: bool=False, failure_stage:
          str | None=None) -> subprocess.CompletedProcess[str]'
        purpose: Implements run_command for Isolated Podman execution and shared helpers for product-neutral Python validation.
      - name: git_output
        signature: 'git_output(root: Path, arguments: Sequence[str], code: str) -> str'
        purpose: Implements git_output for Isolated Podman execution and shared helpers for product-neutral Python validation.
      - name: verify_exact_source
        signature: 'verify_exact_source(source_root: Path, admitted_sha: str) -> None'
        purpose: Implements verify_exact_source for Isolated Podman execution and shared helpers for product-neutral Python validation.
      - name: copy_source
        signature: 'copy_source(source_root: Path, destination: Path) -> None'
        purpose: Implements copy_source for Isolated Podman execution and shared helpers for product-neutral Python validation.
      - name: command_environment
        signature: 'command_environment(plan: PythonValidationPlan, base: Mapping[str, str]) -> dict[str, str]'
        purpose: Implements command_environment for Isolated Podman execution and shared helpers for product-neutral Python validation.
      - name: podman_command
        signature: 'podman_command(state_root: Path) -> list[str]'
        purpose: Implements podman_command for Isolated Podman execution and shared helpers for product-neutral Python validation.
      - name: container_script
        signature: 'container_script(plan: PythonValidationPlan) -> str'
        purpose: Implements container_script for Isolated Podman execution and shared helpers for product-neutral Python validation.
      - name: write_environment_file
        signature: 'write_environment_file(path: Path, values: Mapping[str, str]) -> None'
        purpose: Implements write_environment_file for Isolated Podman execution and shared helpers for product-neutral Python validation.
      - name: cleanup_podman
        signature: 'cleanup_podman(command: Sequence[str], *, containers: Sequence[str], network: str | None, volume: str | None, images: Sequence[str], cwd: Path, environment: Mapping[str,
          str]) -> None'
        purpose: Implements cleanup_podman for Isolated Podman execution and shared helpers for product-neutral Python validation.
      - name: _resource_token
        signature: '_resource_token(plan: PythonValidationPlan, environment: Mapping[str, str]) -> str'
        purpose: Implements _resource_token for Isolated Podman execution and shared helpers for product-neutral Python validation.
      - name: _start_postgres
        signature: '_start_postgres(podman: Sequence[str], *, source_root: Path, state_root: Path, plan: PythonValidationPlan, contract: Mapping[str, Any], environment: Mapping[str,
          str], container: str, network: str, volume: str) -> str'
        purpose: Implements _start_postgres for Isolated Podman execution and shared helpers for product-neutral Python validation.
      - name: _raise_podman_execution_failure
        signature: '_raise_podman_execution_failure(returncode: int, output: str='''') -> None'
        purpose: Implements _raise_podman_execution_failure for Isolated Podman execution and shared helpers for product-neutral Python validation.
      - name: execute_podman_plan
        signature: 'execute_podman_plan(source_root: Path, state_root: Path, plan: PythonValidationPlan, contract: Mapping[str, Any], environment: Mapping[str, str]) -> int'
        purpose: Implements execute_podman_plan for Isolated Podman execution and shared helpers for product-neutral Python validation.
      - name: result_from_plan
        signature: 'result_from_plan(plan: PythonValidationPlan, stage_count: int) -> PythonValidationResult'
        purpose: Implements result_from_plan for Isolated Podman execution and shared helpers for product-neutral Python validation.
      internal_dependencies:
      - foundation_types
      - python_contract
      - python_types
    - path: src/ci_workflows/python_host_execution.py
      responsibility: General-runner host execution for product-neutral Python validation.
      status: consolidate
      classes:
      - name: _BoundedCommandRunner
        purpose: Defines _BoundedCommandRunner for General-runner host execution for product-neutral Python validation.
        methods:
        - name: run
          signature: 'run(self, argv: Sequence[str], *, cwd: Path, env: Mapping[str, str]) -> ProcessResult'
          purpose: Implements _BoundedCommandRunner.run behavior for General-runner host execution for product-neutral Python validation.
      functions:
      - name: _raise_failure
        signature: '_raise_failure(code: str, stage: str, error: BaseException) -> None'
        purpose: Implements _raise_failure for General-runner host execution for product-neutral Python validation.
      - name: _inspect_python
        signature: '_inspect_python(executable: Path, *, cwd: Path, environment: Mapping[str, str], runner: CommandRunner) -> str'
        purpose: Implements _inspect_python for General-runner host execution for product-neutral Python validation.
      - name: _matches_runtime
        signature: '_matches_runtime(actual: str, expected: str) -> bool'
        purpose: Implements _matches_runtime for General-runner host execution for product-neutral Python validation.
      - name: _create_venv
        signature: '_create_venv(interpreter: Path, root: Path, *, cwd: Path, environment: Mapping[str, str], runner: CommandRunner) -> PythonVenv'
        purpose: Implements _create_venv for General-runner host execution for product-neutral Python validation.
      - name: _install_dependencies
        signature: '_install_dependencies(interpreter: Path, source_root: Path, dependency_file: str, *, environment: Mapping[str, str], runner: CommandRunner) -> None'
        purpose: Implements _install_dependencies for General-runner host execution for product-neutral Python validation.
      - name: execute_host_plan
        signature: 'execute_host_plan(source_root: Path, state_root: Path, plan: PythonValidationPlan, environment: Mapping[str, str]) -> tuple[int, str]'
        purpose: Implements execute_host_plan for General-runner host execution for product-neutral Python validation.
      - name: result_from_host_plan
        signature: 'result_from_host_plan(plan: PythonValidationPlan, stage_count: int, resolved_python_version: str) -> PythonValidationResult'
        purpose: Implements result_from_host_plan for General-runner host execution for product-neutral Python validation.
      internal_dependencies:
      - foundation_types
      - language_primitives
      - python_contract
      - python_execution
      - python_types
      - runtime_primitives
    - path: src/ci_workflows/python_types.py
      responsibility: Typed models and stable errors for reusable Python validation.
      status: consolidate
      classes:
      - name: PythonValidationError
        purpose: Fail-closed Python validation error carrying one stable code.
        methods: []
      - name: PythonValidationRequest
        purpose: Defines PythonValidationRequest for Typed models and stable errors for reusable Python validation.
        methods: []
      - name: PythonValidationPlan
        purpose: Defines PythonValidationPlan for Typed models and stable errors for reusable Python validation.
        methods:
        - name: planning_outputs
          signature: planning_outputs(self) -> dict[str, str]
          purpose: Implements PythonValidationPlan.planning_outputs behavior for Typed models and stable errors for reusable Python validation.
      - name: PythonValidationResult
        purpose: Defines PythonValidationResult for Typed models and stable errors for reusable Python validation.
        methods:
        - name: output_values
          signature: output_values(self) -> dict[str, str]
          purpose: Implements PythonValidationResult.output_values behavior for Typed models and stable errors for reusable Python validation.
      functions: []
      internal_dependencies: []
  release:
    target_module: src/ci_workflows/release.py
    responsibility: Shared Git tag, release, private release-asset, and publication mechanics.
    files:
    - path: src/ci_workflows/private_release_asset.py
      responsibility: Materialize one immutable private GitHub release asset into a trusted source checkout.
      status: consolidate
      classes:
      - name: PrivateReleaseAssetError
        purpose: Stable non-sensitive release-asset failure.
        methods: []
      - name: PrivateReleaseAssetSpec
        purpose: Defines PrivateReleaseAssetSpec for Materialize one immutable private GitHub release asset into a trusted source checkout.
        methods:
        - name: parse
          signature: 'parse(cls, value: Mapping[str, object]) -> ''PrivateReleaseAssetSpec'''
          purpose: Implements PrivateReleaseAssetSpec.parse behavior for Materialize one immutable private GitHub release asset into a trusted source checkout.
        - name: as_payload
          signature: as_payload(self) -> dict[str, str]
          purpose: Implements PrivateReleaseAssetSpec.as_payload behavior for Materialize one immutable private GitHub release asset into a trusted source checkout.
      - name: _NoRedirect
        purpose: Defines _NoRedirect for Materialize one immutable private GitHub release asset into a trusted source checkout.
        methods:
        - name: redirect_request
          signature: 'redirect_request(self, req: Any, fp: Any, code: int, msg: str, headers: Any, newurl: str) -> None'
          purpose: Implements _NoRedirect.redirect_request behavior for Materialize one immutable private GitHub release asset into a trusted source checkout.
      - name: PrivateReleaseAssetResult
        purpose: Defines PrivateReleaseAssetResult for Materialize one immutable private GitHub release asset into a trusted source checkout.
        methods: []
      functions:
      - name: _require
        signature: '_require(condition: bool, code: str) -> None'
        purpose: Implements _require for Materialize one immutable private GitHub release asset into a trusted source checkout.
      - name: _safe_text
        signature: '_safe_text(value: object, *, maximum: int, code: str) -> str'
        purpose: Implements _safe_text for Materialize one immutable private GitHub release asset into a trusted source checkout.
      - name: _safe_tag
        signature: '_safe_tag(value: object) -> str'
        purpose: Implements _safe_tag for Materialize one immutable private GitHub release asset into a trusted source checkout.
      - name: _safe_asset_name
        signature: '_safe_asset_name(value: object) -> str'
        purpose: Implements _safe_asset_name for Materialize one immutable private GitHub release asset into a trusted source checkout.
      - name: _safe_relative
        signature: '_safe_relative(value: object, code: str) -> PurePosixPath'
        purpose: Implements _safe_relative for Materialize one immutable private GitHub release asset into a trusted source checkout.
      - name: optional_spec
        signature: 'optional_spec(value: object) -> PrivateReleaseAssetSpec | None'
        purpose: Implements optional_spec for Materialize one immutable private GitHub release asset into a trusted source checkout.
      - name: _repository_path
        signature: '_repository_path(repository: str) -> str'
        purpose: Implements _repository_path for Materialize one immutable private GitHub release asset into a trusted source checkout.
      - name: _no_redirect_open
        signature: '_no_redirect_open(request: urllib.request.Request, *, timeout: int) -> Any'
        purpose: Implements _no_redirect_open for Materialize one immutable private GitHub release asset into a trusted source checkout.
      - name: _read_json_response
        signature: '_read_json_response(response: Any) -> Mapping[str, Any]'
        purpose: Implements _read_json_response for Materialize one immutable private GitHub release asset into a trusted source checkout.
      - name: _open_request
        signature: '_open_request(opener: Callable[..., Any], request: urllib.request.Request, *, code: str) -> Any'
        purpose: Implements _open_request for Materialize one immutable private GitHub release asset into a trusted source checkout.
      - name: _release_metadata
        signature: '_release_metadata(*, repository: str, tag: str, token: str, api_opener: Callable[..., Any]) -> Mapping[str, Any]'
        purpose: Implements _release_metadata for Materialize one immutable private GitHub release asset into a trusted source checkout.
      - name: _asset_record
        signature: '_asset_record(release: Mapping[str, Any], *, tag: str, asset_name: str) -> Mapping[str, Any]'
        purpose: Implements _asset_record for Materialize one immutable private GitHub release asset into a trusted source checkout.
      - name: _redirect_location
        signature: '_redirect_location(error: urllib.error.HTTPError) -> str | None'
        purpose: Implements _redirect_location for Materialize one immutable private GitHub release asset into a trusted source checkout.
      - name: _asset_response
        signature: '_asset_response(request: urllib.request.Request, *, api_opener: Callable[..., Any], asset_opener: Callable[..., Any]) -> Any'
        purpose: Implements _asset_response for Materialize one immutable private GitHub release asset into a trusted source checkout.
      - name: _download_asset
        signature: '_download_asset(*, repository: str, asset: Mapping[str, Any], token: str, target: Path, expected_sha256: str, api_opener: Callable[..., Any], asset_opener: Callable[...,
          Any]) -> int'
        purpose: Implements _download_asset for Materialize one immutable private GitHub release asset into a trusted source checkout.
      - name: _normalized_link_target
        signature: '_normalized_link_target(entry: PurePosixPath, target: str) -> PurePosixPath'
        purpose: Implements _normalized_link_target for Materialize one immutable private GitHub release asset into a trusted source checkout.
      - name: _inspect_zip
        signature: '_inspect_zip(path: Path) -> None'
        purpose: Implements _inspect_zip for Materialize one immutable private GitHub release asset into a trusted source checkout.
      - name: _ditto_extract
        signature: '_ditto_extract(archive: Path, destination: Path) -> None'
        purpose: Implements _ditto_extract for Materialize one immutable private GitHub release asset into a trusted source checkout.
      - name: _require_ignored
        signature: '_require_ignored(source_root: Path, destination: str) -> None'
        purpose: Implements _require_ignored for Materialize one immutable private GitHub release asset into a trusted source checkout.
      - name: materialize_private_release_asset
        signature: 'materialize_private_release_asset(*, spec: PrivateReleaseAssetSpec, token: str, source_root: Path, state_root: Path, api_opener: Callable[..., Any]=_no_redirect_open,
          asset_opener: Callable[..., Any]=urllib.request.urlopen, provider_factory: Callable[[str], GitHubSourceProvider]=GitHubSourceProvider, extractor: Callable[[Path, Path], None]=_ditto_extract)
          -> PrivateReleaseAssetResult'
        purpose: Implements materialize_private_release_asset for Materialize one immutable private GitHub release asset into a trusted source checkout.
      - name: cleanup_private_release_asset
        signature: 'cleanup_private_release_asset(result: PrivateReleaseAssetResult | None) -> None'
        purpose: Implements cleanup_private_release_asset for Materialize one immutable private GitHub release asset into a trusted source checkout.
      - name: _environment_spec
        signature: '_environment_spec(environment: Mapping[str, str]) -> PrivateReleaseAssetSpec | None'
        purpose: Implements _environment_spec for Materialize one immutable private GitHub release asset into a trusted source checkout.
      - name: _required_path
        signature: '_required_path(environment: Mapping[str, str], name: str) -> Path'
        purpose: Implements _required_path for Materialize one immutable private GitHub release asset into a trusted source checkout.
      - name: _state_file
        signature: '_state_file(state_root: Path, spec: PrivateReleaseAssetSpec, *, create: bool) -> Path'
        purpose: Implements _state_file for Materialize one immutable private GitHub release asset into a trusted source checkout.
      - name: _write_state
        signature: '_write_state(state_root: Path, spec: PrivateReleaseAssetSpec, result: PrivateReleaseAssetResult) -> None'
        purpose: Implements _write_state for Materialize one immutable private GitHub release asset into a trusted source checkout.
      - name: _load_state
        signature: '_load_state(state_root: Path, spec: PrivateReleaseAssetSpec, source_root: Path) -> PrivateReleaseAssetResult'
        purpose: Implements _load_state for Materialize one immutable private GitHub release asset into a trusted source checkout.
      - name: _write_outputs
        signature: '_write_outputs(environment: Mapping[str, str], values: Mapping[str, str]) -> None'
        purpose: Implements _write_outputs for Materialize one immutable private GitHub release asset into a trusted source checkout.
      - name: run_phase
        signature: 'run_phase(phase: str, environment: Mapping[str, str]=os.environ) -> None'
        purpose: Implements run_phase for Materialize one immutable private GitHub release asset into a trusted source checkout.
      - name: parser
        signature: parser() -> argparse.ArgumentParser
        purpose: Implements parser for Materialize one immutable private GitHub release asset into a trusted source checkout.
      - name: main
        signature: 'main(argv: Sequence[str] | None=None, environment: Mapping[str, str]=os.environ) -> int'
        purpose: Implements main for Materialize one immutable private GitHub release asset into a trusted source checkout.
      internal_dependencies:
      - source_admission
      - source_github
    - path: src/ci_workflows/private_release_asset_action.py
      responsibility: Action adapter that resolves one bounded private release asset from checked-out product config.
      status: compatibility
      classes: []
      functions:
      - name: _require
        signature: '_require(condition: bool, code: str) -> None'
        purpose: Implements _require for Action adapter that resolves one bounded private release asset from checked-out product config.
      - name: _source_root
        signature: '_source_root(environment: Mapping[str, str]) -> Path'
        purpose: Implements _source_root for Action adapter that resolves one bounded private release asset from checked-out product config.
      - name: _read_config
        signature: '_read_config(source_root: Path) -> Mapping[str, object]'
        purpose: Implements _read_config for Action adapter that resolves one bounded private release asset from checked-out product config.
      - name: _config_spec
        signature: '_config_spec(source_root: Path, workflow_key: str) -> PrivateReleaseAssetSpec | None'
        purpose: Implements _config_spec for Action adapter that resolves one bounded private release asset from checked-out product config.
      - name: hydrate_environment
        signature: 'hydrate_environment(environment: Mapping[str, str]) -> dict[str, str]'
        purpose: Implements hydrate_environment for Action adapter that resolves one bounded private release asset from checked-out product config.
      - name: main
        signature: 'main(argv: Sequence[str] | None=None, environment: Mapping[str, str]=os.environ) -> int'
        purpose: Implements main for Action adapter that resolves one bounded private release asset from checked-out product config.
      internal_dependencies:
      - private_release_asset
    - path: src/ci_workflows/release_primitives.py
      responsibility: Product-neutral Git tag and GitHub Release primitives.
      status: consolidate
      classes:
      - name: ReleasePrimitiveError
        purpose: Fail closed with one stable, non-secret release primitive code.
        methods: []
      - name: VersionTag
        purpose: Validated Git tag and normalized SemVer.
        methods: []
      - name: GitCommitMetadata
        purpose: Non-secret commit metadata resolved from one release tag.
        methods: []
      - name: GitTagMetadata
        purpose: Tag object metadata plus the commit reached by the tag.
        methods: []
      - name: GitHubReleaseRequest
        purpose: Caller-owned metadata for one ordinary GitHub Release.
        methods: []
      - name: GitHubReleaseResult
        purpose: Structured result from creating or updating a GitHub Release.
        methods: []
      - name: GitHubAssetResult
        purpose: Structured optional release-asset result.
        methods: []
      - name: GitProcessResult
        purpose: Minimal mockable Git command boundary.
        methods: []
      - name: GitRunner
        purpose: Defines GitRunner for Product-neutral Git tag and GitHub Release primitives.
        methods:
        - name: __call__
          signature: '__call__(self, arguments: Sequence[str], cwd: Path) -> GitProcessResult'
          purpose: Implements GitRunner.__call__ behavior for Product-neutral Git tag and GitHub Release primitives.
      - name: AssetUploader
        purpose: Defines AssetUploader for Product-neutral Git tag and GitHub Release primitives.
        methods:
        - name: __call__
          signature: '__call__(self, url: str, headers: Mapping[str, str], path: Path, content_type: str) -> Mapping[str, Any]'
          purpose: Implements AssetUploader.__call__ behavior for Product-neutral Git tag and GitHub Release primitives.
      functions:
      - name: _require
        signature: '_require(condition: bool, code: str) -> None'
        purpose: Implements _require for Product-neutral Git tag and GitHub Release primitives.
      - name: _single_line
        signature: '_single_line(value: Any, *, code: str, maximum: int) -> str'
        purpose: Implements _single_line for Product-neutral Git tag and GitHub Release primitives.
      - name: _optional_single_line
        signature: '_optional_single_line(value: Any, *, code: str, maximum: int) -> str | None'
        purpose: Implements _optional_single_line for Product-neutral Git tag and GitHub Release primitives.
      - name: _repository
        signature: '_repository(value: Any) -> str'
        purpose: Implements _repository for Product-neutral Git tag and GitHub Release primitives.
      - name: _sha
        signature: '_sha(value: Any, *, code: str) -> str'
        purpose: Implements _sha for Product-neutral Git tag and GitHub Release primitives.
      - name: derive_version_from_ref
        signature: 'derive_version_from_ref(ref_or_tag: str) -> VersionTag'
        purpose: Validate a Git tag/ref and derive normalized SemVer without a leading ``v``.
      - name: _default_git_runner
        signature: '_default_git_runner(arguments: Sequence[str], cwd: Path) -> GitProcessResult'
        purpose: Implements _default_git_runner for Product-neutral Git tag and GitHub Release primitives.
      - name: _git
        signature: '_git(runner: GitRunner, arguments: Sequence[str], cwd: Path, *, code: str) -> str'
        purpose: Implements _git for Product-neutral Git tag and GitHub Release primitives.
      - name: _timestamp
        signature: '_timestamp(value: str, *, code: str) -> int'
        purpose: Implements _timestamp for Product-neutral Git tag and GitHub Release primitives.
      - name: _email
        signature: '_email(value: str) -> str'
        purpose: Implements _email for Product-neutral Git tag and GitHub Release primitives.
      - name: inspect_git_tag
        signature: 'inspect_git_tag(repository_root: Path, ref_or_tag: str, *, runner: GitRunner=_default_git_runner) -> GitTagMetadata'
        purpose: Inspect a lightweight or annotated SemVer tag and its fully peeled commit.
      - name: _github_token
        signature: '_github_token(environment: Mapping[str, str], token_environment: str) -> str'
        purpose: Implements _github_token for Product-neutral Git tag and GitHub Release primitives.
      - name: _github_api_url
        signature: '_github_api_url(environment: Mapping[str, str], api_url: str | None) -> str'
        purpose: Implements _github_api_url for Product-neutral Git tag and GitHub Release primitives.
      - name: _github_headers
        signature: '_github_headers(token: str) -> dict[str, str]'
        purpose: Implements _github_headers for Product-neutral Git tag and GitHub Release primitives.
      - name: _read_json_body
        signature: '_read_json_body(response: Any) -> Mapping[str, Any]'
        purpose: Implements _read_json_body for Product-neutral Git tag and GitHub Release primitives.
      - name: _github_json_request
        signature: '_github_json_request(method: str, url: str, *, headers: Mapping[str, str], payload: Mapping[str, Any] | None, opener: Callable[..., Any], missing_ok: bool=False)
          -> Mapping[str, Any] | None'
        purpose: Implements _github_json_request for Product-neutral Git tag and GitHub Release primitives.
      - name: _release_request
        signature: '_release_request(request: GitHubReleaseRequest) -> GitHubReleaseRequest'
        purpose: Implements _release_request for Product-neutral Git tag and GitHub Release primitives.
      - name: _release_result
        signature: '_release_result(payload: Mapping[str, Any], *, request: GitHubReleaseRequest, action: str) -> GitHubReleaseResult'
        purpose: Implements _release_result for Product-neutral Git tag and GitHub Release primitives.
      - name: create_or_update_github_release
        signature: 'create_or_update_github_release(request: GitHubReleaseRequest, *, environment: Mapping[str, str]=os.environ, token_environment: str=''GITHUB_TOKEN'', api_url: str
          | None=None, opener: Callable[..., Any]=urllib.request.urlopen) -> GitHubReleaseResult'
        purpose: Create or update one GitHub Release using only a named token environment variable.
      - name: _asset_path
        signature: '_asset_path(asset_path: Path | None) -> Path | None'
        purpose: Implements _asset_path for Product-neutral Git tag and GitHub Release primitives.
      - name: _default_asset_uploader
        signature: '_default_asset_uploader(url: str, headers: Mapping[str, str], path: Path, content_type: str) -> Mapping[str, Any]'
        purpose: Implements _default_asset_uploader for Product-neutral Git tag and GitHub Release primitives.
      - name: upload_github_release_asset
        signature: 'upload_github_release_asset(release: GitHubReleaseResult, asset_path: Path | None, *, environment: Mapping[str, str]=os.environ, token_environment: str=''GITHUB_TOKEN'',
          asset_name: str | None=None, content_type: str=''application/octet-stream'', uploader: AssetUploader=_default_asset_uploader) -> GitHubAssetResult'
        purpose: Optionally upload one caller-selected release asset with no temporary auth state.
      internal_dependencies: []
    - path: src/ci_workflows/release_tag_authority.py
      responsibility: Resolve and revalidate exact release-tag authority for trusted publication.
      status: keep
      classes:
      - name: ReleaseTagError
        purpose: Fail-closed, response-free release tag error.
        methods: []
      - name: ReleaseInputs
        purpose: Defines ReleaseInputs for Resolve and revalidate exact release-tag authority for trusted publication.
        methods: []
      - name: ReleaseEvent
        purpose: Defines ReleaseEvent for Resolve and revalidate exact release-tag authority for trusted publication.
        methods: []
      - name: TagResolution
        purpose: Defines TagResolution for Resolve and revalidate exact release-tag authority for trusted publication.
        methods: []
      - name: ReleaseAuthority
        purpose: Defines ReleaseAuthority for Resolve and revalidate exact release-tag authority for trusted publication.
        methods: []
      - name: TagProvider
        purpose: Defines TagProvider for Resolve and revalidate exact release-tag authority for trusted publication.
        methods:
        - name: repository_metadata
          signature: 'repository_metadata(self, repository: str) -> Mapping[str, Any]'
          purpose: Implements TagProvider.repository_metadata behavior for Resolve and revalidate exact release-tag authority for trusted publication.
        - name: collaborator_permission
          signature: 'collaborator_permission(self, repository: str, actor: str) -> Mapping[str, Any]'
          purpose: Implements TagProvider.collaborator_permission behavior for Resolve and revalidate exact release-tag authority for trusted publication.
        - name: branch_sha
          signature: 'branch_sha(self, repository: str, branch: str) -> str'
          purpose: Implements TagProvider.branch_sha behavior for Resolve and revalidate exact release-tag authority for trusted publication.
        - name: tag_ref
          signature: 'tag_ref(self, repository: str, tag_name: str) -> Mapping[str, Any]'
          purpose: Implements TagProvider.tag_ref behavior for Resolve and revalidate exact release-tag authority for trusted publication.
        - name: tag_object
          signature: 'tag_object(self, repository: str, sha: str) -> Mapping[str, Any]'
          purpose: Implements TagProvider.tag_object behavior for Resolve and revalidate exact release-tag authority for trusted publication.
        - name: commit
          signature: 'commit(self, repository: str, sha: str) -> Mapping[str, Any]'
          purpose: Implements TagProvider.commit behavior for Resolve and revalidate exact release-tag authority for trusted publication.
      - name: GitHubTagProvider
        purpose: Minimal read-only GitHub REST client with sanitized failures.
        methods:
        - name: _get
          signature: '_get(self, repository: str, path: str, *, missing_code: str) -> Mapping[str, Any]'
          purpose: Implements GitHubTagProvider._get behavior for Resolve and revalidate exact release-tag authority for trusted publication.
        - name: repository_metadata
          signature: 'repository_metadata(self, repository: str) -> Mapping[str, Any]'
          purpose: Implements GitHubTagProvider.repository_metadata behavior for Resolve and revalidate exact release-tag authority for trusted publication.
        - name: collaborator_permission
          signature: 'collaborator_permission(self, repository: str, actor: str) -> Mapping[str, Any]'
          purpose: Implements GitHubTagProvider.collaborator_permission behavior for Resolve and revalidate exact release-tag authority for trusted publication.
        - name: branch_sha
          signature: 'branch_sha(self, repository: str, branch: str) -> str'
          purpose: Implements GitHubTagProvider.branch_sha behavior for Resolve and revalidate exact release-tag authority for trusted publication.
        - name: tag_ref
          signature: 'tag_ref(self, repository: str, tag_name: str) -> Mapping[str, Any]'
          purpose: Implements GitHubTagProvider.tag_ref behavior for Resolve and revalidate exact release-tag authority for trusted publication.
        - name: tag_object
          signature: 'tag_object(self, repository: str, sha: str) -> Mapping[str, Any]'
          purpose: Implements GitHubTagProvider.tag_object behavior for Resolve and revalidate exact release-tag authority for trusted publication.
        - name: commit
          signature: 'commit(self, repository: str, sha: str) -> Mapping[str, Any]'
          purpose: Implements GitHubTagProvider.commit behavior for Resolve and revalidate exact release-tag authority for trusted publication.
      functions:
      - name: _require
        signature: '_require(condition: bool, code: str) -> None'
        purpose: Implements _require for Resolve and revalidate exact release-tag authority for trusted publication.
      - name: _mapping
        signature: '_mapping(value: Any, code: str) -> Mapping[str, Any]'
        purpose: Implements _mapping for Resolve and revalidate exact release-tag authority for trusted publication.
      - name: _exact_optional
        signature: '_exact_optional(value: str | None, code: str) -> str | None'
        purpose: Implements _exact_optional for Resolve and revalidate exact release-tag authority for trusted publication.
      - name: _full_sha
        signature: '_full_sha(value: Any, code: str) -> str'
        purpose: Implements _full_sha for Resolve and revalidate exact release-tag authority for trusted publication.
      - name: _semver
        signature: '_semver(value: Any, code: str=''invalid_release_version'') -> str'
        purpose: Implements _semver for Resolve and revalidate exact release-tag authority for trusted publication.
      - name: _repository
        signature: '_repository(value: Any, code: str=''invalid_repository'') -> str'
        purpose: Implements _repository for Resolve and revalidate exact release-tag authority for trusted publication.
      - name: _object
        signature: '_object(payload: Mapping[str, Any], code: str) -> tuple[str, str]'
        purpose: Implements _object for Resolve and revalidate exact release-tag authority for trusted publication.
      - name: resolve_tag
        signature: 'resolve_tag(provider: TagProvider, repository: str, tag_name: str) -> TagResolution'
        purpose: Resolve one lightweight or bounded annotated tag to an exact commit.
      - name: _validate_event_repository
        signature: '_validate_event_repository(event: ReleaseEvent) -> None'
        purpose: Implements _validate_event_repository for Resolve and revalidate exact release-tag authority for trusted publication.
      - name: _validate_existing_tag_caller
        signature: '_validate_existing_tag_caller(event: ReleaseEvent, provider: TagProvider, *, require_current_branch: bool=True) -> None'
        purpose: Implements _validate_existing_tag_caller for Resolve and revalidate exact release-tag authority for trusted publication.
      - name: resolve_release_authority
        signature: 'resolve_release_authority(inputs: ReleaseInputs, event: ReleaseEvent, provider: TagProvider) -> ReleaseAuthority'
        purpose: Validate mode/event authority and resolve the exact immutable tag tuple.
      - name: revalidate_release_authority
        signature: 'revalidate_release_authority(authority: ReleaseAuthority, event: ReleaseEvent, provider: TagProvider) -> ReleaseAuthority'
        purpose: Revalidate the exact tag object and commit immediately before publication.
      - name: event_from_environment
        signature: 'event_from_environment(environment: Mapping[str, str]) -> ReleaseEvent'
        purpose: Build the exact caller event context without trusting caller inputs.
      - name: authority_from_expected
        signature: 'authority_from_expected(*, release_mode: str, release_version: str, release_source_sha: str, tag_object_sha: str, tag_commit_sha: str) -> ReleaseAuthority'
        purpose: Implements authority_from_expected for Resolve and revalidate exact release-tag authority for trusted publication.
      - name: write_outputs
        signature: 'write_outputs(path: Path, authority: ReleaseAuthority) -> None'
        purpose: Implements write_outputs for Resolve and revalidate exact release-tag authority for trusted publication.
      internal_dependencies: []
  service:
    target_module: src/ci_workflows/service.py
    responsibility: Service/Compose/PostgreSQL validation primitives and Service runner smoke lifecycle.
    files:
    - path: src/ci_workflows/service_compose_primitives.py
      responsibility: Product-neutral ephemeral Compose service-stack primitives for shared CI.
      status: consolidate
      classes:
      - name: ServiceComposeError
        purpose: Fail closed with stable non-secret primary and cleanup codes.
        methods: []
      - name: ComposeProject
        purpose: Defines ComposeProject for Product-neutral ephemeral Compose service-stack primitives for shared CI.
        methods:
        - name: public_projection
          signature: public_projection(self) -> dict[str, object]
          purpose: Implements ComposeProject.public_projection behavior for Product-neutral ephemeral Compose service-stack primitives for shared CI.
      - name: ComposeCommandResult
        purpose: Defines ComposeCommandResult for Product-neutral ephemeral Compose service-stack primitives for shared CI.
        methods: []
      - name: ComposeServiceState
        purpose: Defines ComposeServiceState for Product-neutral ephemeral Compose service-stack primitives for shared CI.
        methods: []
      - name: ComposePSResult
        purpose: Defines ComposePSResult for Product-neutral ephemeral Compose service-stack primitives for shared CI.
        methods: []
      - name: ComposeLogCapture
        purpose: Defines ComposeLogCapture for Product-neutral ephemeral Compose service-stack primitives for shared CI.
        methods: []
      - name: ComposeReadinessCheck
        purpose: Defines ComposeReadinessCheck for Product-neutral ephemeral Compose service-stack primitives for shared CI.
        methods: []
      - name: ComposeReadinessStatus
        purpose: Defines ComposeReadinessStatus for Product-neutral ephemeral Compose service-stack primitives for shared CI.
        methods: []
      - name: ComposeStackResult
        purpose: Defines ComposeStackResult for Product-neutral ephemeral Compose service-stack primitives for shared CI.
        methods: []
      functions:
      - name: _require
        signature: '_require(condition: bool, code: str) -> None'
        purpose: Implements _require for Product-neutral ephemeral Compose service-stack primitives for shared CI.
      - name: _plain
        signature: '_plain(value: object, *, code: str, maximum: int=4096) -> str'
        purpose: Implements _plain for Product-neutral ephemeral Compose service-stack primitives for shared CI.
      - name: _service
        signature: '_service(value: object) -> str'
        purpose: Implements _service for Product-neutral ephemeral Compose service-stack primitives for shared CI.
      - name: _services
        signature: '_services(values: Sequence[str]) -> tuple[str, ...]'
        purpose: Implements _services for Product-neutral ephemeral Compose service-stack primitives for shared CI.
      - name: _duration
        signature: '_duration(value: float | int, code: str) -> float'
        purpose: Implements _duration for Product-neutral ephemeral Compose service-stack primitives for shared CI.
      - name: _arguments
        signature: '_arguments(values: Sequence[str], *, code: str) -> tuple[str, ...]'
        purpose: Implements _arguments for Product-neutral ephemeral Compose service-stack primitives for shared CI.
      - name: _options
        signature: '_options(values: Sequence[str]) -> tuple[str, ...]'
        purpose: Implements _options for Product-neutral ephemeral Compose service-stack primitives for shared CI.
      - name: _environment
        signature: '_environment(environment: Mapping[str, str]) -> dict[str, str]'
        purpose: Implements _environment for Product-neutral ephemeral Compose service-stack primitives for shared CI.
      - name: _project_root
        signature: '_project_root(root: Path) -> Path'
        purpose: Implements _project_root for Product-neutral ephemeral Compose service-stack primitives for shared CI.
      - name: _bounded_file
        signature: '_bounded_file(root: Path, value: Path | str, *, code: str, suffixes: tuple[str, ...]=()) -> tuple[Path, str]'
        purpose: Implements _bounded_file for Product-neutral ephemeral Compose service-stack primitives for shared CI.
      - name: validate_compose_project
        signature: 'validate_compose_project(*, project_root: Path, compose_file: Path | str, project_name: str, tool: str, env_files: Sequence[Path | str]=()) -> ComposeProject'
        purpose: Validate one job-local Compose identity and its caller-owned files.
      - name: _validated_project
        signature: '_validated_project(project: ComposeProject) -> ComposeProject'
        purpose: Implements _validated_project for Product-neutral ephemeral Compose service-stack primitives for shared CI.
      - name: _base_argv
        signature: '_base_argv(project: ComposeProject) -> tuple[str, ...]'
        purpose: Implements _base_argv for Product-neutral ephemeral Compose service-stack primitives for shared CI.
      - name: _invoke
        signature: '_invoke(project: ComposeProject, arguments: Sequence[str], *, environment: Mapping[str, str], timeout_seconds: float | int, runner: ProcessRunner) -> ProcessResult'
        purpose: Implements _invoke for Product-neutral ephemeral Compose service-stack primitives for shared CI.
      - name: _command_result
        signature: '_command_result(operation: str, result: ProcessResult, *, service: str='''') -> ComposeCommandResult'
        purpose: Implements _command_result for Product-neutral ephemeral Compose service-stack primitives for shared CI.
      - name: compose_up
        signature: 'compose_up(project: ComposeProject, *, environment: Mapping[str, str], services: Sequence[str]=(), options: Sequence[str]=(), timeout_seconds: float | int=600, runner:
          ProcessRunner=run_process) -> ComposeCommandResult'
        purpose: Implements compose_up for Product-neutral ephemeral Compose service-stack primitives for shared CI.
      - name: compose_down
        signature: 'compose_down(project: ComposeProject, *, environment: Mapping[str, str], options: Sequence[str]=(), timeout_seconds: float | int=300, runner: ProcessRunner=run_process)
          -> ComposeCommandResult'
        purpose: Implements compose_down for Product-neutral ephemeral Compose service-stack primitives for shared CI.
      - name: compose_exec
        signature: 'compose_exec(project: ComposeProject, *, service: str, command: Sequence[str], environment: Mapping[str, str], options: Sequence[str]=(), timeout_seconds: float |
          int=600, runner: ProcessRunner=run_process) -> ComposeCommandResult'
        purpose: Implements compose_exec for Product-neutral ephemeral Compose service-stack primitives for shared CI.
      - name: compose_run
        signature: 'compose_run(project: ComposeProject, *, service: str, command: Sequence[str], environment: Mapping[str, str], options: Sequence[str]=(), timeout_seconds: float |
          int=600, runner: ProcessRunner=run_process) -> ComposeCommandResult'
        purpose: Implements compose_run for Product-neutral ephemeral Compose service-stack primitives for shared CI.
      - name: _json_rows
        signature: '_json_rows(text: str) -> list[Mapping[str, object]]'
        purpose: Implements _json_rows for Product-neutral ephemeral Compose service-stack primitives for shared CI.
      - name: _optional_text
        signature: '_optional_text(value: object, *, maximum: int=255) -> str'
        purpose: Implements _optional_text for Product-neutral ephemeral Compose service-stack primitives for shared CI.
      - name: _exit_code
        signature: '_exit_code(value: object) -> int | None'
        purpose: Implements _exit_code for Product-neutral ephemeral Compose service-stack primitives for shared CI.
      - name: compose_ps
        signature: 'compose_ps(project: ComposeProject, *, environment: Mapping[str, str], services: Sequence[str]=(), timeout_seconds: float | int=60, runner: ProcessRunner=run_process)
          -> ComposePSResult'
        purpose: Implements compose_ps for Product-neutral ephemeral Compose service-stack primitives for shared CI.
      - name: capture_compose_logs
        signature: 'capture_compose_logs(project: ComposeProject, *, environment: Mapping[str, str], service: str='''', tail_lines: int=200, max_bytes: int=64 * 1024, timeout_seconds:
          float | int=60, runner: ProcessRunner=run_process) -> ComposeLogCapture'
        purpose: Implements capture_compose_logs for Product-neutral ephemeral Compose service-stack primitives for shared CI.
      - name: _readiness_environment
        signature: '_readiness_environment(base: Mapping[str, str], overlay: Mapping[str, str]) -> dict[str, str]'
        purpose: Implements _readiness_environment for Product-neutral ephemeral Compose service-stack primitives for shared CI.
      - name: wait_for_compose_services
        signature: 'wait_for_compose_services(project: ComposeProject, checks: Sequence[ComposeReadinessCheck], *, environment: Mapping[str, str]) -> tuple[ComposeReadinessStatus, ...]'
        purpose: Implements wait_for_compose_services for Product-neutral ephemeral Compose service-stack primitives for shared CI.
      - name: start_compose_stack
        signature: 'start_compose_stack(project: ComposeProject, *, environment: Mapping[str, str], readiness: Sequence[ComposeReadinessCheck], services: Sequence[str]=(), up_options:
          Sequence[str]=(), down_options: Sequence[str]=(), up_timeout_seconds: float | int=600, down_timeout_seconds: float | int=300, runner: ProcessRunner=run_process) -> ComposeStackResult'
        purpose: Bring a stack up, wait for readiness, and tear it down immediately on failure.
      - name: cleanup_compose_stack
        signature: 'cleanup_compose_stack(project: ComposeProject, *, environment: Mapping[str, str], options: Sequence[str]=(), timeout_seconds: float | int=300, runner: ProcessRunner=run_process)
          -> ComposeCommandResult'
        purpose: Terminal cleanup for one exact validated run-owned Compose project.
      - name: cleanup_compose_temporary_state
        signature: 'cleanup_compose_temporary_state(paths: Sequence[Path], *, root: Path) -> int'
        purpose: Remove only caller-registered temporary Compose state beneath a reviewed root.
      internal_dependencies:
      - runtime_primitives
      - service_primitives
    - path: src/ci_workflows/service_primitives.py
      responsibility: Product-neutral PostgreSQL and service-test primitives for shared CI.
      status: consolidate
      classes:
      - name: ServicePrimitiveError
        purpose: Fail closed with one stable non-secret service primitive code.
        methods: []
      - name: PostgreSQLConnection
        purpose: Normalized libpq connection fields with password hidden from repr.
        methods:
        - name: process_environment
          signature: 'process_environment(self, environment: Mapping[str, str]) -> dict[str, str]'
          purpose: Implements PostgreSQLConnection.process_environment behavior for Product-neutral PostgreSQL and service-test primitives for shared CI.
        - name: with_database
          signature: 'with_database(self, database: str) -> ''PostgreSQLConnection'''
          purpose: Implements PostgreSQLConnection.with_database behavior for Product-neutral PostgreSQL and service-test primitives for shared CI.
      - name: ServiceReadinessResult
        purpose: Defines ServiceReadinessResult for Product-neutral PostgreSQL and service-test primitives for shared CI.
        methods: []
      - name: RunOwnedPostgreSQLTarget
        purpose: Defines RunOwnedPostgreSQLTarget for Product-neutral PostgreSQL and service-test primitives for shared CI.
        methods: []
      - name: PostgreSQLTargetResult
        purpose: Defines PostgreSQLTargetResult for Product-neutral PostgreSQL and service-test primitives for shared CI.
        methods:
        - name: requested
          signature: requested(self) -> bool
          purpose: Implements PostgreSQLTargetResult.requested behavior for Product-neutral PostgreSQL and service-test primitives for shared CI.
        - name: ok
          signature: ok(self) -> bool
          purpose: Implements PostgreSQLTargetResult.ok behavior for Product-neutral PostgreSQL and service-test primitives for shared CI.
      functions:
      - name: _plain_value
        signature: '_plain_value(value: object, *, maximum: int) -> bool'
        purpose: Implements _plain_value for Product-neutral PostgreSQL and service-test primitives for shared CI.
      - name: _environment_value
        signature: '_environment_value(environment: Mapping[str, str], name: str, *, required: bool, maximum: int) -> str'
        purpose: Implements _environment_value for Product-neutral PostgreSQL and service-test primitives for shared CI.
      - name: _parse_postgres_dsn
        signature: '_parse_postgres_dsn(dsn: str) -> PostgreSQLConnection'
        purpose: Implements _parse_postgres_dsn for Product-neutral PostgreSQL and service-test primitives for shared CI.
      - name: normalize_postgres_connection
        signature: 'normalize_postgres_connection(environment: Mapping[str, str]) -> PostgreSQLConnection'
        purpose: Normalize fixed PostgreSQL environment variables or one POSTGRES_DSN.
      - name: _readiness_duration
        signature: '_readiness_duration(value: float, *, code: str) -> float'
        purpose: Implements _readiness_duration for Product-neutral PostgreSQL and service-test primitives for shared CI.
      - name: _wait_for_probe
        signature: '_wait_for_probe(kind: Literal[''postgres'', ''tcp'', ''http''], probe: Callable[[float], tuple[bool, int | None]], *, timeout_seconds: float, interval_seconds: float)
          -> ServiceReadinessResult'
        purpose: Implements _wait_for_probe for Product-neutral PostgreSQL and service-test primitives for shared CI.
      - name: wait_for_tcp_service
        signature: 'wait_for_tcp_service(environment: Mapping[str, str], *, timeout_seconds: float, interval_seconds: float=0.25) -> ServiceReadinessResult'
        purpose: Implements wait_for_tcp_service for Product-neutral PostgreSQL and service-test primitives for shared CI.
      - name: _http_url
        signature: '_http_url(environment: Mapping[str, str]) -> str'
        purpose: Implements _http_url for Product-neutral PostgreSQL and service-test primitives for shared CI.
      - name: wait_for_http_service
        signature: 'wait_for_http_service(environment: Mapping[str, str], *, timeout_seconds: float, interval_seconds: float=0.25, expected_statuses: Sequence[int]=(200, 204)) -> ServiceReadinessResult'
        purpose: Implements wait_for_http_service for Product-neutral PostgreSQL and service-test primitives for shared CI.
      - name: wait_for_postgres
        signature: 'wait_for_postgres(connection: PostgreSQLConnection, *, cwd: Path, environment: Mapping[str, str], timeout_seconds: float, interval_seconds: float=0.25, executable:
          str=''pg_isready'') -> ServiceReadinessResult'
        purpose: Implements wait_for_postgres for Product-neutral PostgreSQL and service-test primitives for shared CI.
      - name: run_service_command
        signature: 'run_service_command(arguments: Sequence[str], *, cwd: Path, environment: Mapping[str, str], connection: PostgreSQLConnection | None=None, stdin: str='''', timeout_seconds:
          float | None=None) -> ProcessResult'
        purpose: Implements run_service_command for Product-neutral PostgreSQL and service-test primitives for shared CI.
      - name: run_setup_command
        signature: 'run_setup_command(arguments: Sequence[str], *, cwd: Path, environment: Mapping[str, str], connection: PostgreSQLConnection | None=None, timeout_seconds: float | None=None)
          -> ProcessResult'
        purpose: Implements run_setup_command for Product-neutral PostgreSQL and service-test primitives for shared CI.
      - name: run_migration_command
        signature: 'run_migration_command(arguments: Sequence[str], *, cwd: Path, environment: Mapping[str, str], connection: PostgreSQLConnection | None=None, timeout_seconds: float
          | None=None) -> ProcessResult'
        purpose: Implements run_migration_command for Product-neutral PostgreSQL and service-test primitives for shared CI.
      - name: run_test_command
        signature: 'run_test_command(arguments: Sequence[str], *, cwd: Path, environment: Mapping[str, str], connection: PostgreSQLConnection | None=None, timeout_seconds: float | None=None)
          -> ProcessResult'
        purpose: Implements run_test_command for Product-neutral PostgreSQL and service-test primitives for shared CI.
      - name: _bounded_sql_file
        signature: '_bounded_sql_file(cwd: Path, sql_file: Path) -> Path'
        purpose: Implements _bounded_sql_file for Product-neutral PostgreSQL and service-test primitives for shared CI.
      - name: execute_psql
        signature: 'execute_psql(connection: PostgreSQLConnection, *, cwd: Path, environment: Mapping[str, str], sql: str | None=None, sql_file: Path | None=None, executable: str=''psql'',
          timeout_seconds: float | None=None) -> ProcessResult'
        purpose: Implements execute_psql for Product-neutral PostgreSQL and service-test primitives for shared CI.
      - name: _owner_token
        signature: '_owner_token(run_id: str) -> str'
        purpose: Implements _owner_token for Product-neutral PostgreSQL and service-test primitives for shared CI.
      - name: run_owned_postgres_target
        signature: 'run_owned_postgres_target(*, kind: Literal[''database'', ''schema''], run_id: str) -> RunOwnedPostgreSQLTarget'
        purpose: Implements run_owned_postgres_target for Product-neutral PostgreSQL and service-test primitives for shared CI.
      - name: create_run_owned_postgres_target
        signature: 'create_run_owned_postgres_target(connection: PostgreSQLConnection, *, requested: bool, kind: Literal[''database'', ''schema''], run_id: str, cwd: Path, environment:
          Mapping[str, str], executable: str=''psql'', timeout_seconds: float | None=None) -> PostgreSQLTargetResult'
        purpose: Implements create_run_owned_postgres_target for Product-neutral PostgreSQL and service-test primitives for shared CI.
      - name: connection_for_postgres_target
        signature: 'connection_for_postgres_target(connection: PostgreSQLConnection, target: RunOwnedPostgreSQLTarget) -> PostgreSQLConnection'
        purpose: Implements connection_for_postgres_target for Product-neutral PostgreSQL and service-test primitives for shared CI.
      - name: cleanup_run_owned_postgres_target
        signature: 'cleanup_run_owned_postgres_target(connection: PostgreSQLConnection, target: RunOwnedPostgreSQLTarget | None, *, cwd: Path, environment: Mapping[str, str], executable:
          str=''psql'', timeout_seconds: float | None=None) -> ProcessResult | None'
        purpose: Implements cleanup_run_owned_postgres_target for Product-neutral PostgreSQL and service-test primitives for shared CI.
      - name: cleanup_temporary_service_state
        signature: 'cleanup_temporary_service_state(paths: Sequence[Path], *, root: Path) -> int'
        purpose: Implements cleanup_temporary_service_state for Product-neutral PostgreSQL and service-test primitives for shared CI.
      internal_dependencies:
      - runtime_primitives
    - path: src/ci_workflows/service_runner_smoke.py
      responsibility: Exact-source rootless service-runner canary primitives.
      status: consolidate
      classes:
      - name: ServiceRunnerSmokeError
        purpose: Raised when the service runner violates its canary contract.
        methods: []
      functions:
      - name: validate_project_name
        signature: 'validate_project_name(value: str) -> str'
        purpose: Accept only run-owned Compose project names.
      - name: compose_document
        signature: compose_document() -> str
        purpose: Return the fixed two-service Compose fixture used by the live canary.
      - name: _run
        signature: '_run(command: Sequence[str], *, check: bool=True) -> subprocess.CompletedProcess[str]'
        purpose: Implements _run for Exact-source rootless service-runner canary primitives.
      - name: _capture
        signature: '_capture(command: Sequence[str]) -> str'
        purpose: Implements _capture for Exact-source rootless service-runner canary primitives.
      - name: verify_runtime_boundary
        signature: verify_runtime_boundary() -> None
        purpose: Fail closed unless this is the dedicated rootless daemonless runtime.
      - name: _safe_work_dir
        signature: '_safe_work_dir(path: Path, *, create: bool) -> Path'
        purpose: Implements _safe_work_dir for Exact-source rootless service-runner canary primitives.
      - name: write_compose_file
        signature: 'write_compose_file(work_dir: Path) -> Path'
        purpose: Create the fixed Compose fixture in a run-owned non-symlink directory.
      - name: _compose_prefix
        signature: '_compose_prefix(compose_path: Path, project_name: str) -> list[str]'
        purpose: Implements _compose_prefix for Exact-source rootless service-runner canary primitives.
      - name: _project_filter
        signature: '_project_filter(project_name: str) -> str'
        purpose: Implements _project_filter for Exact-source rootless service-runner canary primitives.
      - name: _wait_for_client
        signature: '_wait_for_client(client_id: str, *, timeout_seconds: float=30.0) -> None'
        purpose: Implements _wait_for_client for Exact-source rootless service-runner canary primitives.
      - name: run_smoke
        signature: 'run_smoke(*, project_name: str, work_dir: Path) -> None'
        purpose: Start the fixed two-service fixture and prove cross-service readiness.
      - name: _residual_resources
        signature: '_residual_resources(project_name: str) -> dict[str, str]'
        purpose: Implements _residual_resources for Exact-source rootless service-runner canary primitives.
      - name: cleanup_smoke
        signature: 'cleanup_smoke(*, project_name: str, work_dir: Path) -> None'
        purpose: Remove only the current run's Compose resources and prove zero residue.
      - name: parser
        signature: parser() -> argparse.ArgumentParser
        purpose: Implements parser for Exact-source rootless service-runner canary primitives.
      - name: main
        signature: 'main(argv: Sequence[str] | None=None) -> int'
        purpose: Implements main for Exact-source rootless service-runner canary primitives.
      internal_dependencies: []
  source:
    target_module: src/ci_workflows/source.py
    responsibility: GitHub source resolution, trust admission, exact checkout, dependencies, and repository-token acquisition.
    files:
    - path: src/ci_workflows/dependencies.py
      responsibility: Exact private dependency checkout integrated with source admission contracts.
      status: consolidate
      classes:
      - name: DependencyResult
        purpose: Defines DependencyResult for Exact private dependency checkout integrated with source admission contracts.
        methods:
        - name: output_values
          signature: output_values(self) -> dict[str, str]
          purpose: Implements DependencyResult.output_values behavior for Exact private dependency checkout integrated with source admission contracts.
      functions:
      - name: _run_git
        signature: '_run_git(target: Path, arguments: Sequence[str], *, allow_failure: bool=False) -> str'
        purpose: Implements _run_git for Exact private dependency checkout integrated with source admission contracts.
      - name: _exact_checkout
        signature: '_exact_checkout(**kwargs: object) -> Mapping[str, str]'
        purpose: Implements _exact_checkout for Exact private dependency checkout integrated with source admission contracts.
      - name: _erase_git_connection_state
        signature: '_erase_git_connection_state(target: Path) -> None'
        purpose: Implements _erase_git_connection_state for Exact private dependency checkout integrated with source admission contracts.
      - name: _verify_expected_subpath
        signature: '_verify_expected_subpath(target: Path, raw_subpath: str) -> str'
        purpose: Implements _verify_expected_subpath for Exact private dependency checkout integrated with source admission contracts.
      - name: _verify_detached_credential_free
        signature: '_verify_detached_credential_free(target: Path, admitted_sha: str, token: str) -> None'
        purpose: Implements _verify_detached_credential_free for Exact private dependency checkout integrated with source admission contracts.
      - name: checkout_private_dependency
        signature: 'checkout_private_dependency(*, state_root: Path, repository: str, admitted_sha: str, dependency_id: str, expected_subpath: str, fetch_depth: int, token: str, contract_root:
          Path) -> DependencyResult'
        purpose: Checkout one admitted StreamScapeTV dependency and erase connection state.
      internal_dependencies:
      - foundation_types
      - source
      - workspace
    - path: src/ci_workflows/github_app_token.py
      responsibility: Mint one bounded contents-read GitHub App token for an exact repository.
      status: consolidate
      classes:
      - name: GitHubAppTokenError
        purpose: Stable non-sensitive GitHub App token failure.
        methods: []
      - name: GitHubAppRepositoryTokenClient
        purpose: Minimal GitHub App client that can mint only one repository read token.
        methods:
        - name: _jwt
          signature: '_jwt(self, now: int | None=None) -> str'
          purpose: Implements GitHubAppRepositoryTokenClient._jwt behavior for Mint one bounded contents-read GitHub App token for an exact repository.
        - name: _request
          signature: '_request(self, method: str, path: str, *, authorization: str, body: Mapping[str, object] | None=None, expected: tuple[int, ...]=(200,)) -> Mapping[str, Any]'
          purpose: Implements GitHubAppRepositoryTokenClient._request behavior for Mint one bounded contents-read GitHub App token for an exact repository.
        - name: repository_contents_read_token
          signature: 'repository_contents_read_token(self, repository: object) -> str'
          purpose: Implements GitHubAppRepositoryTokenClient.repository_contents_read_token behavior for Mint one bounded contents-read GitHub App token for an exact repository.
      functions:
      - name: _require
        signature: '_require(condition: bool, code: str) -> None'
        purpose: Implements _require for Mint one bounded contents-read GitHub App token for an exact repository.
      - name: _canonical
        signature: '_canonical(value: object) -> bytes'
        purpose: Implements _canonical for Mint one bounded contents-read GitHub App token for an exact repository.
      - name: _b64url
        signature: '_b64url(value: bytes) -> str'
        purpose: Implements _b64url for Mint one bounded contents-read GitHub App token for an exact repository.
      - name: _repository
        signature: '_repository(value: object) -> tuple[str, str, str]'
        purpose: Implements _repository for Mint one bounded contents-read GitHub App token for an exact repository.
      - name: _app_id
        signature: '_app_id(value: object) -> int'
        purpose: Implements _app_id for Mint one bounded contents-read GitHub App token for an exact repository.
      - name: _private_key
        signature: '_private_key(value: object) -> str'
        purpose: Implements _private_key for Mint one bounded contents-read GitHub App token for an exact repository.
      - name: _token
        signature: '_token(value: object) -> str'
        purpose: Implements _token for Mint one bounded contents-read GitHub App token for an exact repository.
      - name: _openssl_sign
        signature: '_openssl_sign(signing_input: bytes, private_key: str) -> bytes'
        purpose: Implements _openssl_sign for Mint one bounded contents-read GitHub App token for an exact repository.
      - name: _required
        signature: '_required(environment: Mapping[str, str], name: str) -> str'
        purpose: Implements _required for Mint one bounded contents-read GitHub App token for an exact repository.
      - name: issue_repository_token
        signature: 'issue_repository_token(environment: Mapping[str, str]=os.environ, *, client_factory: Callable[[object, object], GitHubAppRepositoryTokenClient]=GitHubAppRepositoryTokenClient)
          -> str'
        purpose: Implements issue_repository_token for Mint one bounded contents-read GitHub App token for an exact repository.
      - name: parser
        signature: parser() -> argparse.ArgumentParser
        purpose: Implements parser for Mint one bounded contents-read GitHub App token for an exact repository.
      - name: main
        signature: 'main(argv: Sequence[str] | None=None) -> int'
        purpose: Implements main for Mint one bounded contents-read GitHub App token for an exact repository.
      internal_dependencies: []
    - path: src/ci_workflows/private_source.py
      responsibility: Resolve one configured private StreamScapeTV branch to an exact commit SHA.
      status: consolidate
      classes:
      - name: PrivateSourceError
        purpose: Fail-closed error whose code never contains a repository, branch, or token.
        methods: []
      functions:
      - name: _require
        signature: '_require(condition: bool, code: str) -> None'
        purpose: Implements _require for Resolve one configured private StreamScapeTV branch to an exact commit SHA.
      - name: _repository_name
        signature: '_repository_name(value: object) -> str'
        purpose: Implements _repository_name for Resolve one configured private StreamScapeTV branch to an exact commit SHA.
      - name: _branch
        signature: '_branch(value: object) -> str'
        purpose: Implements _branch for Resolve one configured private StreamScapeTV branch to an exact commit SHA.
      - name: _mapping
        signature: '_mapping(value: object) -> Mapping[str, Any]'
        purpose: Implements _mapping for Resolve one configured private StreamScapeTV branch to an exact commit SHA.
      - name: resolve_private_branch
        signature: 'resolve_private_branch(*, repository_name: str, branch: str, token: str, opener: Any=urllib.request.urlopen) -> str'
        purpose: Resolve one fixed-organization private branch without exposing mutable source.
      internal_dependencies: []
    - path: src/ci_workflows/source.py
      responsibility: Public facade for exact source admission and checkout primitives.
      status: keep
      classes: []
      functions: []
      internal_dependencies:
      - source_admission
      - source_checkout
      - source_cli
      - source_github
      - source_types
    - path: src/ci_workflows/source_admission.py
      responsibility: Exact event admission and freshness revalidation state machine.
      status: consolidate
      classes: []
      functions:
      - name: _resolve_tag
        signature: '_resolve_tag(provider: SourceProvider, repository: str, tag_name: str) -> tuple[str, str]'
        purpose: Implements _resolve_tag for Exact event admission and freshness revalidation state machine.
      - name: _admit_requested_ref
        signature: '_admit_requested_ref(inputs: SourceInputs, event: EventContext, provider: SourceProvider) -> AdmissionResult'
        purpose: Resolve a caller-selected repository branch/tag to one immutable snapshot.
      - name: admit_source
        signature: 'admit_source(inputs: SourceInputs, event: EventContext, provider: SourceProvider) -> AdmissionResult'
        purpose: Resolve one exact source SHA without executing caller source.
      - name: revalidate_admission
        signature: 'revalidate_admission(result: AdmissionResult, provider: SourceProvider) -> None'
        purpose: Revalidate mutable evidence immediately before privileged publication.
      internal_dependencies:
      - source_evidence
      - source_types
    - path: src/ci_workflows/source_checkout.py
      responsibility: Credential-free exact-SHA detached checkout implementation.
      status: keep
      classes: []
      functions:
      - name: _safe_checkout_path
        signature: '_safe_checkout_path(workspace: Path, raw_path: str) -> Path'
        purpose: Implements _safe_checkout_path for Credential-free exact-SHA detached checkout implementation.
      - name: _run_git
        signature: '_run_git(arguments: Sequence[str], *, cwd: Path, environment: Mapping[str, str] | None=None) -> str'
        purpose: Implements _run_git for Credential-free exact-SHA detached checkout implementation.
      - name: _clean_failed_checkout
        signature: '_clean_failed_checkout(target: Path, *, preserve_empty_path: bool) -> None'
        purpose: Implements _clean_failed_checkout for Credential-free exact-SHA detached checkout implementation.
      - name: exact_checkout
        signature: 'exact_checkout(*, repository: str, admitted_sha: str, path: str, fetch_depth: int, token: str, workspace: Path, remote_url: str | None=None) -> Mapping[str, str]'
        purpose: Fetch one exact commit into a detached checkout and verify equality.
      internal_dependencies:
      - source_types
    - path: src/ci_workflows/source_cli.py
      responsibility: Compatibility CLI adapters for exact source primitives.
      status: compatibility
      classes: []
      functions:
      - name: _read_event
        signature: '_read_event(path: str) -> Mapping[str, Any]'
        purpose: Implements _read_event for Compatibility CLI adapters for exact source primitives.
      - name: _input_environment
        signature: '_input_environment(environment: Mapping[str, str]) -> dict[str, Any]'
        purpose: Implements _input_environment for Compatibility CLI adapters for exact source primitives.
      - name: resolve_from_environment
        signature: 'resolve_from_environment(root: Path, *, environment: Mapping[str, str] | None=None) -> AdmissionResult'
        purpose: Resolve source from an explicit environment mapping or ``os.environ``.
      - name: _legacy_parser
        signature: _legacy_parser() -> argparse.ArgumentParser
        purpose: Implements _legacy_parser for Compatibility CLI adapters for exact source primitives.
      - name: main
        signature: 'main(argv: Sequence[str] | None=None) -> int'
        purpose: Preserve the existing source CLI while delegating to ``ciw``.
      internal_dependencies:
      - ciw
      - source_admission
      - source_github
      - source_types
    - path: src/ci_workflows/source_evidence.py
      responsibility: Current GitHub metadata evidence and stable admission result construction.
      status: consolidate
      classes: []
      functions:
      - name: _mapping
        signature: '_mapping(value: Any, instruction: str) -> Mapping[str, Any]'
        purpose: Implements _mapping for Current GitHub metadata evidence and stable admission result construction.
      - name: _string
        signature: '_string(value: Any, instruction: str) -> str'
        purpose: Implements _string for Current GitHub metadata evidence and stable admission result construction.
      - name: _pr_evidence
        signature: '_pr_evidence(repository: str, raw: Mapping[str, Any]) -> PullRequestEvidence'
        purpose: Implements _pr_evidence for Current GitHub metadata evidence and stable admission result construction.
      - name: _event_pull
        signature: '_event_pull(event: EventContext) -> Mapping[str, Any]'
        purpose: Implements _event_pull for Current GitHub metadata evidence and stable admission result construction.
      - name: _repo_branches
        signature: '_repo_branches(event: EventContext, inputs: SourceInputs, provider: SourceProvider) -> tuple[str, str, str]'
        purpose: Implements _repo_branches for Current GitHub metadata evidence and stable admission result construction.
      - name: _assert_commit
        signature: '_assert_commit(provider: SourceProvider, repository: str, sha: str) -> None'
        purpose: Implements _assert_commit for Current GitHub metadata evidence and stable admission result construction.
      - name: _assert_actor_write
        signature: '_assert_actor_write(provider: SourceProvider, repository: str, event: EventContext) -> None'
        purpose: Implements _assert_actor_write for Current GitHub metadata evidence and stable admission result construction.
      - name: _expected_pr_freshness
        signature: '_expected_pr_freshness(inputs: SourceInputs, evidence: PullRequestEvidence) -> None'
        purpose: Implements _expected_pr_freshness for Current GitHub metadata evidence and stable admission result construction.
      - name: _stable_id
        signature: '_stable_id(prefix: str, evidence: Mapping[str, Any]) -> str'
        purpose: Implements _stable_id for Current GitHub metadata evidence and stable admission result construction.
      - name: _result
        signature: '_result(*, repository: str, default_branch: str, integration_branch: str, trust_mode: TrustMode, source_repository: str, source_sha: str, inputs: SourceInputs, pr:
          PullRequestEvidence | None=None, tag_name: str | None=None, tag_object_sha: str | None=None, tag_commit_sha: str | None=None, requires_freshness: bool) -> AdmissionResult'
        purpose: Implements _result for Current GitHub metadata evidence and stable admission result construction.
      internal_dependencies:
      - source_types
    - path: src/ci_workflows/source_github.py
      responsibility: Read-only GitHub metadata provider for source admission.
      status: consolidate
      classes:
      - name: GitHubSourceProvider
        purpose: Small read-only GitHub API client for exact source metadata.
        methods:
        - name: _get
          signature: '_get(self, path: str) -> Mapping[str, Any]'
          purpose: Implements GitHubSourceProvider._get behavior for Read-only GitHub metadata provider for source admission.
        - name: _repository_path
          signature: '_repository_path(repository: str) -> str'
          purpose: Implements GitHubSourceProvider._repository_path behavior for Read-only GitHub metadata provider for source admission.
        - name: repository
          signature: 'repository(self, repository: str) -> Mapping[str, Any]'
          purpose: Implements GitHubSourceProvider.repository behavior for Read-only GitHub metadata provider for source admission.
        - name: collaborator_permission
          signature: 'collaborator_permission(self, repository: str, actor: str) -> str'
          purpose: Implements GitHubSourceProvider.collaborator_permission behavior for Read-only GitHub metadata provider for source admission.
        - name: pull_request
          signature: 'pull_request(self, repository: str, number: int) -> Mapping[str, Any]'
          purpose: Implements GitHubSourceProvider.pull_request behavior for Read-only GitHub metadata provider for source admission.
        - name: commit
          signature: 'commit(self, repository: str, sha: str) -> Mapping[str, Any]'
          purpose: Implements GitHubSourceProvider.commit behavior for Read-only GitHub metadata provider for source admission.
        - name: branch_sha
          signature: 'branch_sha(self, repository: str, branch: str) -> str'
          purpose: Implements GitHubSourceProvider.branch_sha behavior for Read-only GitHub metadata provider for source admission.
        - name: tag_ref
          signature: 'tag_ref(self, repository: str, tag_name: str) -> Mapping[str, Any]'
          purpose: Implements GitHubSourceProvider.tag_ref behavior for Read-only GitHub metadata provider for source admission.
        - name: tag_object
          signature: 'tag_object(self, repository: str, sha: str) -> Mapping[str, Any]'
          purpose: Implements GitHubSourceProvider.tag_object behavior for Read-only GitHub metadata provider for source admission.
      functions: []
      internal_dependencies:
      - source_evidence
      - source_types
    - path: src/ci_workflows/source_types.py
      responsibility: Typed exact-source contracts and bounded input validation.
      status: consolidate
      classes:
      - name: TrustMode
        purpose: Defines TrustMode for Typed exact-source contracts and bounded input validation.
        methods: []
      - name: SourceMode
        purpose: Defines SourceMode for Typed exact-source contracts and bounded input validation.
        methods: []
      - name: SourceAdmissionError
        purpose: Fail-closed error with a stable, non-secret instruction code.
        methods: []
      - name: SourceInputs
        purpose: Defines SourceInputs for Typed exact-source contracts and bounded input validation.
        methods: []
      - name: EventContext
        purpose: Defines EventContext for Typed exact-source contracts and bounded input validation.
        methods: []
      - name: PullRequestEvidence
        purpose: Defines PullRequestEvidence for Typed exact-source contracts and bounded input validation.
        methods: []
      - name: AdmissionResult
        purpose: Defines AdmissionResult for Typed exact-source contracts and bounded input validation.
        methods:
        - name: output_values
          signature: output_values(self) -> dict[str, str]
          purpose: Implements AdmissionResult.output_values behavior for Typed exact-source contracts and bounded input validation.
      - name: SourceProvider
        purpose: Defines SourceProvider for Typed exact-source contracts and bounded input validation.
        methods:
        - name: repository
          signature: 'repository(self, repository: str) -> Mapping[str, Any]'
          purpose: Implements SourceProvider.repository behavior for Typed exact-source contracts and bounded input validation.
        - name: collaborator_permission
          signature: 'collaborator_permission(self, repository: str, actor: str) -> str'
          purpose: Implements SourceProvider.collaborator_permission behavior for Typed exact-source contracts and bounded input validation.
        - name: pull_request
          signature: 'pull_request(self, repository: str, number: int) -> Mapping[str, Any]'
          purpose: Implements SourceProvider.pull_request behavior for Typed exact-source contracts and bounded input validation.
        - name: commit
          signature: 'commit(self, repository: str, sha: str) -> Mapping[str, Any]'
          purpose: Implements SourceProvider.commit behavior for Typed exact-source contracts and bounded input validation.
        - name: branch_sha
          signature: 'branch_sha(self, repository: str, branch: str) -> str'
          purpose: Implements SourceProvider.branch_sha behavior for Typed exact-source contracts and bounded input validation.
        - name: tag_ref
          signature: 'tag_ref(self, repository: str, tag_name: str) -> Mapping[str, Any]'
          purpose: Implements SourceProvider.tag_ref behavior for Typed exact-source contracts and bounded input validation.
        - name: tag_object
          signature: 'tag_object(self, repository: str, sha: str) -> Mapping[str, Any]'
          purpose: Implements SourceProvider.tag_object behavior for Typed exact-source contracts and bounded input validation.
      functions:
      - name: _require
        signature: '_require(condition: bool, instruction: str) -> None'
        purpose: Implements _require for Typed exact-source contracts and bounded input validation.
      - name: _optional_string
        signature: '_optional_string(value: Any) -> str | None'
        purpose: Implements _optional_string for Typed exact-source contracts and bounded input validation.
      - name: _optional_bool
        signature: '_optional_bool(value: Any, instruction: str) -> bool | None'
        purpose: Implements _optional_bool for Typed exact-source contracts and bounded input validation.
      - name: _full_sha
        signature: '_full_sha(value: Any, instruction: str) -> str'
        purpose: Implements _full_sha for Typed exact-source contracts and bounded input validation.
      - name: _optional_sha
        signature: '_optional_sha(value: Any, instruction: str) -> str | None'
        purpose: Implements _optional_sha for Typed exact-source contracts and bounded input validation.
      - name: _positive_int
        signature: '_positive_int(value: Any, instruction: str, *, maximum: int=1000) -> int'
        purpose: Implements _positive_int for Typed exact-source contracts and bounded input validation.
      - name: load_contract
        signature: 'load_contract(root: Path) -> Mapping[str, Any]'
        purpose: Implements load_contract for Typed exact-source contracts and bounded input validation.
      - name: validate_inputs
        signature: 'validate_inputs(raw: Mapping[str, Any], contract: Mapping[str, Any]) -> SourceInputs'
        purpose: Implements validate_inputs for Typed exact-source contracts and bounded input validation.
      - name: load_event_context
        signature: 'load_event_context(environment: Mapping[str, str], payload: Mapping[str, Any]) -> EventContext'
        purpose: Implements load_event_context for Typed exact-source contracts and bounded input validation.
      internal_dependencies: []
  tooling:
    target_module: src/ci_workflows/tooling.py
    responsibility: Pinned tooling acquisition, verification, and capability inspection.
    files:
    - path: src/ci_workflows/tooling.py
      responsibility: Pinned tool, version, checksum, digest, and runtime capability verification.
      status: keep
      classes:
      - name: ToolEvidence
        purpose: Defines ToolEvidence for Pinned tool, version, checksum, digest, and runtime capability verification.
        methods: []
      - name: ToolSetEvidence
        purpose: Defines ToolSetEvidence for Pinned tool, version, checksum, digest, and runtime capability verification.
        methods:
        - name: output_values
          signature: output_values(self) -> dict[str, str]
          purpose: Implements ToolSetEvidence.output_values behavior for Pinned tool, version, checksum, digest, and runtime capability verification.
      - name: RuntimeCapabilityEvidence
        purpose: Defines RuntimeCapabilityEvidence for Pinned tool, version, checksum, digest, and runtime capability verification.
        methods:
        - name: output_values
          signature: output_values(self) -> dict[str, str]
          purpose: Implements RuntimeCapabilityEvidence.output_values behavior for Pinned tool, version, checksum, digest, and runtime capability verification.
      - name: InstalledAsset
        purpose: Defines InstalledAsset for Pinned tool, version, checksum, digest, and runtime capability verification.
        methods:
        - name: output_values
          signature: output_values(self) -> dict[str, str]
          purpose: Implements InstalledAsset.output_values behavior for Pinned tool, version, checksum, digest, and runtime capability verification.
      functions:
      - name: _version_tuple
        signature: '_version_tuple(value: str) -> tuple[int, ...]'
        purpose: Implements _version_tuple for Pinned tool, version, checksum, digest, and runtime capability verification.
      - name: _resolve_executable
        signature: '_resolve_executable(executable: str) -> Path'
        purpose: Implements _resolve_executable for Pinned tool, version, checksum, digest, and runtime capability verification.
      - name: _run_version
        signature: '_run_version(executable: str, arguments: Sequence[str]) -> str'
        purpose: Implements _run_version for Pinned tool, version, checksum, digest, and runtime capability verification.
      - name: verify_tool
        signature: 'verify_tool(tool_id: str, *, contract_root: Path) -> ToolEvidence'
        purpose: Implements verify_tool for Pinned tool, version, checksum, digest, and runtime capability verification.
      - name: verify_tool_set
        signature: 'verify_tool_set(tool_set: str, *, contract_root: Path) -> ToolSetEvidence'
        purpose: Implements verify_tool_set for Pinned tool, version, checksum, digest, and runtime capability verification.
      - name: _normalize_operating_system
        signature: '_normalize_operating_system(value: str) -> str'
        purpose: Implements _normalize_operating_system for Pinned tool, version, checksum, digest, and runtime capability verification.
      - name: _normalize_architecture
        signature: '_normalize_architecture(value: str) -> str'
        purpose: Implements _normalize_architecture for Pinned tool, version, checksum, digest, and runtime capability verification.
      - name: verify_runtime_capability
        signature: 'verify_runtime_capability(capability_profile: str, *, declared_os: str | None, declared_architecture: str | None, contract_root: Path) -> RuntimeCapabilityEvidence'
        purpose: Verify a semantic OS/architecture profile without exposing host identity.
      - name: checksum_file
        signature: 'checksum_file(path: Path, algorithm: str=''sha256'') -> str'
        purpose: Implements checksum_file for Pinned tool, version, checksum, digest, and runtime capability verification.
      - name: verify_checksum
        signature: 'verify_checksum(path: Path, *, algorithm: str, expected: str) -> str'
        purpose: Implements verify_checksum for Pinned tool, version, checksum, digest, and runtime capability verification.
      - name: verify_digest
        signature: 'verify_digest(path: Path, expected_digest: str) -> str'
        purpose: Implements verify_digest for Pinned tool, version, checksum, digest, and runtime capability verification.
      - name: _asset_entry
        signature: '_asset_entry(contract: Mapping[str, Any], asset_id: str) -> Mapping[str, Any]'
        purpose: Implements _asset_entry for Pinned tool, version, checksum, digest, and runtime capability verification.
      - name: install_locked_asset
        signature: 'install_locked_asset(asset_id: str, *, destination_root: Path, contract_root: Path) -> InstalledAsset'
        purpose: Download one contract-selected immutable asset and verify its digest.
      internal_dependencies:
      - foundation_types
  validation:
    target_module: src/ci_workflows/validation_harness.py
    responsibility: Repository validation harness, graph/model/policy/expression checks, and locked validation runtime.
    files:
    - path: src/ci_workflows/validation_contracts.py
      responsibility: Public API, fixture, and dependency-lock compatibility validation.
      status: consolidate
      classes: []
      functions:
      - name: _validate_public_compatibility
        signature: '_validate_public_compatibility(document: ParsedDocument, public_contract: PublicContract, config: HarnessConfig, findings: list[Finding], functions: Mapping[str,
          tuple[str, int]], docs_text: str) -> None'
        purpose: Implements _validate_public_compatibility for Public API, fixture, and dependency-lock compatibility validation.
      - name: _function_index
        signature: '_function_index(root: Path, python_sources: Sequence[Path]) -> dict[str, tuple[str, int]]'
        purpose: Implements _function_index for Public API, fixture, and dependency-lock compatibility validation.
      - name: _validate_fixture_coverage
        signature: '_validate_fixture_coverage(root: Path, config: HarnessConfig, findings: list[Finding]) -> None'
        purpose: Implements _validate_fixture_coverage for Public API, fixture, and dependency-lock compatibility validation.
      - name: _validate_caller_fixture
        signature: '_validate_caller_fixture(document: ParsedDocument, public_contract: PublicContract, config: HarnessConfig, findings: list[Finding]) -> None'
        purpose: Implements _validate_caller_fixture for Public API, fixture, and dependency-lock compatibility validation.
      internal_dependencies:
      - validation_helpers
      - validation_model
    - path: src/ci_workflows/validation_expression_contexts.py
      responsibility: Offline GitHub Actions expression-context availability validation.
      status: consolidate
      classes:
      - name: _ContextRule
        purpose: Defines _ContextRule for Offline GitHub Actions expression-context availability validation.
        methods: []
      functions:
      - name: _contexts
        signature: '_contexts(value: str) -> frozenset[str]'
        purpose: Implements _contexts for Offline GitHub Actions expression-context availability validation.
      - name: _rule
        signature: '_rule(github_key: str, allowed: frozenset[str], *, implicit_expression: bool=False) -> _ContextRule'
        purpose: Implements _rule for Offline GitHub Actions expression-context availability validation.
      - name: _expression_bodies
        signature: '_expression_bodies(value: str) -> Iterator[str]'
        purpose: Yield ``${{ ...
      - name: _without_single_quoted_literals
        signature: '_without_single_quoted_literals(expression: str) -> str'
        purpose: Blank GitHub expression string literals before detecting context roots.
      - name: _contexts_in_expression
        signature: '_contexts_in_expression(expression: str) -> set[str]'
        purpose: Implements _contexts_in_expression for Offline GitHub Actions expression-context availability validation.
      - name: _contexts_in_value
        signature: '_contexts_in_value(value: str, *, implicit_expression: bool) -> frozenset[str]'
        purpose: Implements _contexts_in_value for Offline GitHub Actions expression-context availability validation.
      - name: _scalar_leaves
        signature: '_scalar_leaves(value: Any, location: str) -> Iterator[tuple[str, Any]]'
        purpose: Implements _scalar_leaves for Offline GitHub Actions expression-context availability validation.
      - name: _validate_value
        signature: '_validate_value(document: ParsedDocument, config: HarnessConfig, findings: list[Finding], rule: _ContextRule, value: Any, location: str) -> None'
        purpose: Implements _validate_value for Offline GitHub Actions expression-context availability validation.
      - name: _validate_environment
        signature: '_validate_environment(document: ParsedDocument, config: HarnessConfig, findings: list[Finding], job_id: str, value: Any) -> None'
        purpose: Implements _validate_environment for Offline GitHub Actions expression-context availability validation.
      - name: _validate_workflow_expression_contexts
        signature: '_validate_workflow_expression_contexts(document: ParsedDocument, config: HarnessConfig, findings: list[Finding]) -> None'
        purpose: Reject context roots unavailable at their documented YAML location.
      internal_dependencies:
      - validation_helpers
      - validation_model
    - path: src/ci_workflows/validation_graph.py
      responsibility: Reusable workflow and composite-action call graph validation.
      status: consolidate
      classes: []
      functions:
      - name: _workflow_edges
        signature: '_workflow_edges(document: ParsedDocument) -> set[str]'
        purpose: Implements _workflow_edges for Reusable workflow and composite-action call graph validation.
      - name: _action_edges
        signature: '_action_edges(document: ParsedDocument) -> set[str]'
        purpose: Implements _action_edges for Reusable workflow and composite-action call graph validation.
      - name: _find_cycles
        signature: '_find_cycles(graph: Mapping[str, set[str]]) -> list[list[str]]'
        purpose: Implements _find_cycles for Reusable workflow and composite-action call graph validation.
      - name: _longest_depth
        signature: '_longest_depth(graph: Mapping[str, set[str]], start: str) -> int'
        purpose: Implements _longest_depth for Reusable workflow and composite-action call graph validation.
      - name: _validate_call_graphs
        signature: '_validate_call_graphs(root: Path, workflow_documents: Mapping[str, ParsedDocument], action_documents: Mapping[str, ParsedDocument], public_contract: PublicContract,
          config: HarnessConfig, findings: list[Finding]) -> None'
        purpose: Implements _validate_call_graphs for Reusable workflow and composite-action call graph validation.
      internal_dependencies:
      - validation_helpers
      - validation_model
    - path: src/ci_workflows/validation_harness.py
      responsibility: Canonical static validation harness for StreamScapeTV GitHub Actions.
      status: keep
      classes: []
      functions:
      - name: validate_repository
        signature: 'validate_repository(root: Path, *, include_public_api_validator: bool=True) -> ValidationResult'
        purpose: Validate the complete repository and return deterministic evidence.
      - name: render_summary
        signature: 'render_summary(result: ValidationResult) -> str'
        purpose: Render one stable machine-readable validation summary.
      internal_dependencies:
      - public_api
      - public_ci_admission
      - readability
      - validation_contracts
      - validation_expression_contexts
      - validation_graph
      - validation_helpers
      - validation_model
      - validation_policy
    - path: src/ci_workflows/validation_helpers.py
      responsibility: Shared semantic and source-shape helpers for workflow validation.
      status: consolidate
      classes: []
      functions:
      - name: _finding
        signature: '_finding(findings: list[Finding], config: HarnessConfig, rule: str, relative_path: str, message: str, line: int=0) -> None'
        purpose: Implements _finding for Shared semantic and source-shape helpers for workflow validation.
      - name: _events
        signature: '_events(document: ParsedDocument) -> set[str]'
        purpose: Implements _events for Shared semantic and source-shape helpers for workflow validation.
      - name: _iter_jobs
        signature: '_iter_jobs(document: ParsedDocument) -> Iterator[tuple[str, Mapping[str, Any]]]'
        purpose: Implements _iter_jobs for Shared semantic and source-shape helpers for workflow validation.
      - name: _iter_steps
        signature: '_iter_steps(job: Mapping[str, Any]) -> Iterator[tuple[int, Mapping[str, Any]]]'
        purpose: Implements _iter_steps for Shared semantic and source-shape helpers for workflow validation.
      - name: _run_line_count
        signature: '_run_line_count(run: Any) -> int'
        purpose: Implements _run_line_count for Shared semantic and source-shape helpers for workflow validation.
      - name: _normalize_run
        signature: '_normalize_run(run: str) -> str'
        purpose: Implements _normalize_run for Shared semantic and source-shape helpers for workflow validation.
      - name: _is_public_workflow
        signature: '_is_public_workflow(relative_path: str) -> bool'
        purpose: Implements _is_public_workflow for Shared semantic and source-shape helpers for workflow validation.
      - name: _is_internal_workflow
        signature: '_is_internal_workflow(relative_path: str) -> bool'
        purpose: Implements _is_internal_workflow for Shared semantic and source-shape helpers for workflow validation.
      - name: _permission_mapping
        signature: '_permission_mapping(value: Any) -> Mapping[str, Any]'
        purpose: Implements _permission_mapping for Shared semantic and source-shape helpers for workflow validation.
      - name: _matrix_size
        signature: '_matrix_size(matrix: Any) -> int | None'
        purpose: Implements _matrix_size for Shared semantic and source-shape helpers for workflow validation.
      - name: _uses_entries
        signature: '_uses_entries(document: ParsedDocument) -> Iterator[tuple[str, str | None, int]]'
        purpose: Implements _uses_entries for Shared semantic and source-shape helpers for workflow validation.
      - name: _validate_action_reference
        signature: '_validate_action_reference(uses: str, relative_path: str, line_number: int, config: HarnessConfig, findings: list[Finding]) -> None'
        purpose: Validate only the functional shape of one ``uses`` reference.
      - name: _validate_checkout_step
        signature: '_validate_checkout_step(step: Mapping[str, Any], document: ParsedDocument, config: HarnessConfig, findings: list[Finding]) -> None'
        purpose: Implements _validate_checkout_step for Shared semantic and source-shape helpers for workflow validation.
      - name: _validate_runner
        signature: '_validate_runner(runner: Any, relative_path: str, config: HarnessConfig, findings: list[Finding]) -> None'
        purpose: Implements _validate_runner for Shared semantic and source-shape helpers for workflow validation.
      - name: _workflow_call_contract
        signature: '_workflow_call_contract(document: ParsedDocument) -> Mapping[str, Any]'
        purpose: Implements _workflow_call_contract for Shared semantic and source-shape helpers for workflow validation.
      - name: _actual_names
        signature: '_actual_names(value: Any) -> set[str]'
        purpose: Implements _actual_names for Shared semantic and source-shape helpers for workflow validation.
      - name: _contract_input_map
        signature: '_contract_input_map(record: Mapping[str, Any]) -> dict[str, Mapping[str, Any]]'
        purpose: Implements _contract_input_map for Shared semantic and source-shape helpers for workflow validation.
      internal_dependencies:
      - validation_model
    - path: src/ci_workflows/validation_model.py
      responsibility: Canonical static validation harness for StreamScapeTV GitHub Actions.
      status: consolidate
      classes:
      - name: ActionsLoader
        purpose: YAML 1.2-ish loader that preserves GitHub's literal ``on`` key.
        methods: []
      - name: Finding
        purpose: One deterministic policy failure.
        methods:
        - name: render
          signature: render(self) -> str
          purpose: Implements Finding.render behavior for Canonical static validation harness for StreamScapeTV GitHub Actions.
      - name: HarnessFailure
        purpose: Raised when repository validation produces findings.
        methods: []
      - name: ParsedDocument
        purpose: Defines ParsedDocument for Canonical static validation harness for StreamScapeTV GitHub Actions.
        methods: []
      - name: RepositoryInventory
        purpose: Defines RepositoryInventory for Canonical static validation harness for StreamScapeTV GitHub Actions.
        methods: []
      - name: PublicContract
        purpose: Defines PublicContract for Canonical static validation harness for StreamScapeTV GitHub Actions.
        methods: []
      - name: HarnessConfig
        purpose: Defines HarnessConfig for Canonical static validation harness for StreamScapeTV GitHub Actions.
        methods:
        - name: excepts
          signature: 'excepts(self, relative_path: str, rule: str) -> bool'
          purpose: Implements HarnessConfig.excepts behavior for Canonical static validation harness for StreamScapeTV GitHub Actions.
      - name: ValidationResult
        purpose: Defines ValidationResult for Canonical static validation harness for StreamScapeTV GitHub Actions.
        methods:
        - name: require_success
          signature: require_success(self) -> 'ValidationResult'
          purpose: Implements ValidationResult.require_success behavior for Canonical static validation harness for StreamScapeTV GitHub Actions.
      functions:
      - name: _read_json
        signature: '_read_json(path: Path) -> Any'
        purpose: Implements _read_json for Canonical static validation harness for StreamScapeTV GitHub Actions.
      - name: _relative
        signature: '_relative(root: Path, path: Path) -> str'
        purpose: Implements _relative for Canonical static validation harness for StreamScapeTV GitHub Actions.
      - name: load_actions_yaml
        signature: 'load_actions_yaml(path: Path, root: Path | None=None) -> ParsedDocument'
        purpose: Parse a workflow/action while preserving the literal ``on`` key.
      - name: discover_repository
        signature: 'discover_repository(root: Path) -> RepositoryInventory'
        purpose: Discover every supported source type in stable lexical order.
      - name: _collect_runner_profiles_from_contracts
        signature: '_collect_runner_profiles_from_contracts(root: Path) -> set[str]'
        purpose: Implements _collect_runner_profiles_from_contracts for Canonical static validation harness for StreamScapeTV GitHub Actions.
      - name: _collect_runner_selectors_from_contracts
        signature: '_collect_runner_selectors_from_contracts(root: Path) -> set[tuple[str, ...]]'
        purpose: Implements _collect_runner_selectors_from_contracts for Canonical static validation harness for StreamScapeTV GitHub Actions.
      - name: load_harness_config
        signature: 'load_harness_config(root: Path) -> HarnessConfig'
        purpose: Implements load_harness_config for Canonical static validation harness for StreamScapeTV GitHub Actions.
      - name: load_public_contract
        signature: 'load_public_contract(root: Path) -> PublicContract'
        purpose: Implements load_public_contract for Canonical static validation harness for StreamScapeTV GitHub Actions.
      internal_dependencies: []
    - path: src/ci_workflows/validation_policy.py
      responsibility: Workflow, action, readability, source, trust, and cleanup policy checks.
      status: consolidate
      classes: []
      functions:
      - name: _contains_mutable_publication_tag
        signature: '_contains_mutable_publication_tag(raw: str) -> bool'
        purpose: Return true only when a publication command selects ``latest``.
      - name: _is_always_cleanup_step
        signature: '_is_always_cleanup_step(step: Mapping[str, object]) -> bool'
        purpose: Recognize explicit unconditional cleanup steps.
      - name: _validate_runner
        signature: '_validate_runner(runner: Any, relative_path: str, config: HarnessConfig, findings: list[Finding]) -> None'
        purpose: Accept an exact capability array or the trusted planner output only.
      - name: _validate_workflow
        signature: '_validate_workflow(document: ParsedDocument, root: Path, config: HarnessConfig, public_contract: PublicContract, functions: Mapping[str, tuple[str, int]], docs_text:
          str, findings: list[Finding], duplicated_runs: dict[str, list[str]]) -> None'
        purpose: Implements _validate_workflow for Workflow, action, readability, source, trust, and cleanup policy checks.
      - name: _validate_action
        signature: '_validate_action(document: ParsedDocument, config: HarnessConfig, findings: list[Finding], duplicated_runs: dict[str, list[str]]) -> None'
        purpose: Implements _validate_action for Workflow, action, readability, source, trust, and cleanup policy checks.
      - name: _validate_global_duplicate_blocks
        signature: '_validate_global_duplicate_blocks(duplicated_runs: Mapping[str, list[str]], config: HarnessConfig, findings: list[Finding]) -> None'
        purpose: Implements _validate_global_duplicate_blocks for Workflow, action, readability, source, trust, and cleanup policy checks.
      internal_dependencies:
      - validation_contracts
      - validation_helpers
      - validation_model
  web:
    target_module: src/ci_workflows/web.py
    responsibility: Static-web build/output verification and deployment primitives.
    files:
    - path: src/ci_workflows/ciw_web.py
      responsibility: Bounded product-neutral static-web build and verification adapter.
      status: compatibility
      classes:
      - name: StaticWebValidationError
        purpose: Stable static-web adapter failure with optional cleanup detail.
        methods: []
      - name: StaticWebPlan
        purpose: Defines StaticWebPlan for Bounded product-neutral static-web build and verification adapter.
        methods: []
      - name: _RuntimeVerificationRunner
        purpose: Defines _RuntimeVerificationRunner for Bounded product-neutral static-web build and verification adapter.
        methods:
        - name: run
          signature: 'run(self, argv: Sequence[str], *, cwd: Path, env: Mapping[str, str], timeout_seconds: int) -> ProcessOutcome'
          purpose: Implements _RuntimeVerificationRunner.run behavior for Bounded product-neutral static-web build and verification adapter.
      functions:
      - name: configure_static_web_validate
        signature: 'configure_static_web_validate(_parser: argparse.ArgumentParser) -> None'
        purpose: The reusable workflow supplies its bounded request through INPUT_* values.
      - name: _fail
        signature: '_fail(code: str) -> None'
        purpose: Implements _fail for Bounded product-neutral static-web build and verification adapter.
      - name: _plain
        signature: '_plain(value: object, *, code: str, maximum_bytes: int=_MAX_ARGUMENT_BYTES) -> str'
        purpose: Implements _plain for Bounded product-neutral static-web build and verification adapter.
      - name: _relative
        signature: '_relative(value: object, *, code: str, allow_dot: bool=False) -> Path'
        purpose: Implements _relative for Bounded product-neutral static-web build and verification adapter.
      - name: _arguments
        signature: '_arguments(value: object, *, code: str) -> tuple[str, ...]'
        purpose: Implements _arguments for Bounded product-neutral static-web build and verification adapter.
      - name: _expected_files
        signature: '_expected_files(value: object) -> tuple[str, ...]'
        purpose: Implements _expected_files for Bounded product-neutral static-web build and verification adapter.
      - name: _timeout
        signature: '_timeout(value: object, *, code: str, default: int) -> int'
        purpose: Implements _timeout for Bounded product-neutral static-web build and verification adapter.
      - name: _parse_plan
        signature: '_parse_plan(environment: Mapping[str, str]) -> StaticWebPlan'
        purpose: Implements _parse_plan for Bounded product-neutral static-web build and verification adapter.
      - name: _workspace
        signature: '_workspace(environment: Mapping[str, str]) -> Path'
        purpose: Implements _workspace for Bounded product-neutral static-web build and verification adapter.
      - name: _bounded_directory
        signature: '_bounded_directory(workspace: Path, value: str) -> Path'
        purpose: Implements _bounded_directory for Bounded product-neutral static-web build and verification adapter.
      - name: _bounded_executable
        signature: '_bounded_executable(root: Path, value: str, *, code: str) -> Path'
        purpose: Implements _bounded_executable for Bounded product-neutral static-web build and verification adapter.
      - name: _output_candidate
        signature: '_output_candidate(root: Path, value: str) -> Path'
        purpose: Implements _output_candidate for Bounded product-neutral static-web build and verification adapter.
      - name: _selected_environment
        signature: '_selected_environment(environment: Mapping[str, str], *, admitted_sha: str, output: Path, output_relative: str) -> dict[str, str]'
        purpose: Implements _selected_environment for Bounded product-neutral static-web build and verification adapter.
      - name: _sanitize_diagnostic
        signature: '_sanitize_diagnostic(text: str, *, root: Path) -> str'
        purpose: Implements _sanitize_diagnostic for Bounded product-neutral static-web build and verification adapter.
      - name: _emit_build_diagnostic
        signature: '_emit_build_diagnostic(context: CIWContext, outcome: ProcessResult, *, root: Path) -> None'
        purpose: Implements _emit_build_diagnostic for Bounded product-neutral static-web build and verification adapter.
      - name: _verify_expected_files
        signature: '_verify_expected_files(output: Path, expected: Sequence[str]) -> None'
        purpose: Implements _verify_expected_files for Bounded product-neutral static-web build and verification adapter.
      - name: _summary
        signature: '_summary(*, status: str, build_result: str, output_digest: str='''', output_file_count: int=0, expected_file_count: int=0, verification_result: str=''skipped'') ->
          str'
        purpose: Implements _summary for Bounded product-neutral static-web build and verification adapter.
      - name: _failure_outputs
        signature: '_failure_outputs(context: CIWContext, *, code: str, cleanup_result: str, cleanup_code: str, build_result: str) -> None'
        purpose: Implements _failure_outputs for Bounded product-neutral static-web build and verification adapter.
      - name: _adapt_error
        signature: '_adapt_error(error: BaseException) -> StaticWebValidationError'
        purpose: Implements _adapt_error for Bounded product-neutral static-web build and verification adapter.
      - name: execute_static_web_validate
        signature: 'execute_static_web_validate(_args: argparse.Namespace, context: CIWContext) -> CIWResult'
        purpose: Build, inspect and optionally verify one caller-owned static web export.
      - name: main
        signature: 'main(argv: Sequence[str] | None=None) -> int'
        purpose: Implements main for Bounded product-neutral static-web build and verification adapter.
      internal_dependencies:
      - ciw_types
      - runtime_primitives
      - web_primitives
    - path: src/ci_workflows/web_primitives.py
      responsibility: Product-neutral static-web validation and Cloudflare Pages deployment primitives.
      status: consolidate
      classes:
      - name: WebPrimitiveError
        purpose: Stable fail-closed web primitive error.
        methods: []
      - name: ProcessOutcome
        purpose: Defines ProcessOutcome for Product-neutral static-web validation and Cloudflare Pages deployment primitives.
        methods: []
      - name: CommandRunner
        purpose: Defines CommandRunner for Product-neutral static-web validation and Cloudflare Pages deployment primitives.
        methods:
        - name: run
          signature: 'run(self, argv: Sequence[str], *, cwd: Path, env: Mapping[str, str], timeout_seconds: int) -> ProcessOutcome'
          purpose: Implements CommandRunner.run behavior for Product-neutral static-web validation and Cloudflare Pages deployment primitives.
      - name: StaticFile
        purpose: Defines StaticFile for Product-neutral static-web validation and Cloudflare Pages deployment primitives.
        methods: []
      - name: StaticManifest
        purpose: Defines StaticManifest for Product-neutral static-web validation and Cloudflare Pages deployment primitives.
        methods:
        - name: canonical_json
          signature: canonical_json(self) -> str
          purpose: Implements StaticManifest.canonical_json behavior for Product-neutral static-web validation and Cloudflare Pages deployment primitives.
      - name: VerificationResult
        purpose: Defines VerificationResult for Product-neutral static-web validation and Cloudflare Pages deployment primitives.
        methods: []
      - name: PagesDeploymentResult
        purpose: Defines PagesDeploymentResult for Product-neutral static-web validation and Cloudflare Pages deployment primitives.
        methods: []
      functions:
      - name: _require
        signature: '_require(condition: bool, code: str) -> None'
        purpose: Implements _require for Product-neutral static-web validation and Cloudflare Pages deployment primitives.
      - name: _safe_cli_value
        signature: '_safe_cli_value(value: str, code: str, *, maximum: int=255) -> str'
        purpose: Implements _safe_cli_value for Product-neutral static-web validation and Cloudflare Pages deployment primitives.
      - name: _directory
        signature: '_directory(path: Path, code: str) -> Path'
        purpose: Implements _directory for Product-neutral static-web validation and Cloudflare Pages deployment primitives.
      - name: _file_digest
        signature: '_file_digest(path: Path) -> tuple[int, str]'
        purpose: Implements _file_digest for Product-neutral static-web validation and Cloudflare Pages deployment primitives.
      - name: inspect_static_output
        signature: 'inspect_static_output(directory: Path, *, maximum_files: int=100000, maximum_total_bytes: int | None=None) -> StaticManifest'
        purpose: Validate a static output tree and return a deterministic content manifest.
      - name: run_static_verification
        signature: 'run_static_verification(directory: Path, command: Sequence[str], runner: CommandRunner, *, environment: Mapping[str, str], timeout_seconds: int=600) -> VerificationResult'
        purpose: Run one direct-argv product verifier and prove it did not mutate output.
      - name: _state_parent
        signature: '_state_parent(path: Path) -> Path'
        purpose: Implements _state_parent for Product-neutral static-web validation and Cloudflare Pages deployment primitives.
      - name: _remove_state
        signature: '_remove_state(path: Path, *, parent: Path) -> None'
        purpose: Implements _remove_state for Product-neutral static-web validation and Cloudflare Pages deployment primitives.
      - name: _pages_environment
        signature: '_pages_environment(environment: Mapping[str, str], *, account_id: str, workspace: Path, output_file: Path) -> dict[str, str]'
        purpose: Implements _pages_environment for Product-neutral static-web validation and Cloudflare Pages deployment primitives.
      - name: _pages_record
        signature: '_pages_record(path: Path) -> Mapping[str, object]'
        purpose: Implements _pages_record for Product-neutral static-web validation and Cloudflare Pages deployment primitives.
      - name: _record_text
        signature: '_record_text(record: Mapping[str, object], names: Sequence[str]) -> str | None'
        purpose: Implements _record_text for Product-neutral static-web validation and Cloudflare Pages deployment primitives.
      - name: _deployment_url
        signature: '_deployment_url(record: Mapping[str, object]) -> str'
        purpose: Implements _deployment_url for Product-neutral static-web validation and Cloudflare Pages deployment primitives.
      - name: deploy_cloudflare_pages
        signature: 'deploy_cloudflare_pages(directory: Path, runner: CommandRunner, *, project_name: str, account_id: str, branch: str, environment: Mapping[str, str], state_parent:
          Path, commit_hash: str | None=None, timeout_seconds: int=1800) -> PagesDeploymentResult'
        purpose: Deploy a validated static directory with isolated Wrangler state.
      internal_dependencies: []
  workspace:
    target_module: src/ci_workflows/workspace.py
    responsibility: Isolated CI workspace/state setup, cleanup, residue, and core runtime primitives.
    files:
    - path: src/ci_workflows/runtime_primitives.py
      responsibility: Small product-neutral runtime primitives for shared CI functions.
      status: consolidate
      classes:
      - name: RuntimePrimitiveError
        purpose: Fail closed with one stable non-secret runtime primitive code.
        methods: []
      - name: ProcessResult
        purpose: Captured result from one bounded subprocess execution.
        methods:
        - name: ok
          signature: ok(self) -> bool
          purpose: Implements ProcessResult.ok behavior for Small product-neutral runtime primitives for shared CI functions.
      - name: CheckoutResult
        purpose: Stable exact-checkout result without credential material.
        methods: []
      functions:
      - name: _text
        signature: '_text(value: str | bytes | None) -> str'
        purpose: Implements _text for Small product-neutral runtime primitives for shared CI functions.
      - name: run_process
        signature: 'run_process(arguments: Sequence[str], *, cwd: Path, environment: Mapping[str, str], stdin: str='''', timeout_seconds: float | None=None) -> ProcessResult'
        purpose: Run one process with explicit cwd, environment, stdin and timeout.
      - name: normalize_input
        signature: 'normalize_input(name: str, *, argument: str | None=None, environment: Mapping[str, str], default: str='''', required: bool=False, choices: Sequence[str] | None=None)
          -> str'
        purpose: Resolve a non-secret value from an argument or INPUT_* environment.
      - name: secret_environment
        signature: 'secret_environment(name: str, *, environment: Mapping[str, str], required: bool=True) -> str'
        purpose: Read one fixed named secret environment variable without logging it.
      - name: _real_directory
        signature: '_real_directory(path: Path, *, code: str) -> Path'
        purpose: Implements _real_directory for Small product-neutral runtime primitives for shared CI functions.
      - name: create_temporary_workspace
        signature: 'create_temporary_workspace(parent: Path, *, prefix: str=''ciw'') -> Path'
        purpose: Create one mode-0700 temporary workspace beneath a reviewed parent.
      - name: _bounded_target
        signature: '_bounded_target(path: Path, *, root: Path) -> Path'
        purpose: Implements _bounded_target for Small product-neutral runtime primitives for shared CI functions.
      - name: finalize_temporary_path
        signature: 'finalize_temporary_path(path: Path, *, root: Path) -> bool'
        purpose: Idempotently remove a file, symlink or directory beneath a reviewed root.
      - name: finalize_temporary_paths
        signature: 'finalize_temporary_paths(paths: Sequence[Path], *, root: Path) -> int'
        purpose: Finalize bounded temporary/auth paths and return the removal count.
      - name: checkout_exact_repository
        signature: 'checkout_exact_repository(repository: str, source_sha: str, *, workspace: Path, path: str=''source'', fetch_depth: int=1, token_environment: str | None=None, environment:
          Mapping[str, str]) -> CheckoutResult'
        purpose: Check out one exact GitHub repository SHA using an optional named token.
      - name: canonical_json
        signature: 'canonical_json(value: Any) -> str'
        purpose: Serialize JSON deterministically for command outputs and evidence.
      - name: github_output_values
        signature: 'github_output_values(values: Mapping[str, Any]) -> dict[str, str]'
        purpose: Normalize scalar and structured values into single-line GitHub outputs.
      - name: write_github_outputs
        signature: 'write_github_outputs(path: Path, values: Mapping[str, Any]) -> dict[str, str]'
        purpose: Append normalized outputs to a GitHub command file and return them.
      internal_dependencies:
      - source_checkout
    - path: src/ci_workflows/workspace.py
      responsibility: Workflow-scoped workspace preparation, path registration, and cleanup.
      status: keep
      classes:
      - name: WorkspaceContext
        purpose: Defines WorkspaceContext for Workflow-scoped workspace preparation, path registration, and cleanup.
        methods: []
      - name: RegisteredPath
        purpose: Defines RegisteredPath for Workflow-scoped workspace preparation, path registration, and cleanup.
        methods: []
      - name: WorkspaceState
        purpose: Defines WorkspaceState for Workflow-scoped workspace preparation, path registration, and cleanup.
        methods:
        - name: output_values
          signature: output_values(self) -> dict[str, str]
          purpose: Implements WorkspaceState.output_values behavior for Workflow-scoped workspace preparation, path registration, and cleanup.
      - name: CleanupReport
        purpose: Defines CleanupReport for Workflow-scoped workspace preparation, path registration, and cleanup.
        methods:
        - name: output_values
          signature: output_values(self) -> dict[str, str]
          purpose: Implements CleanupReport.output_values behavior for Workflow-scoped workspace preparation, path registration, and cleanup.
      functions:
      - name: _path_entry
        signature: '_path_entry(raw: Any) -> RegisteredPath'
        purpose: Implements _path_entry for Workflow-scoped workspace preparation, path registration, and cleanup.
      - name: _profile_order
        signature: '_profile_order(contract: Mapping[str, Any], profile: str) -> list[str]'
        purpose: Implements _profile_order for Workflow-scoped workspace preparation, path registration, and cleanup.
      - name: _contract_paths
        signature: '_contract_paths(contract: Mapping[str, Any], profile: str) -> list[RegisteredPath]'
        purpose: Implements _contract_paths for Workflow-scoped workspace preparation, path registration, and cleanup.
      - name: _profile_environment
        signature: '_profile_environment(contract: Mapping[str, Any], profile: str, paths: Mapping[str, Path]) -> dict[str, str]'
        purpose: Implements _profile_environment for Workflow-scoped workspace preparation, path registration, and cleanup.
      - name: _cache_settings
        signature: '_cache_settings(contract_root: Path, *, mode: str, repository: str, source_sha: str | None, lock_digest: str | None, runner_os: str, profile: str, trust_mode: str
          | None) -> tuple[str, str | None]'
        purpose: Implements _cache_settings for Workflow-scoped workspace preparation, path registration, and cleanup.
      - name: _state_id
        signature: '_state_id(context: WorkspaceContext) -> str'
        purpose: Implements _state_id for Workflow-scoped workspace preparation, path registration, and cleanup.
      - name: _state_files
        signature: '_state_files(root: Path, contract: Mapping[str, Any]) -> tuple[Path, Path]'
        purpose: Implements _state_files for Workflow-scoped workspace preparation, path registration, and cleanup.
      - name: _write_marker
        signature: '_write_marker(path: Path, *, state_id: str, context: WorkspaceContext, root: Path, profile: str, status: str) -> None'
        purpose: Implements _write_marker for Workflow-scoped workspace preparation, path registration, and cleanup.
      - name: _write_registry
        signature: '_write_registry(path: Path, root: Path, state_id: str, entries: Sequence[RegisteredPath]) -> None'
        purpose: Implements _write_registry for Workflow-scoped workspace preparation, path registration, and cleanup.
      - name: prepare_workspace
        signature: 'prepare_workspace(context: WorkspaceContext, *, profile: str, cache_mode: str=''disabled'', source_sha: str | None=None, lock_digest: str | None=None, trust_mode:
          str | None=None, contract_root: Path) -> WorkspaceState'
        purpose: Create one isolated workflow-scoped state root and strict environment.
      - name: resolve_state_root
        signature: 'resolve_state_root(*, runner_temp: Path, state_id: str, declared_root: str, contract_root: Path) -> Path'
        purpose: Derive state beneath protected runner temp and reject path substitution.
      - name: _load_state
        signature: '_load_state(root: Path, contract_root: Path) -> tuple[Mapping[str, Any], Mapping[str, Any] | None, Mapping[str, Any]]'
        purpose: Implements _load_state for Workflow-scoped workspace preparation, path registration, and cleanup.
      - name: _lexical_target
        signature: '_lexical_target(root: Path, relative: str) -> Path'
        purpose: Implements _lexical_target for Workflow-scoped workspace preparation, path registration, and cleanup.
      - name: register_state_path
        signature: 'register_state_path(state_root: Path, *, name: str, relative: str, kind: str, contract_root: Path, create: bool=True) -> Path'
        purpose: Register one bounded dynamic path beneath a reviewed state root.
      - name: _open_bounded_directory
        signature: '_open_bounded_directory(boundary: Path, directory: Path) -> int'
        purpose: Implements _open_bounded_directory for Workflow-scoped workspace preparation, path registration, and cleanup.
      - name: _make_directory_writable
        signature: '_make_directory_writable(descriptor: int) -> None'
        purpose: Implements _make_directory_writable for Workflow-scoped workspace preparation, path registration, and cleanup.
      - name: _remove_directory_contents
        signature: '_remove_directory_contents(descriptor: int) -> None'
        purpose: Implements _remove_directory_contents for Workflow-scoped workspace preparation, path registration, and cleanup.
      - name: _make_parent_writable
        signature: '_make_parent_writable(path: Path, boundary: Path) -> None'
        purpose: Implements _make_parent_writable for Workflow-scoped workspace preparation, path registration, and cleanup.
      - name: _remove_tree
        signature: '_remove_tree(path: Path, runner_os: str, *, boundary: Path) -> None'
        purpose: Implements _remove_tree for Workflow-scoped workspace preparation, path registration, and cleanup.
      - name: remove_registered_path
        signature: 'remove_registered_path(state_root: Path, *, name: str, contract_root: Path) -> bool'
        purpose: Implements remove_registered_path for Workflow-scoped workspace preparation, path registration, and cleanup.
      - name: cleanup_workspace
        signature: 'cleanup_workspace(state_root: Path, *, expected_state_id: str | None, contract_root: Path) -> CleanupReport'
        purpose: Remove only marker-bound registered state and verify zero residue.
      internal_dependencies:
      - foundation_types
  runners:
    target_module: src/ci_workflows/runners.py
    responsibility: Semantic runner profiles, backend resolution, and runner-image product contracts.
    files:
    - path: src/ci_workflows/execution_backends.py
      responsibility: Small bounded execution-backend selector for reviewed Central work.
      status: consolidate
      classes:
      - name: ExecutionBackendError
        purpose: Defines ExecutionBackendError for Small bounded execution-backend selector for reviewed Central work.
        methods: []
      - name: ExecutionBackendResolution
        purpose: Defines ExecutionBackendResolution for Small bounded execution-backend selector for reviewed Central work.
        methods:
        - name: as_dict
          signature: as_dict(self) -> dict[str, object]
          purpose: Implements ExecutionBackendResolution.as_dict behavior for Small bounded execution-backend selector for reviewed Central work.
      functions:
      - name: resolve_execution_backend
        signature: 'resolve_execution_backend(*, execution_backend: str, execution_profile: str, organization_runs_on: Sequence[str]) -> ExecutionBackendResolution'
        purpose: Preserve organization scheduling or use one fixed reviewed hosted selector.
      internal_dependencies: []
    - path: src/ci_workflows/runner_images.py
      responsibility: Simple shared contract for centrally owned runner images.
      status: keep
      classes:
      - name: RunnerImageError
        purpose: Raised when a runner-image request is outside the fixed contract.
        methods: []
      - name: RunnerImage
        purpose: Defines RunnerImage for Simple shared contract for centrally owned runner images.
        methods:
        - name: dockerfile_path
          signature: dockerfile_path(self) -> str
          purpose: Implements RunnerImage.dockerfile_path behavior for Simple shared contract for centrally owned runner images.
        - name: smoke_script_path
          signature: smoke_script_path(self) -> str
          purpose: Implements RunnerImage.smoke_script_path behavior for Simple shared contract for centrally owned runner images.
      - name: RunnerImagePlan
        purpose: Defines RunnerImagePlan for Simple shared contract for centrally owned runner images.
        methods:
        - name: to_dict
          signature: to_dict(self) -> dict[str, str]
          purpose: Implements RunnerImagePlan.to_dict behavior for Simple shared contract for centrally owned runner images.
      functions:
      - name: validate_source_sha
        signature: 'validate_source_sha(value: str) -> str'
        purpose: Implements validate_source_sha for Simple shared contract for centrally owned runner images.
      - name: validate_release_tag
        signature: 'validate_release_tag(value: str) -> str'
        purpose: Implements validate_release_tag for Simple shared contract for centrally owned runner images.
      - name: resolve_image
        signature: 'resolve_image(image_id: str) -> RunnerImage'
        purpose: Implements resolve_image for Simple shared contract for centrally owned runner images.
      - name: _require_regular_file
        signature: '_require_regular_file(root: Path, relative: str) -> None'
        purpose: Implements _require_regular_file for Simple shared contract for centrally owned runner images.
      - name: build_plan
        signature: 'build_plan(source_root: Path, *, image_id: str, source_sha: str, release_tag: str | None=None) -> RunnerImagePlan'
        purpose: Resolve one fixed image into local and optional GHCR references.
      - name: cleanup_runner_state
        signature: 'cleanup_runner_state(*, image_id: str, workspace: Path, runner_temp: Path, run_id: str, run_attempt: str) -> None'
        purpose: Idempotently remove fixed per-run runner-image temporary state.
      - name: release_matrix
        signature: release_matrix() -> tuple[str, ...]
        purpose: Implements release_matrix for Simple shared contract for centrally owned runner images.
      - name: write_github_outputs
        signature: 'write_github_outputs(handle: TextIO, values: dict[str, str]) -> None'
        purpose: Implements write_github_outputs for Simple shared contract for centrally owned runner images.
      - name: plan_outputs
        signature: 'plan_outputs(plan: RunnerImagePlan) -> dict[str, str]'
        purpose: Implements plan_outputs for Simple shared contract for centrally owned runner images.
      - name: release_outputs
        signature: 'release_outputs(tag: str, source_sha: str) -> dict[str, str]'
        purpose: Implements release_outputs for Simple shared contract for centrally owned runner images.
      internal_dependencies: []
    - path: src/ci_workflows/runners.py
      responsibility: Validate, resolve, and generate the central runner capability contract.
      status: keep
      classes:
      - name: RunnerContractError
        purpose: Defines RunnerContractError for Validate, resolve, and generate the central runner capability contract.
        methods: []
      - name: Resolution
        purpose: Defines Resolution for Validate, resolve, and generate the central runner capability contract.
        methods:
        - name: as_dict
          signature: as_dict(self) -> dict[str, Any]
          purpose: Implements Resolution.as_dict behavior for Validate, resolve, and generate the central runner capability contract.
      functions:
      - name: require
        signature: 'require(value: bool, code: str, message: str) -> None'
        purpose: Implements require for Validate, resolve, and generate the central runner capability contract.
      - name: read_json
        signature: 'read_json(path: Path) -> Any'
        purpose: Implements read_json for Validate, resolve, and generate the central runner capability contract.
      - name: strings
        signature: 'strings(value: Any, code: str, *, empty: bool=True) -> list[str]'
        purpose: Implements strings for Validate, resolve, and generate the central runner capability contract.
      - name: selector
        signature: 'selector(value: Any) -> tuple[str, ...]'
        purpose: Implements selector for Validate, resolve, and generate the central runner capability contract.
      - name: profile_index
        signature: 'profile_index(contract: Mapping[str, Any]) -> dict[str, Mapping[str, Any]]'
        purpose: Implements profile_index for Validate, resolve, and generate the central runner capability contract.
      - name: workflow_binding_index
        signature: 'workflow_binding_index(contract: Mapping[str, Any]) -> dict[str, Mapping[str, Any]]'
        purpose: Implements workflow_binding_index for Validate, resolve, and generate the central runner capability contract.
      - name: profile_alias_index
        signature: 'profile_alias_index(contract: Mapping[str, Any]) -> dict[str, str]'
        purpose: Implements profile_alias_index for Validate, resolve, and generate the central runner capability contract.
      - name: approved_selector_index
        signature: 'approved_selector_index(contract: Mapping[str, Any]) -> dict[tuple[str, ...], str]'
        purpose: Implements approved_selector_index for Validate, resolve, and generate the central runner capability contract.
      - name: validate_runner_contract
        signature: 'validate_runner_contract(contract: Mapping[str, Any]) -> None'
        purpose: Implements validate_runner_contract for Validate, resolve, and generate the central runner capability contract.
      - name: load_runner_contract
        signature: 'load_runner_contract(root: Path) -> dict[str, Any]'
        purpose: Implements load_runner_contract for Validate, resolve, and generate the central runner capability contract.
      - name: validate_caller_inputs
        signature: 'validate_caller_inputs(contract: Mapping[str, Any], inputs: Mapping[str, Any] | None) -> None'
        purpose: Implements validate_caller_inputs for Validate, resolve, and generate the central runner capability contract.
      - name: validate_source
        signature: 'validate_source(profile: Mapping[str, Any], source_trust: str) -> None'
        purpose: Implements validate_source for Validate, resolve, and generate the central runner capability contract.
      - name: validate_device_lock
        signature: 'validate_device_lock(profile: Mapping[str, Any], family: str | None, evidence: Mapping[str, Any] | None) -> str'
        purpose: Implements validate_device_lock for Validate, resolve, and generate the central runner capability contract.
      - name: resolve_runner_profile
        signature: 'resolve_runner_profile(contract: Mapping[str, Any], *, workflow_api: str, source_trust: str, requested_profile: str | None=None, caller_inputs: Mapping[str, Any]
          | None=None, device_family: str | None=None, lock_evidence: Mapping[str, Any] | None=None) -> Resolution'
        purpose: Implements resolve_runner_profile for Validate, resolve, and generate the central runner capability contract.
      - name: validate_direct_selector
        signature: 'validate_direct_selector(contract: Mapping[str, Any], labels: Sequence[str] | str) -> str'
        purpose: Implements validate_direct_selector for Validate, resolve, and generate the central runner capability contract.
      - name: parse_bytes
        signature: 'parse_bytes(value: str) -> int'
        purpose: Implements parse_bytes for Validate, resolve, and generate the central runner capability contract.
      - name: select_buildah_tier
        signature: 'select_buildah_tier(contract: Mapping[str, Any], *, peak_memory_bytes: int, peak_local_storage_bytes: int, headroom_percent: int=20) -> str'
        purpose: Implements select_buildah_tier for Validate, resolve, and generate the central runner capability contract.
      - name: validate_buildah_evidence
        signature: 'validate_buildah_evidence(contract: Mapping[str, Any], evidence: Mapping[str, Any]) -> None'
        purpose: Implements validate_buildah_evidence for Validate, resolve, and generate the central runner capability contract.
      - name: generate_runner_mappings
        signature: 'generate_runner_mappings(contract: Mapping[str, Any]) -> dict[str, Any]'
        purpose: Implements generate_runner_mappings for Validate, resolve, and generate the central runner capability contract.
      - name: load_workflow_inventory
        signature: 'load_workflow_inventory(root: Path) -> Mapping[str, Any]'
        purpose: Implements load_workflow_inventory for Validate, resolve, and generate the central runner capability contract.
      - name: generate_compatibility_report
        signature: 'generate_compatibility_report(contract: Mapping[str, Any], inventory: Mapping[str, Any]) -> dict[str, Any]'
        purpose: Implements generate_compatibility_report for Validate, resolve, and generate the central runner capability contract.
      - name: render_compatibility_markdown
        signature: 'render_compatibility_markdown(report: Mapping[str, Any]) -> str'
        purpose: Implements render_compatibility_markdown for Validate, resolve, and generate the central runner capability contract.
      - name: canonical_json
        signature: 'canonical_json(data: Any) -> str'
        purpose: Implements canonical_json for Validate, resolve, and generate the central runner capability contract.
      - name: generated_outputs
        signature: 'generated_outputs(root: Path) -> dict[Path, str]'
        purpose: Implements generated_outputs for Validate, resolve, and generate the central runner capability contract.
      - name: write_generated_outputs
        signature: 'write_generated_outputs(root: Path, *, check: bool) -> None'
        purpose: Implements write_generated_outputs for Validate, resolve, and generate the central runner capability contract.
      - name: optional_object
        signature: 'optional_object(value: str | None) -> Mapping[str, Any] | None'
        purpose: Implements optional_object for Validate, resolve, and generate the central runner capability contract.
      - name: parser
        signature: parser() -> argparse.ArgumentParser
        purpose: Implements parser for Validate, resolve, and generate the central runner capability contract.
      - name: main
        signature: 'main(argv: Sequence[str] | None=None) -> int'
        purpose: Implements main for Validate, resolve, and generate the central runner capability contract.
      internal_dependencies: []
  private_ci:
    target_module: src/ci_workflows/ci_private.py
    responsibility: Opaque private-CI profile projection and product-neutral trusted execution.
    files:
    - path: src/ci_workflows/central_profile.py
      responsibility: Bounded Central product CI profile projection.
      status: keep
      classes:
      - name: CentralProfileError
        purpose: Defines CentralProfileError for Bounded Central product CI profile projection.
        methods: []
      - name: PrivateDependency
        purpose: Defines PrivateDependency for Bounded Central product CI profile projection.
        methods:
        - name: parse
          signature: 'parse(cls, value: Mapping[str, object]) -> ''PrivateDependency'''
          purpose: Implements PrivateDependency.parse behavior for Bounded Central product CI profile projection.
      - name: LegacyAppleProfile
        purpose: Defines LegacyAppleProfile for Bounded Central product CI profile projection.
        methods: []
      - name: LegacyAppleConfig
        purpose: Defines LegacyAppleConfig for Bounded Central product CI profile projection.
        methods:
        - name: parse
          signature: 'parse(cls, value: Mapping[str, object]) -> ''LegacyAppleConfig'''
          purpose: Implements LegacyAppleConfig.parse behavior for Bounded Central product CI profile projection.
        - name: profile
          signature: 'profile(self, name: object, workflow_key: object) -> LegacyAppleProfile'
          purpose: Implements LegacyAppleConfig.profile behavior for Bounded Central product CI profile projection.
      - name: CentralProfileResolution
        purpose: Defines CentralProfileResolution for Bounded Central product CI profile projection.
        methods:
        - name: output_values
          signature: output_values(self) -> dict[str, str]
          purpose: Implements CentralProfileResolution.output_values behavior for Bounded Central product CI profile projection.
        - name: canonical_inputs
          signature: canonical_inputs(self) -> dict[str, str]
          purpose: Implements CentralProfileResolution.canonical_inputs behavior for Bounded Central product CI profile projection.
        - name: release_asset
          signature: release_asset(self) -> PrivateReleaseAssetSpec | None
          purpose: Implements CentralProfileResolution.release_asset behavior for Bounded Central product CI profile projection.
      functions:
      - name: _require
        signature: '_require(condition: bool, code: str) -> None'
        purpose: Implements _require for Bounded Central product CI profile projection.
      - name: _safe
        signature: '_safe(value: object, pattern: re.Pattern[str], code: str) -> str'
        purpose: Implements _safe for Bounded Central product CI profile projection.
      - name: _safe_project
        signature: '_safe_project(value: object) -> str'
        purpose: Implements _safe_project for Bounded Central product CI profile projection.
      - name: _safe_profile
        signature: '_safe_profile(value: object) -> str'
        purpose: Implements _safe_profile for Bounded Central product CI profile projection.
      - name: _safe_workflow_key
        signature: '_safe_workflow_key(value: object) -> str'
        purpose: Implements _safe_workflow_key for Bounded Central product CI profile projection.
      - name: _safe_repository
        signature: '_safe_repository(value: object) -> str'
        purpose: Implements _safe_repository for Bounded Central product CI profile projection.
      - name: _safe_sha
        signature: '_safe_sha(value: object) -> str'
        purpose: Implements _safe_sha for Bounded Central product CI profile projection.
      - name: _safe_product_scalar
        signature: '_safe_product_scalar(value: object, code: str) -> str'
        purpose: Implements _safe_product_scalar for Bounded Central product CI profile projection.
      - name: _safe_workspace
        signature: '_safe_workspace(value: object) -> str'
        purpose: Implements _safe_workspace for Bounded Central product CI profile projection.
      - name: _safe_dependency_subdirectory
        signature: '_safe_dependency_subdirectory(value: object) -> str'
        purpose: Implements _safe_dependency_subdirectory for Bounded Central product CI profile projection.
      - name: apple_host_validation_plan
        signature: 'apple_host_validation_plan(workspace: object, scheme: object, test_target: object) -> str'
        purpose: Implements apple_host_validation_plan for Bounded Central product CI profile projection.
      - name: _source_root
        signature: '_source_root(value: object) -> Path'
        purpose: Implements _source_root for Bounded Central product CI profile projection.
      - name: _read_config
        signature: '_read_config(source_root: Path) -> Mapping[str, object]'
        purpose: Implements _read_config for Bounded Central product CI profile projection.
      - name: _strip_release_assets
        signature: '_strip_release_assets(value: Mapping[str, object]) -> tuple[dict[str, object], dict[str, PrivateReleaseAssetSpec]]'
        purpose: Implements _strip_release_assets for Bounded Central product CI profile projection.
      - name: _relative_path
        signature: '_relative_path(value: object, code: str, *, allow_empty: bool=False) -> str'
        purpose: Implements _relative_path for Bounded Central product CI profile projection.
      - name: _json_plan
        signature: '_json_plan(value: object, code: str, *, allow_empty: bool=False) -> str'
        purpose: Implements _json_plan for Bounded Central product CI profile projection.
      - name: _generic_inputs
        signature: '_generic_inputs(workflow_key: str, value: object) -> dict[str, str]'
        purpose: Implements _generic_inputs for Bounded Central product CI profile projection.
      - name: _select_v2_profile
        signature: '_select_v2_profile(raw_profiles: Mapping[str, object], *, checked_profile: str, checked_workflow: str) -> dict[str, object]'
        purpose: Implements _select_v2_profile for Bounded Central product CI profile projection.
      - name: _resolve_v1_apple
        signature: '_resolve_v1_apple(value: Mapping[str, object], *, checked_project: str, checked_workflow: str, checked_profile: str, checked_repository: str, checked_sha: str) ->
          CentralProfileResolution'
        purpose: Implements _resolve_v1_apple for Bounded Central product CI profile projection.
      - name: _resolve_v2_generic
        signature: '_resolve_v2_generic(value: Mapping[str, object], *, checked_project: str, checked_workflow: str, checked_profile: str, checked_repository: str, checked_sha: str)
          -> CentralProfileResolution'
        purpose: Implements _resolve_v2_generic for Bounded Central product CI profile projection.
      - name: resolve_profile
        signature: 'resolve_profile(*, source_root: object, project_key: object, workflow_key: object, test_profile: object, source_repository: object, admitted_sha: object) -> CentralProfileResolution'
        purpose: Implements resolve_profile for Bounded Central product CI profile projection.
      - name: _required
        signature: '_required(environment: Mapping[str, str], name: str) -> str'
        purpose: Implements _required for Bounded Central product CI profile projection.
      - name: resolve_from_environment
        signature: 'resolve_from_environment(environment: Mapping[str, str]=os.environ) -> CentralProfileResolution'
        purpose: Implements resolve_from_environment for Bounded Central product CI profile projection.
      - name: parser
        signature: parser() -> argparse.ArgumentParser
        purpose: Implements parser for Bounded Central product CI profile projection.
      - name: main
        signature: 'main(argv: Sequence[str] | None=None) -> int'
        purpose: Implements main for Bounded Central product CI profile projection.
      internal_dependencies:
      - private_release_asset
    - path: src/ci_workflows/ci_private.py
      responsibility: Product-neutral opaque private Central CI execution.
      status: keep
      classes:
      - name: PrivateCiError
        purpose: Stable public-safe failure for product-neutral opaque execution.
        methods: []
      functions:
      - name: _require
        signature: '_require(condition: bool, code: str) -> None'
        purpose: Implements _require for private CI execution.
      - name: _required
        signature: '_required(environment: Mapping[str, str], name: str) -> str'
        purpose: Implements _required for private CI execution.
      - name: _positive_int
        signature: '_positive_int(environment: Mapping[str, str], name: str, maximum: int) -> int'
        purpose: Implements _positive_int for private CI execution.
      - name: _runner_temp
        signature: '_runner_temp(environment: Mapping[str, str]) -> Path'
        purpose: Implements _runner_temp for private CI execution.
      - name: _workspace
        signature: '_workspace(environment: Mapping[str, str]) -> Path'
        purpose: Implements _workspace for private CI execution.
      - name: _state_root
        signature: '_state_root(environment: Mapping[str, str], ci_run_id: str) -> Path'
        purpose: Implements _state_root for private CI execution.
      - name: _state_path
        signature: '_state_path(environment: Mapping[str, str], ci_run_id: str) -> Path'
        purpose: Implements _state_path for private CI execution.
      - name: _log_path
        signature: '_log_path(environment: Mapping[str, str], ci_run_id: str) -> Path'
        purpose: Implements _log_path for private CI execution.
      - name: _write_state
        signature: '_write_state(environment: Mapping[str, str], ci_run_id: str, value: Mapping[str, object]) -> None'
        purpose: Implements _write_state for private CI execution.
      - name: _read_state
        signature: '_read_state(environment: Mapping[str, str], ci_run_id: str) -> dict[str, Any] | None'
        purpose: Implements _read_state for private CI execution.
      - name: _ensure_log
        signature: '_ensure_log(environment: Mapping[str, str], ci_run_id: str) -> Path'
        purpose: Implements _ensure_log for private CI execution.
      - name: _append
        signature: '_append(log: TextIO, message: str) -> None'
        purpose: Implements _append for private CI execution.
      - name: _stable_code
        signature: '_stable_code(error: BaseException) -> str'
        purpose: Implements _stable_code for private CI execution.
      - name: _remove_path
        signature: '_remove_path(path: Path) -> None'
        purpose: Implements _remove_path for private CI execution.
      - name: _append_state_logs
        signature: '_append_state_logs(root: Path, log: TextIO) -> None'
        purpose: Implements _append_state_logs for private CI execution.
      - name: _claim_request
        signature: '_claim_request(client: AgentStateCiClient, ci_run_id: str) -> RelayRequest'
        purpose: Implements _claim_request for private CI execution.
      - name: _start_lifecycle
        signature: '_start_lifecycle(client: AgentStateCiClient, request: RelayRequest, environment: Mapping[str, str]) -> None'
        purpose: Implements _start_lifecycle for private CI execution.
      - name: _source_token_client
        signature: '_source_token_client(environment: Mapping[str, str]) -> GitHubAppRepositoryTokenClient'
        purpose: Implements _source_token_client for private CI execution.
      - name: _resolve_source_sha
        signature: '_resolve_source_sha(request: RelayRequest, token: str) -> str'
        purpose: Implements _resolve_source_sha for private CI execution.
      - name: _checkout_source
        signature: '_checkout_source(*, repository: str, source_sha: str, token: str, workspace: Path, log: TextIO) -> Path'
        purpose: Implements _checkout_source for private CI execution.
      - name: _dependency_receipts
        signature: '_dependency_receipts(*, resolution: CentralProfileResolution, token_client: GitHubAppRepositoryTokenClient, workspace_state_root: Path, contract_root: Path, log:
          TextIO) -> DependencyResult | None'
        purpose: Implements _dependency_receipts for private CI execution.
      - name: _execution_environment
        signature: '_execution_environment(*, request: RelayRequest, source_sha: str, resolution: CentralProfileResolution, dependency: DependencyResult | None, base: Mapping[str, str],
          workspace_environment: Mapping[str, str]) -> dict[str, str]'
        purpose: Implements _execution_environment for private CI execution.
      - name: _product_base_environment
        signature: '_product_base_environment(base: Mapping[str, str]) -> dict[str, str]'
        purpose: Remove opaque transport/action inputs before entering product validators.
      - name: _canonical_environment
        signature: '_canonical_environment(*, request: RelayRequest, source_sha: str, resolution: CentralProfileResolution, dependency: DependencyResult | None, base: Mapping[str, str],
          workspace_environment: Mapping[str, str]) -> dict[str, str]'
        purpose: Implements _canonical_environment for private CI execution.
      - name: _runner_os
        signature: '_runner_os(environment: Mapping[str, str]) -> str'
        purpose: Implements _runner_os for private CI execution.
      - name: _workspace_state
        signature: '_workspace_state(*, request: RelayRequest, source_sha: str, environment: Mapping[str, str], profile: str)'
        purpose: Implements _workspace_state for private CI execution.
      - name: _family_for_request
        signature: '_family_for_request(request: RelayRequest) -> str'
        purpose: Implements _family_for_request for private CI execution.
      - name: _verify_family
        signature: '_verify_family(resolution: CentralProfileResolution, environment: Mapping[str, str]) -> None'
        purpose: Implements _verify_family for private CI execution.
      - name: plan_private_ci
        signature: 'plan_private_ci(environment: Mapping[str, str]=os.environ) -> str'
        purpose: Privately reclaim one request and expose only its coarse hosted family.
      - name: _execute_apple
        signature: '_execute_apple(*, request: RelayRequest, source_sha: str, resolution: CentralProfileResolution, token_client: GitHubAppRepositoryTokenClient, environment: Mapping[str,
          str], log: TextIO) -> tuple[bool, bool]'
        purpose: Implements _execute_apple for private CI execution.
      - name: _execute_android
        signature: '_execute_android(*, request: RelayRequest, source_sha: str, resolution: CentralProfileResolution, token_client: GitHubAppRepositoryTokenClient, environment: Mapping[str,
          str], log: TextIO) -> tuple[bool, bool]'
        purpose: Implements _execute_android for private CI execution.
      - name: _execute_python
        signature: '_execute_python(*, request: RelayRequest, source_sha: str, resolution: CentralProfileResolution, environment: Mapping[str, str], log: TextIO) -> tuple[bool, bool]'
        purpose: Implements _execute_python for private CI execution.
      - name: _execute_family
        signature: '_execute_family(*, request: RelayRequest, source_sha: str, resolution: CentralProfileResolution, token_client: GitHubAppRepositoryTokenClient, environment: Mapping[str,
          str], log: TextIO) -> tuple[bool, bool]'
        purpose: Implements _execute_family for private CI execution.
      - name: _upload_private_log
        signature: '_upload_private_log(*, ci_run_id: str, log_path: Path, environment: Mapping[str, str]) -> R2DiagnosticResult'
        purpose: Implements _upload_private_log for private CI execution.
      - name: _receipt
        signature: '_receipt(result: R2DiagnosticResult) -> str'
        purpose: Implements _receipt for private CI execution.
      - name: execute_private_ci
        signature: 'execute_private_ci(environment: Mapping[str, str]=os.environ) -> bool'
        purpose: Implements execute_private_ci for private CI execution.
      - name: recover_private_ci
        signature: 'recover_private_ci(environment: Mapping[str, str]=os.environ) -> None'
        purpose: Implements recover_private_ci for private CI execution.
      - name: parser
        signature: parser() -> argparse.ArgumentParser
        purpose: Implements parser for private CI execution.
      - name: main
        signature: 'main(argv: Sequence[str] | None=None, environment: Mapping[str, str]=os.environ) -> int'
        purpose: Implements main for private CI execution.
      internal_dependencies:
      - central_profile
      - ci_lifecycle
      - ciw_android
      - ciw_apple
      - ciw_python
      - ciw_types
      - dependencies
      - github_app_token
      - private_release_asset
      - r2_diagnostics
      - source_admission
      - source_checkout
      - source_github
      - workspace
  agent_state:
    target_module: src/ci_workflows/agent_state.py
    responsibility: CI-only Agent State lifecycle request/evidence operations.
    files:
    - path: src/ci_workflows/ci_lifecycle.py
      responsibility: Small Agent State lifecycle client shared by canonical Central CI workflows.
      status: consolidate
      classes:
      - name: CiLifecycleError
        purpose: Stable non-sensitive lifecycle failure.
        methods: []
      - name: RelayRequest
        purpose: Canonical claimed Agent State request used only inside trusted Central CI.
        methods:
        - name: from_claimed_run
          signature: 'from_claimed_run(cls, value: Mapping[str, object]) -> ''RelayRequest'''
          purpose: Implements RelayRequest.from_claimed_run behavior for Small Agent State lifecycle client shared by canonical Central CI workflows.
      - name: WorkflowIdentity
        purpose: Defines WorkflowIdentity for Small Agent State lifecycle client shared by canonical Central CI workflows.
        methods:
        - name: from_values
          signature: 'from_values(cls, *, project_key: object, repository: object, ref: object, is_tag: object, workflow_key: object, profile: object, environment: Mapping[str, str]=os.environ)
            -> ''WorkflowIdentity'''
          purpose: Implements WorkflowIdentity.from_values behavior for Small Agent State lifecycle client shared by canonical Central CI workflows.
        - name: registration
          signature: registration(self) -> dict[str, object]
          purpose: Implements WorkflowIdentity.registration behavior for Small Agent State lifecycle client shared by canonical Central CI workflows.
        - name: running_patch
          signature: running_patch(self) -> dict[str, object]
          purpose: Implements WorkflowIdentity.running_patch behavior for Small Agent State lifecycle client shared by canonical Central CI workflows.
      - name: AgentStateCiClient
        purpose: RPC-only CI lifecycle client for GitHub Actions.
        methods:
        - name: from_environment
          signature: 'from_environment(cls, environment: Mapping[str, str]=os.environ, *, opener: Any=urllib.request.urlopen) -> ''AgentStateCiClient'''
          purpose: Implements AgentStateCiClient.from_environment behavior for Small Agent State lifecycle client shared by canonical Central CI workflows.
        - name: _rpc
          signature: '_rpc(self, name: str, args: Mapping[str, object]) -> dict[str, Any]'
          purpose: Implements AgentStateCiClient._rpc behavior for Small Agent State lifecycle client shared by canonical Central CI workflows.
        - name: _run
          signature: '_run(value: Mapping[str, object]) -> dict[str, Any]'
          purpose: Implements AgentStateCiClient._run behavior for Small Agent State lifecycle client shared by canonical Central CI workflows.
        - name: start
          signature: 'start(self, identity: WorkflowIdentity, ci_run_id: str | None=None) -> str'
          purpose: Implements AgentStateCiClient.start behavior for Small Agent State lifecycle client shared by canonical Central CI workflows.
        - name: evidence
          signature: 'evidence(self, ci_run_id: str, observed_sha: str) -> None'
          purpose: Implements AgentStateCiClient.evidence behavior for Small Agent State lifecycle client shared by canonical Central CI workflows.
        - name: finish
          signature: 'finish(self, ci_run_id: str, *, status: str, error_summary: str | None=None, diagnostic_status: str | None=None, diagnostic_key: str | None=None) -> None'
          purpose: Implements AgentStateCiClient.finish behavior for Small Agent State lifecycle client shared by canonical Central CI workflows.
      functions:
      - name: _require
        signature: '_require(condition: bool, code: str) -> None'
        purpose: Implements _require for Small Agent State lifecycle client shared by canonical Central CI workflows.
      - name: _canonical
        signature: '_canonical(value: object) -> bytes'
        purpose: Implements _canonical for Small Agent State lifecycle client shared by canonical Central CI workflows.
      - name: _uuid
        signature: '_uuid(value: object) -> str'
        purpose: Implements _uuid for Small Agent State lifecycle client shared by canonical Central CI workflows.
      - name: _safe_project
        signature: '_safe_project(value: object) -> str'
        purpose: Implements _safe_project for Small Agent State lifecycle client shared by canonical Central CI workflows.
      - name: _safe_repository
        signature: '_safe_repository(value: object) -> str'
        purpose: Implements _safe_repository for Small Agent State lifecycle client shared by canonical Central CI workflows.
      - name: _safe_ref
        signature: '_safe_ref(value: object) -> str'
        purpose: Implements _safe_ref for Small Agent State lifecycle client shared by canonical Central CI workflows.
      - name: _safe_workflow
        signature: '_safe_workflow(value: object) -> str'
        purpose: Implements _safe_workflow for Small Agent State lifecycle client shared by canonical Central CI workflows.
      - name: _safe_profile
        signature: '_safe_profile(value: object) -> str'
        purpose: Implements _safe_profile for Small Agent State lifecycle client shared by canonical Central CI workflows.
      - name: _safe_sha
        signature: '_safe_sha(value: object) -> str'
        purpose: Implements _safe_sha for Small Agent State lifecycle client shared by canonical Central CI workflows.
      - name: _safe_optional_scalar
        signature: '_safe_optional_scalar(value: object, code: str, limit: int=256) -> str | None'
        purpose: Implements _safe_optional_scalar for Small Agent State lifecycle client shared by canonical Central CI workflows.
      - name: _bool
        signature: '_bool(value: object) -> bool'
        purpose: Implements _bool for Small Agent State lifecycle client shared by canonical Central CI workflows.
      - name: _required_environment
        signature: '_required_environment(environment: Mapping[str, str], name: str) -> str'
        purpose: Implements _required_environment for Small Agent State lifecycle client shared by canonical Central CI workflows.
      - name: _write_output
        signature: '_write_output(name: str, value: str, environment: Mapping[str, str]) -> None'
        purpose: Implements _write_output for Small Agent State lifecycle client shared by canonical Central CI workflows.
      - name: _identity_from_environment
        signature: '_identity_from_environment(environment: Mapping[str, str]) -> WorkflowIdentity'
        purpose: Implements _identity_from_environment for Small Agent State lifecycle client shared by canonical Central CI workflows.
      - name: lifecycle_start
        signature: 'lifecycle_start(environment: Mapping[str, str]=os.environ) -> None'
        purpose: Implements lifecycle_start for Small Agent State lifecycle client shared by canonical Central CI workflows.
      - name: lifecycle_evidence
        signature: 'lifecycle_evidence(environment: Mapping[str, str]=os.environ) -> None'
        purpose: Implements lifecycle_evidence for Small Agent State lifecycle client shared by canonical Central CI workflows.
      - name: lifecycle_finish
        signature: 'lifecycle_finish(environment: Mapping[str, str]=os.environ) -> None'
        purpose: Implements lifecycle_finish for Small Agent State lifecycle client shared by canonical Central CI workflows.
      - name: parser
        signature: parser() -> argparse.ArgumentParser
        purpose: Implements parser for Small Agent State lifecycle client shared by canonical Central CI workflows.
      - name: main
        signature: 'main(argv: Sequence[str] | None=None, environment: Mapping[str, str]=os.environ) -> int'
        purpose: Implements main for Small Agent State lifecycle client shared by canonical Central CI workflows.
      internal_dependencies: []
  r2:
    target_module: src/ci_workflows/r2.py
    responsibility: Cloudflare R2 private diagnostic storage, read-back, compression, digest, and receipt helpers.
    files:
    - path: src/ci_workflows/r2_diagnostics.py
      responsibility: Upload or retrieve one bounded private CI diagnostic through Cloudflare R2.
      status: consolidate
      classes:
      - name: R2DiagnosticError
        purpose: Fail-closed error whose code contains no remote configuration or content.
        methods: []
      - name: R2DiagnosticResult
        purpose: Defines R2DiagnosticResult for Upload or retrieve one bounded private CI diagnostic through Cloudflare R2.
        methods: []
      functions:
      - name: _require
        signature: '_require(condition: bool, code: str) -> None'
        purpose: Implements _require for Upload or retrieve one bounded private CI diagnostic through Cloudflare R2.
      - name: _sha256
        signature: '_sha256(data: bytes) -> str'
        purpose: Implements _sha256 for Upload or retrieve one bounded private CI diagnostic through Cloudflare R2.
      - name: _sign
        signature: '_sign(key: bytes, value: str) -> bytes'
        purpose: Implements _sign for Upload or retrieve one bounded private CI diagnostic through Cloudflare R2.
      - name: _authorization_headers
        signature: '_authorization_headers(*, method: str, url: str, payload_hash: str, access_key_id: str, secret_access_key: str, when: datetime, storage_class: str | None=None) ->
          dict[str, str]'
        purpose: Implements _authorization_headers for Upload or retrieve one bounded private CI diagnostic through Cloudflare R2.
      - name: _r2_url
        signature: '_r2_url(account_id: str, bucket: str, object_key: str) -> str'
        purpose: Implements _r2_url for Upload or retrieve one bounded private CI diagnostic through Cloudflare R2.
      - name: _credentials
        signature: '_credentials(access_key_id: str, secret_access_key: str) -> None'
        purpose: Implements _credentials for Upload or retrieve one bounded private CI diagnostic through Cloudflare R2.
      - name: _validated_identity
        signature: '_validated_identity(request_id: str, run_id: int, attempt: int) -> tuple[str, int, int]'
        purpose: Implements _validated_identity for Upload or retrieve one bounded private CI diagnostic through Cloudflare R2.
      - name: _object_capability
        signature: '_object_capability(*, request_id: str, run_id: int, attempt: int, digest: str, secret_access_key: str) -> str'
        purpose: Implements _object_capability for Upload or retrieve one bounded private CI diagnostic through Cloudflare R2.
      - name: _capability_object_key
        signature: '_capability_object_key(*, request_id: str, run_id: int, attempt: int, digest: str, secret_access_key: str) -> str'
        purpose: Implements _capability_object_key for Upload or retrieve one bounded private CI diagnostic through Cloudflare R2.
      - name: _validated_existing_object_key
        signature: '_validated_existing_object_key(object_key: str) -> str'
        purpose: Implements _validated_existing_object_key for Upload or retrieve one bounded private CI diagnostic through Cloudflare R2.
      - name: is_capability_object_key
        signature: 'is_capability_object_key(object_key: object) -> bool'
        purpose: Implements is_capability_object_key for Upload or retrieve one bounded private CI diagnostic through Cloudflare R2.
      - name: _read_diagnostic
        signature: '_read_diagnostic(path: Path) -> bytes'
        purpose: Implements _read_diagnostic for Upload or retrieve one bounded private CI diagnostic through Cloudflare R2.
      - name: download_private_diagnostic
        signature: 'download_private_diagnostic(*, object_key: str, expected_sha256: str, account_id: str, bucket: str, access_key_id: str, secret_access_key: str, opener: Any=urllib.request.urlopen,
          clock: Callable[[], datetime]=lambda: datetime.now(timezone.utc)) -> bytes'
        purpose: GET one exact generated object and verify its bounded compressed digest.
      - name: _as_readback_error
        signature: '_as_readback_error(error: R2DiagnosticError) -> R2DiagnosticError'
        purpose: Implements _as_readback_error for Upload or retrieve one bounded private CI diagnostic through Cloudflare R2.
      - name: upload_private_diagnostic
        signature: 'upload_private_diagnostic(*, diagnostic_path: Path, request_id: str, run_id: int, attempt: int, account_id: str, bucket: str, access_key_id: str, secret_access_key:
          str, opener: Any=urllib.request.urlopen, clock: Callable[[], datetime]=lambda: datetime.now(timezone.utc)) -> R2DiagnosticResult'
        purpose: Compress, PUT, GET, and digest-verify exactly one private diagnostic object.
      internal_dependencies: []
  packaging:
    target_module: src/ci_workflows/packaging_primitives.py
    responsibility: Container and Helm packaging tool primitives.
    files:
    - path: src/ci_workflows/packaging_primitives.py
      responsibility: Product-neutral OCI/Docker/Helm packaging process primitives.
      status: keep
      classes:
      - name: PackagingError
        purpose: Raised when a packaging primitive receives invalid input or a tool fails.
        methods: []
      - name: ImageReference
        purpose: Structured image reference returned by build/tag/push primitives.
        methods: []
      - name: ImageInspection
        purpose: Local image inspection without publication/read-back policy.
        methods: []
      - name: HelmPackage
        purpose: One chart archive created by ``helm package``.
        methods: []
      - name: HelmPublishResult
        purpose: Structured result of an ordinary Helm OCI push.
        methods: []
      - name: PlatformExecution
        purpose: One requested platform and the runner execution strategy that can run it.
        methods: []
      - name: PlatformBuildResult
        purpose: Structured result of one platform-specific image build.
        methods: []
      - name: OCIManifestDescriptor
        purpose: Normalized descriptor in an OCI index or Docker manifest list.
        methods: []
      - name: OCIIndexInspection
        purpose: Structured local multi-platform manifest/index inspection.
        methods: []
      - name: MultiPlatformBuildResult
        purpose: Per-platform builds together with the assembled final index.
        methods: []
      functions:
      - name: _require
        signature: '_require(condition: bool, message: str) -> None'
        purpose: Implements _require for Product-neutral OCI/Docker/Helm packaging process primitives.
      - name: _text
        signature: '_text(value: str, field: str) -> str'
        purpose: Implements _text for Product-neutral OCI/Docker/Helm packaging process primitives.
      - name: _tool
        signature: '_tool(value: str, allowed: frozenset[str]) -> str'
        purpose: Implements _tool for Product-neutral OCI/Docker/Helm packaging process primitives.
      - name: _environment
        signature: '_environment(environment: Mapping[str, str] | None) -> dict[str, str]'
        purpose: Implements _environment for Product-neutral OCI/Docker/Helm packaging process primitives.
      - name: _run
        signature: '_run(argv: Sequence[str], *, cwd: Path | None, environment: Mapping[str, str] | None, stdin: str | None=None, timeout: int=300) -> subprocess.CompletedProcess[str]'
        purpose: Implements _run for Product-neutral OCI/Docker/Helm packaging process primitives.
      - name: _secret
        signature: '_secret(environment: Mapping[str, str], name: str) -> str'
        purpose: Implements _secret for Product-neutral OCI/Docker/Helm packaging process primitives.
      - name: registry_authenticate
        signature: 'registry_authenticate(registry: str, *, environment: Mapping[str, str], tool: str=''docker'', cwd: Path | None=None) -> None'
        purpose: Authenticate using the fixed central registry credential environment names.
      - name: registry_logout
        signature: 'registry_logout(registry: str, *, environment: Mapping[str, str] | None=None, tool: str=''docker'', cwd: Path | None=None) -> None'
        purpose: Remove registry login state for a supported client.
      - name: build_image
        signature: 'build_image(context: Path, dockerfile: Path, tag: str, *, build_args: Mapping[str, str] | None=None, environment: Mapping[str, str] | None=None, tool: str=''docker'')
          -> ImageReference'
        purpose: Build one local image with Docker, Podman, or Buildah.
      - name: inspect_image
        signature: 'inspect_image(reference: str, *, environment: Mapping[str, str] | None=None, tool: str=''docker'', cwd: Path | None=None) -> ImageInspection'
        purpose: Inspect one local image and normalize common identity fields.
      - name: tag_image
        signature: 'tag_image(source: str, target: str, *, environment: Mapping[str, str] | None=None, tool: str=''docker'', cwd: Path | None=None) -> ImageReference'
        purpose: Apply one caller-owned image tag.
      - name: push_image
        signature: 'push_image(reference: str, *, environment: Mapping[str, str] | None=None, tool: str=''docker'', cwd: Path | None=None) -> ImageReference'
        purpose: Push one caller-owned image reference without adding read-back policy.
      - name: _chart
        signature: '_chart(chart: Path) -> Path'
        purpose: Implements _chart for Product-neutral OCI/Docker/Helm packaging process primitives.
      - name: _values
        signature: '_values(values: Sequence[Path]) -> list[str]'
        purpose: Implements _values for Product-neutral OCI/Docker/Helm packaging process primitives.
      - name: helm_dependency_build
        signature: 'helm_dependency_build(chart: Path, *, environment: Mapping[str, str] | None=None, tool: str=''helm'') -> None'
        purpose: Run ``helm dependency build`` for a caller-owned chart.
      - name: helm_lint
        signature: 'helm_lint(chart: Path, *, values: Sequence[Path]=(), strict: bool=True, environment: Mapping[str, str] | None=None, tool: str=''helm'') -> None'
        purpose: Lint a caller-owned chart with optional values files.
      - name: helm_template
        signature: 'helm_template(chart: Path, *, values: Sequence[Path]=(), release_name: str=''release'', namespace: str | None=None, environment: Mapping[str, str] | None=None, tool:
          str=''helm'') -> str'
        purpose: Render a caller-owned chart and return the rendered manifest.
      - name: helm_package
        signature: 'helm_package(chart: Path, destination: Path, *, version: str | None=None, app_version: str | None=None, environment: Mapping[str, str] | None=None, tool: str=''helm'')
          -> HelmPackage'
        purpose: Package a caller-owned chart and return the created archive path.
      - name: helm_push
        signature: 'helm_push(package: Path | HelmPackage, repository: str, *, environment: Mapping[str, str] | None=None, tool: str=''helm'') -> HelmPublishResult'
        purpose: Push one chart archive to a caller-owned OCI repository.
      - name: _remove_no_follow
        signature: '_remove_no_follow(path: Path) -> None'
        purpose: Implements _remove_no_follow for Product-neutral OCI/Docker/Helm packaging process primitives.
      - name: cleanup_packaging_state
        signature: 'cleanup_packaging_state(paths: Sequence[Path]) -> None'
        purpose: Remove caller-designated registry/layout/package state without following symlinks.
      - name: _normalize_platform
        signature: '_normalize_platform(value: str) -> str'
        purpose: Implements _normalize_platform for Product-neutral OCI/Docker/Helm packaging process primitives.
      - name: normalize_platforms
        signature: 'normalize_platforms(platforms: Sequence[str]) -> tuple[str, ...]'
        purpose: Normalize a caller platform set into one deterministic canonical tuple.
      - name: _emulated_platforms
        signature: '_emulated_platforms(environment: Mapping[str, str]) -> tuple[str, ...]'
        purpose: Implements _emulated_platforms for Product-neutral OCI/Docker/Helm packaging process primitives.
      - name: resolve_platform_execution
        signature: 'resolve_platform_execution(platform: str, *, environment: Mapping[str, str] | None=None) -> PlatformExecution'
        purpose: Resolve native versus explicit runner-provided emulation for one platform.
      - name: plan_platform_executions
        signature: 'plan_platform_executions(platforms: Sequence[str], *, environment: Mapping[str, str] | None=None) -> tuple[PlatformExecution, ...]'
        purpose: Preflight every requested platform before any Dockerfile build starts.
      - name: _platform_tags
        signature: '_platform_tags(platform_tags: Mapping[str, str]) -> dict[str, str]'
        purpose: Implements _platform_tags for Product-neutral OCI/Docker/Helm packaging process primitives.
      - name: _platform_build_argv
        signature: '_platform_build_argv(context: Path, dockerfile: Path, tag: str, execution: PlatformExecution, *, build_args: Mapping[str, str] | None, tool: str) -> list[str]'
        purpose: Implements _platform_build_argv for Product-neutral OCI/Docker/Helm packaging process primitives.
      - name: _execute_platform_build
        signature: '_execute_platform_build(context: Path, dockerfile: Path, tag: str, execution: PlatformExecution, *, build_args: Mapping[str, str] | None, environment: Mapping[str,
          str] | None, tool: str) -> PlatformBuildResult'
        purpose: Implements _execute_platform_build for Product-neutral OCI/Docker/Helm packaging process primitives.
      - name: build_platform_image
        signature: 'build_platform_image(context: Path, dockerfile: Path, tag: str, platform: str, *, build_args: Mapping[str, str] | None=None, environment: Mapping[str, str] | None=None,
          tool: str=''docker'') -> PlatformBuildResult'
        purpose: Build one platform only after the runner proves compatible execution capacity.
      - name: build_platform_images
        signature: 'build_platform_images(context: Path, dockerfile: Path, platforms: Sequence[str], platform_tags: Mapping[str, str], *, build_args: Mapping[str, str] | None=None, environment:
          Mapping[str, str] | None=None, tool: str=''docker'') -> tuple[PlatformBuildResult, ...]'
        purpose: Preflight the full platform set, then build every platform in canonical order.
      - name: _descriptor_platform
        signature: '_descriptor_platform(value: Mapping[str, object]) -> str'
        purpose: Implements _descriptor_platform for Product-neutral OCI/Docker/Helm packaging process primitives.
      - name: inspect_multi_platform_index
        signature: 'inspect_multi_platform_index(reference: str, expected_platforms: Sequence[str], *, environment: Mapping[str, str] | None=None, tool: str=''buildah'', cwd: Path |
          None=None) -> OCIIndexInspection'
        purpose: Inspect and normalize one local OCI index or Docker manifest list.
      - name: assemble_multi_platform_index
        signature: 'assemble_multi_platform_index(reference: str, builds: Sequence[PlatformBuildResult], *, environment: Mapping[str, str] | None=None, tool: str=''buildah'', cwd: Path
          | None=None) -> OCIIndexInspection'
        purpose: Assemble successful platform images and verify the final local index shape.
      - name: build_multi_platform_image
        signature: 'build_multi_platform_image(context: Path, dockerfile: Path, index_reference: str, platforms: Sequence[str], platform_tags: Mapping[str, str], *, build_args: Mapping[str,
          str] | None=None, environment: Mapping[str, str] | None=None, tool: str=''buildah'', cwd: Path | None=None) -> MultiPlatformBuildResult'
        purpose: Build every compatible platform and return the verified assembled index.
      - name: cleanup_multi_platform_state
        signature: 'cleanup_multi_platform_state(paths: Sequence[Path]) -> None'
        purpose: Remove caller-designated per-platform OCI layouts/build state no-follow.
      internal_dependencies: []
  public_api:
    target_module: src/ci_workflows/public_api.py
    responsibility: Public reusable-workflow API registry, compatibility contract, and public CI admission policy.
    files:
    - path: src/ci_workflows/public_api.py
      responsibility: Public API contract facade for the owner-selected initial ``@main`` channel.
      status: keep
      classes: []
      functions:
      - name: permission_profiles
        signature: 'permission_profiles(data: ContractData) -> dict[str, Mapping[str, Any]]'
        purpose: Validate exactly the permission profiles used by supported public APIs.
      - name: validate_workflows
        signature: 'validate_workflows(data: ContractData, profiles: Mapping[str, Mapping[str, Any]]) -> dict[str, Mapping[str, Any]]'
        purpose: Validate only demonstrated callable APIs as the supported catalogue.
      - name: validate_reference_policy
        signature: 'validate_reference_policy(types: Mapping[str, Any]) -> None'
        purpose: Validate protected ``main`` plus fixed SHA and SemVer alternatives.
      - name: validate
        signature: 'validate(root: Path) -> ContractData'
        purpose: Validate the complete supported API registry under current policy.
      internal_dependencies:
      - public_api_contract
    - path: src/ci_workflows/public_api_contract.py
      responsibility: Public workflow API registry validation, rendering, and compatibility checks.
      status: keep
      classes:
      - name: ContractError
        purpose: Raised when a public API contract fails closed.
        methods: []
      - name: ContractData
        purpose: Defines ContractData for Public workflow API registry validation, rendering, and compatibility checks.
        methods: []
      functions:
      - name: require
        signature: 'require(condition: bool, message: str) -> None'
        purpose: Implements require for Public workflow API registry validation, rendering, and compatibility checks.
      - name: read_json
        signature: 'read_json(path: Path) -> Any'
        purpose: Implements read_json for Public workflow API registry validation, rendering, and compatibility checks.
      - name: read_text
        signature: 'read_text(path: Path) -> str'
        purpose: Implements read_text for Public workflow API registry validation, rendering, and compatibility checks.
      - name: nonempty
        signature: 'nonempty(value: Any, field: str) -> str'
        purpose: Implements nonempty for Public workflow API registry validation, rendering, and compatibility checks.
      - name: unique_strings
        signature: 'unique_strings(value: Any, field: str, *, allow_empty: bool=True) -> list[str]'
        purpose: Implements unique_strings for Public workflow API registry validation, rendering, and compatibility checks.
      - name: permission_map
        signature: 'permission_map(value: Any, field: str) -> dict[str, str]'
        purpose: Implements permission_map for Public workflow API registry validation, rendering, and compatibility checks.
      - name: _load_fragments
        signature: '_load_fragments(root: Path, index: Mapping[str, Any]) -> tuple[Mapping[str, Any], ...]'
        purpose: Implements _load_fragments for Public workflow API registry validation, rendering, and compatibility checks.
      - name: load_contract
        signature: 'load_contract(root: Path) -> ContractData'
        purpose: Implements load_contract for Public workflow API registry validation, rendering, and compatibility checks.
      - name: _validate_policy
        signature: '_validate_policy(types: Mapping[str, Any]) -> None'
        purpose: Implements _validate_policy for Public workflow API registry validation, rendering, and compatibility checks.
      - name: permission_profiles
        signature: 'permission_profiles(data: ContractData) -> dict[str, Mapping[str, Any]]'
        purpose: Implements permission_profiles for Public workflow API registry validation, rendering, and compatibility checks.
      - name: _validate_input
        signature: '_validate_input(row: Mapping[str, Any], catalog: Mapping[str, Any], label: str) -> tuple[str, bool]'
        purpose: Implements _validate_input for Public workflow API registry validation, rendering, and compatibility checks.
      - name: validate_workflows
        signature: 'validate_workflows(data: ContractData, profiles: Mapping[str, Mapping[str, Any]]) -> dict[str, Mapping[str, Any]]'
        purpose: Implements validate_workflows for Public workflow API registry validation, rendering, and compatibility checks.
      - name: immutable_reference
        signature: 'immutable_reference(value: str) -> bool'
        purpose: Implements immutable_reference for Public workflow API registry validation, rendering, and compatibility checks.
      - name: validate_bootstrap_workflow
        signature: 'validate_bootstrap_workflow(data: ContractData, workflows: Mapping[str, Mapping[str, Any]], profiles: Mapping[str, Mapping[str, Any]]) -> None'
        purpose: Implements validate_bootstrap_workflow for Public workflow API registry validation, rendering, and compatibility checks.
      - name: validate_release_schema
        signature: 'validate_release_schema(root: Path) -> None'
        purpose: Implements validate_release_schema for Public workflow API registry validation, rendering, and compatibility checks.
      - name: validate_caller
        signature: 'validate_caller(case: Mapping[str, Any], data: ContractData, workflows: Mapping[str, Mapping[str, Any]], profiles: Mapping[str, Mapping[str, Any]]) -> str | None'
        purpose: Implements validate_caller for Public workflow API registry validation, rendering, and compatibility checks.
      - name: validate_caller_fixtures
        signature: 'validate_caller_fixtures(data: ContractData, workflows: Mapping[str, Mapping[str, Any]], profiles: Mapping[str, Mapping[str, Any]]) -> None'
        purpose: Implements validate_caller_fixtures for Public workflow API registry validation, rendering, and compatibility checks.
      - name: _input_map
        signature: '_input_map(record: Mapping[str, Any]) -> dict[str, Mapping[str, Any]]'
        purpose: Implements _input_map for Public workflow API registry validation, rendering, and compatibility checks.
      - name: valid_acknowledgement
        signature: 'valid_acknowledgement(value: Any, api_name: str) -> bool'
        purpose: Implements valid_acknowledgement for Public workflow API registry validation, rendering, and compatibility checks.
      - name: classify_change
        signature: 'classify_change(baseline: Mapping[str, Any], current: Mapping[str, Any], acknowledgement: Any=None) -> str'
        purpose: Implements classify_change for Public workflow API registry validation, rendering, and compatibility checks.
      - name: validate_compatibility_fixtures
        signature: 'validate_compatibility_fixtures(root: Path) -> None'
        purpose: Implements validate_compatibility_fixtures for Public workflow API registry validation, rendering, and compatibility checks.
      - name: validate_docs
        signature: 'validate_docs(root: Path) -> None'
        purpose: Implements validate_docs for Public workflow API registry validation, rendering, and compatibility checks.
      - name: validate
        signature: 'validate(root: Path) -> ContractData'
        purpose: Implements validate for Public workflow API registry validation, rendering, and compatibility checks.
      - name: _cell
        signature: '_cell(value: Any) -> str'
        purpose: Implements _cell for Public workflow API registry validation, rendering, and compatibility checks.
      - name: _input_label
        signature: '_input_label(item: Mapping[str, Any]) -> str'
        purpose: Implements _input_label for Public workflow API registry validation, rendering, and compatibility checks.
      - name: render
        signature: 'render(data: ContractData) -> str'
        purpose: Implements render for Public workflow API registry validation, rendering, and compatibility checks.
      - name: compare_contracts
        signature: 'compare_contracts(baseline: ContractData, current: ContractData) -> list[dict[str, Any]]'
        purpose: Implements compare_contracts for Public workflow API registry validation, rendering, and compatibility checks.
      internal_dependencies: []
    - path: src/ci_workflows/public_ci_admission.py
      responsibility: Public-repository self-CI event and runner-admission policy.
      status: consolidate
      classes: []
      functions:
      - name: _disabled_job
        signature: '_disabled_job(condition: object) -> bool'
        purpose: Implements _disabled_job for Public-repository self-CI event and runner-admission policy.
      - name: _load_policy
        signature: '_load_policy(root: Path, config: HarnessConfig, findings: list[Finding]) -> Mapping[str, object] | None'
        purpose: Implements _load_policy for Public-repository self-CI event and runner-admission policy.
      - name: validate_public_ci_admission
        signature: 'validate_public_ci_admission(root: Path, workflow_documents: Mapping[str, ParsedDocument], config: HarnessConfig, findings: list[Finding]) -> None'
        purpose: Validate exact event classes and fail-closed PR runner admission.
      internal_dependencies:
      - validation_helpers
      - validation_model
