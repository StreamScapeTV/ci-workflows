{
  "positive": [
    "exact YAML syntax/style/schema",
    "SOPS ciphertext/reference structure",
    "locked vendored Helm dependency",
    "deterministic Helm template",
    "root-restricted Kustomize build",
    "changed-tree target selection",
    "checked-in fixed policy script",
    "zero-residue cleanup"
  ],
  "negative": [
    "duplicate YAML key",
    "plaintext SOPS value",
    "mutable or missing Helm dependency lock",
    "remote Kustomize resource",
    "render drift",
    "duplicate object ownership",
    "source mutation",
    "symlink escape",
    "caller-selected command/runner/registry/cluster",
    "cleanup failure"
  ]
}
