name: Agent State lifecycle caller
on:
  workflow_dispatch:
permissions:
  issues: write
  pull-requests: read
jobs:
  execute_lifecycle:
    name: Execute lifecycle transport
    uses: StreamScapeTV/ci-workflows/.github/workflows/reusable-agent-state-lifecycle.yml@aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa
    with:
      project_id: iptv-backend
      action: resume
      session_name: Agent 1
      request_id: fixture-request-0001
    secrets:
      agent_state_api_token: ${{ secrets.AGENT_STATE_API_TOKEN }}
