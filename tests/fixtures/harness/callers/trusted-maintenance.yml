name: Trusted maintenance caller
on:
  workflow_dispatch:
permissions:
  actions: write
  contents: read
jobs:
  cleanup_artifacts:
    name: Clean registered artifacts
    uses: StreamScapeTV/ci-workflows/.github/workflows/reusable-artifact-cleanup.yml@aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa
    with:
      dry_run: true
      request_id: fixture-request-0002
    secrets:
      organization_maintenance_token: ${{ secrets.ORGANIZATION_MAINTENANCE_TOKEN }}
