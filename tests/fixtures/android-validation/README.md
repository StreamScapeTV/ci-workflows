# Android validation fixtures

`cases.json` contains bounded public-request cases. `smoke-project/` is an issue-owned, non-product fixture used only to prove the mobile JDK/SDK/wrapper workflow. Its launcher supports `--version` and cannot build, sign, deploy, acquire a device, or certify a product.
