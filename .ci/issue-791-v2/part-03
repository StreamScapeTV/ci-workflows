+                  raise SystemExit("targeted-tests test_selectors must be one bounded JSON-array string")
+              try:
+                  selectors = json.loads(selector_payload)
+              except json.JSONDecodeError as exc:
+                  raise SystemExit("targeted-tests test_selectors must be valid JSON") from exc
+              if not isinstance(selectors, list) or not 1 <= len(selectors) <= 20:
+                  raise SystemExit("targeted-tests requires 1 through 20 selectors")
+              if any(not isinstance(value, str) for value in selectors):
+                  raise SystemExit("targeted-tests selectors must be strings")
+              if len(selectors) != len(set(selectors)):
+                  raise SystemExit("targeted-tests selectors must be unique")
+
+              if workflow == "validation.android":
+                  if platform == "jvm":
+                      pattern = re.compile(r"[A-Za-z0-9_.$*?-]{1,240}")
+                  elif platform == "instrumentation":
+                      pattern = re.compile(r"(?:[A-Za-z_][A-Za-z0-9_$]*\.)+[A-Za-z_][A-Za-z0-9_$]*(?:#[A-Za-z_][A-Za-z0-9_$]*)?")
+                  else:
+                      raise SystemExit("Android targeted-tests requires test_platform=jvm or instrumentation")
+                  if any(not pattern.fullmatch(value) or value.startswith("-") for value in selectors):
+                      raise SystemExit("Android targeted-tests contains an invalid selector")
+              elif workflow == "validation.apple":
+                  if platform not in {"macos", "ios", "tvos"}:
+                      raise SystemExit("Apple targeted-tests requires test_platform=macos, ios, or tvos")
+                  pattern = re.compile(r"[A-Za-z0-9_.$/-]{1,240}")
+                  if any(not pattern.fullmatch(value) or value.startswith("-") or "/" not in value for value in selectors):
+                      raise SystemExit("Apple targeted-tests contains an invalid selector")
+              else:
+                  raise SystemExit("unsupported native targeted workflow")
+          elif workflow == "validation.android" and profile == "targeted-unit":
+              if selector_payload not in (None, "[]") or platform not in (None, ""):
+                  raise SystemExit("legacy targeted-unit does not accept new targeted-tests inputs")
+              if not isinstance(legacy_filter, str) or not re.fullmatch(r"[A-Za-z0-9_.$*?-]{1,240}", legacy_filter):
+                  raise SystemExit("legacy targeted-unit requires one bounded test_filter")
+          else:
+              if selector_payload not in (None, "[]") or platform not in (None, ""):
+                  raise SystemExit("targeted test inputs are accepted only by targeted-tests")
+              if legacy_filter not in (None, ""):
+                  raise SystemExit("test_filter is accepted only by legacy Android targeted-unit")
+          PY_VALIDATE_NATIVE_TARGETED
+
       - name: Validate Maven release request
         if: ${{ steps.claim.outputs.workflow_key == 'release.maven' }}
         shell: bash
@@ -242,6 +314,8 @@
       ref: ${{ needs.request.outputs.ref }}
       source_is_tag: ${{ needs.request.outputs.is_tag == 'true' }}
       test_profile: ${{ needs.request.outputs.test_profile }}
+      test_selectors: ${{ fromJSON(needs.request.outputs.inputs_json).test_selectors || '[]' }}
+      test_platform: ${{ fromJSON(needs.request.outputs.inputs_json).test_platform || '' }}
       ci_run_id: ${{ inputs.ci_run_id }}
     secrets: inherit
 
@@ -301,6 +375,8 @@
       test_profile: ${{ needs.request.outputs.test_profile }}
       project_directory: ${{ fromJSON(needs.request.outputs.inputs_json).project_directory || '.' }}
       test_filter: ${{ fromJSON(needs.request.outputs.inputs_json).test_filter || '' }}
+      test_selectors: ${{ fromJSON(needs.request.outputs.inputs_json).test_selectors || '[]' }}
+      test_platform: ${{ fromJSON(needs.request.outputs.inputs_json).test_platform || '' }}
       room_schema: ${{ fromJSON(needs.request.outputs.inputs_json).room_schema || false }}
       ci_run_id: ${{ inputs.ci_run_id }}
     secrets: inherit
diff -ruN '--exclude=__pycache__' ci-workflows-baseline/.github/workflows/self-check.yml ci-workflows-791/.github/workflows/self-check.yml
--- ci-workflows-baseline/.github/workflows/self-check.yml	2026-09-05 01:54:05.000000000 +0000
+++ ci-workflows-791/.github/workflows/self-check.yml	2026-09-05 13:23:28.889522896 +0000
@@ -31,6 +31,7 @@
         run: >-
           python3 -m unittest
           tests.test_ci_helpers_current
+          tests.test_native_targeted_profiles
           tests.test_android_search_performance_profile
           tests.test_oci_reproducibility
           tests.test_apple_workflow
diff -ruN '--exclude=__pycache__' ci-workflows-baseline/tests/test_android_search_performance_profile.py ci-workflows-791/tests/test_android_search_performance_profile.py
--- ci-workflows-baseline/tests/test_android_search_performance_profile.py	2026-09-05 01:54:05.000000000 +0000
+++ ci-workflows-791/tests/test_android_search_performance_profile.py	2026-09-05 13:19:49.128425087 +0000
@@ -244,6 +244,8 @@
         preflight_script = preflight["run"]
         self.assertIn('test "${PROJECT_DIRECTORY}" = "."', preflight_script)
         self.assertIn('test -z "${TEST_FILTER}"', preflight_script)
+        self.assertIn('test "${TEST_SELECTORS}" = "[]"', preflight_script)
+        self.assertIn('test -z "${TEST_PLATFORM}"', preflight_script)
         self.assertIn('test "${ROOM_SCHEMA}" = "false"', preflight_script)
         self.assertIn("scripts/ci/run-android-hosted-validation.sh", preflight_script)
 
@@ -271,7 +273,9 @@
     def test_emulator_profile_uses_fixed_central_boot_and_cleanup(self) -> None:
         prepare = self.by_name["Prepare generic Android emulator"]
         cleanup = self.by_name["Stop generic Android emulator"]
-        self.assertEqual(prepare["if"], "${{ inputs.test_profile == 'emulator' }}")
+        self.assertIn("inputs.test_profile == 'emulator'", prepare["if"])
+        self.assertIn("inputs.test_profile == 'targeted-tests'", prepare["if"])
+        self.assertIn("inputs.test_platform == 'instrumentation'", prepare["if"])
         script = prepare["run"]
         self.assertIn("system-images;android-36;google_apis;x86_64", script)
         self.assertIn("central-android-api36", script)
@@ -280,10 +284,9 @@
         self.assertIn("sys.boot_completed", script)
         self.assertIn("printf 'ANDROID_SERIAL=%s\\n'", script)
         self.assertNotIn("inputs.", script)
-        self.assertEqual(
-            cleanup["if"],
-            "${{ always() && inputs.test_profile == 'emulator' }}",
-        )
+        self.assertIn("inputs.test_profile == 'emulator'", cleanup["if"])
+        self.assertIn("inputs.test_profile == 'targeted-tests'", cleanup["if"])
+        self.assertIn("inputs.test_platform == 'instrumentation'", cleanup["if"])
         self.assertIn("emu kill", cleanup["run"])
 
 
diff -ruN '--exclude=__pycache__' ci-workflows-baseline/tests/test_apple_workflow.py ci-workflows-791/tests/test_apple_workflow.py
--- ci-workflows-baseline/tests/test_apple_workflow.py	2026-09-05 01:54:05.000000000 +0000
+++ ci-workflows-791/tests/test_apple_workflow.py	2026-09-05 13:14:03.042135820 +0000
@@ -55,10 +55,9 @@
         self.assertIn("run_xcode_logged macos-test xcodebuild test", script)
         self.assertNotIn("build-for-testing", script)
         self.assertNotIn("macos-build", script)
-        self.assertIn(
-            "-only-testing:streamscapetvTests/SelectedBackendStateSyncRoutingIntegrationTests",
-            script,
-        )
+        self.assertNotIn("SelectedBackendStateSyncRoutingIntegrationTests", script)
+        macos_full = script[script.index("macos-test)"):script.index("macos-targeted-tests)")]
+        self.assertNotIn("-only-testing:", macos_full)
 
     def test_optional_swiftpm_xcode_args_are_strict_bash_safe(self) -> None:
         execute = self.workflow["jobs"]["execute"]
@@ -68,7 +67,7 @@
         script = command_step["run"]
         safe_expansion = '"${swiftpm_xcode_args[@]+"${swiftpm_xcode_args[@]}"}"'
 
-        self.assertEqual(script.count(safe_expansion), 6)
+        self.assertEqual(script.count(safe_expansion), 9)
         self.assertNotIn('"${swiftpm_xcode_args[@]}"', script.replace(safe_expansion, ""))
 
